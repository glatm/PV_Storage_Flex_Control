# 读研学习方向计划（研究方向3 · 极致详细版 v5 · 含"学到什么程度"+周任务清单+代码练习+公式+误区）

> **你的方向（申报书原文）**：多时间尺度"能—碳"指标智能调控策略——计及建筑光伏和用能负荷的多重不确定性、单位电能碳排放量的时变性，提出"源—荷"多时间尺度预测方法；建立基于**鲁棒优化和随机规划**的"能—碳"指标提升调控模型，在**日前和实时**两个时间尺度实现对"光储直柔"系统的主动调控；提出基于**云计算**的"光储直柔"智能调控系统框架，实现基于**大数据挖掘**的调控策略优化。
>
> **硬件底座**：Intel Ultra 7 270K（24核）· 微星 Z890M · 微星 PRO A850GL 850W · 冰刃A13 360水冷 · PANO M11OL 海景房 · 单根 24GB DDR5（**待补一条同款凑 48G 双通道**）· 待购 RTX 5070（12GB，Blackwell 架构）。
> **核心定位**：CPU 密集型科研机 + 轻量 DL 辅助。方向3 的本质是**预测 + 优化 + 控制 + 数据挖掘**，显卡只在"源-荷预测"那步用，5070 完全够；导师组做设备故障诊断（CNN-LSTM/GNN）但无大模型课题，别被 AI 热潮带偏。
>
> **已核实的关键决策（写进档案，免得纠结）**：
> 1. **显卡选 5070，不上 5070Ti**：导师全部方向（能-碳优化 + 设备诊断）都不需要 16GB 显存；5070 的 12GB 跑小模型绰绰有余，Ti 多花的 1500–2300 元不如投给内存。
> 2. **内存优先补 48G 双通道**：单根 24G 单通道带宽减半，对"优化求解 + 数据预处理"拖累比显卡升级更大；宏碁凌霜 24G C30 同款条约 ¥2299，小众会断货，趁早买。
> 3. **真实场景数据是你的壁垒**：你挂"福州烟草绿色园区"实体项目，有实地光伏/负荷数据，这是纯仿真论文最大的软肋。
>
> **v5 更新说明（2026-08-09 / 环境已全部搭建完成后修正）**：
> - 环境名实际为 **`PV_Storage_Flex_Control`**（v2 写的 `power` 作废）。
> - Python 实际为 **3.14.6**（v2 写的 3.11 目标版；3.14 上所有 CPU 包已实测可用，仅将来装 5070 的 cu128 torch 若轮子滞后需另建 3.11 的 DL 环境，不影响当前）。
> - **环境已全部搭建完成**：numpy/pandas/scipy/sklearn/xgboost/lightgbm/cvxpy/gurobipy/pandapower/pypsa/networkx/statsmodels/openpyxl/xlrd/Pyomo/sktime/plotly 全部装好；Gurobi 学术许可已激活；torch 按计划等 5070 到货再装（非缺口）。详见 §0.5。
> - **v5 最大新增（"更细"部分）**：① 新增「📅 每日/每周时间分配模板」（研究生不误科研）；② 每个阶段拆成 **周任务打勾清单 + 必写代码练习（含函数签名）+ 论文怎么读（提取点）+ 必懂公式 + 常见误区 + 自检打勾**；③ 新增附录 **公式速查手册 / 组会与导师沟通 / 里程碑甘特图 / 求职与证书路线图**。
> - **2026-08-10 知识点补全（合入 `知识点补全补丁.md`）**：④ 各阶段内插 **「📚 阶段X 核心知识点清单」**（补电力电子/MPPT/下垂、STL-LSTM/Keras/自适应修正、MPC/CCP/场景缩减/NSGA-II、PID/故障保护、碳核算 Scope、MQTT/Grafana、LaTeX 等）；⑤ 新增 **附R 知识点补全与课题四层对齐**（课题四层对齐表 + 跨阶段共性缺口 + 自检速查）。

---

## 如何使用本计划

- 按 `〇 → 阶段0~8 → 附A~附R` 顺序推进；`〇` 是地基，必须第 1 个月完成。
- 每个阶段拆成 **月度目标 → 周计划 → 具体动作 → 练手代码/产出 → 自检标准** 五层，**自检标准达成才进下一阶段**。
- 时间轴按 **学硕 3 年（36 个月）** 排；专硕/提前毕业自行压缩，但阶段顺序不变。
- 所有课程、论文均经联网核实真实存在；数据集、软件给出可落地名称。

### 📅 每日 / 每周时间分配模板（研究生不误科研）

读研不是全职备考，要**同时**上课、看论文、写代码、做项目。下面是一份"可持续"的时间表，按**精力波峰**排活：

| 时段 | 做什么 | 时长 | 对应阶段 |
|---|---|---|---|
| 早 8:00–11:30（脑力巅峰） | 写代码 / 推公式 / 跑实验（最难的事放这） | 3.5h | 阶段2/3/4 主力 |
| 下午 14:00–17:00 | 看论文 / 上课 / 读教材（中等脑力） | 3h | 阶段1/文献 |
| 晚 19:30–22:00 | 整理笔记（Obsidian）/ 组会 PPT / 英语（碎片+中低） | 2.5h | 阶段0/7 + 英语 |
| 碎片（通勤/吃饭/睡前） | 墨墨背单词 30 新词 + 复习 | 0.6h | 英语 |

**每周节奏（参考）**：
- 周一~周五：按上表推进 1 个"本周代码任务" + 读 2–3 篇论文（精读 1 篇、泛读 2 篇）。
- 周六上午：本周复盘（实验记录归档、卡点记录）；周六下午：弹性（补进度/运动）。
- 周日：组会准备（如果有）+ 周计划（写下周要完成的 3 个具体产出）。
- **红线**：每天至少 1 小时"动手写代码"，光看不写 = 没学。

### 🎯 掌握程度等级定义（先看懂这个，才知道"学到什么程度"指什么）

为了把"学会"说清楚，全文档统一用 **5 级掌握尺度** 标注每一项技能的目标水平：

| 等级 | 名称 | 判定标准（达到这一级意味着你能……） |
|---|---|---|
| **L1 了解** | 听过、能复述 | 看懂别人在说什么，能口头讲出概念是什么 |
| **L2 理解** | 懂原理、能解释 | 能画出逻辑/结构、解释"为什么"，能对比不同方法差异 |
| **L3 应用** | 照例能跑、能套用 | 照着示例代码改数据/参数能跑通，能套到自己的问题里 |
| **L4 熟练** | 独立实现、能调参排错 | 从零自己写、遇到报错能定位原因、能讲清"该选哪个、为什么" |
| **L5 精通** | 能改进、能创新、能教人 | 能改进方法/提出新变体，能带别人做，能支撑一篇论文创新点 |

> **你的总目标线**：研究方向3 的核心技能（预测 L4、优化 L4、仿真 L4、碳建模 L3、挖掘 L3）到 L4 即足够支撑硕士小论文与大论文；其余（FastAPI L3、英语 L2→L3 读文献、数学 L2–L3）达标即可，不必追 L5。

### 各能力"学到什么程度"总览（一句话达标线）

| 能力                     | 目标等级   | 一句话达标线（自检可勾）                                |
| ---------------------- | ------ | ------------------------------------------- |
| Git / GitHub           | L3     | 能独立完成 clone→branch→commit→push→merge，不丢代码   |
| Obsidian / Zotero 文献管理 | L3     | 茉莉花自动重命名中文文献；≥50 篇文献分类入库                    |
| 电力系统分析（稳态）             | L2–L3  | 能口述有功-频率/无功-电压；能 pandapower 跑通微电网潮流         |
| 凸优化 / 鲁棒随机规划           | L2–L4  | 能写对偶问题；能 CCG 主从收敛；能解释 Γ 保守度权衡               |
| 源-荷预测（经典+树+DL）         | **L4** | 能独立跑 ARIMA/XGBoost/LSTM pipeline，RMSE 达基线水平 |
| 两阶段鲁棒/随机优化（Gurobi）     | **L4** | 能 CVXPY/Pyomo+Gurobi 建模并求解，出最优解+目标值         |
| pandapower / PyPSA 仿真  | **L4** | 能搭光储直柔系统、跑通潮流、做日前-实时 MPC 闭环                 |
| 时变碳因子建模                | L3     | 碳因子随小时/季节变化，嵌入目标函数结果有差异                     |
| 数据挖掘（聚类/关联）            | L3     | 挖出≥3 条"什么场景下该放电/调柔"规则                       |
| FastAPI / 云框架          | L3     | 能写接口、能画架构图，三服务串联跑通                          |
| 学术英语（读文献）              | L2→L3  | 能不查字典读懂 IEEE 论文摘要+方法段                       |
| 英语六级                   | 过线 425 | 在校期间考过，国企线加分                                |
| 学术论文写作/投稿              | L3     | 能独立写出 SCI/b 中文核心初稿并回复审稿意见                   |

---

## 〇、软件环境安装清单（地基，入学第 1 个月完成）

### 0.1 基础平台

| 软件 | 用途 | 安装方式 | 备注 |
|---|---|---|---|
| Windows 11 家庭中文版 | 你的系统（已激活 Retail 正版） | 已装 | 家庭版无 Hyper-V/组策略，科研够用 |
| **Anaconda**（你实际装的是 Anaconda 2026.07，conda 26.5.3） | Python 环境管理 | 官网下载，勾选 "Add to PATH" | 体积 3GB 但预装 numpy 等；Miniconda 更精简但都行，**已装就不必重装** |
| Git | 版本管理 | `winget install Git.Git` | 装时勾 "Add to PATH" |
| **GitHub Desktop** | 图形化管理代码仓（你已在用） | desktop.github.com | 英文界面，无中文选项（跟随系统语言，软件内不可改） |
| VS Code | 主力编辑器 | 官网 | 装插件：Python / Jupyter / GitLens / Pylance / MATLAB / Markdown Preview Enhanced |
| WSL2（可选） | Linux 工具链 | `wsl --install` | 电力仿真 Windows 也行，非必需 |

> **Anaconda vs Miniconda 二选一即可**：你已装 Anaconda，就保持。conda 命令两都一样。

### 0.2 Python 科研环境（终端里敲，不在 Python/Jupyter 里）

```bash
# 0) 一次性：让 PowerShell 支持 conda activate（做完关掉 PowerShell 重开）
conda init powershell
# ===== 重开 PowerShell 后再继续 ↓ =====

# 0.5) conda 走国内镜像（建环境/装包更快，可选但推荐）
conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/
conda config --set show_channel_urls yes

# 1) 激活你已建好的环境（环境名实际是 PV_Storage_Flex_Control，Python 3.14）
conda activate PV_Storage_Flex_Control

# 2) pip 全局走清华镜像（一次设置，永久生效，国内飞快）
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

# 3) 基础数据处理 / 可视化
pip install numpy pandas scipy matplotlib seaborn scikit-learn
pip install openpyxl xlrd        # 读 .xlsx/.xls，真实园区数据第一步

# 4) 预测基线：经典时序 + 树模型
pip install statsmodels           # ARIMA/SARIMA/季节分解，DL 之前的经典对比基准
pip install xgboost lightgbm      # 树模型，阶段2 主力基线
pip install sktime                # 统一时序 ML 框架，多模型对比省事

# 5) 优化求解（方向3核心，纯 CPU 跑，你的 24 核主场）
pip install cvxpy                 # 凸优化建模
pip install Pyomo                # 代数建模语言，MILP/MINLP 更顺手，可接 Gurobi
# Gurobi：先去 gurobi.com 用学校邮箱(@fjut.edu.cn)申请学术免费许可，
#         拿到密钥后 pip install gurobipy，再在终端跑：grbgetkey <你的密钥> 激活

# 6) 电力系统仿真（光储直柔建模验证）
pip install pandapower pypsa
pip install networkx             # 图/网络分析（数据挖掘也用）

# 7) 文档/交互
pip install jupyter notebook ipykernel
pip install plotly               # 交互图，组会汇报好看

# 8) 深度学习 —— 等 5070 到货再装！现在装无 GPU=白装
#    RTX 5070 是 Blackwell 架构，PyTorch 2.7+ 才支持，预编译 wheel 必须用 CUDA 12.8。
#    国内可用阿里云镜像（清华 pytorch-wheels 暂未同步 cu128，会报"找不到版本"）：
#    pip install torch torchvision torchaudio --index-url https://mirrors.aliyun.com/pytorch-wheels/cu128/
#    若阿里云也失败，退回官方源（慢但保真）：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
```

**环境存盘（换机/重装一键恢复）**：

```bash
conda env export > environment.yml   # 存一份到仓库根目录
# 将来重装：conda env create -f environment.yml
```

> ⚠️ 装好后（5070 到货后）跑 `python -c "import torch; print(torch.cuda.is_available())"`，输出 `True` 才算 5070 识别成功。**买卡前这步先跳过。**

### 0.3 科研工具（文献 + 笔记）

- **MATLAB + Simulink**：学校一般有正版，控制/仿真验证双修（主力仍用 Python，开源可复现更好发论文）。你已用 Simulink 建完光储直流微电网 Day1–17，这块已是强项。
- **Obsidian / Notion**：读书笔记、实验记录、组会汇报。Obsidian 免费+本地 md+图谱，配合你 GitHub 仓最划算。
- **Zotero + 茉莉花（抓知网文献，必装）**——完整流程见下方「附B 文献获取与管理工作流」。

### 0.4 项目代码仓库结构（照这个搭，你 GitHub 上的 `Optimal-Energy-System-Sc...` 就用它）

```
Optimal-Energy-System-Scheduling/   # 建议改个清晰名，如 PV-Microgrid-Optimization
├── README.md                       # 项目说明 + 环境依赖 + 运行步骤
├── environment.yml                 # 0.2 导出的依赖
├── data/                           # 原始数据，大文件用 git-lfs 或放外部
│   ├── raw/                        # 光伏出力、负荷、气象原始（.xlsx 用 openpyxl 读）
│   └── processed/                  # 清洗后、特征工程后
├── notebooks/                      # 探索性分析、可视化、调参记录
├── src/
│   ├── data/                       # 数据清洗、特征工程脚本
│   ├── forecasting/                # 源-荷预测（statsmodels / LightGBM / LSTM / PatchTST）
│   ├── optimization/               # 两阶段鲁棒/随机优化（Gurobi + CCG/Benders，Pyomo 建模）
│   ├── simulation/                 # pandapower/PyPSA 仿真、日前-实时闭环
│   ├── carbon/                     # 时变碳因子、碳计量
│   └── mining/                     # 数据挖掘增强策略
├── results/                        # 实验输出、图表、对比表
├── docs/                           # 论文草稿、文献笔记
└── tests/                          # 单元测试（养成习惯，审稿人/自己都省心）
```

> 用 GitHub Desktop 的日常流程：改完代码 → Open in VS Code → 回 GitHub Desktop 填 Summary → **Commit to main** → **Push origin**。别把大文件（数据/模型权重）直接传 GitHub（超 100MB 会被拒），用 `.gitignore` 排除 `data/raw/` 和 `*.pt`。

### 0.5 阶段0 现状核对（你已做的，✅ 勾掉不再重复）

> **2026-08-09 更新：环境已全部搭建完成，下列安装类项目全部打勾。**

- ✅ Anaconda 2026.07（conda 26.5.3）已装，PATH 已配
- ✅ 环境 `PV_Storage_Flex_Control` 已建（Python 3.14）
- ✅ numpy/pandas/scipy/sklearn/xgboost/lightgbm/cvxpy/gurobipy/pandapower/pypsa/networkx/requests/notebook/plotly **全部装好且导入正常**
- ✅ **补装包 openpyxl / xlrd / statsmodels / Pyomo / sktime 已装好**（DataFrame 读 Excel、ARIMA、代数建模、时序 ML 框架均可用）
- ✅ **Gurobi 学术许可已激活**（`grbgetkey` 跑过，gurobipy 可正常求解）
- ✅ pip 全局镜像已设清华（写进 `C:\Users\PC\AppData\Roaming\pip\pip.ini`）
- ✅ 旧 `C:\Users\PC\Miniconda`（conda 2.2.8 古董）已删除防混淆
- 🟡 **torch 系列按计划等 5070 到货再装**（cu128）——**这是设计上的延迟，不算缺口**，买卡后按 0.2 第 8 步装
- ⬜ **48G 内存待补**（单根 24G 单通道，趁早补同款，优先级高于买显卡）

### 🎯 阶段0 学到什么程度（工具素养达标线）

- **Git / GitHub（L3）**：能独立完成 clone → 建 branch → 改文件 → add → commit → push → 合并/PR；知道 `.gitignore` 怎么排除大文件；知道"别把数据/模型权重传 GitHub"。
- **VS Code + conda（L3）**：能 `Ctrl+Shift+P` → Select Interpreter 选对你的环境；知道 Python 代码在终端跑、不在 Python REPL(`>>>`) 里跑；`pip install` 装到当前激活环境。
- **文献管理（L3）**：Zotero 建"方向3"集合；茉莉花自动重命名中文文献；≥50 篇文献分类入库；能用 GB/T 7714 导出引文。
- **Obsidian 笔记（L2）**：能用三套模板（文献库/实验日志/组会汇报）记录，养成"读一篇记一篇、跑一次记一次"的习惯。

---

## 一、阶段0：工具链搭建与科研素养（第1个月）

**目标**：科研基础设施一次性配齐，养成工程好习惯。

| 周次 | 具体动作 | 练手/产出 | 自检标准 |
|---|---|---|---|
| 第1周 | 装完 0.1–0.3 全部软件；GitHub Desktop 克隆/建好自己的仓库并按 0.4 建目录 | 仓库目录骨架 | `import openpyxl, statsmodels, pyomo` 全部成功 |
| 第2周 | Git 实操（clone/commit/push/branch/merge）；把本科毕设或课程作业代码推上私有仓练手 | 至少一个私有仓有提交记录 | 能独立完成 add→commit→push |
| 第3周 | Obsidian 建「文献库 + 实验日志 + 组会汇报」三套模板；Zotero 建"方向3"集合，按 附B 流程装好茉莉花并试抓 3 篇知网论文 | 3 篇知网 PDF 入库 + 重命名正常 | 茉莉花自动重命名中文文献成功 |
| 第4周 | **下单同款 24GB 内存补成 48G 双通道**（见附D 硬件）；用 CPU-Z 确认现有条 SKU，照买同款同色 | 48G 内存到手并装好 | 任务管理器显示 48.0 GB，双通道已启用 |

**看什么（课程）**

- B站「宋浩老师」：高数/线代/概率论基础（补数学直觉，后面优化/概率预测用得上）。
- Git：B站搜「Git 教程 狂神/尚硅谷」（2 小时够用）。

**产出**：可运行 Python 环境、GitHub 仓（含目录骨架）、Zotero 文献库、48G 内存到手。

### 🎯 阶段0 学到什么程度（详细）

- 能**不看教程**在 PowerShell 里 `conda activate` 你的环境、`pip install` 任意包。
- 能**看懂报错**：区分"模块没装"（ModuleNotFoundError）、"环境问题"（import 成功但运行报版本错）、"代码逻辑错"（IndexError/KeyError）。
- 能**用 Git 不当作黑盒**：理解 working tree / stage / commit / remote 四态；误删文件能从 commit 找回。

### 📋 阶段0 周任务打勾清单（直接照抄到 Obsidian 周计划）

- [ ] 第1周：Anaconda / Git / VS Code / GitHub Desktop 全部装好；建仓库目录骨架；跑通 `import openpyxl, statsmodels, pyomo`
- [ ] 第2周：在私有仓完成 "改文件 → commit → push" 全流程；建一个 `test_branch` 练 merge
- [ ] 第3周：Obsidian 三套模板建好并各写 1 篇；Zotero 装茉莉花、抓 3 篇知网、导出 GB/T 7714 引文
- [ ] 第4周：CPU-Z 查现有内存 SKU → 下单同款 24G → 装好确认 48G 双通道

### 🛠 阶段0 必写代码练习（检验环境真通了）

```python
# 练习0.1：env_check.py  —— 跑一遍，全部 OK 才算环境达标
import importlib
for m in ["numpy","pandas","scipy","sklearn","xgboost","lightgbm",
          "cvxpy","gurobipy","pyomo","pandapower","pypsa","networkx",
          "statsmodels","openpyxl","plotly"]:
    try:
        importlib.import_module(m); print("OK ", m)
    except Exception as e:
        print("MISS", m, e)
# 进阶：写一行 pandapower 最小潮流（见手册 §11），能跑出 res_bus 就算真通
```

### ⚠️ 阶段0 常见误区

- 在 Python `>>>` 里敲 `conda` → 必报 SyntaxError；conda 只在**终端**跑。
- 装包前忘了 `conda activate` → 包装进 base 或系统 Python，跑代码时找不到。
- 把数据/模型权重 `git push` 到 GitHub → 超 100MB 被拒，且泄露大文件；用 `.gitignore` 排除。
- 内存没补就猛跑 pandas 大数据 → 单通道 24G 会卡；先补内存再重度用。

### 📚 阶段0 核心知识点清单（补丁·切法A）

- **LaTeX / Markdown 学术写作基础（L2）**：认识 `.tex` 结构、`\section`/`\cite`、`bibtex`；知道 IEEE/Elsevier 模板多为 LaTeX，阶段7 论文写作前置。
- **Python 工程规范（L2）**：`requirements.txt` vs `environment.yml` 区别；`if __name__=='__main__'` 入口约定；`src/` 可导入包结构（`from src.xxx import yyy` 需 `__init__.py` 或 `pip install -e .`）。

---

## 二、阶段1：专业基础夯实（研一上，第1–4月）

**目标**：补电力系统 + 优化数学底子，吃透方向3 物理背景与术语。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第1月 | 系统学《电力系统分析》（稳态）：搞懂潮流计算、频率/电压调整、短路、稳定性概念 | 手写节点法潮流 2 总线小例 | 能口述"有功-频率、无功-电压"调节逻辑 |
| 第2月 | 重点理解**光储直柔**：光伏出力特性（辐照/温度）、储能等效模型、柔性负荷、直流配电优势 | 用 pandapower 搭 1 个光伏+储能 母线小网，跑通潮流 | pandapower 脚本输出母线电压/功率，无报错 |
| 第3月 | 学凸优化基础（凸集、对偶、KKT），为鲁棒/随机规划打底；精读申报书方向3 | 复现 Boyd 一个对偶例题 | 能写出"对偶问题"并解释 KKT 含义 |
| 第4月 | 圈定子课题：预测选什么模型？鲁棒/随机如何建模？日前-实时怎么切分？碳指标怎么量化？读综述 | 子课题开题雏形文档 + 文献笔记≥15 篇 | 能列出 3 个候选研究问题并说清差异 |

**看什么（课程，按章节）**

- **电力系统分析**：清华大学 孙宏斌（爱课程 icourses.cn，权威）；或 北京交通大学 郝亮亮（中国大学MOOC，10万+人选课，最系统）——重点看"电力系统稳态分析""潮流计算""有功/无功调整"。
- **凸优化**：Stephen Boyd《Convex Optimization》中文字幕（B站）——重点看 Ch2 凸集、Ch5 对偶、Ch11 鲁棒优化。
- **数学补强**：宋浩 线代/概率 完结（概率论是随机规划/场景法基础）。

**读什么（论文，国内为主）**

- 杨新法 等. 微电网技术综述. 中国电机工程学报, 2014.（领域全景，必读）
- 马恺珧 等. 不确定性环境下微电网优化调度综述. 2023.（覆盖两阶段鲁棒优化、CCG 算法、深度强化学习，必读）
- 微电网群协同运行关键技术综述. 浙江电力, 2023.（多微网/集群方向）

**产出**：专业课笔记、方向3 子课题开题雏形、pandapower 微电网潮流脚本。

### 🎯 阶段1 学到什么程度（详细）

- **电力系统（L2→L3）**：
  - L2：能画出"有功→频率、无功→电压"的调节链路；能解释短路/稳定性的物理意义。
  - L3：能**独立**用 pandapower 搭一个含光伏+储能+负荷的小微网，跑出各母线电压/功率；能读懂潮流不收敛时改线路参数/电压初值来排错。
- **凸优化（L2）**：能写出 LP/QP 的对偶问题；能说清 KKT 条件四部分的含义；能看懂"强对偶成立"意味着主从问题最优值相等（这是 CCG 算法能收敛的理论前提）。
- **方向3 物理背景（L3）**：能不看资料、用一张图讲清"源-荷预测 → 鲁棒优化 → 日前/实时调控 → 碳指标"这条主线，并说清每一步在解决什么不确定性。

### 📋 阶段1 周任务打勾清单

- [ ] 第1月：完成《电力系统分析》稳态章节；手写 2 总线节点法潮流（高斯-赛德尔 3 次迭代）
- [ ] 第2月：pandapower 搭 光伏(30kW)+储能+负荷 小网，跑通潮流，记录 res_bus.vm_pu
- [ ] 第3月：手抄 Boyd 例题——把原问题写成对偶；默写出 KKT 四条件
- [ ] 第4月：读 3 篇综述，写"子课题开题雏形.md"，列出 3 个候选问题 + 差异

### 🛠 阶段1 必写代码练习

```python
# 练习1.1：pandapower 最小微网潮流（阶段1 达标线）
import pandapower as pp
net = pp.create_empty_network()
bus = pp.create_bus(net, vn_kv=0.4, name="母线")
pp.create_ext_grid(net, bus=bus, vm_pu=1.0)
pp.create_sgen(net, bus=bus, p_mw=0.03, name="光伏")   # 30kW
pp.create_load(net, bus=bus, p_mw=0.02, name="负荷")   # 20kW
pp.runpp(net)
print(net.res_bus[["vm_pu","p_mw"]])   # vm_pu 接近 1.0 即健康
```

### 📐 阶段1 必懂公式

- **节点功率方程（极坐标）**：
  $P_i = \sum_j V_i V_j (G_{ij}\cos\theta_{ij} + B_{ij}\sin\theta_{ij})$，
  $Q_i = \sum_j V_i V_j (G_{ij}\sin\theta_{ij} - B_{ij}\cos\theta_{ij})$
- **标幺值**：$Z_{pu} = Z / Z_{base},\; Z_{base}=V_{base}^2/S_{base}$（小系统 V_base=0.4kV）
- **储能能量平衡**：$SOC_t = SOC_{t-1} + \eta_c P_{ch,t}\Delta t - P_{dis,t}\Delta t/\eta_d$

### ⚠️ 阶段1 常见误区

- 把 kW 直接喂给 pandapower（它要 MW）→ 数值差 1000 倍，潮流结果离谱。
- 直流微网当交流跑 → 简化可接受，但写论文要说明"直流用交流模型近似"。
- 只听课不推公式 → 阶段3 优化会卡在"写不出模型"，现在把 KKT 吃透。

### 📚 阶段1 核心知识点清单（补丁·切法A）

🔴 **电力电子基础（L3，课题①核心）**：
- Buck / Boost / **双向 DAB** 变换器原理、占空比与电压变比关系、平均模型。
- **MPPT**：扰动观察法(P&O) 与电导增量法(INC) 原理与区别。
- 直流母线稳定：电压源/电流源型换流器、母线电压纹波来源。

🔴 **直流微电网控制（L3）**：**下垂控制（droop）**P-f / Q-V 曲线与多源并联均流；并网 ↔ 孤岛切换逻辑。

🔴 **储能技术建模（L3）**：电池等效电路（Rint / Thevenin 一阶 RC）、SOC/SOH；**循环寿命与退化成本**（每 kWh 吞吐的等效老化成本，为何要进优化目标）。

🔴 **需求响应与柔性负荷（L3，"柔性空调负载"含义）**：价格型 DR（TOU/峰谷）/ 激励型 DR（可中断/可转移）；空调温控负荷的**聚合模型**与一阶热力学模型。

🔴 **电价与电力市场基础（L2，阶段3 目标函数 π_t 来源）**：分时电价(TOU)、实时电价(RTP)、尖峰电价；绿电交易、碳交易机制。

🟡 **最优潮流(OPF)概念（L2）**：经济调度 vs 潮流，OPF 与阶段3 优化的衔接。

---

## 三、阶段2：源-荷多时间尺度预测建模（研一上~下，第4–8月）

**目标**：掌握"源-荷预测"完整流水线，跑出光伏/负荷预测基线——方向3 的 DL 落点，也是**小论文①**来源。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第4月 | **数据采集与清洗**：从公开数据集 + 园区数据做起；pandas/openpyxl 读 xlsx，处理缺失/异常，做特征工程（天气、时段、节假日、温湿度、历史同期） | `src/data/` 清洗脚本 + 一张特征相关性热力图 | 原始→清洗后数据各一份，缺失率<1% |
| 第5月 | **经典基线先上**：用 statsmodels 跑 ARIMA/SARIMA + 季节分解，作为对比基准（审稿人必看 vs 经典方法） | `forecasting/baseline_statsmodels.py` | 出 ARIMA 的 RMSE/MAE，能画预测 vs 真实 |
| 第6月 | **树模型基线**：XGBoost/LightGBM 做监督预测——比深度学习更稳更快出结果 | `forecasting/tree_models.py` 含调参 | 树模型 RMSE ≤ 经典基线（否则调参） |
| 第7月 | **多时间尺度实践**：超短期（分钟级）、短期（日前）、滚动/多步预测；用 MAE/RMSE/MAPE 评估 | 多时间粒度预测对比表 | 3 种时间尺度各有指标 |
| 第8月 | **不确定性量化**（为阶段3 不确定集铺垫）：分位数/高斯给出预测区间而非单点；若 5070 到货，上 LSTM/GRU/PatchTST 小模型 | 预测区间图 + （可选）DL 模型对比 | 区间覆盖率 90% 左右；→ 整理小论文① |

**看什么（课程）**

- **李宏毅《机器学习》**（台大，B站搬运，华语最佳）：重点 RNN/LSTM/Attention 章节。
- **李沐《动手学深度学习》**（d2l.ai + B站）：边学边写，Transformer/循环神经网络章节。

**读什么（论文）**

- 国内：**《基于 SE-CNN-BiLSTM 与改进 Transformer 的光伏功率多时间尺度预测方法》2026**（直接对口"多时间尺度"）；**《基于地基云图与气象因素多模态融合的光伏功率预测方法》**邓芳明 等，中国电机工程学报, 2025（改进 Transformer+TCN+LSTM）。
- 国外（时序预测标杆）：**Informer**(2021 AAAI)、**Autoformer**(2021 NeurIPS)、**TimesNet**(2023 ICLR)、**PatchTST**(2023)。定义了"长时序预测"主流架构。

**产出**：源-荷预测 baseline 代码库（经典+树模型 + ≥1 深度学习模型对比）、预测精度报告（含不确定性区间）、多时间尺度数据预处理 pipeline。**→ 可整理成小论文①。**

### 🎯 阶段2 学到什么程度（详细，预测是核心 L4）

- **数据工程（L4）**：能独立用 pandas/openpyxl 读 Excel/CSV → 处理缺失（插值/删除）、异常（3σ/Z-score）、构建时序特征（滞后、滑动窗口、周期编码）；能画出特征相关性热力图挑特征。
- **经典时序（L3）**：能跑 ARIMA/SARIMA，能解释"差分阶数 d、自回归 p、移动平均 q"各自管什么；能判断序列是否平稳（ADF 检验）。
- **树模型（L4）**：能独立用 XGBoost/LightGBM 做监督预测，会用 `train_test_split` 防泄露、会用 `GridSearchCV`/`early_stopping` 调参、能看 `feature_importances_` 解释"哪个因素主导预测"。**达标线：树模型 RMSE 不差于 ARIMA 基线。**
- **深度学习（L3，5070 到货后）**：能照 d2l 或用 PyTorch 搭 LSTM/GRU/PatchTST，理解"序列→embedding→encoder→预测头"；能处理训练/验证/测试切分不泄露未来信息；知道过拟合时降模型/加 dropout/早停。
- **评估与不确定（L3）**：能算 RMSE/MAE/MAPE 并解释单位含义；能用分位数回归/高斯给出 90% 预测区间，区间覆盖率≈90%。
- **多时间尺度（L3）**：能分别做超短期（分钟）、短期（日前 24h）、滚动预测，并对比三种时间粒度的误差来源。

### 📋 阶段2 周任务打勾清单

- [ ] 第4月：用 pandas 读完一份数据；写 `clean_data.py`（缺失/异常/特征）；出相关性热力图
- [ ] 第5月：写 `baseline_arima.py`，输出 RMSE/MAE + 预测vs真实图
- [ ] 第6月：写 `tree_models.py`，GridSearchCV 调参，对比 Ridge/XGBoost/LightGBM
- [ ] 第7月：写多时间尺度对比（超短/短期/滚动），出对比表
- [ ] 第8月：写预测区间（分位数）；若 5070 到 → `lstm_model.py`；整理小论文①初稿

### 🛠 阶段2 必写代码练习（函数签名级）

```python
# 练习2.1：预测流水线骨架（src/forecasting/pipeline.py）
def load_and_clean(path: str) -> pd.DataFrame:
    """读 Excel/CSV → 处理缺失/异常 → 返回干净 DataFrame。"""
    ...
def make_features(df: pd.DataFrame, lags=(1, 24), freq="h") -> pd.DataFrame:
    """造滞后/周期特征，返回 X,y。"""
    ...
def evaluate(y_true, y_pred) -> dict:
    """返回 {'RMSE':..,'MAE':..,'MAPE':..}。"""
    ...
def quantile_forecast(model, X, alpha=0.9):
    """返回 90% 预测区间上下界。"""
    ...
```

### 📐 阶段2 必懂公式

- **RMSE / MAE / MAPE**：
  $RMSE=\sqrt{\frac1n\sum (y_i-\hat y_i)^2}$，
  $MAE=\frac1n\sum|y_i-\hat y_i|$，
  $MAPE=\frac{100\%}{n}\sum|\frac{y_i-\hat y_i}{y_i}|$
- **自相关 ACF**：
  $\rho_k=\frac{\sum_{t}(y_t-\bar y)(y_{t-k}-\bar y)}{\sum_t(y_t-\bar y)^2}$
- **差分平稳化**：$y'_t = y_t - y_{t-1}$（d=1）；ADF 检验 p<0.05 判平稳
- **分位数损失（Pinball）**：
  $L_\alpha=\max(\alpha(e),\;(\alpha-1)(e)),\; e=\hat y-y$

### ⚠️ 阶段2 常见误区

- **数据泄露**：用 `shuffle=True` 切分时间序列 → 未来信息进训练；应 `shuffle=False` 或按时间切。
- 只报 RMSE 不报 MAE → 审稿人要综合指标；MAPE 遇 0 值爆炸，光伏夜间为 0 慎用。
- 树模型喂了标准化特征 → 白做，树不 care 量纲。
- 深度学习先上、跳过基线 → 没有对比基准，论文被质疑"提升从哪来"。

### 📚 阶段2 核心知识点清单（补丁·切法A）

🔴 **STL-LSTM 混合建模（L4，课题②点名）**：
- STL 分解（趋势/季节/残差）原理与 `statsmodels.tsa.seasonal.STL`；"先分解再对各分量分别预测再叠加"为何提升精度。
- **Keras 实现路径**：`keras.Sequential` / Functional API 搭 LSTM/GRU；与 PyTorch 取舍（课题点名 Keras，上手快）。
- **自适应修正策略**：基于滚动误差的在线校正（误差反馈 / Kalman 式修正 / 误差补偿器）。

🔴 **概率预测评价指标（L3，补 Pinball 之外）**：**CRPS**（连续概率排布评分，审稿人更认）；预测区间覆盖率(PICP)与平均宽度(PINAW)联合评价。

🟡 **时序特征工程系统化（L3）**：周期性 sin/cos 编码、节假日效应、外生变量（气象）滞后构造；多步滚动"直接法 vs 递归法"误差累积差异。

🟡 **机理融合 / 物理约束预测（L3）**：光伏晴雨物理边界、晴空指数上限等物理损失函数。

---

## 四、阶段3：鲁棒优化与随机规划建模（研一下，第8–12月）

**目标**：把"能—碳调控模型"落成可求解的数学规划——方向3 的**核心壁垒**，你 24 核 CPU 最值钱的地方。也是**小论文②（大论文核心）**来源。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第8月 | **两阶段鲁棒优化理论**：第一阶段定日前计划，第二阶段应对实时不确定性；学 **Benders 分解 / CCG（列与约束生成）** | 手写一个 2 变量 CCG 小例收敛 | CCG 主从问题交替收敛，迭代≤20 次 |
| 第9月 | **随机规划**：场景法/蒙特卡洛刻画光伏/负荷不确定性；理解**分布鲁棒（DRO）**作为鲁棒与随机的折中 | 场景树生成脚本（N 个典型日） | 场景概率和=1，覆盖极端日 |
| 第10月 | **CVXPY + Pyomo + Gurobi 实现**：日前-实时两阶段调度，目标含**经济成本 + 碳排放**，约束含储能 SOC、柔性负荷、电网交互功率 | `optimization/two_stage.py` 可求解 | Gurobi 返回最优解+目标值，无不可行 |
| 第11月 | **量化单位电能碳排放量时变性**：把碳因子做成随时间/电源结构变化的参数，嵌入目标函数（差异化点） | 时变碳因子参数表 + 嵌入后目标函数 | 碳目标随小时变化，结果有差异 |
| 第12月 | **保守度敏感度分析**：预算不确定 Γ 扫描，看成本-鲁棒权衡 | Γ 扫描曲线图 | 给出"最优 Γ"建议区间 |

**看什么（课程）**

- Boyd《Convex Optimization》对偶/鲁棒章节；中科大/清华《最优化方法》公开课。
- 运筹学：清华/中科院《线性与整数规划》公开课（B站）。

**读什么（论文）**

- 国内：**《考虑极限场景的并网型微电网分布鲁棒优化方法》**曹金声 等，电力系统自动化, 2022；**《基于分布鲁棒模型预测控制的微电网多时间尺度优化调度》**（对口，重点复现框架）。
- 国外（鲁棒优化奠基）：**Ben-Tal, El Ghaoui, Nemirovski《Robust Optimization》**(2009)；**Bertsimas & Sim** "The Price of Robustness"(2004)。

**产出**：能-碳两阶段鲁棒/随机优化模型代码（Gurobi 可求解）、不确定集设计说明、保守度（Γ）敏感度分析。

### 🎯 阶段3 学到什么程度（详细，优化是核心 L4）

- **两阶段鲁棒（L4）**：能写出"主问题（第一阶段）+ 子问题（第二阶段 worst-case）+ CCG 割"的三段式；能独立用 Gurobi/Pyomo 实现；能解释"为什么主问题下界↑、子问题上界↓，两者相遇即最优"。**达标线：手写 2–3 变量小例能在 ≤20 次迭代收敛。**
- **随机规划/场景法（L3）**：能用蒙特卡洛/K-means 生成 N 个典型日场景树，场景概率和=1 且覆盖极端日；能解释"场景越多越接近真实但越慢"的权衡。
- **分布鲁棒 DRO（L3）**：能说清 DRO 相对"纯鲁棒（太保守）"和"纯随机（依赖分布假设）"的位置；能写出含 ambiguity set 的形式。
- **建模落地（L4）**：能独立把"经济成本+碳排放"双目标写成可求解模型（加权或 ε-约束法）；约束含 SOC 上下界/充放电互斥/电网交互限值；Gurobi 返回最优解+目标值、无不可行/无数值病态。
- **敏感度（L3）**：能扫描预算参数 Γ，画出"成本 vs 鲁棒性"曲线，给出"最优 Γ 区间"的工程建议。

### 📋 阶段3 周任务打勾清单

- [ ] 第8月：手写 CCG 2 变量小例（主/子问题交替），画出"下界↑上界↓收敛"图
- [ ] 第9月：写 `gen_scenarios.py`（K-means 典型日，概率和=1，含极端日）
- [ ] 第10月：写 `two_stage.py`（Pyomo+Gurobi），目标含成本+碳，跑出最优解
- [ ] 第11月：写 `carbon_factor.py`，嵌入目标，打印"各小时碳成本差异"
- [ ] 第12月：Γ 扫描脚本，出"成本-鲁棒"权衡曲线 + 建议区间

### 🛠 阶段3 必写代码练习（函数签名级）

```python
# 练习3.1：两阶段鲁棒 CCG 骨架（optimization/ccg.py）
def master_problem(gamma) -> (x_first, lb):
    """主问题：最小化 第一阶段成本 + 最差第二阶段成本上界估计。"""
    ...
def sub_problem(x_first, uncertainty_set) -> (worst_y, ub, cut):
    """子问题：给定第一阶段决策，求不确定性下的 worst-case。"""
    ...
def ccg_loop(gamma, max_iter=20):
    """主从交替，直到 |ub-lb|<tol。返回最优 (x*, 目标值)。"""
    ...
```

### 📐 阶段3 必懂公式

- **两阶段鲁棒**：
  $\min_{x}\; c^Tx + \max_{u\in\mathcal U}\min_{y}\; d^Ty\quad s.t.\; Ax+By\ge h(u)$
- **Bertsimas–Sim 预算不确定集**：
  $\mathcal U=\{\|u\|_\infty\le1,\;\sum_i \frac{u_i}{\hat u_i}\le\Gamma\}$
  （Γ 越大越保守）
- **CCG 收敛**：主问题下界 $LB\uparrow$，子问题上界 $UB\downarrow$，$|UB-LB|<\epsilon$ 即停
- **碳目标**：
  $\min \sum_t (\pi_t P_{grid,t} + \lambda_{CO2,t} E_{grid,t})$，
  $E_{grid,t}=EF_t\cdot P_{grid,t}$（$EF_t$ 时变碳因子）

### ⚠️ 阶段3 常见误区

- 主/子问题变量耦合写错 → CCG 不收敛；先在小例上验证（练习3.1）。
- 不确定集 Γ 拍脑袋 → 必须做敏感度扫描给"建议区间"。
- 模型不可行（Infeasible）→ 多半是约束冲突（如 SOC 上下界 + 能量平衡矛盾），先松约束定位。
- 只做单目标经济、碳当常数 → 丧失"能-碳"差异化，论文被归为普通调度。

### 📚 阶段3 核心知识点清单（补丁·切法A）

- **为何偏离 PuLP**：课题写 PuLP，本计划用 Pyomo+Gurobi——PuLP 仅适合中小 LP/MILP；CCG 主从需 Gurobi 强求解器，故升级；属合理升级，在此说明避免自我矛盾。

🔴 **模型预测控制(MPC)理论（L3，课题③点名）**：滚动时域优化、反馈校正；与"日前-实时"双时间尺度切分的对应；为何滚动比一次性优化抗不确定性（闭环 vs 开环）。

🟡 **机会约束规划 CCP（L3）**：`P(g(x,ξ)≤0)≥1-ε` 概率约束；与鲁棒/随机的三角关系；SAA / 凸近似求解。

🟡 **场景缩减（L3）**：从 1000 蒙特卡洛场景缩到 10–20 典型场景（Fast Forward Selection / 聚类代表），保留概率与极端场景。

🟡 **多目标优化（L3）**：Pareto 前沿；**NSGA-II** 进化算法；碳-经济双目标权衡可视化（补 ε-约束/加权）。

🟡 **鲁棒 max-min 对偶变换（L4，写 CCG 必用）**：内层 `max_{u∈U} min_y d^Ty` 通过对偶转可解规划；强对偶成立条件。

---

## 五、阶段4：系统仿真与多时间尺度主动调控（研一下~研二上，第12–16月）

**目标**：把预测 + 优化串成"光储直柔系统日前-实时主动调控"闭环并仿真验证——**小论文②主力素材**。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第12月 | 用 **pandapower / PyPSA** 搭光储直柔系统（光伏、储能、柔性负荷、电网接口、直流母线） | `simulation/microgrid_model.py` | 模型可求解潮流，无数值发散 |
| 第13月 | 实现**日前-实时两时间尺度滚动**：日前用预测+鲁棒优化出计划，实时用新预测滚动修正（MPC 思路） | 滚动优化主循环 | 实时层能在日前计划附近修正 |
| 第14月 | 场景仿真：典型日 / 极端天气 / 高比例光伏，对比"无调控 / 随机优化 / 鲁棒优化" | 3 类场景数据集 | 每场景可复现 |
| 第15月 | 出对比图：能效 / 碳排 / 成本三维 | `results/` 对比图 | 鲁棒/随机均优于无调控 |
| 第16月 | 重仿真/大批量场景若本地慢，用**学校 HPC 集群**并行 | HPC 提交脚本 | 批量场景可并行跑 |

**看什么（课程）**

- pandapower 官方教程（pandapower.readthedocs.io）；PyPSA 文档。
- 中国大学MOOC 搜「微电网控制」「电力系统优化运行」。

**读什么（论文）**

- 国内：《基于分布鲁棒模型预测控制的微电网多时间尺度优化调度》（再精读，照框架复现）；微电网技术综述"优化运行"章节。
- 国外：MPC 在微电网应用综述（IEEE Trans on Smart Grid 多篇，搜 "model predictive control microgrid"）。

**产出**：光储直柔仿真平台、日前-实时调控闭环 demo、3 类场景对比图（能效/碳排/成本）。**→ 小论文②成稿。**

### 🎯 阶段4 学到什么程度（详细，仿真 L4）

- **系统建模（L4）**：能用 pandapower/PyPSA 搭含光伏、储能、柔性负荷、电网接口、直流母线的光储直柔系统；能设置元件参数（线路 R/X、变压器、储能容量/效率）；能跑通潮流不发散。
- **闭环调控（L4）**：能实现"日前优化出计划 → 实时用新预测滚动修正"的 MPC 式闭环；能解释"为什么滚动比一次性优化更抗不确定性"。
- **场景仿真（L3）**：能复现典型日/极端日/高比例光伏三类场景，每类可重复（固定随机种子）；能出能效/碳排/成本三维对比图，且鲁棒/随机明显优于无调控。
- **与阶段2/3 衔接（L4）**：能把阶段2 的预测误差当作阶段3 的不确定集输入，把阶段3 的优化结果当作阶段4 的调度指令——三段数据接口打通，不重复造数据。

### 📋 阶段4 周任务打勾清单

- [ ] 第12月：pandapower 搭完整光储直柔系统，含储能 SOC 跟踪
- [ ] 第13月：写 `mpc_loop.py`（日前计划→实时滚动），固定随机种子可复现
- [ ] 第14月：造 3 类场景，每类跑 5 次确认稳定
- [ ] 第15月：出 能效/碳排/成本 三图，附显著性
- [ ] 第16月：写 HPC 提交脚本，批量场景并行

### 🛠 阶段4 必写代码练习

```python
# 练习4.1：日前-实时滚动闭环骨架（simulation/mpc_loop.py）
def day_ahead_plan(forecast_24h) -> dict:
    """调用阶段3 two_stage，返回 24h 调度计划。"""
    ...
def real_time_correct(plan, new_forecast, t) -> float:
    """t 时刻用新预测滚动修正调度。"""
    ...
def run_mpc(days=30, seed=42):
    """跑一个月滚动，返回 能效/碳排/成本 指标。"""
    ...
```

### ⚠️ 阶段4 常见误区

- 仿真与优化数据接口对不上（单位/时间步）→ 阶段2/3/4 共用一份 `processed` 数据。
- 滚动窗口不固定随机种子 → 结果不可复现，论文被质疑。
- 只比"有无调控"不比"鲁棒 vs 随机" → 区分度不够，审稿人要见方法对比。

### 📚 阶段4 核心知识点清单（补丁·切法A）

🔴 **控制理论（L3，Simulink"三环闭环"背后知识）**：经典 PID 三参数物理含义、频域视角；状态空间表示、稳定性判据（特征值 / Routh-Hurwitz）；电压环/电流环/功率环级联关系。

🔴 **故障保护原理（L3，课题①明确"故障保护"）**：过流/过压/欠压/短路保护逻辑；保护动作时序；微电网故障检测与隔离。

🟡 **仿真保真度层级（L2）**：电磁暂态(EMT) vs 机电暂态；平均模型 vs 开关模型；何时用哪种及误差来源。

---

## 六、阶段5：能-碳指标量化与大数据挖掘策略优化（研二上，第16–20月）

**目标**：把"碳"做扎实，用数据挖掘反哺调控策略——方向3 差异化亮点。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第16月 | **碳计量方法**：单位电能碳排放因子时变建模（火电占比、绿电交易、时段差异），做成可调参数库 | `carbon/carbon_factor.py` + 参数表 | 碳因子随小时/季节变化 |
| 第17月 | **数据挖掘增强**：聚类找典型场景、关联规则挖负荷模式、降维找主导因素 | `mining/` 聚类+关联脚本 | 挖出≥3 条"什么天气/负荷下该放电/调柔"规则 |
| 第18月 | 把挖掘得到的规则/典型场景喂回阶段3 优化模型做策略增强 | 增强版优化脚本 | 增强后目标值有改善 |
| 第19月 | （可选）轻量 DL 用 5070 跑：用挖掘特征增强预测 | 增强预测对比 | 加挖掘特征后 RMSE 下降 |
| 第20月 | 出"加挖掘 vs 不加"对比实验 | 对比表+图 | 差异显著，可写进论文 |

**看什么（课程）**

- 中国大学MOOC 搜「数据挖掘」「机器学习」（哈工大/北大）；李宏毅 ML "无监督学习/聚类"章节。

**读什么（论文）**

- 国内：知网搜「单位电能碳排放 时变」「电力系统 碳足迹 优化」「大数据挖掘 能源调控」——电力系统自动化、电网技术近年大量相关，挑高被引。
- 国外：碳感知调度（"carbon-aware scheduling"）综述；典型场景聚类用于调度。

**产出**：碳因子时变模型、数据挖掘增强的调控策略、对比实验（加挖掘 vs 不加）。

### 🎯 阶段5 学到什么程度（详细）

- **碳建模（L3）**：能建"单位电能碳排因子随小时/季节/电源结构变化"的参数表；能解释为什么碳不能当常数（火电占比波动、绿电交易、时段差异）。**达标线：嵌入目标函数后结果随小时有明显差异。**
- **数据挖掘（L3）**：能用 K-means 聚类找典型日场景；能用关联规则（Apriori）挖出"某种天气/负荷组合 → 该放电/调柔"的可解释规则；能用 PCA/特征重要性降维找主导因素。**达标线：挖出 ≥3 条可解释、可回灌优化的策略规则。**
- **反哺闭环（L3）**：能把挖出的规则/典型场景喂回阶段3 优化，且增强后目标值有可量化改善（加挖掘 vs 不加，差异显著）。

### 📋 阶段5 周任务打勾清单

- [ ] 第16月：建 `carbon_factor.py`，输出 24h×4季 碳因子矩阵
- [ ] 第17月：K-means 聚类典型日 + Apriori 关联规则，产出≥3 条规则
- [ ] 第18月：规则回灌 two_stage.py，对比目标值
- [ ] 第19月（可选）：挖掘特征增强预测，RMSE 下降验证
- [ ] 第20月：出 加挖掘 vs 不加 对比表+图

### 🛠 阶段5 必写代码练习

```python
# 练习5.1：碳因子 + 关联规则（carbon/carbon_factor.py, mining/assoc.py）
def build_carbon_matrix(hours=24, seasons=4) -> np.ndarray:
    """返回 (季节,小时) 时变碳因子矩阵。"""
    ...
def mine_rules(df, min_support=0.1, min_conf=0.6) -> list:
    """返回形如 ('高温','高负荷' -> '应放电') 的可解释规则列表。"""
    ...
```

### ⚠️ 阶段5 常见误区

- 碳因子用全国年均常数 → 丧失时变差异化，论文被批"碳建模粗糙"。
- 关联规则 support/confidence 阈值乱设 → 要么无规则要么规则无意义；先画支持度分布再定。
- 挖出的规则不可解释/不能回灌优化 → 沦为"为挖掘而挖掘"，审稿人一眼看穿。

### 📚 阶段5 核心知识点清单（补丁·切法A）

🔴 **碳核算体系（L3，补"只建碳因子表"）**：**Scope 1/2/3** 边界划分；产品/系统 **LCA 生命周期评价**流程；电网排放因子来源层级（全国均值 vs 区域电网 vs 实时碳强度；月度 vs 小时级）。

🟡 **关联规则进阶指标（L3）**：**lift / leverage / conviction**——区分"真相关"与"高频伪相关"。

🟡 **时序专用挖掘（L3）**：**DTW 动态时间规整**聚类（比欧氏 K-means 更适合负荷曲线）；负荷异常检测（孤立森林 / 3σ 时序版）。

---

## 七、阶段6：云计算智能调控系统框架（研二上~下，第20–24月，工程向）

**目标**：把前面算法包装成"可部署调控系统框架"——方向3"基于云计算的框架"那句落地（偏软件工程，提升论文完整度）。

| 月次 | 具体动作 | 练手代码/产出 | 自检标准 |
|---|---|---|---|
| 第20月 | 学基础：REST API（FastAPI）、消息队列、时序数据库（InfluxDB）、容器（Docker） | FastAPI Hello 接口 | 本地能访问接口返回 JSON |
| 第21月 | 搭**轻量调控系统原型**：预测服务 + 优化求解服务 + 可视化看板 | `src/cloud/` 原型 | 三服务可串联跑通 |
| 第22月 | 模块解耦 + 可扩展性设计，画框架图 | 系统架构图（draw.io/PPT） | 框架图能讲清数据流 |
| 第23–24月 | 不要求真上云，重点"框架设计 + 模块解耦 + 可扩展性"写进论文 | 论文"系统框架"章节素材 | 章节初稿完成 |

**看什么（课程）**

- B站「FastAPI 教程」「Docker 入门」；中国大学MOOC 搜「云计算」「微服务」。

**读什么（论文）**

- 国内：知网搜「数字孪生 微电网」「云边协同 能源调控」——近年热门，借鉴架构图。
- 国外：数字孪生/云边协同在微电网综述（IEEE Access / Sensors 等多篇）。

**产出**：调控系统框架设计图 + 可运行原型 + 论文"系统框架"章节素材。

### 🎯 阶段6 学到什么程度（详细）

- **FastAPI（L3）**：能写带路径参数/请求体的接口，本地 `uvicorn` 起服务、用 `requests`/浏览器验证返回 JSON；能画"请求→预测服务→优化服务→返回调度"的调用链。
- **系统设计（L2→L3）**：能讲清"云-边"分工（云端重优化、边缘重实时控制）；能用模块解耦思想把预测/优化/可视化拆成独立服务；能画架构图并讲清数据流。
- **Docker（L2，可选）**：理解容器概念，能写最简 Dockerfile 把服务打包；不强求上线。

### 🛠 阶段6 必写代码练习

```python
# 练习6.1：FastAPI 最小调控接口（src/cloud/main.py）
from fastapi import FastAPI
app = FastAPI()
@app.post("/dispatch")
def dispatch(payload: dict) -> dict:
    """接收预测+电价，返回调度计划（内部调阶段3 two_stage）。"""
    ...
```

### ⚠️ 阶段6 常见误区

- 系统框架写得太虚（只有架构图没有可运行原型）→ 审稿人认为"工程性不足"；至少给个能跑的 demo。
- 把云框架当主线 → 它只是"加分项"，核心是预测+优化，别本末倒置。

### 📚 阶段6 核心知识点清单（补丁·切法A）

🔴 **MQTT 协议（L3，课题④点名）**：发布/订阅模型、Broker/Client、Topic 通配符、QoS 0/1/2；为何物联网/边缘用 MQTT 而非 HTTP（轻量、长连接、低带宽）。

🔴 **Grafana 可视化（L3，课题④点名）**：对接 InfluxDB 建 dashboard；时序面板、告警规则；与 FastAPI 服务的数据流串联。

🟡 **云-边协同权衡（L3）**：云端重优化、边缘重实时控制的算力/延迟/带宽分工；边缘节点算力约束。

🟡 **消息中间件对比（L2）**：MQTT vs Kafka 适用场景差异。

---

## 八、阶段7：论文撰写与投稿（研二下，第24–28月）

**目标**：把阶段4–6 成果凝成小论文并投稿（早投比晚投好）。

| 月次 | 具体动作 | 练手/产出 | 自检标准 |
|---|---|---|---|
| 第24月 | 选目标期刊（见附E）；先投会议/国内核心练手，再冲高水平 | 期刊短名单 | 锁定 2–3 个目标 |
| 第25月 | 学学术写作：中文期刊格式、图表规范、实验对比写法；Zotero 管参考文献（茉莉花导出 GB/T 7714） | 论文初稿 v1 | 全文结构完整 |
| 第26月 | Cover letter、Response to reviewers 套路；组内互审 | 修改稿 v2 | 吸收≥3 条审稿意见 |
| 第27月 | 投稿；被拒快速转投别泄气 | ≥1 篇在投/录用 | 进入审稿流程 |
| 第28月 | 返修跟踪 | 返修稿 | 录用或二轮修改 |

**看什么（课程）**

- B站搜「SCI 论文写作」「科技论文写作」（国内高校开放课）；IEEE 作者中心资源（免费）。

**产出**：小论文初稿 + 投稿 + 返修。

### 🎯 阶段7 学到什么程度（详细）

- **学术写作（L3）**：能写出"摘要（目的-方法-结果-结论四要素）→ 引言（研究空白+你的贡献）→ 方法（可复现）→ 实验（对比基线+消融）→ 结论"的标准结构；图表符合期刊规范（矢量图、字号统一）。
- **投稿与返修（L2→L3）**：会选期刊（看 scope/IF/周期）、会写 Cover Letter；能逐条回复审稿意见（先感谢、再逐点回应、改动处标红）；被拒不慌、快速转投。
- **参考文献（L3）**：Zotero+茉莉花导出 GB/T 7714，正文引用与文末列表一一对应，零格式错误。

### 📋 阶段7 周任务打勾清单

- [ ] 第24月：列出 2–3 个目标期刊 + 各自 scope/周期
- [ ] 第25月：初稿 v1（摘要→结论完整）+ 所有图矢量
- [ ] 第26月：组内互审吸收≥3 条意见 → v2
- [ ] 第27月：投稿，进审稿流程
- [ ] 第28月：返修稿，录用或二轮

### ⚠️ 阶段7 常见误区

- 图用位图（png 放大糊）→ 期刊要矢量（pdf/svg）。
- 回复审稿意见只说"已修改"不标位置 → 必须"改动处标红 + 行号"。
- 被拒就躺平 → 转投周期短的二区/国内核心，保底突破。

### 📚 阶段7 核心知识点清单（补丁·切法A）

🔴 **LaTeX / Overleaf（L3，补"只提 Word/GB-T 7714"）**：IEEEtran / elsarticle 模板；公式、算法、参考文献(BibTeX)写法；Overleaf 协作。

🟡 **矢量图规范（L2）**：pgf/TikZ 或导出 pdf/svg；避免位图放大糊（阶段7 已提矢量，此处补"怎么做"）。

---

## 九、阶段8：大论文整合与答辩（研三，第28–36月）

**目标**：把方向3 全链条串成大论文。

| 月次 | 具体动作 | 练手/产出 | 自检标准 |
|---|---|---|---|
| 第28–30月 | 以"多时间尺度能-碳智能调控"为主线，整合各阶段成"问题—方法—实验—系统"四段式 | 大论文初稿 | 四段式完整 |
| 第31–32月 | 创新点凝练：多时间尺度预测 + 鲁棒/随机能-碳优化 + 数据挖掘增强 + 云框架 | 创新点清单 | ≥3 个清晰创新点 |
| 第33–34月 | 预答辩 → 盲审 | 盲审稿 | 通过盲审 |
| 第35–36月 | 答辩 | 答辩通过 | 拿到学位 |

**产出**：硕士学位论文、答辩通过。

### 🎯 阶段8 学到什么程度（详细）

- **整合（L3）**：能把"预测小论文 + 优化小论文 + 仿真 + 碳/挖掘 + 框架"串成"问题—方法—实验—系统"四段式大论文，不自相矛盾、数据前后一致。
- **创新点（L3）**：能凝练 ≥3 个清晰、可辩护的创新点（如多时间尺度预测、鲁棒/随机能-碳优化、数据挖掘增强、云框架），每个都能对应到实验验证。
- **答辩（L3）**：能 15 分钟讲清主线、能应对"为什么不用深度学习/为什么 Γ 取这个值/数据哪来的"等典型质疑。

### 📚 阶段8 核心知识点清单（补丁·切法A）

🔴 **LaTeX / Overleaf（L3）**：大论文多数模板为 LaTeX（IEEEtran / elsarticle / 学校学位论文 cls）；公式与参考文献 BibTeX 自动化；Overleaf 与本地 TeX Live 双通道。

🟡 **矢量图与版式规范（L2）**：图导出 pdf/svg；正文/图/表字号与字体统一；避免位图放大模糊。

---

## 附A：推荐课程总表（国内优先）

| 类别 | 课程 | 平台/出处 | 重点章节 |
|---|---|---|---|
| 电力系统 | 电力系统分析（孙宏斌） | 爱课程 icourses.cn | 稳态/潮流/频率电压调整 |
| 电力系统 | 电力系统分析（郝亮亮） | 中国大学MOOC | 最系统，10万+人选 |
| 数学基础 | 高数/线代/概率（宋浩） | B站 | 概率论是随机规划基础 |
| 机器学习 | 李宏毅《机器学习》 | 台大/B站搬运 | RNN/LSTM/Attention/无监督 |
| 深度学习 | 李沐《动手学深度学习》 | d2l.ai + B站 | Transformer/循环网络（代码驱动） |
| 凸优化 | Boyd《Convex Optimization》 | B站中字 | Ch2 凸集/Ch5 对偶/Ch11 鲁棒 |
| 最优化 | 最优化方法 | 清华/中科大公开课 | 线性与整数规划补充 |
| 数据挖掘 | 数据挖掘 | 哈工大/北大 MOOC | 聚类/关联规则（阶段5） |
| 软件工程 | FastAPI / Docker 入门 | B站 | 阶段6 云框架 |

## 附B：文献获取与管理工作流（Zotero + 茉莉花 + 知网免费下载）

### B.1 安装三件套
1. **Zotero 桌面端**：zotero.org/download 安装。
2. **浏览器扩展 Zotero Connector**：Edge/Chrome 扩展商店搜 "Zotero Connector"，装完右上角出现抓取图标。
3. **茉莉花（Jasminum）插件**（否则中文文献变乱码）：
   - 打开 github.com/l0o0/jasminum/releases，下**支持 Zotero 7 的最新 .xpi**（别解压）。
   - Zotero → 工具 → 插件 → 齿轮 → Install Add-on From File → 选 .xpi → 重启。
   - 重启后 编辑 → 设置 → 茉莉花：勾选"下载时自动重命名"，并把茉莉花翻译器优先级调最高。

### B.2 免费下知网（福建理工大学，合法免费用学校已购权限）
- **首选 CARSI（不用装软件）**：浏览器开 fsso.cnki.net → 搜"福建理工大学"→ 跳统一身份认证（学号+信息门户密码）登录 → 直接进知网免费下。
- **兜底 WebVPN**：webvpn.fjut.edu.cn 登录 → 融合门户 → 图书信息系统 → 所有数据库 → 清华同方知网。
- **校内网（宿舍/实验室 WiFi）**：从 lib.fjut.edu.cn 点"清华同方知网数据库"直接下。
- **密码忘了**：getpsw.fjut.edu.cn 找回。
- ⚠️ 别用"知网免费破解站/镜像站"——盗版+钓鱼，学校权限才是正道。

### B.3 抓取流程
1. 经 B.2 入口进知网，打开论文**详情页**（有摘要那页，非列表页）→ 点浏览器 Zotero 图标 → 元数据进 Zotero。
2. 下 PDF 全文：右键文献 → 知网助手 → 查找/下载附件；首次需填知网 Cookie（登录知网后 F12 → 应用程序 → Cookie → cnki.net 复制 SID/Ecp_ClientId 等值，粘到茉莉花设置里）。
3. 导出引文：Zotero 选中条目 → 右键"生成参考文献"→ 选 GB/T 7714（中文期刊标准格式）。

## 附C：数据集与数据获取（阶段2 用）

| 数据 | 来源 | 用途 | 读取方式 |
|---|---|---|---|
| 园区光伏/负荷 SCADA | 福州烟草绿色园区项目（你的实体数据，壁垒） | 主数据 | **Excel/.csv，用 openpyxl/pandas 读** |
| 公开光伏出力 | NREL PVWatts / NSRDB（美国）、国内光伏电站公开集 | 补充/对比 | csv |
| 公开负荷 | Open Power System Data（欧洲）、国内电网公开负荷曲线 | 负荷预测基线 | csv |
| 气象 | 中国气象数据网（nmc.cn）、ERA5 再分析 | 预测特征 | csv/netcdf |
| 碳因子 | 省级电网排放因子（生态环境部/电网公司年报）、实时碳强度 API | 时变碳建模 | csv/API |

> 公开数据注意版权与单位换算（kW/kWh、UTC+8）；原始大文件不进 GitHub，放 `data/raw/` 并写 `README` 说明来源。

## 附D：硬件调度与避坑（结合你的机器）

- **CPU（24核）= 主力**：优化求解（Gurobi）、数据预处理、仿真、多任务并行——方向3 约 80% 算力在这。
- **内存（48G 双通道）= 关键资产**：大数据预处理、特征工程、多任务不卡。**补内存优先级高于买显卡。**
- **GPU（5070 12GB）= 辅助**：只跑源-荷预测 DL；训练 `batch_size` 别贪大，开 `torch.cuda.amp` 混合精度省显存。12GB 小模型够，别堆大模型。
- **别碰**：本地大模型推理/微调（显存放不下，且导师无课题）。
- **重仿真用学校 HPC**，别死磕本地机。

## 附E：SCI / 核心期刊发表规划

### E.1 你的方向能发吗？——能，且是顶刊高产赛道
光储直柔 + 能-碳调控 + 多时间尺度 + 鲁棒/随机优化，是"双碳"国家战略下的绝对热点。2024–2025 年 **Applied Energy（IF~11，一区 TOP）**、**Energy Conversion and Management（IF~10，一区 TOP）** 密集发了同方向论文。连一区 TOP 都抢稿，说明不是发不出去，只在于发多好、发几篇。

### E.2 目标期刊分级
| 层级 | 期刊 | 分区/IF | 难度 | 定位 |
|---|---|---|---|---|
| 一区 TOP | Applied Energy / Energy Conversion and Management | 一区/10+ | 高（严、周期长） | 锦上添花 |
| 二区 | Energy / Renewable Energy / IEEE TSG / Sustainable Energy | 二区/9左右 | 中（硕士生可达） | **保底突破** |
| 二/三区 | IJEPES / Journal of Cleaner Production / Sustainable Cities and Society | 二/三区 | 中低 | 备选 |
| 国内核心 | 中国电机工程学报 / 电力系统自动化 / 电网技术 / 电工技术学报 | 国内顶 | 中 | 中文主力 |

### E.3 SCI 标准论文骨架（照着套）
1. **建模**：园区光储直柔系统 + 源荷不确定性（预测误差/光伏波动）；
2. **方法**：两阶段鲁棒/分布鲁棒(DRO) 或 随机规划，日前-实时滚动 + MPC；
3. **求解**：C&CG / Benders 分解（Gurobi）；
4. **碳维度**：时变碳因子约束 / 碳-经济 Pareto 权衡；
5. **算例**：真实/典型园区数据仿真，对比基线。

### E.4 你的差异化加分项（审稿人喜欢）
- **真实园区数据**（福州烟草绿色园区）——纯仿真论文最大软肋，你天生有数据壁垒。
- **时变碳因子建模**——申报书明确写了，多数论文把碳当常数。
- **云边调控框架落地**——别人给算法，你写系统型论文，偏工程差异化。

### E.5 现实预期
- 一篇**二区 SCI** 作小论文，周期 6–12 个月（含返修），硕士生完全可达。
- 大论文把"预测小论文 + 优化小论文"串起来，补仿真/框架章节。
- 一区 TOP 冲一冲可以，但审稿严、周期长，作"锦上添花"而非"保底"。

## 附F：你能产出的论文类型（按可发程度）

| 类型 | 核心方法 | 创新点 | 目标期刊 | 难度 |
|---|---|---|---|---|
| ①源-荷多尺度预测 | statsmodels基线→LightGBM→LSTM/PatchTST+概率预测 | 多尺度融合+不确定性量化 | 太阳能学报/电网技术 | 低（先上手） |
| ②两阶段DRO优化调度 | 分布鲁棒+日前-实时滚动+MPC | 碳约束+C&CG求解 | 中国电机工程学报/电力系统自动化/电工技术学报 | 中（核心） |
| ③能-碳协同优化 | 时变碳因子+碳-经济Pareto | 时变碳强度建模 | 电力系统自动化/中国电力 | 中 |
| ④云边调控框架 | 云计算+大数据挖掘策略优化 | 系统框架落地 | 自动化学报/系统仿真学报 | 中-高 |
| ⑤综述 | 梳理光储直柔能-碳 | 时间紧保底 | 电网技术/供用电 | 低但价值低 |

> 硕士生一般冲 **①+②**（一篇预测小论文 + 一篇优化小论文），把②做大论文核心，③④作延伸。

## 附G：读书清单（实体/电子，按阶段）

- **专业基础**：《电力系统分析》（何仰赞/温增银 或 对应教材）、《微电网运行控制》（国内教材）。
- **数学优化**：Boyd《Convex Optimization》（中译/英文）、Ben-Tal《Robust Optimization》、最优化方法教材（袁亚湘等）。
- **机器学习**：李宏毅讲义、李沐《动手学深度学习》(d2l.ai 免费在线书)。
- **电力系统仿真**：pandapower / PyPSA 官方文档（在线）。

## 附H：可复用 environment.yml（阶段2/3 直接落地）

```yaml
name: PV_Storage_Flex_Control
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11        # 注：你实际环境是 3.14，已实测可用；新装建议 3.11 以兼容未来 cu128 torch
  - pip
  - numpy
  - pandas
  - scipy
  - matplotlib
  - seaborn
  - scikit-learn
  - pip:
      - openpyxl            # 读 .xlsx 真实数据
      - xlrd                # 读旧 .xls
      - statsmodels         # ARIMA/SARIMA 经典预测基线
      - sktime              # 统一时序 ML 框架（可选）
      - xgboost
      - lightgbm
      - cvxpy
      - pyomo               # 代数建模语言，MILP/MINLP
      - gurobipy            # 需 grbgetkey 激活（你已激活）
      - pandapower
      - pypsa
      - networkx
      - jupyter
      - notebook
      - ipykernel
      - plotly
      # 5070 到货后再装（cu128 确保 Blackwell 识别）：
      # torch torchvision torchaudio --index-url https://mirrors.aliyun.com/pytorch-wheels/cu128/
```

> 注意：`gurobipy` 装完仍需 `grbgetkey` 激活学术许可才能用（你已完成）。torch 的 `cu128` 索引确保 5070（Blackwell）可被识别，**买卡前不要装**。

## 附I：导师组设备诊断方向（可选横向拓展，非主线）

若导师安排做设备故障诊断（CNN-LSTM / GNN / 迁移学习 / 零样本），可额外学：PyTorch 进阶、PyG（图神经网络）、迁移学习（DANN/CDAN）。这部分用 5070 跑小模型也够，但**主线始终围绕方向3**。

## 附J：第一个练手项目（现在就能跑，验证环境）

你已有的 `D:\python project\PV_Storage_Flex_Control` 项目就是研究内容三的代码实现（源-荷预测 → 鲁棒/随机优化 → 云边 MPC → 仿真验证，目标含经济成本+碳排放双优化）。环境装齐后第一步：

```bash
conda activate PV_Storage_Flex_Control
cd "D:\python project\PV_Storage_Flex_Control"
python run_content3_local.py --no-validation
```

能出"月度预测 RMSE ≈ 18.3%、稳定运行占比 100%"就说明整套 CPU 科研栈在你的机器上通了——这就是你正式接手研究内容三的第一步。Gurobi 没激活时，代码会自动退回到 scipy/CVXPY 的免费解法，不影响跑通（你已激活，直接用 Gurobi）。

## 附K：公式速查手册（方向3 高频公式，背+推）

> 写论文/建模时直接抄，单位统一用 kW/kWh（小系统）或 MW/MWh（pandapower）。

**1. 预测评估**
- RMSE $= \sqrt{\frac1n\sum (y_i-\hat y_i)^2}$（越小越好，单位同 y）
- MAE $= \frac1n\sum|y_i-\hat y_i|$
- MAPE $= \frac{100\%}{n}\sum|\frac{y_i-\hat y_i}{y_i}|$（光伏夜间为0慎用）
- R² $= 1-\frac{\sum(y_i-\hat y_i)^2}{\sum(y_i-\bar y)^2}$（越接近1越好）

**2. 优化（两阶段鲁棒）**
- 主问题：$\min_x c^Tx + \eta,\; \eta\ge \max_{u\in\mathcal U}\min_y d^Ty$
- 不确定集（盒+预算）：$\mathcal U=\{u:\underline u\le u\le\bar u,\;\sum_i\frac{|u_i-\hat u_i|}{\hat u_i}\le\Gamma\}$
- CCG：下界 $LB\uparrow$、上界 $UB\downarrow$，$|UB-LB|<\epsilon$ 停

**3. 储能 / SOC**
- $SOC_t = SOC_{t-1} + (\eta_c P_{ch,t} - P_{dis,t}/\eta_d)\Delta t/C_{cap}$
- 充放电互斥：$u_{ch}+u_{dis}\le 1$（0/1 变量）
- 功率限值：$0\le P_{ch}\le u_{ch}P^{max},\;0\le P_{dis}\le u_{dis}P^{max}$

**4. 碳计量（时变）**
- 电网碳排：$E_t = EF_t\cdot P_{grid,t}$（$EF_t$ 第 t 小时碳因子，kgCO₂/kWh）
- 目标项：$\min \sum_t (\pi_t P_{grid,t} + \lambda EF_t P_{grid,t})$
- 碳-经济 Pareto：固定碳上限 $E\le \bar E$ 求最小成本，或加权 $\min \alpha\cdot Cost + \beta\cdot E$

**5. 电力系统**
- 标幺：$Z_{pu}=Z/Z_{base},\;Z_{base}=V_{base}^2/S_{base}$
- 节点功率（极坐标）见阶段1

## 附L：组会与导师沟通话术（少走弯路）

- **周报/组会结构**：本周做了什么（1–3 点，带数字）→ 遇到什么卡点 → 下周计划（具体产出）。
- **找导师定方向**：别问"老师我该做什么"，改问"我列了 3 个候选问题（A/B/C），您看哪个更贴合课题/更容易出论文？"——给选项而非抛问号。
- **被质疑"为什么不用深度学习"**：答"本项目 GPU 资源有限且数据规模适中，树模型+XGBoost 已逼近深度学习精度，且可解释性更强；深度学习作为后续增强（阶段2 末尾/阶段5）"。
- **被质疑"为什么 Γ 取这个值"**：答"做了 Γ∈[0,5] 敏感度扫描，在 Γ=2 附近成本-鲁棒权衡最优（附曲线）"。
- **数据来源说清**：福州烟草绿色园区实体项目（SCADA 采集），已脱敏，合法合规。

## 附M：里程碑甘特图（36 个月总览）

```
月份  1   4   8   12  16  20  24  28  32  36
阶段0 ██
阶段1 ████████
阶段2      ████████
阶段3            ████████
阶段4                  ████████
阶段5                        ████████
阶段6                              ████████
阶段7                                    ████████
阶段8                                          ████████████
小论文①         ▲(第8月成稿)
小论文②                   ▲(第16月成稿)
英语四级    ▲(第6月)   六级 ▲(第12月)
48G内存  ▲(第1月)
```

> ▲ = 该里程碑达成时间点。实际进度以你每周打卡为准，甘特图只是"别拖太狠"的预警线。

## 附N：求职与证书路线图（结合你点名的城市）

> 详细薪资/证书已在对话中核实，这里给"在校必办清单"。

**在校必办（成本低、双线加分）**
- [ ] 英语四级 → 六级（在校唯一窗口，毕业不能报）
- [ ] 计算机二级（Python）
- [ ] 注册电气工程师**基础考试**（应届可报，工作后考专业）
- [ ] 碳资产管理师（初级，认准人社部备案机构，别报山寨）

**求职目标城市对照（2025-2026 校招核实值 · 硕士）**

> 数据来自 2025-2026 校招公开报道（储能并网/储能方向报道、宁德官网硕士薪资区间、阳光电源/CSDN 报告、电气研究生薪资文）。校招薪资 = 月薪 × 月薪数（通常 14–15），签字费/股票另计。**SP = Special Offer（给最顶尖候选，普遍 985/强科研），SSP 更高**。

| 企业 / 方向 | 硕士校招（2025-2026 核实） | 岗位序列 / 职级 | 主要 base | 院校门槛 |
|---|---|---|---|---|
| 华为数字能源（储能方向） | 30K×15 ≈ **45 万**；顶尖 SP 可 35K×16+ | 研发工程师（19–20 级）；储能并网/EMS/PCS 算法 | 东莞/深圳/上海 | 双非可投，SP 难 |
| 宁德时代（储能事业部） | 28K×15 + 签字费；官网硕士 **20–50 万** 起 | 电芯/系统研发、BMS 算法、储能集成 | **福建宁德**（总部） | **不设院校硬门槛**，地利 |
| 阳光电源（储能 / 电力电子） | 储能 25K×14 ≈ **35 万**；硬件岗 23.5K×15 ≈ 35 万 | 电力电子硬件 / 储能软件算法 | 合肥/深圳 | 双非可投，看项目 |
| 华为研发（电力电子 / 新能源） | 硕士 **25–30 万**，顶尖 SP 30 万+ | 硬件研发（19 级起） | 深圳/上海/西安 | 双非校招偏难 |
| 储能方向整体 | 硕士起薪中位数 **18K(2024)→26.5K(2025)，涨幅 47%**（电气方向第一） | — | — | 供需 ≈2:1 利好 |

**分城市锚点**
- 福建（福州/厦门）：走**南网**（对双非更友好），市局 18–25 万；**宁德时代 35–45 万**（最推荐：总部在省内、不卡学历）。
- 广东（广州/深圳）：广州南网市局 18–25 万；深圳民企上限最高 **30–50 万+**（华为/比亚迪/储能创业）。
- 杭州：正泰 / 阿里数字能源 / 储能创业，算法硕士 20–35 万。

**双非务实定位（你最敏感的点，给概率判断，不灌鸡汤）**
- **客观事实**：院校梯队公开名单（清华/西交/上交/华科 一梯；厦大/天大/哈工大/华北电力 二梯；三峡/长沙理工 区域）**未含福建理工**——不回避。
- **关键缓冲有三**：① 储能供需严重失衡——2025 秋招某头部储能 31 个硕博岗仅 63 份简历（≈2:1），远好于国网热门市局 30:1；② 宁德时代总部在**福建宁德**，地缘 + 不设院校硬门槛；③ 你的**硬筹码**是"鲁棒/随机优化 + 真实园区数据 + 碳因子建模 + 可演示 demo"，在算法/研发岗比院校标签更被看重。
- **概率判断（务实版）**：
  - 进华为/宁德/阳光**算法岗**：双非被初筛刷掉 ≈ **30–40%**（民企更看项目）；
  - 进**国网市局**：双非劣势更大（热门市局 30:1，建议走**南网福建**，对双非明显更友好）；
  - 用 **1–2 篇论文 + 可跑 demo** 能直接对冲掉大部分初筛劣势——这是你手上杠杆最大的事。
- **各厂"卡/不卡"与简历绕法**：
  - 华为：校招系统有院校筛选，但**储能算法岗更看笔试+项目**，简历写清"鲁棒优化+园区数据+碳因子+可演示"，附 GitHub 链接；走**天才少年/实习生转正**比纯校招更绕开学历。
  - 宁德：**几乎不卡双非**（总部福建），重点突出"BMS/储能系统集成/电池退化建模"相关；提前投**暑期实习**转正是最短路径。
  - 阳光/正泰/储能创业：**基本不卡**，作品集 > 学历，把 demo 录屏 + 论文 preprint 放简历最前面。
  - 国网/南网：电网体制内**院校+户籍**权重高，福建理工走**南网福建**比国网华北/华东市局现实得多。
- **时间线建议**：研一上搞定环境与论文方向 → 研一下出小论文①+投宁德/阳光暑期实习 → 研二上小论文②+秋招提前批（民企 7–8 月就开始）→ 研二下答辩+签约。

**核心议价筹码**：把"鲁棒/随机优化 + 真实园区数据 + 碳因子建模"做成 1–2 篇论文/可演示 demo——这是深圳/杭州储能算法岗最硬的筹码；**宁德时代**是双非最友好的"高薪+地缘"选项，优先级排第一。

---

## 附O：每周执行脚本（前 12 周可直接复制，后面照阶段套）

> 原则：每周一先看"这周要敲什么"，周五对照"这周该出什么"。命令默认在 **PowerShell（已 `conda activate PV_Storage_Flex_Control`）** 里跑，Python 代码在 VS Code 新建 `.py` 跑。

### 第 1 周：把现有项目跑通（验证环境真能用）
终端：
```powershell
conda activate PV_Storage_Flex_Control
cd "D:\python project\PV_Storage_Flex_Control"
python run_content3_local.py --no-validation
```
预期输出：打印"月度预测 RMSE ≈ 18.3%、仿真稳定运行占比 100%"。
- 若报错 `ModuleNotFoundError: xxx` → `pip install xxx`（自动走已设的清华镜像）。
- 若报 Gurobi 许可错 → 暂时无妨，代码会回退免费解法；正式激活见 §0.2 第 5 条。

### 第 2 周：项目纳入 Git（防丢 + 练 GitHub Desktop）
终端（项目根目录）：
```powershell
git init
git add .
git commit -m "init: 研究方向3 初版（预测+优化+仿真）"
```
再用 **GitHub Desktop**（你已装、账号 @glatm）→ Add Existing Repository → 推到 `Optimal-Energy-System-Sc...` 仓。以后每次大改动 `commit` 一次，这就是你的科研版本保险箱。

### 第 3–4 周：pandapower 最小微网潮流（阶段1 达标线）
在 `src/` 下新建 `stage1_pandapower_demo.py`，粘贴阶段1 §练习1.1 代码，跑通后 `res_bus` 能看到每条母线电压。目标：搞懂"建元件 → 连成网 → runpp → 读结果"四步。

### 第 5–6 周：第一个预测基线（阶段2 起步，直接调你项目现成函数）
```python
from src.forecasting.content3_forecast import build_features, train_linear, evaluate_nextstep
import pandas as pd
df = pd.read_csv("data/processed/hourly_load.csv", parse_dates=["time"])
X, y = build_features(df, lags=[1, 2, 3, 24], windows=[3, 6])
w = train_linear(X, y, reg=1e-2)
print("下一步预测 RMSE:", evaluate_nextstep(df))
```
预期：打印一个 RMSE 数值（kW 级）。这步让你**读懂项目预测模块的输入输出**，不用从零写。

### 第 7–8 周：把预测画出来（matplotlib）
```python
from src.forecasting.content3_forecast import forecast_from_recent
import matplotlib.pyplot as plt
fc = forecast_from_recent(recent_values, steps=24)
plt.plot(fc); plt.title("未来24h负荷预测"); plt.savefig("fig_forecast.png", dpi=300)
```
预期：生成 `fig_forecast.png`（dpi=300，能直接贴组会）。

### 第 9–10 周：第一个优化（确定性调度，能-碳双目标）
```python
from src.optimization.energy_carbon_optimizer import solve_deterministic
from src.optimization.scenarios import build_scenarios
sc = build_scenarios(24)
res = solve_deterministic(sc[0], soc0=0.5)
print("购电成本:", res["cost"], "碳排:", res["carbon"])
```
预期：打印经济成本与碳排放两个数字 → 你已经跑通"能-碳双目标优化"。

### 第 11–12 周：鲁棒 vs 随机 对比（阶段3 起步）
```python
from src.optimization.energy_carbon_optimizer import solve_robust, solve_stochastic
rob = solve_robust(scenarios, probs)
sto = solve_stochastic(scenarios, probs)
# 对比两者在最坏场景下的成本，理解 Γ/保守度 权衡（公式见附K）
```
预期：两个结果都能跑出；这周重点是**读懂 `build_scenarios` 怎么构造光伏/负荷区间带**（就是鲁棒优化的"不确定集"）。

> 第 13 周以后，按阶段3→阶段8 的"周任务打勾清单"继续，模式一样：**先调项目现有函数 → 再换你的真实数据 → 最后加方法（深度学习/两阶段/EMS）**。

## 附P：论文精读方法 + 必读短清单（带提取点）

### P.1 三遍读法（别从头读到尾，研究生时间贵）
- **第 1 遍（10 分钟，扫）**：标题/摘要/结论/图表。判断——跟方向3 有关吗？值不值得精读？
- **第 2 遍（30 分钟，拆）**：引言（找研究空白）+ 方法（找可复现算法/公式）+ 实验（找对比基线）。**边读边在 Obsidian 填下面的提取表**。
- **第 3 遍（1 小时，抠）**：复现核心公式/算法，想"我能改哪里变成创新点"。

### P.2 提取模板（每读一篇填一张，直接进 Obsidian）
| 字段 | 填什么 |
|---|---|
| 题名/作者/年/来源 | 完整引用，方便 Zotero 导入 |
| 解决什么问题 | 一句话 |
| 用什么方法 | 算法名 + 关键公式编号 |
| 数据集/案例 | 真实还是仿真？规模？ |
| 对比基线 | 它和谁比，赢在哪 |
| 我的可借鉴点 | 哪个函数/思路能搬进 `PV_Storage_Flex_Control` |
| 我的差异化 | 它没做、但我方向3 能补的（真实园区数据/时变碳/云边） |

### P.3 必读短清单（先读这 6 篇，带提取点）
1. **马恺珧 等 (2023). 微电网优化调度综述**（知网搜）→ 提取：方向3 的"研究空白"地图，帮你定位创新点。
2. **李嘉伟 等 (2024). 基于分布鲁棒模型预测控制的微电网多时间尺度优化调度. 电力工程技术** → 提取：DRO-MPC 怎么做多时间尺度（直接对标阶段3/4）。
3. **Bertsimas, D., & Sim, M. (2004). The Price of Robustness. Operations Research** → 提取：鲁棒优化不确定集（Γ 参数）标准构造，是你 `solve_robust` 的理论根。
4. **Gabash, A., & Li, P. (2012). Active-reactive optimal power flow with storage** → 提取：储能参与调度的约束写法（对应 `energy_carbon_optimizer` 的 SOC 约束）。
5. **搜索 `carbon-aware microgrid scheduling` / `time-varying carbon intensity optimization`** → 提取：时变碳因子主流建模（阶段5 核心）。
6. **搜索 `cloud-edge energy management microgrid`** → 提取：云边两级架构（阶段6 框架来源）。

> 第 3、4 篇是英文，正好当**学术英语训练材料**（配合 `英语学习路线.md`）：先读摘要+结论，再逐句拆长难句。

---

## 附Q：核心阶段「逐行代码实现」练习（已用你的真实数据验证可运行）

> 下面两个文件已放进你项目 `exercises/` 目录，并用 `PV_Storage_Flex_Control` 环境的真实 `hourly_load.csv` 跑通验证过。
> 它们是「阶段2/阶段3」从"看文档"到"真会写"的桥梁：**每一行都有中文注释**，照着读一遍等于把那章核心代码手写一遍。

### Q.1 `exercises/ex02_forecast_practice.py` → 阶段2 预测入门
**它在干啥（逐段对应代码注释）**：
1. 加载数据：优先 `common.load_hourly_load()`（学会复用项目函数），失败兜底直读 csv
2. 构造样本：用「过去 24 小时负荷 + 小时 + 星期几」预测「下一小时负荷」（时序监督学习标准做法）
3. 划分：按时间切，最后 7 天当测试（**时序不能随机洗牌，否则泄露**）
4. 训练：naïve 基线 / 岭回归 / XGBoost 三个模型
5. 评估：RMSE、MAE
6. 画图：测试集前 48 小时真实 vs 预测

**期望输出（你跑出来应看到）**：
```
[数据] 通过 common.load_hourly_load() 加载，共 2184 行
[样本] 共构造 2160 条样本，每条特征维度 26
naïve 基线   : RMSE=2.910  MAE=1.224
XGBoost      : RMSE=4.069  MAE=1.967
⚠️ 本数据上 XGBoost 没能超过 naïve 基线……重要教训：强周期/小样本时简单基线很难被超越
```
> ⚠️ 注意：脚本**不会**骗你说"XGBoost 一定赢"。本数据上它确实没赢——这正是真实现象，也是阶段2 要解决的"如何用更聪明特征/更多数据打赢基线"的真功夫。

**你改哪里练手**：
- 把 `L = 24` 改成 48/168，看窗口变长误差怎么变
- 在特征里加「历史同日同时刻负荷」「温度」（阶段2 后期）
- 调 `XGBRegressor(n_estimators=, max_depth=, learning_rate=)` 做 GridSearch（见手册 §6 进阶）

### Q.2 `exercises/ex03_optim_practice.py` → 阶段3 优化入门
**它在干啥（逐段对应代码注释）**：
1. 准备 24 小时的电价 `prices`、碳因子 `carbon`、净负荷 `net`（负荷−光伏）
2. 定义变量：`charge/discharge`（充放电功率）、`soc`（电量）、`grid`（购电）
3. 约束：SOC 递推、功率上限、SOC 范围、功率平衡
4. 目标：min（购电成本 + λ·碳成本），双目标加权
5. 求解：`prob.solve()`
6. 对比"不用储能"成本 + 画图（自动存 `exercises/ex03_result.png`）

**期望输出（你跑出来应看到）**：
```
总经济成本   : 157.46 元
总碳排放     : 0.2237 吨 CO2
[对比] 不用储能时的购电成本: 291.60 元
[结论] 用储能省下: 134.14 元 (46.0%)
```

**你改哪里练手**：
- 调 `lam`（碳权重）：调大 → 放电更偏向碳因子低的时段
- 调 `Pmax / Emax`：看储能容量对省钱幅度的影响（阶段3 容量优化配置雏形）
- 加"充放电互斥"整数约束：当前是连续松弛版，严格版见手册 §10（Pyomo+Gurobi）
- 把 `net` 换成你项目 `compute_net_load` 的真实净负荷

---

## 附R：知识点补全与课题四层对齐（2026-08-10 合入，来源 `知识点补全补丁.md`）

> 本附录与各阶段 `📚 阶段X 核心知识点清单`（切法A 内插）配套：提供"课题四层 → 计划"的对齐视角、跨阶段共性缺口与每周自检速查。公式/误区已在各阶段正文，此处不重复抄。

### R.1 课题四层 vs v5 计划对齐表

| 课题层 | 申报书/自述原文要求 | v5 对应情况 | 差距判定 |
|---|---|---|---|
| ① Simulink 48V/700V 直流微电网物理模型（光伏/储能/双向DC-DC/柔性空调/故障保护） | 电力电子 + 控制理论 + 故障保护 | 原只提 pandapower/PyPSA 稳态潮流；现阶段1+4 已补 DC/DC、MPPT、PID、下垂、故障保护知识点 | ✅ 已补（见阶段1/4 📚） |
| ② Keras STL-LSTM 混合 + 自适应修正 | STL 分解 + LSTM + Keras + 自适应修正 | 阶段2 原只有 LSTM/PatchTST；现补 STL-LSTM 混合 + Keras + 自适应修正 | ✅ 已补（见阶段2 📚） |
| ③ PuLP 鲁棒优化 + 日内 MPC | PuLP + 鲁棒 + MPC | 阶段3 用 Pyomo+Gurobi（合理升级，已加说明）；MPC 理论补为知识点 | ✅ 已补（见阶段3 📚） |
| ④ Docker + MQTT + InfluxDB + Grafana | MQTT、Grafana 明确在列 | 阶段6 原漏 MQTT/Grafana；现已补 | ✅ 已补（见阶段6 📚） |

### R.2 跨阶段共性缺口（已部分落入各阶段）

1. **电池退化/循环寿命成本建模**：阶段1 已补 Thevenin+退化成本概念；阶段3 目标函数可在 SOC 约束旁加一项 `C_deg·∑|P_ch/dis|`（建议写成可选软约束，避免不可行）。
2. **PuLP vs Pyomo/Gurobi 取舍**：已在阶段3 📚 首条说明（PuLP 仅适合中小 LP/MILP；CCG 主从需 Gurobi 强求解器）。
3. **记忆索引**：本计划现已至 附R（A–R 共 18 个附录），长期记忆索引已同步更新。

### R.3 知识掌握自检速查（按课题线归并，每周勾）

**课题① 物理建模线**（阶段1+4）：
- [ ] 双向 DAB / Buck-Boost 原理与占空比
- [ ] MPPT（P&O / INC）
- [ ] 下垂控制与母线电压稳定
- [ ] 电池 Thevenin 模型 + 退化成本
- [ ] 需求响应（价格型/激励型）
- [ ] PID 三环闭环 + 稳定性判据
- [ ] 故障保护逻辑（过流/短路隔离）

**课题② 预测线**（阶段2）：
- [ ] STL 分解 + 分量预测叠加
- [ ] Keras LSTM/GRU 实现
- [ ] 自适应修正策略
- [ ] CRPS / 区间覆盖率评价

**课题③ 优化线**（阶段3）：
- [ ] MPC 滚动时域理论
- [ ] 机会约束规划 CCP
- [ ] 场景缩减
- [ ] NSGA-II / Pareto
- [ ] 鲁棒 max-min 对偶变换

**课题④ 系统线**（阶段6）：
- [ ] MQTT 发布订阅 + QoS
- [ ] Grafana + InfluxDB 看板
- [ ] 云-边协同分工

**通用**（阶段0/7/8）：
- [ ] LaTeX / Overleaf 模板
- [ ] 矢量图导出规范

---

* v5 增补附O/附P/附Q、附R：新增「附O 每周执行脚本」「附P 论文三遍读法+提取模板+必读6篇」「附Q 核心阶段逐行代码实现练习（exercises/ex02、ex03，已用真实数据验证）」「附R 知识点补全与课题四层对齐（含课题四层对齐表 + 跨阶段共性缺口 + 自检速查，并已在阶段0–8 内插 📚 核心知识点清单）」。配套文档见 `D:\python project\Python模块速查手册.md`（已新增 §23 实战菜谱 / §24 你的真实项目逐文件导读 / §25 核心模块逐行精解）。*
