# 光伏储能直流微电网仿真 —— 完整知识体系

> 覆盖全部理论基础、建模规则、调试经验与实验数据。本文档力求让**完全零基础的初学者**也能看懂并复刻整个系统。所有公式使用标准 LaTeX 语法，适配 Typora。

---

## 目录

- **第一篇 · 原理（理论篇）**
 - [1. 光伏组件特性与 MPPT](#1-光伏组件特性与-mppt)
 - [1.1 单二极管模型（Simscape Solar Cell 的物理基础）](#11-单二极管模型simscape-solar-cell-的物理基础)
 - [1.2 MPPT 算法原理与步骤（700V 系统将实现）](#12-mppt-算法原理与步骤700v-系统将实现)
 - [2. Boost 升压变换器原理](#2-boost-升压变换器原理)
 - [2.1 CCM/DCM 边界与纹波计算](#21-ccmdcm-边界与纹波计算)
 - [2.2 二极管反向恢复与 Boost 开关损耗](#22-二极管反向恢复与-boost-开关损耗)
 - [3. 双向 Buck-Boost 变换器原理](#3-双向-buck-boost-变换器原理)
 - [4. 半桥浮地驱动与死区时间](#4-半桥浮地驱动与死区时间)
 - [4.1 死区时间定量设计准则](#41-死区时间定量设计准则)
 - [5. 稳态功率平衡校验](#5-稳态功率平衡校验)
 - [5.1 稳态功率平衡的量化校验与效率估算](#51-稳态功率平衡的量化校验与效率估算)
 - [6. PID 控制理论](#6-pid-控制理论)
 - [二、平均电流控制（备选方案，§6 子节）](#二平均电流控制电流模式备选方案day-7-学)
 - [7. 软启动原理](#7-软启动原理)
 - [7.1 软启动实现方式](#71-软启动实现方式)
 - [8. Simulink / Simscape 建模规则](#8-simulink--simscape-建模规则)
 - [8.1 代数环识别与处理](#81-代数环识别与处理)
 - [9. 双源并联下垂控制（理论）](#9-双源并联下垂控制理论)
 - [9.1 线路阻抗对下垂均流的影响](#91-线路阻抗对下垂均流的影响)
 - [11. 扰动测试步骤与波形分析](#11-扰动测试步骤与波形分析)
 - [11.1 暂态指标与阻尼比/带宽的对应关系](#111-暂态指标与阻尼比带宽的对应关系)
 - [12. 隔离型双向 DC/DC 与软开关](#12-隔离型双向-dcdc-与软开关)
 - [12.1 深化：扩展移相、回流功率与 SiC 选型](#121-深化扩展移相回流功率与-sic-选型)
 - [12.2 DAB 损耗定量分解与效率优化](#122-dab-损耗定量分解与效率优化)
 - [13. 直流微电网故障特性与保护（前瞻，极间短路已实测验证）](#13-直流微电网故障特性与保护前瞻极间短路已实测验证)
 - [13.1 光伏侧电压在极间短路时的行为（对应 实测 V\_pv 波形）](#131-光伏侧电压在极间短路时的行为对应-实测-v_pv-波形)
 - [13.2 直流故障为何"不可控"：与交流的本质区别](#132-直流故障为何不可控与交流的本质区别)
 - [14. 直流微电网分层控制架构（EMS 前瞻）](#14-直流微电网分层控制架构ems-前瞻)
 - [15. 直流微电网稳定性与恒功率负载 CPL（稳定性前瞻）](#15-直流微电网稳定性与恒功率负载-cpl稳定性前瞻)
 - [15.1 负载的三种类型与等效模型](#一负载的三种类型与等效模型day-15-学)
 - [15.2 CPL 的 Simulink 建模与柔性负载仿真验证（仿真落地，对接申报书第②项）](#152-cpl-的-simulink-建模与柔性负载仿真验证仿真落地对接申报书第②项)
 - [16. 储能电池等效模型与 SOC 估算（电池建模基础）](#16-储能电池等效模型与-soc-估算电池建模基础)
 - [17. 母线支撑电容与功率电感选型（参数依据）](#17-母线支撑电容与功率电感选型参数依据)
 - [19. 数字逻辑基础：触发器 SR/D/T（保护用）](#19-数字逻辑基础触发器-srdt保护用)
- **第二篇 · 名词解释（术语与定义）**
 - [20. 名词术语解释](#20-名词术语解释)
 - [20.0 基础术语（电力电子 / 控制 / 储能 / 仿真 / 故障保护 / 700V 隔离）](#200-基础术语电力电子--控制--储能--仿真--故障保护--700v-隔离)
 - [21. 系统拓扑演进史](#21-系统拓扑演进史)
 - [21.1 开环基线（48V 物理底座）](#211-开环基线48v-物理底座)
 - [21.2 插入半桥双向 Buck-Boost（电池能量可控）](#212-插入半桥双向-buck-boost电池能量可控)
 - [21.3 加 PI 电压闭环（母线自动稳 48V）](#213-加-pi-电压闭环母线自动稳-48v)
 - [21.4 扰动测试台（拓扑不变，加扰动+记录）](#214-扰动测试台拓扑不变加扰动记录)
 - [21.5 双源并联下垂（多源无通信均流）](#215-双源并联下垂多源无通信均流)
 - [21.6 报告整合（拓扑无变化）](#216-报告整合拓扑无变化)
 - [21.7 极间短路故障注入（在 2.5 之上加故障支路）](#217-极间短路故障注入在-25-之上加故障支路)
 - [21.8 过流+欠压保护（给故障支路装"跳闸"）](#218-过流欠压保护给故障支路装跳闸)
 - [21.9 700V + 隔离 DAB（电压等级与架构升级）](#219-700v--隔离-dab电压等级与架构升级)
 - [21.10 700V 终态三环闭环集成](#2110-700v-终态三环闭环集成day-1415-验收版)
 - [21.11 平均模型（信号流功率平衡，无功率拓扑）](#2111-平均模型day-16信号流功率平衡无功率拓扑)
 - [21.12 直流电压等级优选对比仿真（对接申报书第①项）](#2112-直流电压等级优选对比仿真对接申报书第①项)
 - [21.13 多场景直流微电网拓扑与可复制性验证（对接技术路线"多场景拓扑模型"）](#2113-多场景直流微电网拓扑与可复制性验证对接技术路线多场景拓扑模型)
 - [22. 元器件参数库（按元器件类型分类）](#22-元器件参数库按元器件类型分类)
 - [22.1 半导体开关器件（MOSFET / 二极管 / 理想开关 / 双全桥）](#221-半导体开关器件mosfet--二极管--理想开关--双全桥)
 - [22.2 无源储能与滤波元件（电感 / 电容）](#222-无源储能与滤波元件电感--电容--电阻)
 - [22.3 能量源（光伏 / 电池）](#223-能量源光伏--电池)
 - [22.4 负载与故障注入（电阻）](#224-负载与故障注入电阻--开关)
 - [22.5 控制器与信号处理（PI/PID / 下垂 / 饱和 / 延时 / 比较 / PWM / 类型转换 / 增益 / 软启动 / 前馈）](#225-控制器与信号处理pi--下垂--饱和--延时--比较--pwm--类型转换--增益--软启动--前馈)
 - [22.6 传感与测量（电压 / 电流传感器）](#226-传感与测量电压--电流传感器--域接口)
 - [22.7 故障与保护链（开关 / 断路器 / 锁存 / 逻辑）](#227-故障与保护链开关--断路器--锁存--逻辑)
 - [22.8 700V DAB 专用（变压器 / 载波 / 移相 / 阈值 / 求解器）](#228-700v-dab-专用变压器--载波--移相--阈值--求解器)
 - [22.9 绝缘监测（IMD，系统级安全）](#229-绝缘监测imd系统级安全)
 - [23. 公式速查表](#23-公式速查表)
 - [23.1 电力电子 — Boost（CCM）](#231-电力电子--boostpv-侧ccm)
 - [23.2 双向 Buck-Boost（电池侧半桥）](#232-双向-buck-boost电池侧半桥)
 - [23.3 DAB（单移相 SPS）](#233-dab单移相-sps)
 - [23.4 PID / PI](#234-pid--pi)
 - [23.5 下垂控制（Droop）](#235-下垂控制droop)
 - [23.6 电池 / 储能](#236-电池--储能)
 - [23.7 MPPT（最大功率点跟踪）](#237-mppt最大功率点跟踪)
 - [23.8 故障与保护](#238-故障与保护)
 - [23.9 仿真工程（选型原则与公式）](#239-仿真工程选型原则与公式)
 - [25. 模块固定搭配与功能电路](#25-模块固定搭配与功能电路)
 - [25.1 Boost 升压电路（PV → 母线，固定占空比）](#251-boost-升压电路pv--母线固定占空比)
 - [25.2 半桥浮地驱动搭配（栅极驱动）](#252-半桥浮地驱动搭配栅极驱动)
 - [25.3 PWM 调制搭配（载波 + 比较 → 门极方波）](#253-pwm-调制搭配载波--比较--门极方波)
 - [25.4 PI 电压闭环 + 软启动搭配（母线调压）](#254-pi-电压闭环--软启动搭配母线调压)
 - [25.5 双向半桥电池变换器搭配（电池 ↔ 母线）](#255-双向半桥电池变换器搭配电池--母线)
 - [25.6 双源下垂并联搭配（无通信均流）](#256-双源下垂并联搭配无通信均流)
 - [25.7 数据记录搭配（波形导出）](#257-数据记录搭配波形导出)
 - [25.8 极间短路故障注入搭配](#258-极间短路故障注入搭配)
 - [25.9 过流 + 欠压保护逻辑搭配](#259-过流--欠压保护逻辑搭配)
 - [25.10 DAB 隔离双向驱动搭配](#2510-dab-隔离双向驱动搭配)
 - [25.11 信号–物理域接口搭配（Converter 对）](#2511-信号物理域接口搭配converter-对)
 - [25.12 物理网络地基搭配（每个模型的"第0步"，全部通用）](#2512-物理网络地基搭配每个模型的第0步全部通用)
 - [25.13 扰动注入搭配（光照突变 + 负载投切，/12 通用测试台）](#2513-扰动注入搭配光照突变--负载投切12-通用测试台)
 - [25.14 多斜坡软启动对齐搭配（定式）](#2514-多斜坡软启动对齐搭配定式)
 - [25.15 破代数环搭配（Transport Delay 定式，/14 反复用）](#2515-破代数环搭配transport-delay-定式14-反复用)
 - [25.16 电气门极直驱搭配（理想开关版栅极驱动）](#2516-电气门极直驱搭配理想开关版栅极驱动)
 - [25.17 700V 开环建压搭配](#2517-700v-开环建压搭配day-1314-早期验证版闭环前必经)
 - [25.18 Buck 降压负载供电搭配（700V 母线 → 48V 负载，拓扑）](#2518-buck-降压负载供电搭配700v-母线--400v-负载实际执行)
 - [25.19 互补驱动信号生成搭配（半桥上下管，实际实现）](#2519-互补驱动信号生成搭配半桥上下管实际实现)
 - [25.20 DAB 载波移相 + 死区生成搭配（实际实现版，区别于 §25.10 理论版）](#2520-dab-载波移相--死区生成搭配实际实现版区别于-2510-理论版)
 - [25.21 占空比前馈 + PI 微调复合搭配（700V Boost 调压核心）](#2521-占空比前馈--pi-微调复合搭配700v-boost-调压核心)
 - [25.22 MPPT P\&O 算法搭配（已实装）](#2522-mppt-po-算法搭配day17-已实装)
 - [25.23 三节对照索引（§23 ↔ §22 ↔ §25）](#2523-三节对照索引公式速查-23--元器件库-22--功能电路-25)
 - [25.24 IMD 建模搭配（绝缘监测仿真实现）](#2524-imd-建模搭配绝缘监测仿真实现)
## 第一部分：理论知识

### 第一篇 · 原理（理论篇）

### 1. 光伏组件特性与 MPPT

- 光伏等效"理想电流源+二极管并联"，存在唯一最大功率点(MPP)，既非恒压也非恒流。
- 光照影响：$I_{sc}\propto G$（近正比）；$V_{oc}\propto\ln(G)$（变化小）；Simscape Solar Cell 仅线性缩放 $I_{sc,new}=I_{sc,ref}\times G/1000$。
- **两条曲线要分清**：I–V 曲线（横轴电压、纵轴电流）看"工作点落在哪"；P–V 曲线（横轴电压、纵轴功率）看"哪点功率最大"。MPPT 就是在 P–V 曲线上找那个唯一的峰。
- **填充因子 FF（Fill Factor）**：衡量曲线"方不方"，$FF=\frac{V_{mpp}I_{mpp}}{V_{oc}I_{sc}}$，优质晶硅约 0.7~0.85。FF 越小，说明 $R_s$ 太大或 $R_{sh}$ 太小，电池越"软"，同样 Voc/Isc 下能掏出的功率越少。
- **MPP 会随环境跑**：光照变强 → 曲线上移、MPP 功率变大；温度升高 → Voc 下降、MPP 略降。所以 MPPT 必须*实时*追，不能一次设死（为 §1.2 铺垫）。

#### 1.1 单二极管模型（Simscape Solar Cell 的物理基础）

光伏电池本质是 PN 结，工程上用**单二极管模型**描述其 I–V 特性（Simscape`Solar Cell`块即采用此模型，可升级为双二极管更精细）：

$$
I = I_{ph} - I_0\!\left(e^{\frac{V+I R_s}{n V_t}}-1\right) - \frac{V+I R_s}{R_{sh}}
$$

五个核心参数：$I_{ph}$（光生电流）、$I_0$（二极管反向饱和电流）、$R_s$（串联电阻）、$R_{sh}$（并联电阻）、$n$（理想因子，硅电池约 1~2）。其中：

- **$I_{ph}\propto G$（光照线性）**：光照越强光生电流越大，I–V 曲线整体上移；
- **$V_{oc}$ 随 $G$ 变化小**（近似 $\propto\ln G$），且随温度升高而**下降**（硅电池负温度系数，约 −2~−2.5 mV/℃/cell；$I_{sc}$ 随温度略升）；
- $R_s$ 越大填充因子越差、$R_{sh}$ 越小漏电越重，二者共同影响转换效率；
- 阵列/组件：$N_s$ 片串联时 $V_{array}=N_s\cdot V_{cell}$，$I_{array}=I_{cell}$（串联片数越多，阵列电压越高、电流近似不变）。

> 仿真里改`Ir`（辐照度）即可平移 I–V 曲线，无需重设其它参数。

- **从方程看曲线形状（直觉）**：方程里的指数项主导"电压一高电流就暴跌"的膝部（靠近 Voc），而 $I_{ph}$ 决定短路点高度（Isc）。$R_s$ 让大电流区"塌陷"、$R_{sh}$ 让小电压区"漏电变斜"——调这两个电阻就能把理想指数曲线捏成真实电池的歪曲线。
- **热电压 $V_t$ 是什么**：方程里的 $V_t=\frac{kT}{q}$（$k$ 玻尔兹曼常数、$q$ 电子电荷、$T$ 绝对温度），常温约 25.85 mV；它规定了指数项"多陡"，所以温度一变整条曲线都会动（呼应上方负温度系数）。

- **与 Simscape 的对应**：Simscape `Solar Cell` 让你直接填 $I_{sc}, V_{oc}, I_0, R_s, R_{sh}, n$ 等参数而无须手解隐式方程；理解单二极管模型，才知道参数框里每一项在物理上代表什么（权威参考：Sandia PVPMc 单二极管模型说明、Erickson《Fundamentals of Power Electronics》）。

#### 1.2 MPPT 算法原理与步骤

MPPT 的本质：**通过调节 DC/DC 占空比 D，把光伏工作点推到 $P=VI$ 最大处**。MPP 条件为 $\frac{dP}{dV}=0$。

- **P\&O（扰动观察法）步骤**：① 采样当前 $V_k，I_k$，算 $P_k$；② 与上一拍 $P_{k-1}$ 比较；③ 若 $P_k>P_{k-1}$ 则保持上一拍的占空比扰动方向，否则**反向**；④ 更新 $D_{k+1}=D_k+\text{sgn}(\Delta P)\cdot\Delta D$。逻辑直观，但稳态必然在 MPP 两侧**小幅抖动**（步长大→跟踪快但抖，步长小→稳但慢，变步长可兼顾）。
- **INC（电导增量法）判据**：将 $P=VI$ 代入 $\frac{dP}{dV}=0$ 得 $\frac{dI}{dV}=-\frac{I}{V}$ 即 MPP。实时用 $\Delta I/\Delta V$ 估计导数：$\Delta I/\Delta V > -I/V$ → 在 MPP 左侧（电压偏低）→ 增大 D（升压）；$\Delta I/\Delta V < -I/V$ → 右侧 → 减小 D；≈0 → 保持。
- **二者关系**：IEEE 实验表明 P\&O 与 INC **数学上等价（效率差 <0.2%），稳态均抖动**，区别只在实现方式（INC 用电压/电流增量，对快速光照变化略稳）。
- 其它：分数开路电压 $V_{mpp}\approx0.76V_{oc}$（极简、精度低）；智能法（模糊/ANN）动态好但算力高。
- **控制量为什么是占空比 D**：DC/DC 变换器改 D 就等于改变光伏看到的等效负载，从而把工作点沿 I–V 曲线左右挪——MPPT 本质就是"自动找让功率最大的那个 D"。
- **常见误区**：① 以为"MPPT 永远提升电压"——其实升压电路里是调 D 让 PV 侧电压落在 MPP，未必高于开路电压；② 步长一味取大求快，结果稳态抖到吸不上最大功率；③ 光照骤变时 P\&O 会短暂"判错方向"，属正常现象。

### 2. Boost 升压变换器原理

| 阶段 | MOSFET | 电流路径 | 电感电压 | 电流变化 |
| -- | ------ | ----------------- | -------------------- | ---- |
| 导通 | 闭合 | PV+→L→MOSFET→地 | $V_L=V_{in}$ | 上升 |
| 关断 | 断开 | PV+→L→二极管→Cout/负载 | $V_L=V_{in}-V_{out}$ | 下降 |

伏秒平衡 → CCM 传输比：$V_{out}=\frac{1}{1-D}V_{in}$。

**二极管方向规则**：必须"阳极→开关节点，阴极→输出"，反接则MOSFET导通时母线→二极管→MOSFET→地短路。

**CCM vs DCM**：CCM电流始终>0、传输比固定、易控；DCM电流会降到0、严重依赖负载、固定PI难调。**深 CCM**（电感电流谷值始终>0、峰值更高）最稳、易控；校验见 §2.1。

- **零基础直觉——为什么能"升压"**：Boost 的核心是电感。开关管导通时电感从电源"吸"能量存成磁场（电流上涨）；开关管断开时电感为维持电流，把存的能量"推"向输出电容、叠加电源电压，于是输出比输入高。占空比 D 越大、电感存得越多、抬得越高，故有 $V_{out}=V_{in}/(1-D)$。
- **设计三件事**：① 选电感 L 保证 CCM（见 §2.1）；② 选输出电容 C 把纹波压到允许值（见 §2.1）；③ 二极管方向/选型别错（见上方规则与 §2.2）。
- **关键认知**：PV 侧 Boost **固定 50% 占空比、不参与母线调压**，只把光伏电压稳稳抬到母线；最大功率点追踪留给后续 700V 系统再加（呼应 §1.2）。700V 集成系统相反：Boost 成为母线调压器、DAB 开环（见 §21.10 / §25.21）。

#### 2.1 CCM/DCM 边界与纹波计算

由伏秒平衡，Boost CCM 下电感电流纹波峰峰值：

$\Delta I_L = \frac{V_{in} \cdot D \cdot T_s}{L} = \frac{V_{in} \cdot D}{L \cdot f_s}$

CCM 与 DCM 的边界（临界导通模式 BCM）发生在电感电流谷值恰好为零时，即纹波峰峰值等于 2 倍平均电感电流：

$\Delta I_L = 2 I_{L,avg}$

利用功率平衡 $V_{in} I_{L,avg} \approx V_{out} I_{out}$ 与 $D=1-V_{in}/V_{out}$，可得**临界电感**：

$L_{crit} = \frac{V_{in} \cdot D \cdot (1-D)}{2 \cdot f_s \cdot I_{out}} = \frac{R_{load} \cdot D \cdot (1-D)^2}{2 \cdot f_s}$

其中 $R_{load}=V_{out}/I_{out}$。工程上为在全负载范围保持 CCM，常按最小负载电流 $I_{out,min}$ 计算 $L_{crit}$ 并留 1.5~2 倍裕量。

**校验**：$V_{out}=48\,\text{V}$，$D=0.5$（开关 PW=10μs、PER=20μs），$f_s=50\,\text{kHz}$，$L=500\,\mu\text{H}$，$I_{out}=48\,\text{V}/20\,\Omega=2.4\,\text{A}$，反推 $V_{in}=V_{out}(1-D)\approx24\,\text{V}$。代入临界电感公式 $L_{crit}=\frac{R_{load}D(1-D)^2}{2f_s}=\frac{20\times0.5\times0.25}{2\times50\text{k}}\approx25\,\mu\text{H}$，实际 $500\,\mu\text{H}$ 约为临界值的 **20 倍**，故全工况均为**深 CCM**，电感电流谷值≈$I_{L,avg}-\Delta I_L/2\approx4.8-0.25\approx4.5\,\text{A}>0$，不可能跌入 DCM。

输出电压纹波（忽略 ESR，按电容充放电估算）：

$\Delta V_{out} \approx \frac{I_{out} \cdot D}{C \cdot f_s}$

代入 $I_{out}=2.4\,\text{A}$、$D=0.48$、$C=500\,\mu\text{F}$、$f_s=50\,\text{kHz}$：$\Delta V_{out}\approx46\,\text{mV}$，与仿真中母线纹波量级相符。

- **工程判别口诀**：把实际电感 $L$ 与临界电感 $L_{crit}$ 比——$L \gg L_{crit}$ 为深 CCM（最稳），$L \approx L_{crit}$ 为 BCM（边界、可软开关），$L < L_{crit}$ 进入 DCM（轻载常见、控制变难）。
- **为什么尽量别进 DCM**：DCM 下传输比不再只由 D 决定，还随负载/输入电压大幅漂移，固定 PI 参数难在全范围都稳；且小信号模型从二阶变一阶并带右半平面零点（RHPZ），补偿设计更棘手。
- **选型裕量提示**：按最小负载电流算 $L_{crit}$ 并乘 1.5~2 倍只是经验值；若计入电感 DCR、二极管压降等寄生，实际临界电感还会略增，留裕量更保险。留较大裕量属"过度设计但极稳"，适合做教学/验证平台。

#### 2.2 二极管反向恢复与 Boost 开关损耗

- **现象**：二极管正向导通时，漂移区注入并存储大量少数载流子；当开关管导通、二极管被迫从正向切换为反向截止时，这些存储电荷不能瞬间消失，于是在反向电压作用下出现一段**反向恢复电流尖峰** $I_{rr}$、持续时间 $t_{rr}$、总电荷量 $Q_{rr}$。
- **损耗公式**：每次开关的反向恢复能量 $E_{rr}\approx V_R\cdot Q_{rr}$，平均功率 $P_{rr}\approx f_{sw}\cdot V_{bus}\cdot Q_{rr}$（Boost 二极管承受的是母线电压 $V_{bus}$）。硅二极管反向恢复损耗可占硬开关总损耗的 70% 以上。
- **CCM 才严重，DCM 可避**：连续导通模式（CCM）下二极管关断瞬间仍有正向电流，反向恢复不可避免；临界/断续模式（BCM/DCM）电流先归零再关断，几乎无反向恢复。这与 §2.1 结论呼应——深 CCM 下二极管关断存在反向恢复，损耗不可忽略，设计需选快恢复 / SiC 肖特基二极管。
- **危害**：① 反向恢复电流叠加在开关管开通电流上→增大开关管开通损耗；② "硬恢复"（snappy）二极管高 $-di/dt$ 与回路寄生电感作用→电压振铃/尖峰，增加开关管电压应力与 EMI；③ 温升使 $Q_{rr}$ 进一步增大，形成恶性循环。
- **缓解手段**：选用**快恢复 / 超快恢复二极管**；高压场合用 **SiC 肖特基二极管**（反向恢复近乎为零、热稳定好）；或让 Boost 工作于临界导通/软开关；优化开通栅极电阻 $R_{g(on)}$ 限制 $di/dt$；必要时加 RC 缓冲吸收。
- **提示**：仿真中二极管用理想模型，未体现反向恢复损耗与电压尖峰，不影响控制逻辑验证；但实物设计必须计入——这也是升级隔离 DAB、考虑同步整流/宽禁带器件的动机之一。
- **收尾**：当前小功率平台二极管损耗占比有限，理想模型足以验证控制逻辑；但一旦升到大功率，反向恢复损耗与电压应力会急剧放大——这正是 §1.2、§2.2 反复强调"实物必须换快恢复/SiC"的根本原因。

### 3. 双向 Buck-Boost 变换器原理

| 模式 | 主开关 | 同步整流 | 能量方向 | 电压关系 |
| --------- | ------ | ------ | ----- | ------------------------------------- |
| Buck（充电） | S1_top | S2_bot | 母线→电池 | $V_{bat}\approx D_{top}\cdot V_{bus}$ |
| Boost（放电） | S2_bot | S1_top | 电池→母线 | $V_{bus}\approx V_{bat}/(1-D_{bot})$ |

> **核心认知**：电池侧半桥实际特性是 **duty=1(上管常通)→电池直接钳位母线；duty<1→电池放电、母线>标称电压**。所以"把母线调到标称电压"="让PID把duty顶到1"——这是调试的钥匙。

> **拓扑辨析（区别于经典反相 Buck-Boost）**：电池侧变换器是**半桥双向（同步整流，两MOSFET）**、电池与母线共地、输出**不反相**，其传输比为 $V_{bus}=V_{bat}/(1-D_{bot})$（Boost）与 $V_{bat}=D_{top}V_{bus}$（Buck）。而教科书常见的单开关"反相 Buck-Boost"公式为 $V_{out}=-V_{in}\frac{D}{1-D}$（输出极性反转、开关管应力达 $V_{in}+|V_{out}|$），二者拓扑与公式不同，**切勿混淆**。

#### 3.1 为什么需要双向变换器？

##### 1.1 直流微电网中的电池角色

在光伏储能直流微电网中，蓄电池承担着**能量缓冲**的核心任务：

- **光伏发电 > 负载用电**：多余的电能需要**存入电池**（充电）。
- **光伏发电 < 负载用电**：不足的电能需要**从电池取出**（放电）。
- **光伏发电 ≈ 负载用电**：电池处于**待命状态**，既不充电也不放电。

电池就像一个"水库"——水多时蓄水，水少时放水，保证下游（负载）始终有稳定的水流（电压）。

> **补充：用"功率—容量"理解电池 buffering**
>
> 设负载平均功率为 $P_{load}$、光伏平均功率为 $P_{pv}$，则在一段时长 $\Delta t$ 内电池需要吞吐的能量为：
>
> $\Delta E_{bat} \approx (P_{pv} - P_{load})\cdot \Delta t$
>
> - 当 $P_{pv} > P_{load}$：上式为正，电池**吸收**能量（SOC 上升，充电）；
> - 当 $P_{pv} < P_{load}$：上式为负，电池**释放**能量（SOC 下降，放电）。
>
> 变换器的作用，就是按上式符号把功率"搬"到正确方向，并维持母线电压恒定。SOC（State of Charge，荷电状态）是电池剩余容量的百分比；恒压充电后期需转恒压/限流以避免过充。仿真未建电池化学模型，电池端近似为电压源 + 电容，故 SOC 变化不显式出现。

##### 1.2 为什么不能把电池直接挂在母线上？

如果电池直接并联在直流母线上（如模型）：

- **母线电压被电池电压钳死**，无法主动调节。
- 光伏出力变化时，母线电压只能被动跟随电池电压微小波动。
- 无法实现"维持母线电压恒定"的控制目标。
- 未来要将母线电压升高到更高等级时，低电压电池不可能直接并联在高电压母线上。

**结论**：电池和母线之间必须有一个**功率变换器**作为接口，既能隔离两个电压等级，又能控制功率流向。

> **补充：直接并联还有"电流倒灌/环流"风险**
>
> 即使电压等级相同，把电池直接并联在母线上也意味着二者**硬绑定**——光伏大发时多余电流会不受控地灌入电池，可能超过电池最大充电电流而损伤电池；反之负载突变时电池会瞬间被迫承担全部缺口电流，造成母线电压跌落与电池寿命损耗。变换器相当于在二者之间加了一道"可控阀门"，让充电/放电电流被限幅、被调度。
>
> **定量提醒**：若母线电压远高于电池电压，直接并联意味着巨大反向压差加在电池两端，必然烧毁。可见"电压等级隔离"不是可选项，而是硬约束——这也是把电池侧设计成可升降压半桥、并规划升级到隔离型 DAB 的根本原因。

##### 1.3 单向变换器的局限

光伏侧使用的是**Boost升压变换器**，它是**单向**的：

![PV Boost 单向变换器拓扑](figures/topo_boost_pv.svg)

- 二极管只允许电流从光伏流向母线。
- 能量**只能单向流动**：光伏 → 母线。
- 母线无法给电池充电（电流不能逆着二极管流动）。

**结论**：电池侧需要一个**双向变换器**，既能将电池能量送到母线（放电），也能将母线能量送到电池（充电）。

> **补充：单向 Boost 为何"逆不过来"**
>
> 单向 Boost 的续流二极管只允许电流从电感流向母线（阳极接电感侧、阴极接母线侧）。要让母线给电池充电，电流必须**反向**从母线经二极管流回光伏侧，但二极管的单向导电性直接阻断该路径。即便在关断时段，也仅是电感通过二极管向母线"泄能"，绝无能量回流。这与 §2 中 Boost 的"二极管钳位输出"特性一致：二极管既是整流元件，也是**单向阀**。想让能量双向，就必须把这个单向阀换成可主动开通/关断的开关——这正是下一节半桥拓扑的由来。

---

#### 3.2 半桥双向Buck-Boost变换器拓扑

##### 2.1 核心结构

双向变换器采用**半桥拓扑**，由两个MOSFET串联组成桥臂，中间抽头接电感：

![半桥双向 Buck-Boost 变换器拓扑](figures/topo_battery_bidir.svg)

**关键元件**：

- **`S1_top`（上管）**：连接母线与电感，称为"上桥臂开关"
- **`S2_bot`（下管）**：连接电感与地，称为"下桥臂开关"
- **`L_bidir`（双向电感）**：能量传输的核心，在两种工作模式下都承担储能和续流任务
- **`Cin_Bat`（电池侧电容）**：平滑电池端电压，吸收开关纹波
- **`Cout_Bus`（母线电容）**：平滑母线电压

> **补充：互补驱动与"谁是主开关"**
>
> 两管栅极信号**反相（互补）**，设上管导通时间为 $D_{top}T_s$、下管为 $D_{bot}T_s$，则 $D_{top}+D_{bot}=1$（忽略死区）。"主开关"指**主动产生 PWM、决定占空比**的那个管：
>
> - Boost（放电）时，下管 S2_bot 斩波 → 它是主开关，上管 S1_top 作同步整流；
> - Buck（充电）时，上管 S1_top 斩波 → 它是主开关，下管 S2_bot 作同步整流。
>
> 仿真里常用同一路 PWM 取反得到另一路（再插入死区）。**建模要点**：Simulink 中两 MOSFET 的"门极(G)"信号必须互补且带死区；若用 Ideal Switch / MOSFET 模块，注意体二极管方向——上管体二极管从母线指向中点、下管从地指向中点，决定了续流方向。
>
> **常见误区**：以为"上管一直导通就是 Buck、下管一直导通就是 Boost"。实际上"duty=1（上管常通）"是电池**直接钳位母线到 48V** 的边界状态，不是正常 Buck 调压；正常 Buck 是上管按占空比斩波。

##### 2.2 与单向Boost的关键区别

| | 单向Boost | 双向Buck-Boost |
| ----- | ---------------- | ------------------ |
| 开关数量 | 1个MOSFET + 1个二极管 | 2个MOSFET（无二极管） |
| 电流方向 | 单向（源→负载） | 双向（可逆） |
| 二极管 | 被动续流，不可控 | 用MOSFET替代（同步整流），可控 |
| 控制复杂度 | 简单 | 需两路互补驱动信号 |

**本质**：**把不可控的二极管换成了可主动控制的MOSFET**，电流就可以在两个方向上自由流动了。

> **补充：半桥 vs 全桥（四开关）**
>
> 半桥双向只用 2 个 MOSFET，成本低、驱动简单，但单个 MOSFET 承受**全母线电压** $V_{bus}$、且电感电流纹波相对大。更高功率或需更平滑电流时可用**两相交错半桥**（并联两个半桥、占空比错相 180°）或**四开关 Buck-Boost**（去掉半桥的中点钳位限制，占空比可连续扫过 0.5 而不穿越模式边界）。 48V 小功率场景用半桥已足够。

---

#### 3.3 两种工作模式详解

> 下面两图分别给出 Boost（放电）与 Buck（充电）模式下的**高亮导通开关 + 电流环路径**，配合下方表格的"电流路径"列对照看：

![Boost 模式电流环（电池放电）](figures/topo_bidir_boost.svg)

![Buck 模式电流环（电池充电）](figures/topo_bidir_buck.svg)

> **阅读提示**：两幅图分别高亮了 Boost / Buck 下的导通开关与电流环。请对照本节两张表格的"电流路径"列一起看——**同一电感 L_bidir，只是上下管的导通组合不同，能量就走了相反方向**。两模式的本质区别只有一句话：Boost 时**下管斩波、上管续流**；Buck 时**上管斩波、下管续流**。下面的 3.1 / 3.2 会逐周期拆解。

##### 3.1 Boost模式：电池放电，能量从电池→母线

**使用场景**：光伏出力不足，母线电压偏低，需要电池放电来支撑。

##### 开关动作

- **`S2_bot`（下管）** 作为主开关，周期性地导通和关断。
- **`S1_top`（上管）** 作为同步整流管，与下管互补导通（或利用其体二极管续流）。

> **补充**："互补导通"在工程上用一路 PWM 取反得到另一路的栅极信号，再插入几十 ns~μs 级死区。若用 MOSFET 的体二极管自然续流（未主动开通对管），效率会下降、且有反向恢复损耗——所以同步整流要求对管**主动导通**而非仅靠体二极管。

##### 工作过程（一个开关周期）

| 阶段 | S2_bot | S1_top | 电流路径 | 电感状态 |
| -------- | ------ | ------ | ---------------------------------------------------- | ------------ |
| **储能阶段** | **导通** | 关断 | 电池(+) → L_bidir → S2_bot → 地 → 电池(-) | 电流上升，储存磁能 |
| **释能阶段** | **关断** | **导通** | 电池(+) → L_bidir → S1_top → 母线(+) → 负载/电容 → 地 → 电池(-) | 电流下降，释放能量到母线 |

> **补充**：表格中"储能阶段/释能阶段"对应电感电流的上升沿与下降沿。CCM 下电流**不归零**，每个周期都在同一个非零基值上叠加三角波纹波——这也是 CCM 公式 $V_{bus}=V_{bat}/(1-D_{bot})$ 成立的前提。

##### 物理过程描述

1. **下管导通**：电池电压直接加在电感两端（$V_L = V_{bat}$），电流线性上升，电感储存磁场能量。此时上管关断，母线由电容`Cout_Bus`维持供电。
2. **下管关断、上管导通**：电感为了维持电流，会"倒逼"出感应电压，这个电压叠加在电池电压上（$V_{out} = V_{bat} + V_L$），通过上管送到母线，实现**升压**。

**类比**：电感像一根弹簧，下管导通时把弹簧压紧（储能），下管关断时弹簧弹开（释放能量），把能量"弹"到更高的电位。

> **补充：电感电流纹波**
>
> Boost 下电感电流纹波近似：$\Delta I_L \approx \frac{V_{bat}}{L_{bidir}}\cdot D_{bot}T_s = \frac{V_{bat}D_{bot}}{L_{bidir}f_{sw}}$。
>
> 纹波越小电流越平滑、EMI 越低，但需更大电感。仿真建模时 $L_{bidir}$ 取值要兼顾纹波与动态响应；若电感过小进入 DCM，电压比公式失效、控制变难。

##### 电压传输比（CCM）

$V_{bus} = \frac{1}{1-D_{bot}} \times V_{bat}$

其中 $D_{bot}$ 是下管导通占空比（下管导通时间占整个开关周期的比例）。

> **举例**：电池 48V，下管占空比 50%（$D_{bot}=0.5$），则 $V_{bus} = 48/(1-0.5) = 96V$。

> **推导（伏秒平衡法，CCM）**：稳态下电感两端电压在一个开关周期内的平均值为零（否则磁芯持续储能/去磁，电压飘走）。Boost 模式：
>
> - 下管导通期（$D_{bot}T_s$）：电感左端接电池 $V_{bat}$、右端接地，故 $V_L = V_{bat}$；
> - 下管关断、上管导通期（$(1-D_{bot})T_s$）：电感右端被抬到母线 $V_{bus}$、左端仍 $V_{bat}$，故 $V_L = V_{bat} - V_{bus}$（为负，电感释能）。
>
> 伏秒平衡：$V_{bat}\cdot D_{bot}T_s + (V_{bat}-V_{bus})\cdot(1-D_{bot})T_s = 0$
>
> 约去 $T_s$：$V_{bat}D_{bot} + V_{bat}(1-D_{bot}) - V_{bus}(1-D_{bot}) = 0 \Rightarrow V_{bat} - V_{bus}(1-D_{bot}) = 0$
>
> $\Rightarrow V_{bus} = \frac{V_{bat}}{1-D_{bot}}$
>
> **设计准则**：Boost 极限 $D_{bot}\to 1$ 时理论上 $V_{bus}\to\infty$，实际受 MOSFET 最小导通时间、死区与损耗限制；且 $D_{bot}$ 越大电感纹波电流越大。电池 48V，在开环平衡点 $D_{top}\approx0.76$（即 $D_{bot}\approx0.24$）附近，按理想公式仅有轻微升压倾向；正因为此"倾向"恰好被外部功率自平衡抵消，电池电流≈0、母线被电池钳在 ~48V（见 §3）。这也说明：开环下占空比只定"倾向"，实际母线电压由电池钳位与外部功率差额共同决定。
>
> **常见误区**：把经典反相 Buck-Boost 公式 $V_{out}=-V_{in}D/(1-D)$ 套到半桥上。本半桥**共地、不反相**，分子没有负号，且以 $D_{bot}$ 定义时写法为 $V_{bus}=V_{bat}/(1-D_{bot})$（见本节能手辨析与上表）。

---

##### 3.2 Buck模式：电池充电，能量从母线→电池

**使用场景**：光伏出力大于负载，母线电压偏高，需要将多余能量充入电池。

##### 开关动作

- **`S1_top`（上管）** 作为主开关，周期性地导通和关断。
- **`S2_bot`（下管）** 作为同步整流管，与上管互补导通。

> **补充**：Buck 模式下主开关是上管 S1_top，下管 S2_bot 作同步整流。注意此时电感电流方向仍是"从母线流向电池"（充电），与 Boost 的"电池流向母线"相反——同一个电感、相反电流方向，正是双向变换器的特征。

##### 工作过程（一个开关周期）

| 阶段 | S1_top | S2_bot | 电流路径 | 电感状态 |
| -------- | ------ | ------ | -------------------------------------------------- | -------------- |
| **储能阶段** | **导通** | 关断 | 母线(+) → S1_top → L_bidir → 电池(+) → 电池(-) → 地 | 电流上升，储存磁能 |
| **续流阶段** | **关断** | **导通** | L_bidir → 电池(+) → 电池(-) → 地 → S2_bot → L_bidir（回路） | 电流下降，电感释放能量给电池 |

> **补充**：Buck 的"储能阶段"电感从母线取能、电流上升；"续流阶段"电感经下管把剩余能量送给电池、电流下降。同样需 CCM（电流不归零）公式才成立。

##### 物理过程描述

1. **上管导通**：母线电压加在电感两端（$V_L = V_{bus} - V_{bat}$），电流线性上升，能量从母线流向电感和电池。
2. **上管关断、下管导通**：电感电流不能突变，通过下管形成续流回路，继续给电池充电，电感电流线性下降。

**类比**：上管像一个"水龙头"，控制从高压水管（母线）流向水池（电池）的水流。水龙头打开时水流大，关闭时电感这个"惯性轮"维持水流继续转一会儿。

> **补充：Buck 电感电流纹波**：近似 $\Delta I_L \approx \frac{(V_{bus}-V_{bat})D_{top}}{L_{bidir}f_{sw}}$。因 $V_{bus}-V_{bat}$ 较小，Buck 纹波通常比深度 Boost 时更小。

##### 电压传输比（CCM）

$V_{bat} = D_{top} \times V_{bus}$

其中 $D_{top}$ 是上管导通占空比。

> **举例**：母线 48V，上管占空比 75%（$D_{top}=0.75$），则 $V_{bat} = 0.75 \times 48 = 36V$。但实际电池电压被钳位在~48V，所以功率会自动平衡——占空比决定了"充电的倾向"，实际充电电流由功率差额决定。

> **推导（伏秒平衡法，CCM）**：Buck 模式：
>
> - 上管导通期（$D_{top}T_s$）：电感左端接母线 $V_{bus}$、右端接电池 $V_{bat}$，故 $V_L = V_{bus}-V_{bat}$（储能）；
> - 上管关断、下管导通期（$(1-D_{top})T_s$）：电感左端被下管接地、右端仍 $V_{bat}$，故 $V_L = -V_{bat}$（释能给电池）。
>
> 伏秒平衡：$(V_{bus}-V_{bat})D_{top}T_s + (-V_{bat})(1-D_{top})T_s = 0$
>
> 整理：$V_{bus}D_{top} - V_{bat}D_{top} - V_{bat} + V_{bat}D_{top} = 0 \Rightarrow V_{bus}D_{top} - V_{bat} = 0$
>
> $\Rightarrow V_{bat} = D_{top}\cdot V_{bus}$
>
> **设计准则**：Buck 下 $D_{top}$ 越大、电池电压越高；$D_{top}\to 1$ 时 $V_{bat}\to V_{bus}$（近似直通，对应"duty=1→电池钳位母线48V"的边界）。$D_{top}$ 过小则充电电压不足。
>
> **说明**：正文举例"母线48V、D_top=0.75→V_bat=36V"是**理想开环公式值**；实际因电池被钳位在 ~48V，公式只表达"充电倾向"，真实充电电流由光伏-负载功率差额经变换器自动平衡决定（详见 §3.2、§5）。

---

#### 3.4 占空比与电压传输比的关系

##### 4.1 统一视角：占空比决定"倾向"

在半桥双向变换器中，两个MOSFET**互补导通**（忽略死区）：

$D_{top} + D_{bot} = 1$

因此只需要知道一个占空比，就能描述整个系统。

**核心关系**：

$V_{bus} \approx \frac{1}{D_{top}} \times V_{bat} \quad \text{（当 } V_{bus} > V_{bat} \text{，Boost倾向）}$

$V_{bat} \approx D_{top} \times V_{bus} \quad \text{（当 } V_{bus} > V_{bat} \text{，Buck倾向）}$

当 $V_{bus} \approx V_{bat}$（如48V系统）时：

- **$D_{top} > 0.5$** → 上管导通时间长 → 偏**Buck模式** → 倾向给电池**充电**
- **$D_{top} < 0.5$** → 下管导通时间长 → 偏**Boost模式** → 倾向让电池**放电**
- **$D_{top} \approx 0.76$** → 充放电趋势恰好抵消 → 电池电流≈0

> **补充：为什么 48V 系统平衡点 $D_{top}\approx0.76$ 而不是 0.5？**
>
> 理想半桥在 $V_{bus}=V_{bat}$ 时应 $D_{top}=0.5$。但实测平衡点常偏离理想 0.5，偏差来自：**① 两管及驱动并非理想对称**（体二极管压降、导通电阻、死区导致有效占空比偏移）；**② 母线电容/电池等效串联电阻**造成压降；**③ 电感电流连续下的稳态偏置**。这提醒：公式给的是"理想趋势"，真实硬件/仿真要靠标定或 PI 自整定来找平衡点，不要死抠 0.5。

##### 4.2 开环系统中，功率流向由外部功率差额决定

占空比设定的是一个**倾向**，但实际电流大小和方向由**光伏出力和负载的差额**决定：

| 光伏 vs 负载 | 占空比固定时 | 电池电流 | 能量流向 |
| -------- | ------- | ----- | ----- |
| 光伏 > 负载 | 母线有盈余功率 | 正（充电） | 母线→电池 |
| 光伏 < 负载 | 母线有功率缺口 | 负（放电） | 电池→母线 |
| 光伏 ≈ 负载 | 功率自平衡 | ≈0 | 几乎不流动 |

> **补充：开环下"电流大小"怎么估？**
>
> 开环固定占空比时，电池电流 $I_{bat}$ 近似等于光伏与负载的**净功率差额除以电池电压**：
>
> $I_{bat} \approx \frac{P_{pv} - P_{load}}{V_{bat}}$
>
> 光伏盈余（$P_{pv}>P_{load}$）→ $I_{bat}>0$ 充电；缺口 → $I_{bat}<0$ 放电。 §5 开环表（不同光/载组合下，电流方向随功率差额在正/负间切换）正是此规律的体现：占空比固定时，电流方向完全由光/载差额决定。

##### 4.3 闭环系统中，占空比由PI控制器动态调节

当PI控制器工作时，占空比不再固定，而是根据母线电压误差自动调整：

- **Vbus < Vref**（母线电压偏低）→ PI**减小上管占空比**（增强Boost倾向）→ 电池放电→ Vbus回升。
- **Vbus > Vref**（母线电压偏高）→ PI**增大上管占空比**（增强Buck倾向）→ 电池充电→ Vbus回落。

> **补充：PI 如何"翻译"电压误差为占空比**
>
> 控制器输出即上管占空比：$D_{top}(k) = D_{top}(k-1) + K_p\cdot e(k) + K_i\cdot\sum e$，其中误差 $e = V_{ref}-V_{bus}$。$V_{bus}$ 偏低→$e>0$→$D_{top}$ 减小→偏 Boost→电池放电补能→$V_{bus}$ 回升，形成负反馈闭环。比例项快速响应、积分项消除稳态静差（定压 PI 实测母线略超 ±2% 带，此已知偏差由下垂/强积分补偿，见 §9）。若 $K_p$ 过大易振荡、$K_i$ 过大易超调/积分饱和——调试时先调 $K_p$ 再调 $K_i$ 是通用经验；具体增益按 §22.5 整定依据选取，勿在此硬编。

---

#### 3.5 为什么用两个MOSFET而不是"MOSFET+二极管"？

##### 5.1 同步整流的优势

如果将上管换成二极管（类似单向Boost），在Buck模式（充电）时，上管需要主动导通让电流从母线流向电池。但二极管是被动器件，无法被控制——它要么一直通（反了），要么一直不通。

**用MOSFET替代二极管**，可以通过栅极信号精确控制导通和关断，实现：

- 需要它通的时候通（同步整流）
- 需要它断的时候断（防止电流倒灌）

这就是**同步整流**——用可控开关替代不可控的整流器件。

> **补充：MOSFET 的"第三象限"导通**
>
> 同步整流管导通时电流从**源极流向漏极**（与正常开关的漏→源相反），称为**第三象限工作**——体二极管先导通续流，随后栅极主动开通让沟道承担电流，压降从 $V_F$ 降到 $I\cdot R_{on}$。这正是 MOSFET 能当"可控二极管"的物理基础。若漏掉主动开通、只靠体二极管，则退化为普通二极管整流，失去同步整流优势且引入反向恢复损耗。
>
> **补充：防止电流倒灌**
>
> 电池充电结束时若下管仍导通，母线可能经下管反向给电池灌电流或造成环流；MOSFET 可随时关断切断路径，这是二极管做不到的"主动隔离"。

##### 5.2 效率优势

MOSFET导通时相当于一个小电阻，而二极管有固定的正向压降。大电流下，MOSFET的导通损耗远小于二极管。

> **定量对比（与 TI/ADI 同步整流资料一致）**：续流损耗二极管为 $P_F=V_F\cdot I$（随电流线性），MOSFET 为 $P_R=I^2R_{on}$（随电流平方）。令二者相等得 crossover 电流 $I_{cross}=V_F/R_{on}$。代入设定 $V_F=0.8\text{V},\ R_{on}=0.1\Omega$ 得 $I_{cross}=8\text{A}$：
>
> - $I<8\text{A}$：MOSFET 损耗更低（如 $I=2\text{A}$：二极管 $1.6\text{W}$，MOSFET $0.4\text{W}$；电池电流 ±2A 量级属此区）；
> - $I>8\text{A}$：按此 $0.1\Omega$ 简化设定二极管反而更优——这是因为 $0.1\Omega$ 是仿真近似；**实际功率 MOSFET 的 $R_{on}$ 常仅几 mΩ**，届时 $I_{cross}$ 升到几十~上百 A，大电流下 SR 优势显著。
>
> 因此用双 MOSFET 的**主因**不仅是导通损耗，更是：① 双向可控（二极管做不到）；② 无反向恢复损耗；③ 可多管并联自动均流（正温度系数）。原文"大电流下 MOSFET 损耗远小于二极管"在采用低 $R_{on}$ 实管时成立，用仿真的 $0.1\Omega$ 简化值则需留意上述 crossover。

---


### 4. 半桥浮地驱动与死区时间

- **浮地驱动**：MOSFET导通需 $V_{gs}=V_g-V_s>V_{th}$。下管源极接地→驱动负极接地；**上管源极在桥臂中点跳动，驱动负极必须接源极(浮地)**，否则出现−48V负压击穿。真实硬件用自举/隔离驱动IC(如IR2110)。口诀"上管跟源跑，下管踩实地"。
- **死区时间**：上下管同时导通→直通短路(Shoot-through)→烧管。互补切换须插死区(0.5~1μs)。**当前无死区**(仿真碰巧不报错但不严谨)，700V系统必须加。（直通风险与"700V 必须加死区"的实操教训及「 → 死区忘加/太短」）
- **功率器件损耗（选型依据）**：MOSFET 总损耗 = 导通损耗 $P_{cond}=I_{rms}^2 R_{on}$（$R_{on}$ 越小越好，设 0.1Ω）+ 开关损耗 $P_{sw}$（每次开通/关断的 $V\cdot I$ 交叠积分 × 开关频率）。同步整流用 MOSFET 替代二极管，正是因大电流下 MOSFET 的导通损耗 $I^2R_{on}$ 远小于二极管的 $V_f\cdot I$。开关频率越高开关损耗越大，故 50kHz 是效率与体积折中。
- **浮地驱动扩展——自举（Bootstrap）原理**：上管驱动"浮地"在真实硬件中最常用自举电路实现。低管导通时，开关节点被拉到近地，驱动电源 $V_{CC}$ 经自举二极管给自举电容 $C_{BOOT}$ 充电到约 $V_{CC}-V_D$；低管关断、上管要导通时，开关节点电位抬升到母线电压，由于电容两端电压不能突变，电容正端被"举"到 $V_{bus}+V_{CC}$，为高压侧驱动 IC 提供相对源极稳定的悬浮供电。自举电容取值经验式：$C_{BOOT}\ge (Q_g+I_q\cdot t_{on})/\Delta V$（$Q_g$ 栅电荷、$I_q$ 驱动静态电流、$t_{on}$ 上管最大导通时间、$\Delta V$ 允许压降），常用 0.1~1µF 陶瓷电容。局限：必须周期性开通下管来"刷新"电容，故不能长期 100% 占空比；高压/长上管导通场景改用隔离驱动电源或电荷泵。口诀延伸："自举靠下管喂电，下管不导通上管饿死"。
- **死区与开关损耗的定量关系**：死区本身不直接产生开关损耗，但带来"死区导通损耗"——死区内电流被迫流经体二极管/续流管，其压降损耗与**开关频率、负载电流、死区压降、死区时长**成正比：

 $P_{dead}\propto f_{sw}\cdot I_{load}\cdot V_{drop}\cdot t_{dead}$

 即 $P_{dead}\approx I_{load}\cdot V_f\cdot t_{dead}\cdot f_{sw}$。同时死区过短则直通风险指数上升。工程折中：在保证不直通的前提下尽量取小死区（覆盖延迟失配即可，见 §4.1），并用死区补偿算法抵消输出电压畸变。$t_{dead}$ 每减小 100ns、在 50kHz/10A 场景下约省 $10\cdot 0.7\cdot 100\text{ns}\cdot 50\text{k}\approx 3.5\text{mW}$ 量级——单看很小，但高频大电流下积少成多，且更重要的是降低 THD 与转矩脉动。
- **负压击穿风险量化**：上管源极相对驱动负极的电压 $V_{gs}$ 必须为正且 $>V_{th}$ 才导通；若驱动负极误接地固定电位，当开关节点跳到母线 $V_{bus}$ 时，栅源间会被拉到约 $-V_{bus}$，即 48V 系统出现约 −48V、700V 系统出现约 −700V 的负压，远超 MOSFET 栅极额定 $\pm20\text{V}$，瞬间击穿栅氧层永久损坏。这正是"浮地驱动不是可选项，是必选项"的物理根因。

#### 4.1 死区时间定量设计准则

> 双向半桥的"浮地驱动"之外，**死区时间**是半桥不炸管的另一根生命线。

- **定义**：互补 PWM 中，上管关断指令发出后、下管开通指令发出前，人为插入一段"两管都关断"的安全窗口 $t_{dead}$，确保任何时刻不会出现上下管同时导通。
- **定量准则**：最小死区须覆盖所有延迟失配：

$t_{dead,min}=t_{d(off),max}-t_{d(on),min}+\Delta t_{margin}$

其中 $t_{d(off)}$、$t_{d(on)}$ 取自 MOSFET/驱动芯片数据手册的关断/开通延迟，$\Delta t_{margin}$ 为 20~50 ns 设计裕量。延迟源包括：驱动 IC 传播延时 $t_{pd}$、MOSFET 开关延迟（受 $C_{iss}/C_{oss}/C_{rss}$ 与 $R_g$ 影响）、米勒平台持续时间、PCB 寄生电感引起的地弹。

- **典型量级**：Si MOSFET 约 50–100 ns；IGBT 约 1–3 µs（含少子拖尾）；SiC MOSFET 可缩至 20–50 ns。工程上 48V/中频半桥常先取 100–500 ns 再示波器微调。
- **过短的代价——桥臂直通（Shoot-through）**：若死区不足，$V_{DS}$ 未回升、对管已导通，形成 $V_{bus}\to HS\to LS\to GND$ 低阻通路，瞬时峰值电流可达数百安（$di/dt>1000\,\text{A/µs}$），远超器件 SOA，数微秒内热失控炸管。
- **过长的代价——体二极管续流**：死区内电流被迫流经 MOSFET 体二极管，产生 $V_f\approx0.7\,\text{V}$ 压降损耗 $P_d\approx I_{rms}\cdot V_f\cdot t_{dead}\cdot f_{sw}$，可占总损耗 15%；并在电机/逆变场景中引入输出电压畸变与转矩脉动。
- **状态**：当前 48V/50kHz 互补 PWM **未加死区**（仿真碰巧不报错但不严谨），建议取 $\ge 300$ ns（推荐 400–600 ns）并按上述准则示波器验证； 700V 系统必须加死区，否则直通风险极高。（实操踩坑与「 → 死区忘加/太短」）
- **定量算例（示意，非参数）**：取某 Si MOSFET + 驱动 IC 数据手册典型值 $t_{d(off),max}=80\,\text{ns}$、$t_{d(on),min}=30\,\text{ns}$、$\Delta t_{margin}=40\,\text{ns}$，则 $t_{dead,min}=80-30+40=90\,\text{ns}$。考虑 PCB 地弹与米勒耦合再加 50% 裕量，工程取 150~200 ns，再用示波器实测上下管 $V_{gs}$ 交越点微调。
- **示波器验证步骤**：① 同时抓上下管栅极 $V_{gs}$，确认无"同时为高"的重叠区；② 量取上管 $V_{gs}$ 下降到阈值、到下管 $V_{gs}$ 上升到阈值的间隔，即为实际死区；③ 在最小/最大负载、最高结温下复测（温度升高使关断延迟变大，死区需留温漂裕度）；④ 监测桥臂电流尖峰，若死区过短会出现直通尖峰。
- **死区对效率的折中曲线直觉**：横轴死区 $t_{dead}$、纵轴总损耗。曲线左端（死区过短）因直通风险与开关重叠损耗骤升；右端（死区过长）因体二极管续流损耗线性累积；中间存在最小损耗"甜区"。设计目标不是"越大越安全"，而是落在甜区左侧一点点——刚好覆盖延迟失配即可。
- **死区补偿（进阶）**：逆变/电机场景可在调制信号里按电流极性加一个补偿脉冲，抵消死区造成的平均电压跌落与零交越畸变；直流微电网双向变换器若对电压精度敏感，也可在占空比指令里做等效补偿。

### 5. 稳态功率平衡校验

功率平衡校验的本质是**能量守恒**在稳态下的功率形式：流入母线的功率必须等于流出母线的功率。它是"不依赖控制器内部模型"的独立体检表——即使你不知道 PI 参数、不知道电感值，只要把各端口电压电流测准，就能判断模型/接线/读数是否自洽。下面 §5.1 给出量化做法与效率估算。

#### 5.1 稳态功率平衡的量化校验与效率估算

稳态功率平衡不只是一条"公式"，而是一套**用已知量反推/校验未知量**的独立手段（见 §20"功率平衡"词条）。量化做法：

1. **列平衡式**：$P_{pv}+P_{bat}=P_{load}+P_{loss}$。

- 负载功率可直接算：$P_{load}=V_{bus}^2/R_{load}$。 $V_{bus}=48\,\text{V}$、$R_{load}=20\,\Omega$ ⇒ $P_{load}=115.2\,\text{W}$；若负载阶跃到 10Ω ⇒ $230.4\,\text{W}$。
- 电池功率：$P_{bat}=V_{bat}\cdot I_{bat}$（放电为正、充电为负）。
- PV 功率：由 $V_{pv}\cdot I_{pv}$ 实测（或单二极管模型 §1.1 在 MPP 附近估算，需实测确认）。

1. **反推校验**：若已知 PV 与负载，可推电池电流 $I_{bat}\approx (P_{load}-P_{pv})/V_{bus}$；把推算值与示波器实测 $I_{bat}$ 比对，一致则说明电流读数可信（这正是 判断"电流传感器接线对不对"的方法）。

**效率与损耗**：

- 变换器效率定义：$\eta=\frac{P_{out}}{P_{in}}\times100\%$，$P_{loss}=P_{in}-P_{out}$。损耗主要来自：导通损耗 $I^2R$（MOSFET $R_{DS(on)}$、电感 DCR、走线）、开关损耗（随 $f_{sw}$ 增大）、栅极驱动损耗 $P_{gate}=Q_g\cdot V_{gs}\cdot f_{sw}$、磁芯损耗、体二极管/续流损耗、轻载静态电流。
- **Boost 特殊性**：输入电流恒大于输出电流；同一输出功率下输入电压越低、输入电流越大、导通损耗越重，故效率应在**最低输入电压**处校核。
- **电池往返效率 RTE（Round-Trip Efficiency）**：一次完整充放电可回收能量比，$RTE=\frac{E_{out}}{E_{in}}\times100\%$。现代锂电典型值：DC 侧 RTE 约 95–98%，含 PCS 变流的 AC 侧 RTE 约 85–94%；LFP 单体 RTE 约 90–95%。这意味着每天一充一放，约有 5–15% 的能量在"化学⇄电"转换中变成热与辅助损耗——做能量调度经济性分析时**不能忽略**，否则会高估光伏自发自用收益。
- 仿真未建模损耗/效率（理想器件），故功率平衡校验主要用于**查错**（看读数自洽），而非评估真实效率；若后续做"能量流优化/碳核算"，应在 Boost、双向变换器、电池环节叠加 $\eta$ 与 RTE 系数。

$P_{pv}+P_{bat}\approx P_{load}+P_{loss}$，$P_{bat}=V_{bat}\cdot I_{bat}$（放电正/充电负）。代入读数检查自洽，是判断电流读数可信度的独立手段。

- **完整量化算例**：稳态时若光照使 $P_{pv}=150\,\text{W}$、负载 $P_{load}=115.2\,\text{W}$（20Ω），忽略损耗则电池应**放电** $P_{bat}=P_{load}-P_{pv}=-35.2\,\text{W}$（负号=充电，即光伏盈余在给电池充电）；若负载阶跃到 10Ω（$P_{load}=230.4\,\text{W}$），则 $P_{bat}=230.4-150=+80.4\,\text{W}$（正=放电补足缺口）。把此推算 $I_{bat}=P_{bat}/V_{bat}$ 与实测电流传感器比对，一致即读数可信。
- **效率链串联估算（进阶）**：若后续做真实能耗核算，应把各级效率乘起来。例：光伏 MPP 输出 $P_{pv}$ → Boost 效率 $\eta_{boost}$ → 母线 → 双向变换器效率 $\eta_{conv}$ → 电池，再乘电池往返效率 $RTE$。则某日光伏可存储到电池并回放可用的能量约为 $E_{usable}=E_{pv}\cdot\eta_{boost}\cdot\eta_{conv}\cdot RTE$。取 $\eta_{boost}=\eta_{conv}=95\%$、$RTE=92\%$，则 $E_{usable}\approx E_{pv}\times0.95\times0.95\times0.92\approx0.83\,E_{pv}$——即约 17% 在传递与化学转换中损耗，做自发自用收益评估时须计入，否则高估约两成。
- **损耗热点定位清单**：① 导通损耗（大电流路径，选低 $R_{DS(on)}$ MOSFET、低 DCR 电感）；② 开关损耗（高频时占主导，可适度降频或选快管/SiC）；③ 栅极驱动损耗 $P_{gate}=Q_g V_{gs}f_{sw}$（多管并联时累加）；④ 体二极管/续流损耗（死区与同步整流失效时）；⑤ 磁芯与静态损耗。定位后才有针对性优化方向，而非笼统"效率不够"。

### 6. PID 控制理论

$u(t)=K_p e(t)+K_i\int_0^t e(\tau)d\tau+K_d\frac{de}{dt}$。P管"现在"(快速,有静差)；I管"过去"(消静差,可能超调)；D管"未来"(抑超调,放大开关噪声)——电力电子常用PI不用PID。

**静差与积分**：纯P必有静差；积分持续累积消除静差。

**⚠️ 积分增益的时间尺度判据**：Ki 大小**无绝对标准**，判据是"积分器推满执行器量程所需时间 vs 系统/仿真时间窗"。若 Ki 过小，积分器在仿真窗内推不满执行器→环路等效**开环**，母线漂在功率平衡点（不出稳压带）；增大 Ki 使其在仿真窗内推到位→环路闭合稳在目标。一句话：**增益够不够，看它能否在仿真窗内把执行器推到位**。

**静态误差的三重含义（易混，本次厘清）**：

- ① **闭环跟踪误差** = Vbus − Vref：真正的"控制静态误差"。有强积分→趋近0。这是你要压到 0 的指标。
- ② **下垂固有偏移** = Vbus − 母线标称：一个由下垂系数与负荷电流决定的固定小偏移，**不是控制误差**，是下垂"用电压换均流"的**设计代价**。
- ③ **大残差(坏)** = 定压 PI 的 Vbus − Vref 显著大于0：因 Ki 太弱、积分来不及追上→真·过大静态误差。

> 区分：①是控制水平(越小越好)，②是设计选择(故意留)，③是缺陷(要修)。
>
> **抗积分饱和**：输出达限幅且误差仍同向时暂停积分累加，防退出饱和时巨超调。Simulink PID 勾`Limit output`+抗饱和选项。⚠️ 但**抗饱和不是万能**。
>
> **PWM载波比较**：锯齿波(0→1,Ts=20μs) vs 占空比D：锯齿<D→1(导通)。对应硬件CNT vs CCR。
>
> **单环vs双环**：单环(电压PI→占空比)简单但无法限流、DCM下差；双环(电压外环+电流内环)响应快可限流，是工业储能主流，**700V或需限流保护时应升级双环**。
>
> **阶跃响应指标**：超调量 $\sigma\%$、调节时间 $t_s$、稳态误差 $e_{ss}$。定稿：母线稳态偏差控制在 ±2% 带内，扰动恢复 <10ms。

- **为什么电力电子常用 PI 而非完整 PID**：微分项对高频开关噪声极敏感——母线电压信号叠加 50kHz 纹波，D 项会把这些纹波放大成占空比高频抖动，反而激起更大电流纹波甚至不稳定；而积分项恰好能消除稳态静差，性价比最高。故 Boost/双向变换器电压环普遍用 PI。电流内环有时也只用 P（限流快、允许少量静差）。需要 D 的典型是运动控制（位置/速度环），与本题母线稳压场景不同。

#### 一、PI控制器回顾

##### 1.1 连续域PI控制律

$$
u(t) = K_p \cdot e(t) + K_i \int_0^t e(\tau) d\tau
$$

其中 $e(t) = V_{ref} - V_{bus}(t)$（或 $V_{bus} - V_{ref}$，取决于极性定义），$u(t)$ 为控制器输出的占空比指令。

##### 1.2 你在使用的结构

Vref → [减法] → error → [Transport Delay 1μs] → [PID Controller (Continuous)] → 占空比 D。

- **符号约定的连锁影响**：注意 $e(t)=V_{ref}-V_{bus}$ 还是 $V_{bus}-V_{ref}$ 直接决定 PI 输出"增大占空比"对应母线"升"还是"降"。双向变换器为 Boost 升压，占空比↑→母线↑；若采用 $e=V_{ref}-V_{bus}$，则误差为正时需加大占空比→$K_p$ 取正。一旦符号写反（比如写成 $e=V_{bus}-V_{ref}$ 却仍用正 $K_p$），会构成正反馈，母线直接飙到限幅。调试前务必先用"误差为正→期望占空比方向"推一遍符号，这是新手最常见的隐性 bug。
- **连续域到离散域的衔接**：上面连续律用于理论分析与连续 PID 模块；落地到 MCU 必须离散化（见第三节）。两者在 $T_s$ 足够小时动态几乎一致，但离散化会引入一个"采样-保持"延迟（约 $T_s/2$），高控制频率下不可忽略——若用 100µs 控制周期，等效引入 ~50µs 相位滞后，要在环路带宽设计时预留余量。

#### 二、平均电流控制（电流模式备选方案）

计划把"平均电流控制"列为**占空比控制之外的备选方案**。它和你正在用的"单环电压 PI → 直接出占空比"不是替代关系，而是**升级关系**——在电压外环外面再套一级电流内环。

- **直接占空比控制（你现在的做法）**：误差 `e = Vref − Vbus` → 电压 PI → 直接产占空比 `D`。只有一个环，简单，但**无法直接限制电感电流**（电流过大只能靠外部限幅/保护去砍）。
- **平均电流控制（ACMC, Average Current Mode Control）**：结构变成"**电压外环 + 电流内环**"：
 - 电压外环 PI 输出 = **电流参考** `I_ref`；
 - 电感电流采样后做**平均**（不是采峰值）→ 与 `I_ref` 比较 → 电流内环 PI → 占空比。
 - 因为内环直接盯住电感电流，负载或输入突变时电流**先被内环按住**，再慢慢被外环调电压，天然具备**限流**能力。
- **为什么叫"平均"而非"峰值"**：峰值电流控制只采样开关管导通瞬间的峰值，易被开通尖峰/噪声误导；ACMC 对电感电流取平均后再比较，抗噪更好、电流谐波更小，是 PFC 与并网/储能双向变换器的工业主流。
- **与直接占空比控制的对比**：

| 维度 | 直接占空比（单环电压） | 平均电流控制（双环） |
| -------- | ----------- | ------------ |
| 控制对象 | 占空比 | 电感电流平均值 |
| 限流能力 | 无（靠外部保护） | 内环天然限流 |
| 抗输入/负载扰动 | 慢（经电压环才反应） | 快（电流环先动作） |
| 复杂度 | 低 | 中（多一环比+电流采样） |
| 适用 | 教学/小扰动 | 工业储能/大电流冲击 |

- **怎么落地**：双向 Buck-Boost 把电池电流（=电感电流）控在目标值，即可同时"稳压 + 限流"——这正是 §6 末尾"双环可限流"的具体实现。初学者路线：**先用单环 PI 跑通 （本笔记主线）；等 大故障电流出现、发现单环限流吃力时，再回头加 ACMC 内环**，那时你会真正理解"为什么需要双环"。

#### 三、离散 PI 控制器与 Simulink 搭建习惯

##### 3.1 为什么需要离散化？

真实数字控制器（DSP、单片机）以固定采样周期$T_s$执行算法：

1. 采样ADC获取$V_{bus}$
2. 计算误差
3. 执行PI更新
4. 更新PWM占空比

连续域PID模块在仿真中通过极小步长近似连续，但无法直接生成嵌入式代码。使用离散PI模块则与最终实现完全一致。

##### 3.2 离散PI的位置式算法

$$
u[k] = K_p \cdot e[k] + K_i \cdot T_s \sum_{i=0}^{k} e[i]
$$

其中$T_s$为采样周期，$k$为离散时刻序号。积分项用累加实现。

##### 3.3 Simulink中搭建离散PI的推荐方式

**推荐使用`Discrete PID Controller`模块**（Simulink/Discrete库）：

- 设置 **Sample time** 为控制周期，通常取开关周期的1~5倍。你的开关频率50kHz（$T_{sw}=20\mu s$），采样周期可选 $T_s = 100\mu s$（10kHz控制频率）。
- 参数可直接沿用连续域调好的$K_p，K_i$（若$T_s$足够小，动态差异可忽略）。严格转换时：离散积分增益 = $K_i \cdot T_s$。
- 模块内部自动实现了积分累加和输出限幅，并可配置抗积分饱和策略。

**另一种手动搭建方式**（便于理解）：

1. 误差信号进入 **Gain**（$K_p$）得到比例项。
2. 误差信号进入 **Gain**（$K_i \cdot T_s$）→ **Discrete-Time Integrator** → 积分项。
3. 两项相加，进入 **Saturation** 限幅，输出占空比。

手动搭建可以完全控制抗积分饱和逻辑（见第四节）。

##### 3.4 搭建习惯

- **采样时间统一**：离散PI模块、反馈信号的采样保持（如Rate Transition）应使用同一$T_s$。
- **数据量化**：最终部署到MCU时考虑定点数精度，当前仿真阶段用double即可。
- **PWM更新同步**：实际系统中PI计算与PWM载波同步（通常在载波谷底或峰顶触发中断），仿真中用Transport Delay模拟计算延迟。
- **软启动**：离散实现时，Vref的斜坡可用计数器或查表生成，避免阶跃导致积分瞬间饱和。
- **离散积分增益的严格换算**：连续 PI $u=K_p e+K_i\int e\,dt$，若用后向欧拉（矩形）离散，积分项近似 $K_i T_s\sum e[i]$，故**离散积分增益 = $K_i\cdot T_s$**。你的开关周期 $T_{sw}=20\mu s$、控制周期取 $T_s=100\mu s$，则离散积分系数应为连续 $K_i\times 10^{-4}$。调参时若把连续域 $K_i=50$ 直接当离散增益用（漏乘 $T_s$），积分会慢 1 万倍，等同于"积分几乎不动作"——这正是 §6 主文"时间尺度判据"在离散域的等价表述。
- **位置式 vs 增量式（速度式）**：上面 3.2 是**位置式**（直接算占空比绝对值），优点是直观，缺点是积分饱和时整段跳变。MCU 工程实现更常用**增量式/速度式**：只算占空比的变化量 $\Delta u[k]=K_p(e[k]-e[k-1])+K_i T_s e[k]$，输出 $u[k]=u[k-1]+\Delta u[k]$。增量式在限幅/停机时天然"无累积冲击"、便于手自动无扰切换（bump-less transfer），但需注意它把积分隐含在 $u[k-1]$ 里，抗饱和仍要单独处理。
- **Tustin/双线性近似**：比后向欧拉更高精度的离散化用 Tustin 变换 $s\leftarrow\frac{2}{T_s}\frac{z-1}{z+1}$，连续积分 $\frac{K_i}{s}$ 变为 $\frac{K_i T_s}{2}\frac{z+1}{z-1}$，对频率保真更好；代价是实现稍复杂，且对纯积分环节要防初始条件跳变。仿真阶段用连续 PID 足够，代码生成前再选一种离散法并验证阶跃响应对齐。

#### 四、抗积分饱和（Anti-Windup）

##### 4.1 什么是积分饱和？

当控制器输出达到物理极限（如占空比达到上限，上限为 1.0），但误差仍然存在且方向一致时，积分器会继续累加，使内部积分值远大于限幅值。当误差反向时，需要很长时间才能将积分值“放空”回到限幅范围内，这期间输出一直卡在极限值，造成大幅超调或长时间调节。

**你的案例**：软启动初期Vref=0，Vbus=48V，若$e = V_{ref}-V_{bus}$为很大的负值，且PI参数较小，积分器会累积极大的负值，导致输出长期卡在0，上管一直关断，下管一直导通，驱动信号异常。这就是积分饱和的典型表现。

##### 4.2 抗积分饱和原理

基本思想：**当输出达到饱和且误差方向仍然试图让输出进一步深入饱和时，停止积分更新**。

条件：

- 如果 $u_{未限幅} > UpperLimit$ 且 $e > 0$（试图继续增大输出），则冻结积分累加。
- 如果 $u_{未限幅} < LowerLimit$ 且 $e < 0$（试图继续减小输出），则冻结积分累加。

##### 4.3 Simulink中的实现方法

**方法1：使用PID模块内建抗饱和**

在`PID Controller`（连续或离散）参数中，勾选 **Limit output**，并设置上下限。模块内部集成了**条件积分**或**反向计算**抗饱和算法，一般选择默认的“Clamping”（条件积分）即可。

**方法2：手动搭建抗饱和**

在手动搭建的PI结构中，在积分器前增加逻辑：

- 用`Switch`或`Enabled Subsystem`比较输出是否等于限幅值且误差方向是否同向，若同向则使积分器输入为0（或禁止积分器使能）。

**方法3：反向计算法（Back-calculation）**

输出限幅后，将限幅前后的差值乘以一个跟踪增益$K_b$反馈到积分器输入端，主动将积分值拉回限幅边界。

$$
u_{未限幅} = K_p e + I_{积分} \\
u = sat(u_{未限幅}) \\
\dot{I}_{积分} = K_i e + K_b (u - u_{未限幅})
$$

$K_b$通常取$1/K_p$或更小。Simulink PID模块中也提供此选项。

##### 4.4 对你的系统建议

- 当前连续PID模块已勾选 **Limit output**，并设限幅 [0，1.0]（早期曾误设 0.9 导致母线无法稳 48V，已在 修正为 1.0），提供基本的抗积分饱和保护。
- 在软启动和剧烈扰动时，如果出现积分饱和导致的长时间死区，可以考虑：
- 增加 $K_i$（你已验证 $K_i=2.0$ 能快速退出饱和，但稳态可能振荡）。
- 更根本的方法：让Vref从48V附近开始软启动（而非0V），或让母线电容初始电压跟随Vref，从源头消除大误差。
- 使用**反向计算法**可更平滑地退出饱和。
- **三种抗饱和策略对比（权威参考：MathWorks "Anti-Windup Control Using a PID Controller"）**：Simulink PID 模块内建两种——**Back-calculation（反向计算）**&#x4E0E; **Clamping（条件积分/冻结）**，另有 **Tracking 模式** 处理外部复杂饱和。① Clamping：输出饱和且与误差同向时冻结积分，最简单、零额外增益，但退出饱和瞬间可能略有滞后；② Back-calculation：把限幅前后差值经跟踪增益 $K_b$（常取 $1/K_p$，对应时间常数 $T_t\approx T_i$）反馈卸掉积分，退出更平滑，适合级联/温控；③ Conditional Integration：仅在不饱和或误差反向时积分，鲁棒但需额外逻辑。经验：电机/驱动类用 Clamping，温控/级联用 Back-calculation；连续 PID 的默认 Clamping 已够用，若软启动退出慢可切 Back-calculation。
- **抗饱和不是万能（重要提醒）**：第二篇已指出"抗饱和损害扰动响应"——在大幅扰动中，抗饱和会"记住"并抑制积分，使控制器在扰动刚结束时反应迟钝；且抗饱和改的是积分状态，不改变 P 项瞬时冲击。因此它只是"减伤"不是"治本"，治本仍是软启动（从源头消除大误差）与合理增益。不要把"加了抗饱和"当成可以省去软启动的理由。
- **工程量级直觉**：限幅 [0,1.0]，若误差 $e=-48\,\text{V}$（软启动 Vref=0 瞬间），$K_i=50$ 时积分器每秒要往负向推 $50\times48=2400$ 个占空比单位——远超 [0,1] 量程，必然深度饱和；这反证"必须用软启动把初始误差压到几伏以内"，否则再好的抗饱和也救不回启动冲击。

#### 五、小结：PI调试与实现的工程要点

1. **理解P、I的影响**：P管快速性，I管无静差。两者均会引入超调/振荡，需折中。
2. **调试顺序**：先P后I，从0开始逐步增大I。注意启动时的物理冲击可能迷惑P的调节效果。（"P 太小反而超调更大"的根因与软启动治本之策）
3. **离散化习惯**：使用`Discrete PID Controller`采样周期选开关周期的倍数，配合采样保持和计算延迟模型（Transport Delay）。
4. **抗积分饱和是必须的**：任何存在输出限幅的PI都必须配备抗饱和机制，否则饱和-延迟-超调将严重恶化瞬态响应。
5. **软启动是治本之策**：让参考值或初始条件匹配，避免启动瞬间的大误差，可大幅减轻PI及抗饱和的负担。

- **落地自查清单**：① 极性符号推过一遍（误差正→占空比方向对）；② 先整定 P 看稳态与超调，再缓加 I 消静差；③ 离散化时积分增益记得乘 $T_s$；④ 限幅值等于执行器真量程；⑤ 必开抗饱和；⑥ 参考值软启动从源头避饱和；⑦ 采样率避开开关频率整数分频（见 §8）。七条全过，PI 环基本不会翻车。

### 7. 软启动原理

启动瞬间Vref突变→误差巨大→PI饱和→光伏满功率涌入电容→电压冲击。让Vref从0V线性升到48V(Ramp模块)可从根本消除冲击。⚠️ **软启动要加在设定值(Ramp Vref)，不要加在PID输出(Rate Limiter)**——后者会放大冲击。

- **能量视角看冲击**：母线电容储能 $E=\frac12 C V_{bus}^2$。若 Vref 从 0 阶跃到 48V 而 PI 瞬间给满占空比，光伏/电池会以最大功率往电容灌，电容电压被"硬拉"，同时浪涌电流 $i=C\cdot dV/dt$ 冲击开关管与电感。软启动让 Vref 缓升，等效把 $dV/dt$ 限制住，灌入电流被平滑。一句话：**软启动 = 给电容充电限速**。
- **为什么加在 Vref 而非 PID 输出**：若在 PID 输出加 Rate Limiter 限制占空比斜率，PI 内部积分仍在全速追大误差，一旦 Rate Limiter 放开，积攒的"过大输出"会一次性释放，反而造成二次冲击；而把 Vref 做斜坡，使误差本身就被"温和地"引入，PI 自始至终在小误差区工作，不会积累危险积分。故"软启动作用于设定值"是治本，"作用于输出"只是掩盖。

#### 7.1 软启动实现方式

常见软启动手段可分为三类：

1. **参考值斜坡**：用 Ramp/Step 模块让 $V_{ref}$ 从 0 缓慢升至目标值。斜坡时间 $t_{ss}$ 决定启动电流峰值；$t_{ss}$ 越大冲击越小，但启动越慢。适用仿真和数字控制器。
2. **PWM 占空比软启动**：控制器内部在启动阶段对占空比指令做限幅或斜坡，让 duty 从 0 慢慢展开。硬件 PWM 芯片常带`SS`引脚，外接 $C_{ss}$ 决定软启动时间。
3. **预充电 + 限流**：对大电容负载先经限限流电阻/NTC 热敏电阻预充电，电压建立后再短接电阻。公式：$I_{inrush} \approx C \cdot \frac{dV}{dt}$，延长 $dV/dt$ 可将浪涌电流压到安全范围。

**工程判据**：软启动期间电感电流峰值应小于开关管与电感额定电流；输出电压过冲一般要求 $<5\%$。若启动仍振荡，往往是 Ramp 斜率与 PI 积分速度不匹配，需同步放慢参考值斜坡并检查抗饱和。

- **斜坡时间 $t_{ss}$ 的定量估计（示意）**：设母线电容 $C$、目标电压 $V_{target}$、允许最大充电电流 $I_{max}$，则近似 $t_{ss}\gtrsim C\cdot V_{target}/I_{max}$（恒流充电时间常数级）。例如 $C=1\,\text{mF}$、$V_{target}=48\,\text{V}$、$I_{max}=10\,\text{A}$→$t_{ss}\gtrsim4.8\,\text{ms}$；若要更平缓取 20~50ms。 Ramp 斜率 2400（对应约 0.02s 到顶）即落在此区间，既消除冲击又不过慢。
- **三类手段的取舍**：参考值斜坡最简单、零硬件改动，适合仿真与数字控制；PWM 占空比软启动硬件原生（SS 引脚）但只限占空比、不限制参考误差本身；预充电+限流针对"大电容上电"的物理浪涌，常与软启动配合使用。工程上常"预充电解决上电浪涌 + 参考值斜坡解决控制冲击"双保险。

### 8. Simulink / Simscape 建模规则

- **物理域vs信号域**：Simulink信号(细线,无量纲)vs Simscape物理连线(粗线,带V/A/W,遵KCL/KVL)；互转用PS-Simulink Converter。MOSFET栅极必须用物理电压源驱动。
- **唯一接地**：必须有且仅有一个`Electrical Reference`定义零电位，所有功率地统一接它。
- **Solver Configuration物理接地(2025b)**：2025b强制Solver Configuration物理端口接地，否则初始化报错。（实操坑）
- **仿真步长与求解器**：具体求解器选型与步长/StopTime 配置统一见 §25.12（物理网络地基搭配，本笔记唯一权威来源），此处不重复。
- **传感器接线**：电流**串联**(Current Sensor)，电压**并联**(Voltage Sensor)。口诀"电流串、电压并、Converter不能空"。
- **ToWorkspace vs ToFile**：`matlab -batch`下 ToWorkspace 数据被**静默丢弃**，**批处理取数必须用 To File**(SaveFormat=Timeseries)。GUI下两者皆可用。
- **⚠️ 采样率陷阱（最重要）**：`To File`采样周期若等于开关周期，会与 50kHz 开关纹波**同频锁相**，均值被固定相位点污染→产生**虚假直流偏差**并会**误判均流比**。正确做法：采样率取开关频率 10–20 倍(**1µs**)，指标计算前做 **20µs 滑动平均**(movmean)得到开关周期平均值。判据：**采样率必须避开开关频率及其整数分频**。这是把"采样伪影"误判为"控制缺陷"的关键转折。
- **Simscape 求解器与单位一致性**：Simscape 物理网络默认用变步长 + 其自带局部求解器（具体求解器选型与步长配置见 §25.12）。物理量跨域时带单位（V/A/W/Ω），必须经 PS-Simulink Converter 且**显式指定单位**，否则出现"无量纲数当伏特用"的隐性错误。
- **避免物理网络里的隐性代数环**：Simscape 元件（电阻、电感、理想电压源等）本质由 DAE 描述，强塞"理想电压源直接并联理想电容"等会制造无解/刚化代数环。建模时给电容串联极小串联电阻（ESR）、给理想源加内阻，既符合实际又能给求解器一个"抓手"，仿真更稳。
- **模块化与可读性**：功率级（MOSFET/电感/电容/电池）放一个 Subsystem，控制信号（PI、Ramp、传感器）放另一层，用 Goto/From 或总线减少飞线；批处理（`matlab -batch`）下取数坚持 To File，避免 ToWorkspace 静默丢数据（见主文）。

#### 8.1 代数环识别与处理

**代数环**指 Simulink 中由直接馈通模块（Gain、Sum、Transfer Fcn、Saturation 等）构成的无延时闭环。代数环求解器需在每步迭代求解隐式方程，轻则拖慢仿真，重则发散。

识别特征：

- 模型出现`Algebraic loop detected`警告；
- 仿真步长被迫极小、速度显著变慢；
- 含直接馈通模块的闭环中没有 Unit Delay/Memory/Transport Delay。

的代数环：

- 源2 开环下垂律路径`Subtract1 → Gain2 → Saturation3 → Solver Configuration`是模型既有特性，Simscape 会自动启用模式迭代算法继续求解，结果有效。
- PI 闭环中在 PID 输入前插入`Transport Delay 1μs`正是为了破环。

处理原则：

1. **先判断是否为良性环**：Simscape 物理网络本身常出现代数环，由求解器自动处理；若仿真收敛且结果合理，不必强行破环。
2. **控制环中的代数环**：在反馈路径插入小延时（Unit Delay/Transport Delay），延时量取控制周期的 1~5 倍或开关周期的 1/10。
3. **避免误环**：不要把 PID 输出直接连回其输入；不要让 Saturation 输出直接反馈到其输入而不经积分/延时。

- **破环模块选型（重要）**：在连续控制环里破环，**优先用一阶 Transfer Fcn（时间常数取极小，如 1e-6）或 Unit Delay/Transport Delay**，而**不要对连续环用 Memory 块**。MathWorks 官方博客指出：Memory 块虽也延迟一拍，但输出不连续，若它驱动带连续状态的模块（如 State-Space/Integrator），变步长求解器会每变一次就重启动、步数暴涨至数十万乃至卡死；离散环用 Memory 才安全。用 `Transport Delay 1μs` 正是连续安全的破环法。
- **人工代数环（Artificial Algebraic Loop）**：Atomic Subsystem / Model 引用会把"输入直接连到输出"误判成代数环——即使子模块内部并无直接馈通。解法：勾选模块参数的 *Minimize algebraic loop occurrences*，或拆分子系统使直接馈通路单独成块；代码生成时还要清掉 *Single output/update function* 以去除人为直接馈通。
- **先诊断再破环**：用 `Simulink.BlockDiagram.getAlgebraicLoops(model)` 可定位真实代数环并高亮路径；诊断页 *Algebraic loop* 设为 `warning` 先看是否良性。Simscape 物理网络的代数约束由求解器一并处理，通常无需手动破——只有控制环（含直接馈通 Gain/Sum/Saturation）才需要插延时。切勿"见环就破"，否则引入多余动态、改变环路相位。

### 9. 双源并联下垂控制（理论）

- **为何从定压切下垂**：定压(Vref固定48V)多源并联会"抢"、分配不可控且须通信；下垂让Vref随自身电流略降，多源按虚拟电阻反比自然均流，**无需通信**，代价是稳态母线略低于48V。
- **公式与符号陷阱（最关键）**：教材 $V_{ref}=V_0-k\cdot I_{out}$(Iout流出为正)；**** $V_{ref}=V_0+k\cdot I_{bat}$(Ibat流入电池=充为正、流出=放为负)。代入Ibat<0：$V_0+k\cdot(负)=V_0-|k\cdot I_{bat}|$电压确降✅；误用减号则放电时电压反抬❌。⚠️ 前面40天计划写 $V_0-k\cdot I_{battery}$ 属笔误，未改计划原文。（该符号陷阱的实操踩坑与修复）
- **本模型双源结构**：源1(主,电池侧)PID闭环 $V_{ref}=V_0+k_1 I_{bat}$，虚拟电阻 $R_{v1}=1/k_1=0.1⇒k_1=10$；源2(从,受控电流源)开环代数下垂律 $I_2=(V_{02}-V_{bus})/R_{v2}$，$R_{v2}=0.2$。均流比 $I_1:I_2=R_{v2}:R_{v1}=0.2:0.1=2:1$，即**源1（虚拟电阻更小，$R_{v1}=0.1$）出力约为源2的2倍**——虚拟电阻越小该源越"愿意"分担电流，这是下垂均流的核心规律（注：源1为闭环PID、源2为开环代数律，理想下垂律下按此比例分配）。
- **软启动对齐**：V0(Ramp138,slope2400)、V02(Ramp167,slope2400)、光照乘子(Ramp1/140,slope50)三者均≈0.02s到顶，避免启动冲击。
- **电流传感器定位**：I1=Ibat(块102,源1出力)；I2=受控源支路电流(块165,Scope`I2_Sensor`源2出力)；公共母线电压复用同一Vbus测量点。
- **给零基础读者的物理直觉（并联电阻类比）**：把每台电源想象成一个"电压源串联一个小电阻（虚拟电阻 $R_v$）"。两个这样的源并联到同一条母线——就像两个不同阻值的电阻并联接在同一电压上——电流会按"电导"（电阻的倒数）自然分配：阻值小的源分压小、流过的电流大。下垂控制正是给每台电源"人为串联"一个虚拟电阻，从而无需任何通信，电流就按 $1/R_v$ 自动分配。代价是母线电压会比理想 48V 略低（被虚拟电阻分压）。
- **虚拟电阻（下垂系数 $k=1/R_v$）怎么选——设计准则**：
 1. $R_v$ 越小 → 该源越"愿意"出力。
 2. 但 $R_v$ 太小 → 母线电压跌落太小、均流对参数误差敏感；太大 → 母线跌落大、调压精度差。工程经验：先按"允许的最大母线跌落"（如 ≤2%~5%×48V）反推总等效虚拟电阻，再按目标均流比拆分给各源。
 3. 多源应取相同的 $R_v$ 或按容量反比分配，且各源控制器对 $I$ 的测量方向/符号必须一致（见本段"符号陷阱"）。
- **定量算例（教学示例，非实测值）**：设两源虚拟电阻 $R_{v1}=R_{v2}=0.1$Ω，总负载电流 30A，按 $1/R_v$ 均分则各 15A；母线跌落约 $0.1×15=1.5$V，稳态母线≈46.5V。可见"无通信均流"是以牺牲约 1.5V 母线电压为代价的——这正是下垂控制的核心权衡。
- **常见误区**：
 - ❌ 以为下垂能"完全消除"母线压降：不能，虚拟电阻必然分压，只能靠二次控制/电压补偿把压降补回（见 §9.1 改进手段）。
 - ❌ 把 $I_{out}$（流出为正）与 $I_{bat}$（流入电池为正）的符号混用，导致放电时电压反而抬高（详见本段符号陷阱，已踩坑修复）。

#### 9.1 线路阻抗对下垂均流的影响

> 用下垂控制让双源并联"无通信自动均流"，但下垂并非完美——**线路阻抗不等会破坏均流精度**。

- **机理**：下垂控制让每台 DG 按本地测量的 $P/Q$ 自主调整电压/频率。但实际输出电压为 $V_i=V_{ref}-k_i I_i - I_i Z_{line,i}$，当各源到母线的**线路阻抗 $Z_{line}$ 不等**（实际低压微电网普遍存在）时，即使下垂系数 $k_i$ 相同，电流分配也会失衡。
- **阻性线路的额外麻烦**：低压微电网线路呈阻性，破坏 P–f / Q–V 的解耦假设，使**无功均流误差尤其大**。文献综述（ScienceDirect 多篇、PatSnap 50+ 文献分析）给出能力雷达：下垂在"无功均流"仅 3/10，而"虚拟阻抗"达 8/10；"环流抑制"下垂 2/10、虚拟阻抗 9/10。
- **改进手段**：

1. **虚拟阻抗（Virtual Impedance）**：在电压内环参考中叠加软件定义的阻抗，重塑各 DG 的有效输出阻抗，使它们"看起来"线路条件一致，**无需改动物理线路**；代价是增大输出电压跌落。
2. **线路阻抗补偿 / 二次控制**：中央或分布式二次控制估计并补偿阻抗差。
3. **自适应虚拟阻抗**：仅用本地无功反馈实时调节虚拟阻/感，无需通信（ScienceDirect 2025 多篇 HIL 验证，均流误差 <1%、环流抑制 >93%）。

- **衔接**： 双源模型中两源线路阻抗设为对称（理想），故均流良好；真实部署时必须保证线路阻抗对称，或在控制中加入虚拟阻抗，否则轻载/重载下会出现一台源过载、另一台欠载。
- **定量的均流误差公式（为什么阻抗一不等就偏）**：把线路电阻 $R_{line,i}$ 与虚拟电阻 $R_{v,i}$ 合并成"等效下垂电阻" $R_i^{eff}=R_{v,i}+R_{line,i}$。并联母线电压相等 ⇒ $(R_{v,1}+R_{line,1})I_1=(R_{v,2}+R_{line,2})I_2$，即实际均流比由**等效电阻的倒数**决定：

 $\frac{I_1}{I_2}=\frac{R_{v,2}+R_{line,2}}{R_{v,1}+R_{line,1}}$

 若两源虚拟电阻本相同（$R_{v1}=R_{v2}$），但线路电阻一短一长（$R_{line,1}<R_{line,2}$），则靠"短电缆"的源会多承担电流。
- **定量算例（教学示例）**：设 $R_{v1}=R_{v2}=0.1$Ω，$R_{line,1}=0.01$Ω（短缆）、$R_{line,2}=0.05$Ω（长缆）。理想均流比应为 1:1，实际 $I_1/I_2=(0.1+0.05)/(0.1+0.01)=0.15/0.11≈1.36$ ——靠短缆的源多承担了约 36% 的电流，长缆源欠载。线路阻抗差越大、与虚拟电阻相比越不可忽略，均流误差越大。
- **虚拟阻抗如何"抹平"阻抗差（机理，衔接 §9.1 改进手段）**：软件虚拟阻抗相当于在各源控制里再**人为叠加**一个可调电阻/电感，使得"虚拟电阻+线路电阻+虚拟阻抗"三者和对所有源趋于一致。关键是它不碰物理线路、只改软件参考，因此可在不改硬件的前提下把均流误差拉回近零；代价是同样会增大输出电压跌落，需配合二次电压补偿（§9.1 文献里的补偿项 $\Delta V=\frac12(i_{o1}+i_{o2})(R_{line1}+R_{line2})$ 把跌落补回）。
- **仿真建模要点**：在 Simulink 里若想复现"阻抗破坏均流"，只需把两源到母线的串联电阻（Series Resistance / 连线 R）设成不同值；观察 Scope 里 $I_1$、$I_2$ 比值偏离理论值即可验证本段结论。要验证虚拟阻抗修复，则在电压内环参考里减去 $Z_{vir}\cdot I_i$ 项。
- **常见误区**：❌ 认为"只要下垂系数 $k$ 调成一样就能完美均流"——错，线路阻抗不等会直接写进等效下垂电阻，抵消 $k$ 的一致性；❌ 认为低压微电网可忽略线路电抗——低压线路以阻性为主，恰是阻性让无功/电流均流误差更突出（见本段"阻性线路的额外麻烦"）。

### 11. 扰动测试步骤与波形分析

**步骤**：①开闭环模型另存扰动版；②光照阶跃(0~0.03s=1000,0.03s突降500,0.06s恢复1000,接PV_Panel的Ir)；③负载投切(0.04s闭合20→10Ω,0.07s断开)；④4路To File记Vpv/ILboost/Vbus/Ibat；⑤运行0.12s分析。

**预期波形**：光照突降→Vpv略跌、ILboost减半、Vbus瞬跌后PID拉回、Ibat变负(放)，能量流PV不足→电容放→压降→电池放→补偿；负载突增→Vbus下凹、Ibat负(放)；负载突卸→Vbus上凸、Ibat正(充)。

**评估指标**：最大动态偏差、恢复时间(±2%)、ITAE $\int t\cdot|e(t)|dt$。结论：偏差<4%、恢复<10ms、方向全对→通过。

- **仿真建模要点（照做清单）**：① 扰动版务必"另存为"新文件，别在定压/闭环主模型上直接改；② 光照阶跃接 PV 模块的**辐照度输入**（Ir/Irradiance），不是直接改输出；③ 负载投切用理想开关（Ideal Switch / Breaker）在 20Ω↔10Ω 间切换，记得给断开支路留泄放路径；④ 四路 To File 命名与§11 一致（Vpv/ILboost/Vbus/Ibat），采样步长 ≤ 控制步长，否则恢复时间读数失真；⑤ 运行总时长 0.12s 足够覆盖两次扰动（0.03/0.06s）与恢复。
- **波形判读常见误区**：❌ 把"母线电压瞬时偏差"误判为控制失败——任何扰动都会先有瞬时偏差，关键是能否在 ±2% 内恢复；❌ 只看单一通道——必须 Vbus 与 Ibat 对照看，才能确认"方向全对"（放电 Ibat<0、充电 Ibat>0）；❌ 恢复时间从"扰动命令发出"算而非"扰动实际生效"算，导致读数偏短。
- **为什么用 ITAE 而非单纯超调**：超调只关心"最高冲多高"，恢复时间只关心"何时回稳"，二者都看不见"中间长时间小幅振荡"。ITAE 乘了时间权重，对"慢悠悠拖很久才稳"惩罚更重，适合做不同 PI 参数的客观打分（见 §11.1.3）。
- **定量验收门槛（通用准则）**：偏差 <4%、恢复 <10ms、电流方向全对 ⇒ 通过。这三条同时成立才算合格，任一不满足都要回 §6 重新整定 PI。

#### 一、暂态响应评价指标

- **本节在做什么（读前导读）**：下面三个小节（超调、恢复时间、ITAE）给出的是"怎么用数学语言描述一次扰动的暂态波形"。它们是§11 测试步骤的"评分标准"——先会读波形（本节），再懂指标与控制器参数的关系（§11.1），才能把"调 PI"从玄学变成可量化工程。

##### 1.1 超调量（Overshoot，σ%）

**定义**：响应峰值超过稳态值的百分比。

$\sigma\% = \frac{V_{peak} - V_{ss}}{V_{ss}} \times 100\%$

- **现象**：扰动发生后，母线电压瞬间偏离设定值（上冲或下凹）。
- **工程要求**：超调越小越好，通常希望低于5%~10%。
- **观察**：光照突降时，Vbus先下凹再回升；负载突减时，Vbus先上凸再回落。
- **与阻尼比的关系（衔接 §11.1）**：对经典二阶系统，超调量只由阻尼比 $\zeta$ 决定：$\sigma\%=100\cdot e^{-\pi\zeta/\sqrt{1-\zeta^2}}$。$\zeta$ 越小超调越大；$\zeta=0.7$ 时超调仅约 4.6%。所以"看到超调大"≈"系统阻尼不足"，对应 PI 里 $K_p$ 偏小或积分过冲。
- **定量算例**：若实测 Vbus 从 48V 最低跌到 46.2V 再回稳，超调（此处为下凹幅度）$=(48-46.2)/48≈3.75\%$，落在 <4% 的验收线内；若跌到 45V 则≈6.25%，超了——需加阻尼。
- **误区**：❌ 把"下凹"和"上凸"都叫超调——严格说上凸是正向超调、下凹是负向跌落，但工程上二者都归为"最大动态偏差"一并考核（见§11 评估指标"最大动态偏差"）。

##### 1.2 恢复时间（Settling Time，ts）

**定义**：从扰动发生时刻起，系统重新进入并保持在稳态值±2%（或±5%）范围内所需的时间。

- **物理意义**：衡量控制器多快能消除扰动影响。
- **观察方法**：在Scope中标定 Vbus=48V 的 ±2% 容差带（约 ±1V）的上下界限，测量从扰动开始到波形不再超出该界限的时间。
- **观察**：调节时间小于0.02s。
- **±2% 与 ±5% 怎么选**：一般功率电子闭环取 ±2%（更严），对缓慢热/机械系统可取 ±5%。母线 48V 取 ±2% 容差带（即约 47~49V 判定区间）。
- **读数技巧**：在 Scope 里用两条 Constant/参考线画出上下界限，肉眼或用 `find`/`min`/`max` 逻辑自动判定"首次越界后再次永久进入带内"的时刻即为 $t_s$；从扰动发生（如光照阶跃生效时刻）起算，不是从仿真 0 时刻。
- **误区**：❌ 把"第一次回到带内"当恢复时间——必须"之后不再越界"才计，否则短暂回带又冲出会被漏判。

##### 1.3 ITAE（积分时间乘绝对误差）

**定义**：对误差绝对值乘以时间后积分，数值越小表示动态性能越好。

$ITAE = \int_0^T t \cdot |e(t)| dt$

- **优点**：既惩罚大误差，也惩罚长时间未消除的误差，比单纯看超调或恢复时间更全面。
- **用途**：定量比较不同PI参数或控制策略的优劣。
- **与二阶公式的接口（衔接 §11.1）**：ITAE 虽无简洁闭式对应单一 $\zeta$，但经验上 ITAE 小 ⇔ 恢复快且超调小 ⇔ $\zeta\omega_n$ 大。做 PI 参数扫描时，把每组参数的 ITAE 画成柱状图，最低那根就是"综合动态最优"候选，再回看其超调/恢复是否也达标。
- **计算注意**：积分区间取"扰动前一段稳定 → 扰动后恢复"的完整窗口；$e(t)=V_{bus,ref}-V_{bus}(t)$，用绝对值避免正负抵消。Simulink 里可用 `Integrator` 接 `t·|e|` 实时累计，或导出数据在 MATLAB 里 `trapz(t, t.*abs(e))`。
- **误区**：❌ 积分到仿真结束而非到恢复——若末尾仍有小静差，ITAE 会被无限拉长，失去比较意义；建议积分到 $t_s$ 即可。

---

#### 二、能量流向分析

- **本节在做什么（读前导读）**：暂态指标（§一）告诉你"波形好不好"，本节告诉你"能量到底从哪流到哪"——也就是波形背后的物理因果。理解了能量流向，才知道 PI 为什么那么调、占空比为什么那样变。两张表是同一扰动在"时间轴"上的能量剧本。

##### 2.1 光伏不足 → 电池放电

**工况**：光照突降（1000→500 W/m²）或负载突增。

| 时间段 | 物理过程 | 母线电压 | 电池电流 |
| ---- | ------------------------- | ------ | -------------- |
| 扰动瞬间 | 光伏功率减小，负载不变，功率缺口出现 | 开始下跌 | 仍为原值 |
| 暂态 | 母线电容放电，补偿功率缺口 | 下跌至最低点 | 尚未完全响应 |
| 恢复 | PI增大上管占空比（增强Boost倾向），电池放电 | 从最低点回升 | 变为负值（放电） |
| 稳态 | 电池放电功率 = 功率缺口 | 稳定在48V | 负值，幅值约缺口功率/48V |

**能量流向**：光伏不足 → 电容放电 → Vbus下跌 → PI使电池放电 → Vbus恢复。

- **占空比方向澄清（防混）**：双向变换器，"增强 Boost 倾向"= 让电池侧向母线送更多能量 = 放电，对应**增减占空比**取决于你站在哪一侧定义（见§6 符号约定）。判据始终是"Ibat 变负 = 放电"，不要死记"增减"，要看电流符号。
- **电容的"第一响应"量级感（教学示例）**：若负载突增造成 50W 瞬时缺口、母线支撑电容 500µF、允许电压跌 1V，则所需放电电荷 $Q=C\cdot\Delta V=500µF×1V=500µC$，按缺口功率算可持续约 $Q\cdot V/P=500µC×48V/50W≈0.48$ms——说明电容只能顶极短时，真正的补偿必须靠电池（第二响应）接力，PI 反应慢了电压就会凹得更深。
- **误区**：❌ 认为"光伏不足时电池立刻顶上"——实际电容先顶几百 µs，PI 检测到压降、调整占空比后才轮到电池，存在不可忽略的暂态时差，这正是恢复时间 >0 的根源。

##### 2.2 光伏富余 → 电池充电

**工况**：光照恢复（500→1000 W/m²）或负载突减。

| 时间段 | 物理过程 | 母线电压 | 电池电流 |
| ---- | ------------------------ | ------ | -------------- |
| 扰动瞬间 | 光伏功率增大，负载不变，功率盈余出现 | 开始上升 | 仍为原值 |
| 暂态 | 盈余功率给母线电容充电 | 上升至最高点 | 尚未完全响应 |
| 恢复 | PI减小上管占空比（增强Buck倾向），电池充电 | 从最高点回落 | 变为正值（充电） |
| 稳态 | 电池充电功率 = 功率盈余 | 稳定在48V | 正值，幅值约盈余功率/48V |

**能量流向**：光伏盈余 → 电容充电 → Vbus上升 → PI使电池充电 → Vbus恢复。

- **与 2.1 的对称性**：富余工况是不足工况的"镜像"——盈余功率先灌电容使 Vbus 上凸，PI 减小 Boost 倾向（增强 Buck）、让电池吸收盈余，Ibat 转正（充电）。稳态时"电池充电功率 = 功率盈余"。
- **定量感（教学示例）**：若光照恢复带来 100W 盈余、母线 48V，则稳态充电电流约 $100W/48V≈2.08$A，与前文"稳态1000/20Ω→+1.79A(充)"同量级、符号一致——印证能量流向判据自洽。
- **误区**：❌ 把"Vbus 上凸"当成异常——只要随后回落进带内、Ibat 为正，就是正常的充电暂态，不是过压故障（过压由 §13 保护逻辑判定）。

##### 2.3 核心启示

- **电容是“能量的第一时间响应者”**：功率突变瞬间，电容立即提供或吸收电荷，导致电压快速变化。
- **电池是“能量的第二响应者”**：PI控制器检测到电压偏差后，调整双向变换器占空比，让电池接手功率补偿。
- **PI控制器决定了恢复的速度和超调量**：参数过小恢复慢，过大可能振荡。
- **一句话记忆法**：电容=救急（毫秒级、无脑）、电池=主力（秒级、靠控制）、PI=调度（决定救多快、凹多深）。三者时序：扰动→电容瞬时反应→PI 感知偏差→电池接力→回稳。
- **延伸到整定**："恢复慢"多半是 PI 积分增益 $K_i$ 太小（电池接力太慢，见 §11.1）；"凹太深/振荡"多半是阻尼不足（比例 $K_p$ 或虚拟阻抗问题）。把波形异常直接映射到这两个旋钮，调参不再靠试。
- **误区**：❌ 以为加大电容就能解决一切——电容只能改"第一响应深度"，不改变"第二响应速度"，且大电容会拉低 LC 谐振频率、挤占环路带宽（见 §11.1 的 $f_{LC}$ 约束），过犹不及。

#### 11.1 暂态指标与阻尼比/带宽的对应关系

§11 定义的三项指标（超调、恢复时间、ITAE）是"读数口径"，下面给出经典二阶原型系统的**解析对应关系**，作为 PI 调参时的直觉桥梁：

设闭环极点 $-\zeta\omega_n\pm j\omega_n\sqrt{1-\zeta^2}$（$\zeta$ 阻尼比、$\omega_n$ 自然频率、$\omega_d=\omega_n\sqrt{1-\zeta^2}$ 阻尼振荡频率），则：

- **峰值超调**：$M_p=e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}$，$\%OS=100\cdot e^{-\frac{\pi\zeta}{\sqrt{1-\zeta^2}}}$。
- **峰值时间**：$t_p=\frac{\pi}{\omega_d}$。
- **调节时间**：$t_s(±2\%)\approx\frac{4}{\zeta\omega_n}$，$t_s(±5\%)\approx\frac{3}{\zeta\omega_n}$。
- 经验表：$\zeta=0.6$→超调≈9.5%；$\zeta=0.7$→≈4.6%；$\zeta=0.8$→≈1.5%。一般要求超调≤10% 即取 **$\zeta\ge0.6$**。

**对 调试的启示**：

- 本微电网并非纯二阶，但电压环等效可近似看成"被控对象（LC+负载）＋PI"，其动态由一对主导极点决定。调参时：
- **$K_i$（积分增益）决定 $\omega_n$（响应速度/带宽）**：$K_i$ 越大 → $\omega_n$ 越高 → 恢复时间 $t_s$ 越短，但过大易激发 LC 谐振、超调变大。
- **$K_p$（比例增益）主要影响 $\zeta$（阻尼）**：$K_p$ 适当增大会提升阻尼、压低超调；但纯比例无法消除静差（见 §6）。
- 这与 实测一致：扰动后 Vbus 下凹/上凸、恢复时间 <10ms、$|偏差|<4\%$，说明闭环 $\zeta$ 与 $\omega_n$ 配合合理；若某次出现"恢复慢+残留偏差"，多半是 $K_i$ 太小（积分来不及推满执行器，见 §6 时间尺度判据）而非 $K_p$ 问题。
- 用二阶公式可把"恢复时间 <10ms"翻译成带宽要求：$t_s(±2\%)\approx4/(\zeta\omega_n)<0.01\,\text{s}$ ⇒ $\zeta\omega_n>400\,\text{rad/s}$，给 PI 参数定了一个数量级基准。
- **补：输出滤波 LC 谐振频率与环路带宽避让**（§11.1 说"$K_i$ 过大激发 LC 谐振"对应的硬指标）：母线侧输出滤波等效为 **滤波电感 $L$ + 母线支撑电容 $C$** 的二阶网络，其无阻尼谐振频率为

 $f_{LC}=\frac{1}{2\pi\sqrt{L\,C}}$

 算例：$L=500\,\mu\text{H}$、$C_{out}=500\,\mu\text{F}$ ⇒ $f_{LC}\approx 318\,\text{Hz}$。**设计准则**：电压环穿越频率 $f_c$（即环路带宽）须**远低于** $f_{LC}$（一般取 $f_c\lesssim f_{LC}/3\sim f_{LC}/5$）且须靠电容 ESR / 阻尼网络提供该频点阻尼，否则 PI 会去"驱动"这个谐振峰、轻则超调放大、重则自激振荡；同时 $f_c$ 又要**远高于**开关纹波频率（至少 $>f_s/2=25\,\text{kHz}$ 才不会跟 50kHz 纹波较劲）。 $f_s/2=25\,\text{kHz}\gg f_{LC}\approx318\,\text{Hz}$，中间区间极宽裕，故定压 PI 带宽（§11.1 估 $\zeta\omega_n>400\,\text{rad/s}$ 对应 $f_c$ 约百 Hz 量级）天然落在"远低于 $f_{LC}$"的安全侧；母线电容 ESR 再补一道阻尼。这正是"$K_i$ 过大→$\omega_n$ 抬升逼近 $f_{LC}$→激发谐振"量化后的边界。
- **怎么从 Scope 反推 $\zeta$ 与 $\omega_n$（实操）**：抓一段近似二阶的欠阻尼响应，量出超调 $\sigma\%$ 与峰值时间 $t_p$，反解：

 $\zeta=-\frac{\ln(\sigma/100)}{\sqrt{\pi^2+\ln^2(\sigma/100)}},\quad \omega_d=\frac{\pi}{t_p},\quad \omega_n=\frac{\omega_d}{\sqrt{1-\zeta^2}}$

 这样无需拆控制器，就能把"看到的波形"翻译成"等效 $\zeta,\omega_n$"，再对照上面 $K_i↔\omega_n$、$K_p↔\zeta$ 的映射，知道该动哪个增益。
- **设计红线小结**：① 超调≤10% ⇒ $\zeta\ge0.6$；② 恢复<10ms ⇒ $\zeta\omega_n>400$rad/s；③ 环路带宽 $f_c$ 须 $\ll f_{LC}$ 且 $\gg f_s/2$。三条同时满足才有"又快又稳又不自激"的闭环。
- **误区**：❌ 把"二阶近似"当精确模型——本微电网是非线性（含开关、限幅、模式切换），二阶只给数量级直觉，最终以时域仿真验收为准；❌ 只调 $K_p$ 想压超调却忘了 $K_i$ 决定速度，二者要协同。

### 12. 隔离型双向 DC/DC 与软开关

> 起母线升级到 700V，电池(48V)须经**隔离型双向DC/DC**接入，兼获电气隔离与安全。主流拓扑经核实如下：

- **双有源桥 DAB（最常用）**：原副边各一个全桥 + 高频变压器，靠两侧驱动**移相角 φ** 控制功率大小与方向（φ>0 原边→副边，φ 越大功率越大，φ=0 不通）。利用变压器**漏感**实现**零电压开关 ZVS**（开关管在 Vds≈0 时导通，开关损耗≈0），重载效率可达 98%+。优点：双向、宽电压范围、动态响应快、易并联扩容。
- **移相全桥 PSFB**：原边全桥移相实现 ZVS，常用于单向大功率；与 DAB 同属"移相 + 漏感软开关"思路。
- **LLC / CLLLC 谐振**：靠**变频**实现全负载范围 ZVS/ZCS、轻载效率高、EMI 小，但增益范围窄、双向控制复杂；更适合单向或增益变化小的场合。
- **工程取舍**：储能/微电网常选 **DAB**——电池电压变化宽、需双向、且对电压波动不敏感（只调 φ）。 48V↔700V 正属此类。
- **标幺化移植**：把 48V 系统的 PI/下垂控制移植到 700V 时，可用**标幺值(pu)** 设计：电压、电流、功率除以各自额定基准，使控制器参数可跨电压等级复用，避免反复整定。

\*\* 搭建印证\*\*：按本节架构在``新建了 700V 开环模型——光伏经 Boost 升 700V、电池经隔离 DAB 接 700V、负载经 Buck 取电；DAB 用固定移相角 φ 开环驱动，验证了"变压器漏感即串联电感 $L$、靠 φ 控功率与方向"的核心机制（详见`_700V隔离DAB搭建教程与原理报告.md`）。关键确认：① 死区 ≥300ns 是 ZVS 与防直通前提；② 匝比 $n$ 取额定 $V_1\approx nV_2$ 时 ZVS 最易；③ 隔离变压器原副边须各自独立接地。

- **给零基础读者的拓扑选择直觉**：选隔离双向 DC/DC，本质是在"要不要隔离""要不要双向""电压范围多宽""轻载效率"四个维度上取舍。DAB 之所以成为储能/微电网首选，是因为它四项全中：隔离✓、双向✓、宽电压✓、且只调一个移相角 φ 就够，对电池电压波动不敏感。LLC 虽效率高但单向、窄增益，适合"电压基本不变"的场合。
- **ZVS 为什么省电（物理本质）**：硬开关时开关管在还有电压（$V_{ds}$ 高）的瞬间导通/关断，电压电流交叠产生开关损耗；ZVS 利用死区里漏感电流先给结电容 $C_{oss}$ 充放电，让 $V_{ds}$ 先降到≈0 再导通，开通损耗≈0。代价是需要"足够的电感电流"在死区内完成这个充放电——电流太小（轻载）就丢 ZVS，这正是扩展移相要解决的。
- **标幺值移植的设计要点**：跨电压等级复用控制器时，基准值取各侧额定值（如 48V 侧 $V_{base}=48$、700V 侧 $V_{base}=700$），所有电压/电流/功率除以基准得 pu 量，PI/下垂参数即可"一套参数管两边"，避免反复整定。注意功率基准两侧要一致（常用同一 $S_{base}$），否则 pu 电流含义错位。
- **仿真建模要点**：DAB 在 Simulink 里可用两个 `Universal Bridge`（或四个 `MOSFET`+反并联二极管）配高频变压器（`Mutual Inductor` 或 `Transformer` 设漏感）+ 串联漏感 $L$；死区用 `Dead Time` 块（≥300ns）；驱动用 `PWM Generator (DC-DC)` 设相位偏移实现 φ。
- **误区**：❌ 以为"有变压器就一定隔离安全"——必须确认原副边**地独立**、Y/Δ 或独立绕组，否则共模路径仍连通；❌ 以为 DAB 天生全负载 ZVS——轻载会丢，需扩展移相或加辅助电感。

#### 12.1 深化：扩展移相、回流功率与 SiC 选型

- **回流功率**：SPS 下平均净功率只是周期正/负瞬时功率的净值；当 $V_1\neq nV_2$ 时一个周期内存在能量倒灌段（不贡献净传输却增大 RMS 电流与导通损耗），即回流功率。匝比匹配时最小，失配时显著上升并吃掉 ZVS 裕量。
- **扩展移相**：在 SPS 外移相 φ 基础上引入**内移相**（DPS 双移相 / EPS 扩展移相 / TPS 三移相），使桥臂输出变三电平，可在电压失配时把电流应力/导通损耗降约 10–30%、减小回流功率；代价是控制变复杂（需在线优化 φ 组合）。开环只需 SPS 验证机理，效率优化留待后续（呼应 §12.1 的 ZVS 条件（轻载丢 ZVS 可用扩展移相缓解））。
- **700V 为何选 SiC**：700V 母线开关管实际承压 >700V，Si 超结 MOSFET ~900V 到顶、裕量吃紧；**SiC MOSFET 主流 1200V** 余量足、寄生 $C_{oss}$ 小、开关损耗低、结温高（175–200°C），且小 $C_{oss}$ 反过来利好 §12.1 的 ZVS 条件 $|i_L|\ge 2C_{oss}V_{dc}/t_d$。代价是成本高、栅驱需负压关断、EMI 更尖锐。**建议：母线侧 1200V SiC，电池侧 80–100V Si/SiC 皆可**。
- **Simulink 调制实现**：用`PWM Generator (DC-DC)`块（设占空比 + 相位偏移 Phase shift）生成两侧错相方波，死区 ≥300ns；功率方向由"哪侧桥超前"决定（交换 leading bridge 即反转充/放）；四臂均须死区，驱动经`Simulink-PS Converter`进 gate，变压器原副边各自独立接地。
- **SPS 传输功率与极值**：单移相 SPS 下平均功率 $P=\frac{nV_1V_2}{2\pi^2 f_s L}\,\phi(\pi-\phi)$（φ>0 原边超前=正向传输）；对 φ 求极值得最大功率 $P_{max}=\frac{nV_1V_2}{8\,f_s L}$（φ=π/2 时）。匝比 n↑、开关频率 $f_s$↑、漏感 L↑ 均增大功率能力，φ 只决定"用到几成"。
- **SPS 功率公式的零压/满功率点（教学）**：由 $P=\frac{nV_1V_2}{2\pi^2 f_s L}\phi(\pi-\phi)$ 可见，φ=0 时 P=0（无传输），φ=π/2 时达最大值 $P_{max}=\frac{nV_1V_2}{8f_sL}$，φ 再大到 π 又回到 0。所以"功率随 φ 单调增大"只在 φ∈(0,π/2) 成立；φ 超过 π/2 反而功率下降——这是 SPS 调功的硬边界，闭环 φ 限幅应卡在 [0, π/2]。
- **回流功率的直观图像**：把副边电压反射到原边后，与原边方波比较——当二者"同号区间"传输正功率、"异号区间"出现能量倒灌（瞬时功率负）。电压越失配（$|V_1-nV_2|$ 大），异号区间越长，回流功率占比越高，$I_{L,rms}$ 越大、导通损耗越重。扩展移相通过"内移相制造零电压平台"缩短异号区间来砍回流。
- **SiC 选型的补充量化**：Si 超结 MOSFET 耐压约 900V 已到物理天花板，700V 母线 + 开关尖峰极易击穿；SiC MOSFET 1200V 留 ≥40% 裕量，且 $R_{on}$ 随耐压上升更缓、开关速度快，虽单价高但对效率/可靠性的收益在储能长寿命场景划算。栅驱需负压关断（如 −3~−5V）防误开通，PCB 注意驱动回路极小化以压 EMI。
- **误区**：❌ 认为"扩展移相一定比 SPS 好"——DPS/EPS/TPS 在电压匹配、重载时收益大，但控制复杂度与调制生成成本上升，轻载小功率开环验证用 SPS 足矣；❌ 忽略 ZVS 能量条件 $|i_L|\ge 2C_{oss}V/t_d$ 中的死区时间 $t_d$——死区太短充放电不完、太长丢占空比，须联合优化。


#### 12.2 DAB 损耗定量分解与效率优化

> 本节把 §12.1"扩展移相降导通损耗"的定性结论quantify化：DAB 总损耗 = 开关损耗 + 导通损耗（开关管）+ 变压器损耗（铜损+铁损）。公式与结论来自 MDPI *Energies*（en15218262、en17081955）、ScienceDirect DAB 综合损耗模型、core.ac.uk 三电平移相 DAB 损耗论文，多家一致。

**1) 导通损耗（开关管）**

SPS 下任意时刻恒有 4 个开关管处于导通态，开关管导通损耗：

$P_{cond,sw} \approx 4 \cdot I_{L,rms}^2 \cdot R_{on}$

其中 $I_{L,rms}$ 为漏感电流有效值、$R_{on}$ 为单管导通电阻。

**2) 变压器损耗（铜损 + 铁损）**

- 铜损（绕组）：$P_{Cu} = I_{L,rms}^2 \cdot R_w$（$R_w$ 为折算到原/副边的总绕组电阻，$I^2R$ 损耗，随 RMS 电流平方增长）。
- 铁损（磁芯）：方波激励下用 **Steinmetz 公式**估算

$P_{core} = K \cdot f_s^{\alpha} \cdot \hat{B}^{\beta} \cdot V_c$

其中 $K,\alpha,\beta$ 查磁芯手册、$\hat{B}$ 为峰值磁密、$V_c$ 为磁芯体积。$\hat{B}$ 由副边电压与匝数决定（$\hat{B}=V_2/(4N f_s A_{core})$），故**端电压固定时铁损近似恒定**，变压器总损耗主要由铜损随负载变化。

**3) 开关损耗（硬开关段）**

未能 ZVS 的那部分开关动作产生硬开关损耗，单桥臂对近似：

$P_{sw} \approx f_{sw}\cdot\frac{V\cdot |i_L|\cdot t_f + v_{DS}\cdot |i_L|\cdot t_r}{3}$

（分母 3 为开关过渡按三角波 $V\!-\!I$ 交叠、对开关周期平均后的经验因子；准确值以器件手册 $E_{on}/E_{off}$ 损耗曲线为准。）

（$t_f$ 关断下降时间、$t_r$ 开通上升时间，查器件手册）。**ZVS 成立时该部分趋近 0**——这正是 §12 软开关降低损耗的根本机制：把"开关损耗"转嫁为"靠死区漏感电流给 $C_{oss}$ 充放电"，从而总损耗退化为以导通+变压器损耗为主。

**4) 效率优化主线（与 §12.1 衔接）**

- 轻载：桥臂损耗以**开关损耗**为主；重载：导通损耗占比随功率上升（桥臂损耗主要由开关损耗主导，导通份额随输出功率增大）。
- **扩展移相（DPS/EPS/TPS）通过减小回流功率/无功功率，降低 $I_{L,rms}$**，从而同时压低开关管导通损耗与变压器铜损——这就是 §12.1"降电流应力/导通损耗 10–30%"的量化来源。其代价是控制自由度增多、调制变复杂。
- 工程取舍：开环模型只验证功率传输；**效率寻优（选 φ 组合 / 定匝比 n / 选漏感 L）应在闭环或离线损耗模型里做**，这也是闭环后值得回看的点。
- **损耗模型的"算账"流程（教学）**：① 先由 φ、V、n、L 算出漏感电流波形 → 得 $I_{L,rms}$；② 导通损耗 $=4I_{L,rms}^2R_{on}$+变压器铜损 $I_{L,rms}^2R_w$；③ 铁损由 Steinmetz 按 $f_s,\hat B$ 估（电压固定≈恒定）；④ 开关损耗看 ZVS 成立与否：成立≈0，不成立按 $E_{on}/E_{off}$ 曲线累加。总损耗 ÷ 输出功率 = 损耗率，1−损耗率 = 效率。
- **效率优化决策树（与 §12.1 衔接）**：轻载→优先保 ZVS（防开关损耗主导，可用扩展移相/加辅助电感）；重载→降低 $I_{L,rms}$（扩展移相减回流、或选更大 n/更小 L 提 $P_{max}$ 使同功率下 φ 更小）；铁损随电压固定，优化空间小。
- **定量感（教学示例）**：若某工况 $I_{L,rms}=10$A、$R_{on}=20$mΩ，则单管导通损耗 $I^2R_{on}=2$W、四管共≈8W；铁损估 3W、ZVS 成立开关损耗≈0，总损耗≈11W，输出 1kW 时效率≈98.9%——与"重载效率 98%+"吻合，也说明"降 $I_{L,rms}$ 10~30%"直接等于降几 W 导通损耗、抬升效率零点几个点。
- **闭环/离线寻优建议**：开环只验证"能传功率"；真正效率寻优（选 φ 组合使回流最小、定匝比 n 匹配额定、选漏感 L 平衡 $P_{max}$ 与电流应力）应在闭环或离线损耗脚本里扫参，把上面公式写成 MATLAB 函数批量算效率曲面，挑最优工作点。
- **误区**：❌ 把"开关损耗"和"导通损耗"混为一谈——ZVS 只消灭开关损耗，导通损耗（∝$I^2R$）靠降 RMS 电流解决，两者优化手段不同；❌ 以为铁损可随负载大幅变化——端电压固定时铁损近似恒定，真正的负载相关损耗是铜损与导通损耗。

### 13. 直流微电网故障特性与保护（前瞻，极间短路已实测验证）

> 研究短路/过欠压保护，其物理基础与 AC 系统截然不同，经核实要点如下（直流短路物理已由 MathWorks 官方文档及中文学术文献确认）：

- **无自然过零点**：直流故障电流没有 AC 那样的电流过零，传统交流断路器(ACCB)靠过零灭弧，**不能直接用于直流**；必须采用直流断路器(DCCB)或固态/混合断路器，要求在**微秒级**快速开断（文献确认 Si IGBT 短路耐受时间 SCWT <10μs，须 μs 级固态/混合直流断路器隔离）。
- **上升率极快(di/dt 大)**：短路瞬间直流母线电容迅速放电 + 线路阻抗低，故障电流在极短时间内冲到额定值的 20–50 倍（文献：最初几百微秒内由电容放电阶段主导，飙至额定数十–上百倍），功率器件耐受时间极短，必须**快速检测 + 隔离**。
- **保护逻辑**：用比较器 + 锁存器，检测电池电流超 3 倍额定 或 母线电压低于 70% 额定 时输出跳闸信号断开断路器。
- **接地与绝缘监测**：IT/TN-S 等接地方式影响人身与设备安全；直流系统尤须关注**高阻故障** detection 难点。
- **设计取向（⚠️ 工程修正）**：在控制器/变换器层面**可**限制**源侧过载**电流（如让换流器主动限流），与机械断路器配合构成多级保护；但`Saturation3`（±10A）限幅**只约束源2 控制通道的指令电流**，对**跨母线并联的极间短路无效**——故障电流由母线电容放电 + 电池经双向变换器直接灌入故障点，**绕过**限幅支路，仍可达额定数十倍；`Isrc2`被限幅牢牢约束在 ±10A 恰好反证故障电流不走源2 控制通道。故"控制器主动限流"**不能替代**快速直流开断，短路电流必须靠 DCCB/固态断路器在微秒级隔离。

**故障电流定量估算**：

- 稳态故障电流（母线经小阻抗 $R_f$ 被强行拉低、电容+电池持续灌入）：$I_{fault,ss}\approx V_{bus}/R_f$，数量级可达额定数十倍。
- 含母线电容时的初始上升率：$\left.\frac{di}{dt}\right|_{t=0^+}\approx\frac{V_{bus}}{L_{eq}}$（$L_{eq}$ 为母线侧寄生+连接电感），故电流在数百 µs 内冲到额定数十倍——峰值可达额定数十倍。
- 开断过冲（感性回路骤断）：$V_{spike}=L_{loop}\cdot\left.\frac{di}{dt}\right|_{break}$，对应 Brk 开断后母线感应的高压尖峰（$L_{loop}$ 为故障回路总电感）。

**仿真印证（极间短路）**：

- 故障前母线≈额定 → 故障瞬间骤降至≈0 V（甚至轻微反冲成负压），随后被故障支路小阻抗钳位、回升到远低于额定的值；该值低于 70% 额定阈值，正是欠压跳闸逻辑应动作的区域。
- 电流尖峰可达额定数十倍（电容放电 + 电池灌入），而受控源电流被限幅约束在 ±10A —— 印证本条三条核心理论：**无自然过零点 / di/dt 极高 / 故障电流主由电压源提供且绕过控制器限幅**。
- 结论与前瞻：常规交流断路器不能用于直流（文献确认 IGBT 短路耐受 <10μs、须 μs 级固态/混合直流断路器）；"比较器+锁存器检测`Ibat>3×额定`或`Vbus<70%额定`→ 跳闸"的保护逻辑，正是应对此类极间短路的必要手段。

**仿真印证（过流+欠压保护）**：

- 保护采用`Compare To Constant`(Vbus<70%额定 或 |Ibat|>过流阈值) →`Logical Operator`(OR) →`S-R Flip-Flop`(S=OR，R=0 接地) → 经`Simulink-PS Converter`驱动`Brk`(G>0.5V=开断)。**关键工程修正**：① Brk 栅极约定为“G>0.5V=开断”，故应接锁存器 **Q**（Q=0→闭合常通、Q=1→开断）；接 Qbar 会因常态 Qbar=1 使 Brk 从一开始就断开，短路根本注不进——这是首个卡点。② 锁存器 R 接地(永不复位)，若不加**投运门**(启动后延时使能)会令 Vbus 启动升到额定途中(<70%额定)误触发、永久锁存 Q=1 → Brk 恒开。③ 加 微秒级 延时确认窗让短路电流先建立、避免瞬间误动。
- 三测试全过：A 敏感性(仅短路→Vbus 跌≈0V 后 Brk 开断、母线恢复≈48V)、B 选择性(仅光照/负载扰动、不短路→Q 恒0、Brk 不误动)、C 鲁棒性(扰动+短路同时→保护仍抢占跳闸)。
- **开断过电压（与 不同机理）**：Brk 切断含线路/连接电感的故障回路时，电流被骤断 → 由法拉第定律`V=L·di/dt`感应高压尖峰（可达额定数倍）；延时越长短路储能越多、过冲越大。此过压属**开断暂态**，非故障本身；工程上靠 RC 缓冲/Snubber、避雷器/TVS 或软开断抑制，不属于 保护逻辑错误。
- **故障分类（便于选保护方案）**：直流故障主要分**极间短路（PP, pole-to-pole）**&#x4E0E;**单极对地短路（PG, pole-to-ground）**&#x4E24;类。PP 故障电流最大、最危险；PG 故障严重程度取决于接地方式（IT 系统故障电流小、TN 系统较大），但都会破坏电压平衡、危及绝缘。此外还有**高阻故障（HIF, high-impedance fault）**——故障电阻大，电流仅微增、电压仅微降，常规过流保护易漏判，须靠电压变化率/谐波特征识别。文献（ScienceDirect 2026 异化系数法）指出：无通信、仅靠本地电流量的高阻故障检测正成为 DC 微电网保护研究热点。
- **故障电流的三阶段（文献统一模型）**：极间短路典型分三段（J. Mod. Power Syst. Clean Energy 综述《A review on protection of DC microgrids》）：
 1. **电容放电段**（t0 起、数百 µs~数 ms）：母线支撑电容经故障点放电，电流骤升、峰值由电容与回路电感决定，呈 RLC 二阶响应。
 2. **二极管续流段**（t1~t2）：电容放空后，线路/连接电感储能经变换器反并联二极管续流，电流陡然冲击二极管，易损坏器件。
 3. **电网/电源馈入段**：之后电流主要由上游电源（电池经变换器、电网侧）按控制限流持续馈入，进入稳态故障电流。

 电容放电段的 RLC 方程（欠阻尼 $R<2\sqrt{L/C}$ 时）：

 $LC\frac{d^2 u_{dc}}{dt^2}+RC\frac{d u_{dc}}{dt}+u_{dc}=0$

 其解衰减振荡，初始峰值正比于 $U_0/(\omega L)$（$U_0$ 为故障前母线电压，$\omega=1/\sqrt{LC}$），这正是“电容放电主导、冲到额定数十倍”的物理来源；增大母线侧电感或加限流电抗可压低初始电流上升率。
- **检测方法（超越阈值比较）**：除用的“电流阈值 + 电压阈值”外，文献对变换器接口源常采用 **电流变化率检测（ICCR / ROCOC, rate-of-change-of-current）**——故障瞬间 $di/dt$ 远高于正常负载变化，即使绝对幅值被变换器限流，其上升率仍显著，可更快识别（MDPI Energies 19/3 598 在真实 BESS–PV 微电网中即以此作 DC 侧专用保护）。配合**电压变化率（dv/dt）**、**高频暂态能量/谐波**特征，可在数百 µs 内判别故障极性与区段，是固态断路器常用的高速检测思路。

#### 13.1 光伏侧电压在极间短路时的行为（对应 V_pv 波形）

典型仿真的`V_pv`波形特征（稳态工作于 MPP 电压附近，短路瞬间先振荡后深跌至明显负压，随后恢复）可用以下物理过程解释：

1. **稳态**：Boost 固定占空比 $D\approx0.5$，光伏板工作在其 I–V 曲线某一点，$V_{pv}$ 接近其 MPP 电压。
2. **短路瞬间**：Brk 闭合，母线经故障小阻抗被强行拉到接近 0V。Boost 输出电压骤降 → 二极管反向偏置被强行拉低 → 电感电流不能突变，产生剧烈 LC 振荡。
3. **电压反向**：线路与连接电感中的电流试图维持，在 Boost 二极管/开关寄生电容与线路电感之间形成谐振，使`V_pv`瞬间低于 0V（明显负压振铃）。这是感性电流找不到续流通路时的典型振铃，不意味着光伏板真的输出负电压，而是测量点处 L-C 谐振的表现（与 IEEE/ScienceDirect 文献中“直流微电网极间短路时光伏接口变换器端口电压会因 LC 振荡出现负压尖峰”的描述一致）。
4. **恢复**：保护动作后 Brk 断开，故障支路被切除，母线电压恢复，Boost 重新建立稳态，`V_pv`回到其 MPP 电压附近。

该波形说明仿真物理正确：短路时光伏侧并非简单“被拉到 0V”，而是因 Boost 电感、线路电感与器件寄生电容的相互作用出现振荡与反向尖峰。

- **更细的物理图像（定量）**：Boost 电感电流不能突变，短路时其储能转为对开关管/二极管**寄生电容 $C_{par}$** 的充电，形成以谐振频率 $f_0\approx 1/(2\pi\sqrt{L_{boost}C_{par}})$ 为特征的 LC 振铃。负压尖峰幅值可由能量守恒估算：电感储能 $\tfrac12 L_{boost} I_0^2$ 转入寄生电容，峰值电压量级约 $V_{spike}\sim V_{dc}+\sqrt{V_{dc}^2+2L_{boost}I_0^2/C_{par}}$。因此该负压并非光伏板击穿，而是**测量点在 Boost 输入端**感受到的端口振铃（IEEE/ScienceDirect 文献均描述此现象）。工程提示：在 PV 端口并联小吸收电容或加 RC 缓冲可削弱振铃，但根本解决仍需快速切除故障支路；负压振铃的量级与文献一致，可作为模型物理正确性的佐证。

#### 13.2 直流故障为何"不可控"：与交流的本质区别

> 标题核心是"验证直流故障的不可控本质"——这背后的物理是**交流有自然过零点、直流没有**。

- **交流的天然优势——过零熄弧**：交流电流每周期（50/60 Hz）自然过零 100/120 次。当断路器/熔断器开断时，电弧在过零点瞬间失去维持电流，自然冷却去电离而熄灭；灭弧室只需应对有限的能量。
- **直流的困境——无过零、持续燃弧**：直流电流连续单向、无自然过零点，开断瞬间形成的电弧会持续燃烧，直到外部措施强行熄灭。因此直流断路器必须依赖**磁吹灭弧（magnetic blowout）、多断口串联、加长弧道、石英砂填充**等专门结构。
- **保护含义——直流故障更"凶"**：

1. **故障电流持续上升无自熄**：不像交流在过零点被"卡断"，直流故障电流会一直冲到保护动作或电源内阻限流。故直流微电网保护必须在**毫秒级**快速动作（对应 的过流+欠压保护）。
2. **断开感性负载产生过冲**：断路器/开关骤断感性电流时 $V=L\cdot di/dt$ 感应高压尖峰——这正是开断感性负载产生过冲尖峰的物理根源（见 §13.1）。
3. **器件选型差异**：AC 熔断器/断路器不能直接用于 DC（同壳 AC 750V 可能仅 DC 500V 可用），必须用 DC 额定器件。

- **印证**： 极间短路仿真中，母线被 $R_f=0.1\,\Omega$ 强行拉到近 0V、故障电流持续（无自然过零使其"自熄"），正是"直流故障不可控"的直接体现；也说明为何直流微电网比交流更依赖高速保护 + 变换器主动限流。
- **定量对比——为何直流必须"快"**：50 Hz 交流每周期过零 100 次（周期 20 ms），平均每隔 10 ms 就有一个天然熄弧窗口，灭弧室只需在过零点附近动作；直流则"永不过零"，电弧只能靠外力（磁吹、拉长弧道、石英砂冷却）熄灭，故直流断路器必须在故障电流建立后**极短**时间内动作——这正是把保护延时确认窗控制在 20~50 µs 的物理动因（对比交流 ms 级即可）。
- **直流断路器技术取向**：**固态断路器（SSCB, solid-state circuit breaker）**&#x7528; IGBT/SiC 器件实现 µs 级无弧开断，是直流微电网主流发展方向；**混合断路器（机械开关 + 固态支路）**&#x5728;稳态走机械支路（通态损耗低）、开断时快速切换到固态支路完成无弧分断，兼顾效率与速度。采用"比较器+锁存器驱 Brk"的仿真逻辑，正是这类高速保护的思想雏形。

#### 13.3 IT 与 TN-S 接地方式在直流系统中的安全性差异

> 计划 「学」明确要求：**对比 IT 和 TN-S 接地方式在直流系统中的安全性差异，并结合发电厂经验考虑人员安全和绝缘监测工程约束**。下面用一张表讲清两者"故障时电流往哪走、人会不会触电、设备停不停机"，这是 系统级安全方案的设计出发点。

- **两句话定位**：
 - **IT（浮地系统）**：电源侧母线**不接大地**（无人工中性点接地），平时靠**绝缘监测 IMD（见 §22.9）**&#x53D1;现绝缘劣化；单点碰壳不形成大回路。
 - **TN-S（保护接地系统）**：电源一极（通常负极）**直接接地**，设备外壳接独立保护地线 PE；单点碰壳即形成大回路，靠过流/漏电保护电器**速断保人**。

| 维度 | IT 浮地系统 | TN-S 保护接地系统 |
| ------------ | ------------------------------------ | ------------------------------ |
| **接地结构** | 母线不接大地，仅经高阻/IMD 监测 | 一极直接接地 + 设备外壳接 PE |
| **单点接地故障电流** | 极小（仅分布电容/监测回路），设备**不停机** | 大回路大电流 → 断路器/熔断器**立即跳闸停机** |
| **触电风险** | 单点碰壳不致命（无回路）；但**两点同时碰壳 = 极间短路**，风险骤增 | 单点碰壳即形成回路，靠保护电器速断保人；若保护拒动则外壳带电 |
| **绝缘劣化可见性** | IMD 持续测 $R_{iso}$，可**事前预警** | 传统配置无绝缘监测，劣化常等短路才暴露 |
| **适合场景** | 不允许中断的工艺（烟草产线、医院、数据中心） | 一般配电、强调"故障即断电保人"的场所 |
| **代价** | 必须配 IMD（§22.9），否则隐性故障失明 | 故障即停机，对连续生产工艺不友好 |

- **为什么本课题选 IT + IMD**：烟草绿色园区要同时满足"生产不中断 + 人员不触电"——IT 浮地保证单点绝缘故障不停机，IMD（§22.9）兜底绝缘劣化预警，再叠加 DAB/隔离变压器挡穿越性短路（§21.9 / §21.10 三层安全）。这正是 「结合发电厂经验考虑人员安全与绝缘监测约束」和 「系统级安全方案」的落点。
- **与保护逻辑的关系**：IT 系统下短路保护靠 **Brk 硬开断（§25.8）+ IMD 软预警（§22.9）** 双保险；TN-S 系统下靠**过流/漏电保护电器速断**。思路不同、目标一致——把"故障电流"和"触电风险"都压到安全范围。
- **初学者一句话**：IT 像"不接地的绝缘体检房"（平时不跳闸、靠监测体检，但怕同时两点接地）；TN-S 像"接地保命房"（一漏电就断电，人安全但生产停）。的 700V 母线（DAB 隔离、浮地）就是 IT 思路。

### 14. 直流微电网分层控制架构（EMS 前瞻）

> 的下垂控制只是微电网控制的“第一层”。学术界与工业界公认直流微电网采用**三层嵌套控制**（primary / secondary / tertiary），已为多篇 IEEE / ScienceDirect 文献证实：

- **一次控制（Primary，最快·本地·无通信）**：各变换器内部电压/电流双环 PI + 下垂。 PI 闭环、下垂均属此层。优点：无通信、即插即用、抗单点故障；代价：稳态母线电压随负载略降（电压偏差）。
- **二次控制（Secondary，较慢·网络级）**：经通信采集全局母线电压，生成“电压恢复量”回灌各一次环，把下垂造成的稳态压降修正回额定值（如 48V），**且不破坏一次控制建立的均流**。弥补下垂“牺牲电压精度”的固有缺点。
- **三次控制（Tertiary，最慢·调度层）**：经济调度与功率流优化——按成本 / SOC 给各源分配功率设定点、管理储能充放电边界、与外部电网交互。即“边缘计算与整合 / EMS”的核心。
- **工程取向**：去中心化（distributed）优于集中式（centralized）——前者可扩展、无单点故障、通信负担小；下垂正是去中心化思想在初级层的体现。
- **与的衔接**：只完成一次控制（下垂均流）；后续若要“电压零偏差 + 经济最优”，需在此框架上叠加二次 / 三次控制。数据驱动预测可为三次调度提供功率 / 电价预测输入。
- **下垂律定量式（一次控制核心）**：对第 i 个源，母线电压参考随其输出电流线性下垂：

 $V_{ref,i}=V^*-k_i\cdot I_i$

 式中 $V^*$ 为额定母线电压，$k_i$ 为下垂系数（即虚拟电阻 $R_{v,i}=1/k_i$）。虚拟电阻越小（即 $k=1/R_v$ 越大）该源越愿出力——源1 $R_{v1}=0.1$、源2 $R_{v2}=0.2$（对应 $k_1=10、k_2=5$），故 $R_{v1}:R_{v2}=1:2$，均流比 $I_1:I_2=R_{v2}:R_{v1}=2:1$。代价是负载越重、母线电压越低，故需二次控制把 $V_{bus}$ 拉回 $V^*$。
- **二次控制恢复律（直觉）**：集中式或分布式地注入一个电压补偿量 $\delta V$，使 $\lim_{t\to\infty}V_{bus}=V^*$，同时保留各源按 $k_i$ 建立的均流比。分布式二次控制常基于**一致性算法（consensus）**——每个节点只与邻居通信即可全网电压恢复，无需中心控制器，契合"去中心化"取向。
- **三次控制（EMS）优化式（直觉）**：以运行成本/网损最小为目标，约束功率平衡与 SOC 边界，常用 MILP/凸优化求解各源设定点 $P_i^*$：

 $\min\sum_i C_i(P_i)\quad s.t.\quad \sum_i P_i=P_{load},\;SOC_{min}\le SOC\le SOC_{max}$

 目前用"无限容量模式"暂未做真实 SOC 调度，升级到 700V + 真实电池后，应把电池 OCV–SOC 曲线与 SOC 边界喂给三层 EMS，方能实现"经济最优 + 寿命保护"。

### 15. 直流微电网稳定性与恒功率负载 CPL（稳定性前瞻）

#### 一、负载的三种类型与等效模型

直流微电网的"负载"不是笼统一个耗电端，按**端口 V–I 特性**分三类。初学者先分清它们，才能理解后面 §15 的"负阻抗"为何危险。

| 类型 | 端口特性 | 功率随电压 | 增量电阻 $R_{inc}=dV/dI$ | 稳定性直觉 |
| ----------- | ---------------- | ------------ | ----------------------- | ----------- |
| 电阻负载（恒阻 CR） | $V=I\cdot R$ | $P=V^2/R$ | $R>0$（正） | 天然稳定，是"好负载" |
| 恒流负载（CC） | $I=I_{set}$ | $P\propto V$ | $\infty$（竖直线） | 中性，不引入负阻 |
| 恒功率负载（CPL） | $P=V\cdot I=P_0$ | 恒定 | $R_{inc}=-V^2/P_0<0$（负） | **危险，抵消阻尼** |

- **电阻负载**：母线电压越高、电流越大、功率越大，电压-电流同方向变化——正增量电阻。它给系统提供正阻尼，是最容易稳定的负载。前期验证用的 20Ω/40Ω 电阻负载即属此类——所以闭环一调就稳，没遇到振荡。
- **恒流负载**：负载自己闭环把电流锁在设定值，电压怎么变它都吃固定电流；增量电阻无穷大（V–I 曲线垂直），本身不制造负阻，但也不提供额外阻尼。
- **恒功率负载（CPL）**：光伏逆变器、电机驱动器、电池充电器这类"经稳压的电力电子负载"，为保自身输出功率恒定，母线电压略降时反而会**吸更大电流**（$I=P_0/V$），增量电阻为负。它像"越压越反抗"的负载，会抵消系统本来的正阻尼，是直流微电网振荡/崩溃的头号元凶（详见本节后续 CPL 稳定性分析）。

> **一句话串联**：你只用了电阻负载（好负载、易稳）；等你后面接入"电池充电器""光伏并网"这类 CPL 负载时，才会遇到本节说的负阻抗振荡——现在先记住"CPL 负增量电阻"这个概念，将来不慌。

> 直流微电网的**稳定性**不仅取决于单个变换器，更取决于源–网–载之间的阻抗交互。恒功率负载（CPL）是其中最典型、最危险的"负阻抗"源，已为多篇 IEEE / ScienceDirect / Nature 文献证实：

- **CPL 是什么**：光伏逆变器、电机驱动、电池充电器等经闭环稳压的电力电子负载，为维持自身功率恒定，其等效输入呈**负增量电阻**（$R_{inc}=−V^2/P$，电压略降→电流反升），与“电压升→电流升”的正常电阻相反。
- **为什么危险**：CPL 的负阻抗会**抵消系统阻尼**，与母线 LC 回路、线路电感相互作用，引发母线电压振荡甚至崩溃；且 **CPL 渗透率越高、线路电感越大、母线电容越小，越不稳定**（文献结论一致）。
- **的相关性**：电池侧双向变换器在**充电模式**下端口也呈负阻抗特性（储能单元充电时同理），同样会挤压稳定裕度、诱发振荡——这正是“保护 + 稳定性”要面对的本质问题。
- **稳定判据**：常用**阻抗比判据**（源输出阻抗 $Z_o$ / 负载输入阻抗 $Z_i$ 构成的环路增益，Nyquist 曲线不包围 (−1，j0) 即稳定）。
- **抑制手段**：① **无源阻尼**（加实体电容/电阻，简单但有损耗、体积大）；② **有源阻尼 / 虚拟阻抗**（在控制环重塑端口阻抗为正阻特性，无额外器件，主流做法）；③ **自适应虚拟阻抗 / 前馈补偿** 兼顾宽 CPL 变化与电压调节。源端（而非负载端）加阻尼更能保负载性能。
- **工程取向**：升级 700V + 隔离 DAB 后，变换器级联更多、CPL 效应更显著，稳定性分析（阻抗比 / 有源阻尼）应提前纳入设计。
- **CPL 小信号输入阻抗（定量）**：在稳态工作点 $(V_0,P_0)$ 附近线性化，CPL 的增量输入阻抗为负电阻（含变换器内部带宽 $\omega_p$）：

 $Z_{CPL}(s)= -\frac{V_0^2}{P_0}\cdot\frac{1}{1+s/\omega_p}$

 中低频段表现为纯负阻 $-V_0^2/P_0$。当它与源端输出阻抗 $Z_o(s)$ 在母线端口相互作用，负阻会**抵消正阻尼**，使环路增益 $T(s)=Z_o(s)/Z_i(s)$ 的相位裕度下降，轻则振荡、重则母线崩溃。
- **Middlebrook 判据（充分非必要）**：若对所有 $\omega$ 满足 $|Z_o(j\omega)|<|Z_i(j\omega)|$，则级联系统稳定；对含 CPL 系统即要求源端呈现"足够小的输出阻抗"。但 Middlebrook 只是**充分条件**——违反它**不一定**失稳，需再用 Nyquist（不包围 −1,j0）最终判定（文献 Riccobono & Santi 明确指出此点）。这也是为何"看阻抗比 + 有源阻尼"比单一阈值更稳妥。
- **经典稳定界（工程经验准则）**：Kwasinski 等给出已知结论——当 **总 CPL 功率 ≤ 总阻性负载功率** 时，直流微电网可保持稳定；电池充电端口呈 CPL 特性，故论证"总 CPL < 总电阻性负载"是快速判稳的保守但实用准则。文献（ScienceDirect 2024）进一步给出：降低下垂系数 $k_i$ 可放宽 CPL 稳定上界——为后续二次/三次控制整定提供方向。

#### 15.2 CPL 的 Simulink 建模与柔性负载仿真验证（仿真落地，对接申报书第②项）

> **为什么放这里**：§15 上方给出 CPL 负阻理论与阻抗比判据，但现有详细模型（``）与平均模型（``）负载端均用**纯阻性 `R_Load`**（见 §22.4），CPL 尚未在仿真中落地。第②项要求“负载侧直流化改造与柔性接入”“柔性负载控制策略，使负载具备根据供电情况动态调节功率的能力”“在复杂负载场景下稳定高效运行”——本节能把“柔性负载 + 复杂场景稳定”从理论补成可仿真验证的案例，直接坐实第②项的“柔性接入 + 稳定高效运行”。

**CPL 在 Simulink 中的建模方法**
- **方案 A（推荐，Simscape Electrical）**：直接用库里的 **Constant Power Load** 块（Simscape → Electrical → Sources / Loads），给定 `P_set`（恒定有功），内部按 $I=P_{set}/V_{bus}$ 取流，天然呈现负增量电阻 $R_{inc}=-V^2/P_0$（见本节上方理论）。
- **方案 B（信号域自建）**：用 **Controlled Current Source**，反馈母线电压 $V_{bus}$ 经除法 $I_{cmd}=P_{set}/V_{bus}$ 得指令电流——等价于 CPL，且便于在信号域叠加柔性控制环。
- **注意**：CPL 在启动/电压跌落瞬间会“越压越吸流”，须配合软启动或限流，否则初值发散（与 §7 软启动同理）；建议仿真初值给 $V_{bus}$ 稳态值、CPL 限流上限按额定 1.5 倍。

**柔性负载控制策略（让负载“按供电情况调功率”）**
- 在 CPL 的 `P_set` 前加**功率调节环**：监测母线电压或供电裕度（发电−负荷），当 $V_{bus}$ 低于阈值（如 670V，即 700V 带 −4%）时按比例下调 `P_set`，恢复后再上调——即 §14 说的“需求侧响应/柔性调控”在负载端的落地。
- 实现：比较 `V_bus` 与 `V_ref_droop` → 经限幅增益 → 修改 CPL 的 `P_set`；该环响应时间设在 ms 级（慢于一次下垂控制、快于二次调度），避免与下垂环耦合振荡。

**验证矩阵与交付（`` 对比版）**
| 工况 | 负载类型 | 附加控制 | 预期 |
|---|---|---|---|
| 基准 | 纯阻性 R_Load | 无 | 母线稳 700V（已有结论） |
| 场景1 | CPL（高渗透率，$P_{CPL}>P_{阻性}$） | 无阻尼 | 母线振荡/电压崩溃（验证本节上方理论） |
| 场景2 | CPL | 有源阻尼/虚拟阻抗（下垂环重塑端口阻抗，见本节上方） | 振荡抑制、稳定 |
| 场景3 | CPL + 柔性控制环 | 柔性降功率 | 供电不足时自动降载、母线恢复 |

- **判定指标**：母线电压标准差、Nyquist 是否包围 $(-1,j0)$（§15 Middlebrook/Nyquist）、恢复时间。
- **对应申报书**：第②项“柔性负载控制策略 + 复杂负载场景稳定高效运行”——场景1→2→3 对比直接坐实。

---

### 16. 储能电池等效模型与 SOC 估算（电池建模基础）

> 把电池当作「经双向变换器接入的能量缓冲」，踩到「Beta 参数报错」（标称电压48V 与 初始电荷40Ah 在 OCV–SOC 曲线上不匹配）。其背后是电池的数学模型，经核实要点如下：
>
> 实操解决（无限容量模式规避 Beta 报错）。

- **等效电路模型（ECM）**：最简为 **Rint 模型**（开路电压 OCV + 串联内阻 $R_0$）；更精确用 **Thévenin 1RC 模型**：$OCV + R_0 + (R_1 \parallel C_1)$，RC 支路描述极化动态。终端电压 $V_{term}=OCV - I\cdot R_0 - V_{RC}$。
- **OCV–SOC 曲线（核心）**：开路电压随荷电状态呈特征 **S 形**——0–10% 陡升（分辨率高）、中段 50–75% 平缓、90–100% 再陡升。化学差异大：**NMC** 斜率明显（4.2V→3.0V，OCV 估 SOC 较可行）；**LFP** 中段 10–90% 几乎压在 3.2~3.3V（"平顶"），靠 OCV 实时估 SOC 在中间段极不可靠。
- **SOC 定义与库仑计数**：SOC = 剩余容量 / 额定容量；$SOC[k]=SOC[k-1]-\frac{I[k]\cdot\Delta t}{Q_{nom}}$（放电为减）。**陷阱**：电流传感器零偏会随时间累积漂移，初始 SOC 错则永远错——库仑计数需定期用 OCV "锚定"修正。
- **OCV 查表法**：电池静置（无电流）数分钟~数小时后终端电压→OCV→查表得 SOC，适合上电/满电时校正；动态带载时终端电压含 $IR$ 压降，不能直接当 OCV 用。
- **EKF（生产级）**：把库仑计数与 ECM 物理模型结合，实时用"模型预测电压"与"实测电压"之差经卡尔曼增益修正内部 SOC，可自动补偿传感器漂移与初值误差。Simscape Battery 提供`SOC Estimator (Kalman Filter)`块。
- **Simscape 映射与的坑**：`Battery (Table-Based)`块靠 OCV–SOC 查表 + $R_0/R_1/C_1$ 拟合数据工作；若"标称电压"与"初始电荷(对应 SOC)"不在同一条 OCV–SOC 曲线上即报 Beta 参数错。临时用 **无限容量模式**（SOC 固定、不做 SOC 更新）规避，MILP/真实电池调度时应改回有限容量并填好 OCV–SOC 表。
- **电池能量与容量换算**：$E_{bat}=C_{rated}\cdot V_{nom}$（安时×标称电压=瓦时）； 48V/50Ah ⇒ 2400 Wh，700V 系统若 50Ah ⇒ 35 kWh。恒功率放电时间 $t_{dis}=E_{bat}/P_{load}$。
- **Thévenin 1RC 状态方程（离散化后用于 SOC/EKF，文献统一写法）**：

 $\begin{cases}U_L=U_{oc}(SOC)-I\cdot R_0-U_p\\[2pt]\dot U_p=-\dfrac{U_p}{R_1C_1}+\dfrac{I}{C_1}\\[4pt]SOC_{k}=SOC_{k-1}-\dfrac{\eta\,I_k\Delta t}{Q_{nom}}\end{cases}$

 $U_p$ 为极化电压，$R_1C_1$ 表征极化/扩散时间常数（秒~分钟级）。$R_0,R_1,C_1,U_{oc}$ 通常靠 **HPPC 混合脉冲功率实验 + 最小二乘**离线辨识；一阶 RC（= Thévenin）被大量文献认为是最适合车用锂电"精度/算力"折中的结构。
- **EKF 迭代（直觉版）**：① **预测**：用上式由上一时刻 $(SOC,U_p)$ 推当前值；② **校正**：用实测端电压 $U_L$ 与模型预测值之差乘卡尔曼增益 $K_k$ 修正状态；③ **参数自适应**（如 FFRLS 递推最小二乘）可在线更新 $R_0,R_1,C_1$ 以补偿老化/温度。文献实测误差通常可压到 3% 以内（改进 EKF≈2.94%、常规 EKF 对比库仑计数约 3.8%）。
- **温度与老化提醒**：OCV–SOC 曲线与 $R_0$ 随温度/循环次数漂移；低温下可用容量骤降、内阻上升，故真实 BMS 须做温度补偿与容量再学习。"无限容量模式"跳过此环节，仅适合控制逻辑验证，做真实能量管理时应切换回有限容量 + 温度相关 OCV–SOC 表。

### 17. 母线支撑电容与功率电感选型（参数依据）

>  §22 把`Cout_Bus`设为 500µF、`L_Boost`/`L_bidir`设为 500µH，但没写"为什么是这个数"。经核实，工程上靠以下公式初选：

- **母线电容——纹波法**：电容在两次开关充电之间放电，允许峰峰值纹波 $\Delta V_{pp}$ 限定容值：

$C_{ripple}=\frac{I_{load}}{f_{ripple}\cdot\Delta V_{pp}}$

其中 $f_{ripple}$ 为纹波频率（DC/DC 输出电容取开关频率 50kHz）。纹波要求越严，C 越大。

- **母线电容——维持时间（hold-up）法**：电源中断时电容须支撑负载功率 $P$ 坚持 $t_{hold}$ 到最低电压 $V_{min}$：

$C_{hold-up}=\frac{2\,P\,t_{hold}}{V_{nom}^2-V_{min}^2}$

取两者较大值再乘安全/老化系数。 500µF 属"稳定运行 + 快速暂态保护"常用区间（文献建议 150~300µF 起，留余量取 500µF）。

- **功率电感——纹波电流法**：由伏秒平衡 $\Delta I_L=\frac{V_{in}\cdot D\cdot T_s}{L}$ 反推：

$L=\frac{V_{in}\cdot D\cdot T_s}{\Delta I_L}$

通常取 $\Delta I_L$ 为额定电流的 20%~40% 以保证 CCM 且折中体积/损耗。 50kHz、$V_{in}\approx25V$、$D=0.5$ ⇒ $L=500\mu H$ 时 $\Delta I_L=V_{in}D/(Lf_s)=25\times0.5/(500\mu H\times50k)\approx0.5A$，与 §2 实测深 CCM（谷值≈4.5A）一致。

- **选型闭环校验（定量算例）**：以为例，$V_{nom}=48V$、$f_s=50kHz$。若要求母线纹波 $\Delta V_{pp}\le1V$、负载电流 $I_{load}=5A$，纹波法得 $C_{ripple}=5/(50k\times1)=100µF$；若要求 hold-up $t_{hold}=2ms$、$V_{min}=40V$、负载功率 $P=240W$，则 $C_{hold-up}=2\cdot240\cdot0.002/(48^2-40^2)=0.96/704\approx1360µF$。可见**维持时间**往往比纹波更"吃"电容；实际取 500µF 是纹波达标 + 暂态支撑的折中，若需更长 hold-up 应并联多颗或加 UPS 级缓冲。
- **电感附加约束**：电感还决定 CCM 边界电流 $I_{crit}=\Delta I_L/2$；$L$ 过小会进入 DCM（占空比–电压非线性、难控）。 $\Delta I_L\approx0.5A$ 远小于谷值 4.5A，深 CCM 成立。另需注意电感 **饱和电流**须 > 峰值电流、**DCR（直流电阻）**&#x51B3;定导通损耗、$f_s$ 下铁损/铜损权衡体积与温升。
- **ESR/ESL 提示**：电解电容 ESR 会在纹波电流下发热（须看**纹波电流额定值**！），薄膜/陶瓷电容 ESR 低但容量小；母线支撑电容宜选低 ESR 并留纹波电流裕度。电感引脚与布局的 ESL 会影响高频振铃，PCB 布局同样算"选型"一部分——这一点在 §13.1 光伏侧 −20V 振铃中也有体现。

### 19. 数字逻辑基础：触发器 SR/D/T（保护用）

> 保护用`S-R Flip-Flop`（锁存器）实现“故障自锁”。这里把三种最常用触发器说清（JK 作补充），均经 联网核对 Wikibooks / Tutorialspoint / GATE-CS 资料确认。

- **SR 锁存器/触发器（最基础）**：两输入 S(Set)、R(Reset)，两输出 Q、Qbar(恒反相)。**特性表**：S=0,R=0→保持(Q 不变，记忆能力)；S=1,R=0→置位(Q=1)；S=0,R=1→复位(Q=0)；S=1,R=1→**禁止态**(输出不定，绝不可出现)。**特征方程**`Q* = S + R'·Q`约束`SR=0`。 用它：S=故障 OR、R=0(接地)，故一旦故障置位就永久保持、不自动复位——正是“可靠跳闸、不抖合”所需。
- **D 触发器（最常用）**：输出在时钟沿跟随输入，`Q* = D`**无禁止态**。本质是“把 D 的值延迟一个时钟存住”，用作移位寄存器/零阶保持/单 bit 存储。与 SR 区别：D 把 S、R 绑成互补，从根上消除禁止态。
- **T 触发器（翻转器）**：`T=0`→保持、`T=1`→翻转，`Q* = T⊕Q`。T 恒接 1 时每来一个时钟输出二分频，是计数器核心单元。可由 JK(J=K=T) 或 D(D=T⊕Q_prev) 构成。
- **JK 触发器（补充，最通用）**：把 SR 的禁止态改成“翻转”——J=K=1 时输出取反。特征方程`Q* = J·Q' + K'·Q`。SR/D/T 都可由 JK 转换得到。
- **锁存器 vs 触发器**：锁存器是**电平敏感**(使能期间输出随输入变)，触发器是**边沿触发**(只在时钟沿采样)。Simulink 的`S-R Flip-Flop`属边沿型，但 把它当“带记忆的自锁开关”用，关注点在 S/R 逻辑而非时钟沿。
- **三种触发器特性表（快速查阅）**：
 | 触发器 | 输入 | 时钟/使能 | 次态 Q* | 禁止/特殊态 |
 | --- | ---- | --------- | ------------------------ | ----------- |
 | SR | S, R | 电平(锁存)/边沿 | `Q* = S + R'·Q`（约束 SR=0） | S=R=1 禁止 |
 | D | D | 边沿 | `Q* = D` | 无（S、R 互补绑死） |
 | T | T | 边沿 | `Q* = T⊕Q` | 无；T=1 每拍翻转 |
 记忆口诀：**SR 会“禁止”、D 最“听话”、T 爱“翻跟头”**。
- **保护应用落点**：把故障检测 OR 接到 **S**、R 恒接地 ⇒ 一旦故障，Q 置 1 并**自锁保持**，断路器（Brk, G>0.5V 开断）立即跳开；故障清除后也**不自动复位**，需人工/调度重新合闸——这正是“可靠跳闸、不抖合、防误恢复”的硬逻辑。若想“故障消失后自动复归”，可把 R 接一个延时/确认电路（但为安全选永久锁存，靠投运门防启动误动）。
- **抗干扰与毛刺**：实际保护中 S 信号应经**去抖/确认窗**再进锁存器，避免单个噪声尖峰误置位；锁存器对“S、R 同时跳变”敏感，模型/PCB 里要保证 S、R 互斥或至少不同时有效（杜绝禁止态）。这也解释了 §13 中“加 20~50µs 延时确认窗让短路电流先建立、避免瞬间误动”的必要性——确认窗本质就是给锁存器 S 端加一级数字滤波。

---

### 第二篇 · 名词解释（术语与定义）

本篇是“随手查”的术语速查表，把前几篇出现的专业名词（电力电子、控制、储能、仿真、故障保护、高压隔离）集中解释，便于零基础读者回看。阅读建议：先扫 §20 通用控制词条，再按 §20.0 主题分组补基础名词；遇到拓扑演进（§21）与器件选型（§22）里的陌生词，回来查本篇即可。配套速算见 §23 公式表。

### 20. 名词术语解释

- **下垂控制**：让各源Vref随其输出电流线性下降，牺牲电压精度换取多源无通信按比例分担功率。类比抬轿子，谁出力多谁自动矮一点。
- **虚拟电阻/下垂系数k**：下垂律比例系数k等效串联"看不见的电阻" $R_v=1/k$；k越大该源越不愿多出功。两源 $R_v$ 之比反比于功率分配比。
- **均流/无互联线控制**：多源自动按比例分总负荷电流而不依赖通信线，下垂是最典型方法。
- **软启动**：让参考值从0缓升到目标，避免上电全压冲击。
- **尖峰电流**：上电/参考突变瞬间的远超稳态短时大电流。根因：源2开环代数律 $I_2=(V_{02}-V_{bus})/R_{v2}$ 在 t=0 时 V02=0、Vbus=48 ⇒ $I_2$ 出现硬编码负向尖峰，PID/电容压不住。消除方法：在 Gain2 后插 Saturation(±10A) 限幅，把尖峰钳到安全范围。
- **静态误差（稳态误差，steady-state error）**：瞬态过程结束、系统平稳后，被控量离参考值还差多少，公式 $e_{ss}=r(\infty)-y(\infty)$。有无积分环节是关键——有积分(Type-1)对阶跃参考跟踪误差趋近0；若积分增益过小或仿真窗太短，积分器来不及爬到位就留下大残差。**有"三重含义"易混（详见 §6）**：①闭环跟踪误差（真·控制静态误差，应压到0）；②下垂固有偏移（设计代价，非缺陷，是"用电压换均流"的门票）；③定压PI大残差（真·缺陷，因积分增益过小、在仿真窗内来不及推满执行器而等效开环）。**看静态误差**：取平稳窗末段均值相减；图上对比曲线最右端落点——下垂紧贴参考电压灰线、定压PI若明显漂在参考线上方即一眼可辨。
- **抗积分饱和**：PID积分项长期饱和时持续累积，解除后巨超调；Simulink`AntiWindupMode``none`(易饱死)、`clamping`(需外部饱和反馈才生效，单设反恶化)、用 **LimitIntegrator+积分硬限幅±5** 更稳。
- **PID输出限幅**：限制PID整体输出范围；与抗饱和要配套。
- **电流限幅**：用Saturation直接限制支路电流物理上限(如源2±10A)，限的是真实物理量。
- **母线电容预充电**：把Cout_Bus初始电压设48V，消除充电浪涌；但解决不了源2开环硬编码尖峰(根因不同)。
- **受控电流源/开环代数下垂律**：源2电流由纯代数公式实时计算(无积分无闭环)，简单零通信，但t=0有硬编码尖峰。
- **功率平衡**：稳态总发电=总负荷+损耗；推断法由已知量反推未知量(电池电流≈(负载功率−PV功率)/48V)，区分推算值与实测值。
- **占空比钳位**：PID输出被限幅在0或1边界长期不动→变换器失调节能力。中PID上限误设0.9也属此类。
- **功率分配精度**：实际各源出力与按虚拟电阻“反比”理论值的偏差；偏差来源=线路电阻不对称、传感器增益误差、限幅截断。源1/源2 设计 2:1，限幅只限源2（±10A）故重载时源2 可能先触顶、源1 兜底，属预期行为。
- **母线电压偏移（Droop Offset）**：下垂牺牲的稳态电压精度，等于 $k\cdot I_{source}$；不是故障，是“用电压换均流”的门票（与 §20 静态误差②同源）。想减小偏移就加大下垂系数倒数——但会恶化均流精度，二者权衡。
- **环流（Circulating Current）**：多源并联或 DAB 移相失配时产生的、不对外做功的内部循环电流，只增 RMS 与导通损耗（与 §20.0 回流功率同源概念）。
- **母线（DC Bus / 直流母线）**：连接所有源、储、载的公共直流节点；“谁在控母线电压”是判断系统架构的关键问题（见 §21 每阶段“谁定母线电压”）。
- **锁存（Latch）**：故障保护里“一旦跳闸就保持、不因电压回升而复归”的记忆动作；区别于普通比较器输出会随输入抖动。

#### 20.0 基础术语（电力电子 / 控制 / 储能 / 仿真 / 故障保护 / 700V 隔离）

> 本小节收齐涉及的全部基础名词，按主题分组：电力电子 → 储能电池 → 控制与测试 → 仿真与求解器 → 故障与保护 → 700V/隔离 DAB 专题。控制类如下垂 / 静态误差 / 抗积分饱和见上方 §20 通用词条。

**电力电子**

- **Boost 变换器（升压斩波）**：把低压直流升到高压，CCM 关系 $V_{out}=V_{in}/(1-D)$。 48V 系统 PV 25V→48V、700V 系统 PV Vmp≈432V→700V 都用它；升压比越大占空比 D 越接近 1（700V 系统 D≈0.383，升压比≈1.6 倍，见 §25.21）。
- **占空比 D（Duty Cycle）**：开关导通时间占周期比例，直接决定变换器增益； Boost 由 MPPT / 固定脉冲驱动，电池侧半桥 duty=1→钳位母线、duty<1→放电（见 §3.1）。
- **PWM（脉宽调制，Pulse Width Modulation）**：用"载波（锯齿/三角波）与调制量（占空比指令）比较"生成通断方波，通过调节脉冲宽度等效调节平均电压。实现：Repeating Sequence 载波 + Relational Operator 比较（载波<占空比输出 1），见 §25.3。
- **开关频率 $f_s$ / 载波（Carrier）**：开关管每秒通断次数；载波是决定开关时刻的周期基准波形。$f_s$ 越高纹波越小、L/C 可更小，但开关损耗越大。
- **CCM / DCM（连续 / 断续导通模式）**：电感电流一个开关周期内始终 >0 为 CCM（增益公式 $V_{out}=V_{in}/(1-D)$ 才成立）；纹波谷值触 0 则进入 DCM，增益随负载变、公式失效。 Boost 深 CCM（谷值≈4.5A）；Buck 极小占空比要警惕 DCM——早期 48V 负载计划 D≈0.069 即此情况，实际执行版 700V→400V 负载 Buck D≈0.571 为深 CCM（见 §25.18）。
- **续流（Freewheeling）**：开关管关断瞬间电感电流不能突变，必须有替代通路继续流——Boost 里由续流二极管承担；若无续流路径，$V=L\,di/dt$ 会感应出破坏性高压尖峰。
- **直通（Shoot-through）**：半桥/全桥上下管同时导通，母线经两管直接对地短路，真实电路会炸管；防范手段就是**死区**（切换间插入"两管全关"间隙）。理想开关仿真切换瞬时完成故侥幸不炸（§25.19 的无死区互补驱动即此例）。
- **二极管反向恢复（Reverse Recovery）**：二极管从导通转截止时，结内存储电荷被抽走产生反向恢复电流尖峰与电压过冲； Boost 二极管在 50kHz 下有此损耗——正是 DAB 用 ZVS 软开关要消除的那类损耗（对照 §12）。
- **MPPT（最大功率点跟踪）**：让 PV 始终工作在最大功率点；常用 **P\&O（扰动观察法）**、**INC（增量电导法）**，判据 $\frac{dI}{dV}=-\frac{I}{V}$ 处即 MPP。
- **I–V 曲线 / Voc / Isc / MPP**：PV 的输出特性曲线——横轴电压、纵轴电流；**Voc（开路电压）**&#x662F;不带载时的最大电压，**Isc（短路电流）**&#x662F;端口短接时的最大电流（≈8.5A），**MPP（最大功率点）**&#x5728;曲线膝部，P=V×I 最大处。
- **辐照度 Ir（Irradiance）**：单位面积接收的光功率（W/m²，标准测试条件 1000）；光生电流与 Ir 近似线性、Voc 几乎不动，所以**改 Ir = 把 I–V 曲线上下平移**——这是光照突变扰动只改一个参数的原理（§25.13）。
- **单二极管模型（Single-Diode Model）**：PV 电池最常用工程等效电路——一个光生电流源 $I_{ph}$ 并联二极管 $D$（反向饱和电流 $I_0$）、串联内阻 $R_s$、并联内阻 $R_{sh}$，输出 I–V 方程为 $I=I_{ph}-I_0\!\left(e^{\frac{V+I R_s}{n V_T}}-1\right)-\frac{V+I R_s}{R_{sh}}$（$V_T=kT/q$ 为热电压，$n$ 为理想因子，$k$ 玻尔兹曼常数、$T$ 绝对温度、$q$ 元电荷）。$R_s$ 主导填充因子损失，$R_{sh}$ 反映漏电/并联损耗；Simscape `Solar Cell` 块即此模型，是 MPPT 与光照突变扰动（§1.1 / §1.2 / §25.13）的物理底座。
- **纹波（Ripple）**：直流量上叠加的周期性交流分量——母线电压纹波、电感电流纹波 $\Delta I_L=\frac{V_{in}D T_s}{L}$。越小越好；母线电容 / 电感即用来压它（选型见 §17）。
- **母线支撑电容 Cout**：并联在母线上的大电容，稳压、吸收纹波、提供暂态能量； 设初值 48V 做"预充电"消浪涌。
- **功率电感 L**：Boost / Buck 储能元件，决定电流纹波 $\Delta I_L$；太小→电流断续 / 纹波爆炸，太大→动态慢。
- **Simscape 物理建模 / 电气参考（Electrical Reference）**：Simulink 的物理系统建模环境；电路必须挂一个"地"节点（Electrical Reference）才闭合；**隔离变压器两边要各自独立参考**，否则隔离失效（见 §1.2）。
- **双向 Buck-Boost**：单级实现升压 / 降压双向能量流； 48V 电池侧用**非隔离半桥**实现（同地低压，无需隔离）。
- **半桥（Half-Bridge）/ 浮地驱动**：两开关管串联分压、中点接电感；上管栅极相对其源极**浮动**，需电平位移 / 隔离驱动（见 §4）。
- **导通损耗 / 开关损耗**：导通损耗 $P_{cond}=I^2R_{ds(on)}$（与电流平方成正比、靠低 Ron 压）；开关损耗 $P_{sw}\propto f_s\cdot E_{sw}$（每次通断的能量×频率，靠软开关/ZVS 压）。选型要在二者间折中（SiC 低 Coss 同时降开关损耗）。
- **体二极管（Body Diode）/ 反向恢复电荷 Qrr**：MOSFET 内部寄生的续流二极管；其反向恢复电荷 Qrr、恢复时间 trr 决定续流损耗，高速/同步整流应用要看 Qrr（见 §12 反向恢复）。
- **栅电荷 Qg / 米勒电荷 Qgd**：驱动开通需灌入的总电荷 Qg（库仑），其中 Qgd（栅-漏，米勒段）最“难充”；驱动器峰值电流 $I_{pk}\approx Q_g/t_{sw}$ 决定开关速度。SiC 的 Qg 较小、开关更快。
- **击穿电压 BVDSS**：漏源能承受的最大电压（留 20–50% 裕量）；Si 超结约 900V 封顶，故 700V 母线吃紧——这是选 1200V SiC 的根因（见 700V 专题）。
- **跨导 gfs**：栅压对漏流的控制能力，$g_{fs}=\partial I_D/\partial V_{GS}$；越大控制越强。
- **共模 / 差模（CM/DM）**：共模=两线相对地的同向噪声（隔离可阻断），差模=两线间反相噪声；EMI 滤波器按此分类（前瞻）。

**储能电池（、13）**

- **SOC（荷电状态，State of Charge）**：电池剩余电量占额定容量的百分比（0~100%）；Simulink Battery 模块由`charge/AH`决定初始 SOC（48V 系统电池初始 80% SOC；700V 系统电池（经 DAB 隔离接入，副边约 240V）初始设满 100%）。
- **OCV（开路电压，Open-Circuit Voltage）**：电池不带载时的端电压，随 SOC 近似线性变化（OCV≈Vnom×SOC 的简化关系）。\*\* 关键坑\*\*：若把标称 700V 的电池直接并联到 700V 母线，母线会被其 OCV 钳位——该电池 SOC=50% 时 OCV≈350V，母线就升不到 700V。
- **电池等效电路模型（OCV+内阻）**：受控电压源（OCV(SOC)）串联内阻 R1；放电端压=OCV−I·R1，充电端压=OCV+I·R1（详见 §16）。
- **充放电方向约定**：以电池放电为正；母线功率缺额→电池放电补，盈余→电池充电吸收，是"母线调压执行器"的物理基础。
- **内阻损耗 $I^2R_1$**：电池等效串联电阻 R1 在充放电时发热，端压=OCV±I·R1；R1 越小效率越高。
- **容量 Ah（安时）**：电池能放出的电量定量=$I\times t$； 48V/50Ah→约 2.4kWh 可用能量（见 §23⑥）。
- **倍率 C-rate**：充放电电流相对额定容量的倍数（1C=50A 对 50Ah）；越大温升/损耗越大。
- **循环寿命**：容量衰减到 80% 前的充放次数；与 SOC 工作区间、倍率、温度相关（系统级不建模，工程需关注）。
- **库仑计数（Coulomb Counting / 安时积分）**：用电流对时间积分估算 SOC，$SOC[k]=SOC[k-1]-\frac{I[k]\Delta t}{Q_{nom}}$（放电为减、充电为加）。优点：实时、无需复杂模型；致命缺陷是电流传感器零偏会随时间**累积漂移**、初值错则永远错，故需定期用 OCV 查表“锚定”修正（详见 §16）。

**控制与测试（、13–14）**

- **PI / PID 控制器**：比例-积分-微分；用 **PI（禁用 D）**，$u=K_p e+K_i\int e\,dt$；P 管响应、I 消静态误差。
- **电压闭环 / 母线调压**：用 PID 比较`Vbus`与`Vref`自动调占空比使母线稳在目标；调压执行器是**电池侧变换器**（PV 侧 Boost 不调压，见拓扑演进 §21 / §22）。
- **前馈（Feedforward）**：不等误差出现，直接按已知规律算出执行量的大头（如 Boost 稳态公式 D=1−Vin/Vout），PI 只补公式算不准的零头。好处：启动快、PI 增益可以取得很小、抗扰不依赖误差累积。 700V Boost 的`Fcn(1−33/u)`前馈即此（§25.21）。
- **超调量 / 调节时间 / 恢复时间**：阶跃扰动后被控量冲过目标的最大幅度（超调）、重新进入并保持在目标±误差带所需时间（调节/恢复时间）——评价暂态品质的三大基础指标，与 ITAE 配合用。
- **阶跃响应 / 扰动测试**：给光照或负载一个阶跃（如光照 1000→500、负载 20→10Ω），看母线**恢复时间、超调、最大动态偏差**。
- **ITAE**：时间乘绝对误差积分 $\int t|e(t)|dt$，越小说明暂态品质越好（评价指标之一）。
- **虚拟阻抗（Virtual Impedance）**：控制里合成一个等效输出阻抗，改善多源均流与稳定性（前瞻；真实部署线路阻抗不对称时需加）。
- **带宽（Bandwidth）**：闭环能无失真跟踪的最高频率，$\approx f_s/10\sim f_s/20$（见 §23④）；带宽越宽响应越快但越易振荡。
- **稳定性裕度（相位/幅值裕度）**：奈奎斯特/伯德判据里离振荡点的安全距离；下垂+输出阻抗会改变环路增益，是“均流 vs 稳定”权衡的核心（前瞻）。
- **阻尼比（Damping Ratio, ζ）**：二阶系统特征根实部与无阻尼自然频率之比，决定暂态“过冲 vs 速度”的权衡——ζ 越小超调越大、恢复越慢（ζ<1 欠阻尼才出现过冲）；与 §11 的超调量/恢复时间直接对应，也是调 PI 时隐含的目标（把 ζ 提到 ~0.7 附近可得无明显超调的平稳响应）。
- **阻抗比判据（Impedance Ratio Criterion）**：级联系统稳定性看源输出阻抗 $Z_o$ 与负载输入阻抗 $Z_i$ 构成的环路增益 $T=Z_o/Z_i$——Nyquist 曲线不包围 $(-1,j0)$ 即稳定（Middlebrook $|Z_o|<|Z_i|$ 只是充分非必要条件）；含 CPL 时 $Z_i$ 呈负阻、易吃掉相位裕度，故需有源阻尼重塑端口阻抗（详见 §15）。
- **前馈（Feedforward）补充**：已在控制类定义——这里补一句：它不改稳态精度、只加速暂态，与 PI 互补（见 §22.5 HV-5 实例）。
- **状态空间平均（State-Space Averaging）**：把开关变换器在一个周期内的“开通/关断”两段方程按占空比加权平均、消去开关动作，得到连续小信号模型的方法（理解 Boost/Buck 增益公式 $V_{out}=V_{in}/(1-D)$ 的理论来源，进阶可读）。
- **分层控制（Hierarchical Control）**：直流微电网公认的三层嵌套架构——一次（本地无通信，下垂+双环 PI）、二次（网络级，恢复母线电压且保均流）、三次（调度层，经济/功率优化与 EMS）；目前只做到一次（下垂均流），详见 §14。
- **EMS（能量管理系统，Energy Management System）**：对应分层控制的**三次层**，按成本/SOC/电价做经济调度与功率设定点分配、管储能充放电边界、与外部电网交互，是微电网“大脑”； 700V 升级 + 真实电池后应将 OCV–SOC 与 SOC 边界喂入 EMS 方能经济最优（§14/§16）。
- **一次控制（Primary Control）**：最快、本地、无通信层——各变换器内部序列电压/电流双环 PI + 下垂； PI 闭环与下垂均属此层，代价是稳态母线随负载略降（电压偏差）。
- **二次控制（Secondary Control）**：较慢、网络级层——经通信采集全局母线电压，生成电压恢复量回灌一次环，把下垂造成的稳态压降修正回额定值且不破坏均流（常基于一致性 consensus 算法，去中心化）。
- **三次控制（Tertiary Control）**：最慢、调度层——经济调度与功率流优化，常用 MILP/凸优化求解各源设定点，约束功率平衡与 SOC 边界（即 EMS 核心，见上）。
- **主从控制（Master-Slave Control）**：与下垂并列的直流微电网均流架构——一个“主”变换器定母线电压（定压源）、其余“从”源按主给出的电压/电流基准工作；集中式、易实现但依赖主节点（单点故障），采用的下垂属去中心化替代方案（§14 工程取向）。

**仿真与求解器（贯穿）**

- **刚性系统（Stiff System）**：同一模型里同时存在极快（开关 ns 级）与极慢（控制 ms 级）的动态，时间尺度跨度巨大；显式求解器（ode45）被最快尺度逼到极小步长陷入"数值沼泽"，必须用**隐式求解器**（ode23t/ode23tb/daessc）。
- **变步长 / 定步长求解器**：变步长按误差自动调步长（平稳段大步走、瞬态密集算），是 默认；定步长恒定步长推进，牺牲效率换稳健—— 遇到变步长在启动瞬态把步长压到 1e-18"卡死"，改用**定步长 ode1be（后向欧拉）+ Simscape 局部求解器**才跑通。
- **daessc（DAE 求解器）**：Simscape 专用的微分-代数方程组求解器；物理网络导出为 DAE（含纯代数约束，如理想开关、电气节点 KCL）时自动选它。 700V 模型用 daessc，信号+物理混合模型用 ode23t。
- **零交叉检测（Zero-Crossing Detection）**：变步长求解器精确定位开关切换/饱和边界等不连续时刻的机制；开关事件极密时会拖慢仿真甚至卡死，与"最小步长坍缩"是同类症状。
- **代数环（Algebraic Loop）**：信号环路里全是直接馈通模块（Gain/SUM/Fcn 等），当前时刻输出依赖当前时刻输入，求解器必须迭代求解。破环手段：插积分器/Unit Delay/**Transport Delay 1µs**（§25.15）；Simscape 物理侧的代数环是良性的，勿强行破。
- **理想开关 vs 物理开关模型**：理想开关（MOSFET Ideal Switching）通断瞬时完成、无开关损耗，仿真快且稳（用它解 squareness 报错）；物理开关（N-Channel MOSFET）含 Vth/Ron 等细节，更真实但对求解器更苛刻。选择原则：研究系统级行为用理想，研究器件损耗/驱动细节用物理。
- **代数律 / 直接馈通（Direct Feedthrough）**：输出瞬时由输入代数式决定、无内部状态（无积分/记忆），如源2 的开环下垂律 $I_2=(V_{02}-V_{bus})/R_{v2}$；特点是响应零延迟，但 t=0 初值不当会产生硬编码尖峰。
- **布尔信号与数据类型转换**：Relational/Logical 输出是 boolean 类型，不能直接进 Gain/Subtract 等算术模块运算，须先过`Data Type Conversion`转 double（起驱动链标配，§25.3）。
- **相对/绝对容差（RelTol/AbsTol）**：变步长求解器判断“够不够精确”的两个门槛——误差超相对容差或绝对容差就缩步；AbsTol 对接近 0 的小信号（如电流尖峰）尤其重要，设太小会把步长压爆、设太大漏掉瞬态。
- **最小步长坍缩**：开关事件太密时变步长被迫把步长降到 1e-18 仍不满足误差→仿真“卡死”或极慢； 700V 系统即此病，改用**定步长 + 局部求解器**才解（见 §22.8 求解器（Simscape 局部 BE））。
- **刚性比（Stiffness Ratio）**：模型最快/最慢时间常数之比；>>1 即刚性，必须用隐式求解器（详见 §20.0“刚性系统”）。
- **ode45 vs ode23t**：ode45 是显式 RK45、适合非刚性平滑系统；ode23t 是隐式梯形/龙格-库塔、专治刚性（含 Simscape 物理网络）， 48V 系统用它。

**故障与保护**

- **极间短路（Pole-to-Pole Short）**：正负母线直接短接；母线被强行拉到近 0V。
- **故障电流（Fault Current）**：短路瞬间的巨大电流，主由电压源经极小阻抗提供，会持续上升——不像交流有"自然过零点"可自熄，故**直流故障不可控**，必须 μs 级保护动作。
- **固态断路器 / 混合直流断路器**：能高速（μs 级）开断直流故障电流的器件；常规交流断路器不能用于直流。
- **保护逻辑（过流 + 欠压）**：比较器检测 $|I_{bat}|>$ 阈值 或 $V_{bus}<$ 阈值→触发跳闸；常配 **SR 锁存器** 实现"跳闸后自锁、不抖合"（见 §19）。
- **开断过电压 / $V=L\cdot di/dt$**：断路器骤断含电感的回路时，电流被切断→感应高压尖峰（可达额定数倍）；工程上靠 **Snubber / RC 缓冲、避雷器 / TVS、软开断** 抑制。
- **绝缘监测 / 接地方式（IT / TN-S）**：直流系统安全方案骨架——IT（浮地、靠绝缘监测发现漏电）、TN-S（保护接地）；决定故障时的电流通路与人身安全风险（涉及）。
- **继电保护“四性”**：① 选择性（只跳故障支路、不误伤正常）、② 速动性（故障后尽快动作，直流要求 μs 级）、③ 灵敏性（轻微故障也能检出）、④ 可靠性（不乱动也不拒动）。 PRT 系列阈值/延时窗即对应这四性（见 §22.7 注释）。
- **灭弧 / 开断能力**：直流无电流自然过零，断路器必须靠强力分断+磁吹/半导体耗能把电弧能量快速吸收；这是直流断路器比交流贵且难的根本（前瞻）。
- **主保护 / 后备保护（Primary / Backup Protection）**：主保护快速（μs 级）切除故障、最小范围隔离；后备保护在主保护拒动时兜底，二者配合实现"选择性 + 可靠性"。本 700V 安全方案据此分级（呼应 `计划.md` day15 发电厂安全分级理念），详见 §13/§22.7。
- **投运门（Energization / Enable Gate）**：保护链里在系统启动一段时长内封锁锁存器 S 端的逻辑，防止软启动未完成、母线尚未建立时被欠压比较器误锁存。属保护"防误动"的关键时序设计。
- **延时确认窗（Delay Confirmation Window）**：故障检测后插入 20~50µs 延时再确认，滤掉开关纹波/毛刺、避免瞬时干扰误动；延时过短易误动、过长则开断 L·di/dt 过冲变大，须折中。
- **故障限流 / 控制器主动限流（Fault Current Limiting）**：控制器输出限幅（如 Saturation）只管"源侧控制通道"，跨母线并联短路的故障电流由母线电容放电 + 电池经变换器直接灌入、绕过限幅支路——故主动限流不能替代 μs 级快速开断（§13 分析的大电流尖峰即此）。
- **高阻故障（High-Impedance Fault, HIF）**：绝缘劣化/高阻接地等弱故障，电流变化微小、常规过流保护难以检出，需绝缘监测/残流检测，是直流系统安全难点（见上方 绝缘监测 / IT-TN-S）。
- **复合控制（Composite / Feedforward + Feedback Control）**：前馈（按已知稳态公式算执行量大头，如 Boost `D=1−Vin/Vout`）+ 反馈 PI（只补公式算不准的零头）的组合；700V Boost 调压核心（§25.21），好处是启动快、PI 增益可很小、抗扰不依赖误差累积。

**700V / 隔离 DAB 专题**

- **Si（硅）/ 超结 MOSFET（Superjunction）**：传统硅基功率器件；靠交替 P/N 柱承受高压，Si 高压 MOSFET 约 **900V 到顶**，700V 母线裕量吃紧。
- **SiC（碳化硅，Silicon Carbide）**：宽禁带（WBG）半导体代表。700V 母线侧优选 **1200V SiC MOSFET**——耐压余量足、寄生电容 $C_{oss}$ 小、开关损耗低、结温可达 175–200°C；代价是成本高、栅驱需负压关断/低正压开通、开关沿陡 EMI 更尖。建议：母线侧 1200V SiC，电池侧 80–100V 的 Si/SiC 皆可。
- Si 超结 MOSFET 商用最高耐压仅 **900V**、SiC 可在 1700V+ 仍保持低导通电阻（CSDN《SiC 功率器件·应用笔记2》）；800V/700V 母线系统主流选 **1200V SiC**（dianyuan、vbsemi 储能 MOSFET 选型）；SiC 另具低 $R_{ds(on)}$、高开关频率、优高温特性（导通电阻随温升幅度远低于 Si）。
- **宽禁带 WBG（Wide Bandgap）**：禁带宽度显著大于 Si 的半导体（SiC、GaN），共同优点是耐高压、耐高温、开关速度快。
- **$C_{oss}$（输出电容 / 漏源寄生电容）**：MOSFET 漏-源间的寄生电容。ZVS 靠**死区内的电感电流给 $C_{oss}$ 充放电**，把待导通管的 $V_{ds}$ 拉到 0；ZVS 条件 $|i_L|\ge 2C_{oss}V_{dc}/t_d$ 里 $C_{oss}$ 越小越易实现零电压开通。SiC 的 $C_{oss}$ 远小于 Si，所以更"好开"。
- ZVS 条件 $|i_L|\ge 2C_{oss}V/t_d$ 经 **bohrium.com** 明确给出（并举例 800V 母线、80pF、$t_d$=100ns 需 $\ge1.28$A）；更精确的能量形式 $\frac12 L_{tot}I_{pk}^2>2C_{oss}V^2$ 见 **imperix TN119**；死区内电感电流给 $C_{oss}$ 充放电实现零电压开通的机制在 **TI ZHCUD04（GaN/ZVS）、CSDN DAB ZVS** 均有图示印证。
- **ZVS（零电压开关，Zero-Voltage Switching）**：让开关管在 $V_{ds}\approx0$ 时才导通（"先关总闸再拔插头"），几乎无开关损耗与 EMI。DAB 靠**变压器漏感 + 死区**免费获得。
- **硬开关（Hard Switching）**：在高电压下强行通断大电流，产生开关损耗与电压尖峰、EMI 强。
- **软开关（Soft Switching）**：利用谐振/漏感使开关在电压或电流过零处动作（ZVS/ZCS），损耗极低；$C_{oss}$ 充放电把"开关损耗"转嫁为"导通+变压器损耗"。
- **DAB（双有源桥，Dual Active Bridge）**：原边全桥 + 高频变压器 + 副边全桥，中间仅由**漏感 $L$** 连接；靠两侧驱动**移相角 φ** 同时控制功率大小与方向；双向、宽电压范围、易 ZVS。电池↔700V 母线接口选它。
- **PSFB（移相全桥，Phase-Shifted Full Bridge）**：原边全桥移相实现 ZVS 的单向大功率拓扑；短板是**滞后臂轻载难 ZVS**（漏感能量不足→硬开通）。
- **LLC / CLLLC 谐振**：靠**变频**实现全负载 ZVS/ZCS、轻载效率高、EMI 小，但增益范围窄、双向控制复杂，不适合电池宽电压。
- **漏感（Leakage Inductance，$L_k$）**：变压器绕组间未完全耦合磁通对应的电感；在 DAB 里它就是**功率传输媒介（串联电感 $L$）**，ZVS 也靠它——变压器若设成"理想无漏感"功率就传不过去。
- **移相角 φ（Phase Shift）**：两侧全桥 50% 占空比方波之间的相位差。φ=0 不通；**原边超前副边 φ>0 → 原边→副边（如电池充电）**，φ 越大功率越大（$P\propto\phi(\pi-\phi)$，φ=π/2 最大）；副边超前则反向（放电）。这是 DAB 天然双向的核心。
- **回流功率（Backflow / Reactive Power）**：SPS 下平均净功率只是周期正/负瞬时功率的净值；当 $V_1\neq nV_2$ 时周期内存在能量倒灌段，不贡献净传输却增大 RMS 电流与导通损耗 $I^2R$。匝比匹配时最小，失配时显著上升并吃掉 ZVS 裕量。
- **隔离 / 电气隔离（Galvanic Isolation）**：变压器在两绕组间建立**物理绝缘屏障**，阻断直流/危险电压穿越与地环路；700V 系统安全必选项（即使高压侧故障，危险电压也穿不过屏障）。
- **SELV（安全特低电压）**：≤60Vdc 的可触及安全电压等级；48V 电池属此，故**绝不与 700V 直接相连**。
- **地环路（Ground Loop）/ 共模噪声（Common-Mode Noise）**：原副边共用一个地参考会耦合出环路电流与噪声；隔离使两边各有独立地参考，阻断之。
- **死区（Dead Time）**：桥臂上下管切换间的关断间隙；既防直通炸管，又是 ZVS 的前提（死区内漏感电流给 $C_{oss}$ 充放电）。
- **米勒平台（Miller Plateau）**：MOSFET 开通/关断过程中，栅-漏米勒电容 $C_{rss}$ 充放电使 $V_{gs}$ 出现一段近似平台、近乎不上升的时段；死区/开关延迟设计必须留出它（见 §6 死区公式 $t_{dead}$ 含米勒平台时长），否则上管没完全关下管就抢通→直通。
- **同步整流（Synchronous Rectification）**：用 MOSFET 代替二极管整流以降低导通损耗（二极管有正向压降/反向恢复损耗）。
- **标幺化（pu，per-unit）**：把电压/电流/功率除以各自额定基准，使控制器参数可跨电压等级复用（闭环移植 PI/下垂时用）。
- **少子尾电流（Tail Current）**：IGBT/Si 器件关断时少子复合产生的拖尾电流，增大关断损耗；SiC 无此问题，故开关更快。
- **匝比 $n$（Turns Ratio）**：变压器原:副匝数比；设计使额定工况 $V_1\approx nV_2$，ZVS 最易、回流功率最小（如 700V:240V，原:副 ≈ 2.92:1，副边 240V）。
- **SPS（单移相，Single Phase Shift）**：DAB 最基础的调制——原副边两全桥各输出 50% 占空比方波，仅通过两侧方波的**单一移相角 φ** 控制功率大小与方向（φ>0 原边超前=正向传输，φ=π/2 时功率最大，φ=0 不通）。功率律 $P=\frac{nV_1V_2}{2\pi^2 f_s L}\phi(\pi-\phi)$，简单、开关损耗低，但电压失配时回流功率大（见下）。 开环即只用 SPS 验证机理。
- **EPS（扩展移相，Extended Phase Shift）**：在 SPS 单一外移相 φ 基础上，再在原边（或副边）桥臂内引入一个**内移相角**，使该侧桥臂输出变三电平（内移相使对角两管脉宽不等）。可在 $V_1\neq nV_2$ 时把电流应力/导通损耗降约 10–30%、减小回流功率；代价是多一个控制自由度，需在线优化。
- **DPS（双移相，Dual Phase Shift）**：两侧桥臂**各引入一个内移相角**（原边内移相 + 副边内移相），比 EPS 再多一维优化空间，回流功率与电流应力进一步下降，控制更复杂。有时也特指“一侧内移相 + 一侧外移相”的两自由度调制。
- **TPS（三移相，Triple Phase Shift）**：原副边各一内移相 + 一个外移相，共三个移相角，优化空间最大、效率最优但控制最繁。工程上常用于宽电压/轻载效率敏感的储能 DAB。
- **移相调制家族的取舍**：SPS→EPS→DPS→TPS 是“移相自由度递增”的调制族；自由度越多回流功率越小、轻载 ZVS 越易保，但控制/调制实现越复杂。 开环只需 SPS；效率深度优化（失配/轻载工况）留待后续扩展移相。
- **CPL（恒功率负载，Constant Power Load）**：负载近似“吸收恒定功率 P=V·I=const”而非恒定电阻——母线电压跌时电流反而增大，呈**负阻抗特性**，易激发母线谐振/不稳定。新能源直流微电网（含电机驱动、并网变流器直流侧）常见； 48V/700V 用纯电阻负载（R_Load）未体现，但做“负载侧 Buck/变流器”时必须警惕其负阻 destabilizing 效应。
- **原边 / 副边（Primary / Secondary）**：变压器接入高压母线的绕组叫原边，接电池侧的绕组叫副边（接 240V）；匝比 $n=N_1:N_2$ 即原:副。
- **电压匹配比 $k=V_1/(nV_2)$**：额定 $k=1$ 时 ZVS 最易、回流最小；失配越大回流/电流应力越高（见 §23③）。设计原则令 $n\approx V_1/V_2$（ 700:240，原:副 ≈ 2.92:1（副边 240V），见 §20.0 匝比）。
- **电流应力（Current Stress）**：DAB 器件承受的峰值/RMS 电流；越小导通损耗越低。SPS 在轻载/失配时电流应力高，故引入 EPS/DPS/TPS（见 §20.0 移相家族）。
- **峰值电流 $I_{pk}$**：决定 ZVS 能量条件 $\frac12 L I_{pk}^2>2C_{oss}V^2$ 与磁件饱和；设计须保证 $I_{pk}$ 落在磁芯不饱和区（前瞻）。

**电力电子（补充）**

- **自举（Bootstrap）电路**：用一只电容为半桥上管提供"相对其源极"的悬浮驱动电源——下管导通时经自举二极管给电容充电，上管导通时电容两端电压不能突变、把正极"举"到母线电位给上管栅极供电（详见 §4.1 口诀"自举靠下管喂电"）。不能长期 100% 占空比（电容无法刷新），此时改用隔离电源或电荷泵。
- **自举电容 $C_{BOOT}$ / 自举二极管**：Bootstrap 的两个元件；$C_{BOOT}\ge(Q_g+I_q t_{on})/\Delta V$，常用 0.1~1µF 陶瓷；二极管隔离 VCC 与浮地、阻止电容向 VCC 倒灌。
- **电荷泵（Charge Pump）**：另一种无需变压器产生高压驱动/辅助电源的方式，适合需长期 100% 占空比的上管驱动场景。
- **IR2110**：经典高压半桥栅极驱动 IC（Infineon/IR），内部集成自举悬浮供电、可外调死区、UVLO 欠压锁定，单芯片同时驱上下两管，最高约 500V/500kHz/2A 图腾输出；是"浮地驱动"最贴近的硬件实现参考。
- **电平位移 / 电平转换（Level Shift）**：把以地为参考的低压逻辑信号抬到上管浮地参考的驱动信号，是自举驱动 IC 内部的核心功能块。
- **浮地（Floating）**：上管源极随开关节点电位跳动、无固定连接参考点，故其驱动负极必须接源极而非大地；误接地会引入 −Vbus 负压击穿栅氧（见 §4）。
- **伏秒平衡（Volt-Second Balance）**：CCM 下电感两端电压在一个开关周期内平均为零（否则磁芯持续储能/去磁、电压飘走），是推导 $V_{out}=V_{in}/(1-D)$、$V_{bat}=D_{top}V_{bus}$ 等传输比的基础（见 §2、§3）。
- **填充因子 FF（Fill Factor）**：$FF=V_{mpp}I_{mpp}/(V_{oc}I_{sc})$ 衡量 P–V 曲线"方不方"，晶硅约 0.7~0.85；$R_s$ 大/$R_{sh}$ 小→FF 差→电池越"软"（见 §1）。
- **热电压 $V_T$**：$V_T=kT/q\approx25.85\,\text{mV}$（常温），决定单二极管模型指数项陡度，温度变化整条 I–V 曲线随之移动（见 §1.1）。
- **理想因子 $n$**：单二极管模型非理想性系数，硅电池约 1~2，越大复合损失越重（见 §1.1）。
- **右半平面零点 RHPZ**：Boost/DCM 小信号模型中的右半平面零点，使电压环带宽受限制、易自激；PI 带宽须远低于其频率（见 §2.1、§11.1）。
- **小信号模型（状态空间平均）**：对开关变换器在直流工作点线性化得到的连续模型，是 PI 整定与稳定性分析的理论底座（见 §20.0 状态空间平均）。
- **恒阻负载 CR / 恒流负载 CC**：负载三种端口特性的另两类——CR（$V=IR$，正增量电阻、天然稳定）、CC（$I=I_{set}$，中性不制造负阻）；CPL 见 §20.0（见 §15）。
- **增量电阻 $R_{inc}=dV/dI$**：端口微分电阻；CPL 的 $R_{inc}<0$（负阻，危险），CR 的 $R_{inc}>0$，CC 的 $R_{inc}=\infty$（见 §15）。
- **Middlebrook 判据**：级联系统稳定的**充分非必要**条件 $|Z_o(j\omega)|<|Z_i(j\omega)|$；含 CPL 时违反它不一定失稳，需再用 Nyquist 最终判定（见 §15）。
- **有源阻尼 / 无源阻尼**：有源阻尼=在控制环重塑端口阻抗为正阻特性（无额外器件，主流）；无源阻尼=串联/并联实体 R/C（简单但有损、占体积）（见 §15）。
- **ESR / ESL**：电容等效串联电阻 / 电感；ESR 决定纹波电流发热（须看纹波电流额定值），ESL 影响高频振铃（见 §17）。
- **DCR（直流电阻）**：电感绕组直流电阻，决定导通损耗与饱和电流裕量（见 §5、§17）。
- **纹波电流额定值**：电容允许承受的 RMS 纹波电流上限，选型必查，否则发热失效（见 §17）。
- **饱和电流（电感）**：磁芯不饱和允许的最大电流，须 > 电感电流峰值（见 §17）。
- **hold-up 维持时间（电容支撑时间）**：电源中断后电容支撑负载到最低电压的时间，$C_{hold-up}=2P\,t_{hold}/(V_{nom}^2-V_{min}^2)$（见 §17）。

**变压器 / 损耗（补充）**

- **Steinmetz 公式**：方波激励下铁损估算 $P_{core}=K f_s^{\alpha}\hat B^{\beta}V_c$，$\hat B=V_2/(4N f_s A_{core})$；端电压固定时铁损近似恒定（见 §12.2）。
- **铜损 / 铁损（变压器）**：铜损 $P_{Cu}=I_{L,rms}^2 R_w$（绕组 $I^2R$）；铁损由 Steinmetz 估（磁芯），二者构成变压器总损耗（见 §12.2）。

**储能电池（补充）**

- **Rint 模型**：最简电池等效电路——开路电压 OCV 串联内阻 $R_0$（见 §16）。
- **Thévenin 1RC 模型**：$OCV+R_0+(R_1\parallel C_1)$，RC 支路描述极化/扩散动态，是车用锂电"精度/算力"折中最常用结构（见 §16）。
- **HPPC（混合脉冲功率表征）**：离线实验 + 最小二乘辨识 $R_0/R_1/C_1/OCV$ 的标准方法（见 §16）。
- **FFRLS（递推最小二乘）**：在线递归更新电池内阻等参数，补偿老化/温度漂移（见 §16）。
- **EKF（扩展卡尔曼滤波）**：库仑计数与 ECM 物理模型结合，用"模型预测电压 vs 实测电压"之差经卡尔曼增益修正 SOC，误差可压到 3% 内；Simscape Battery 提供 `SOC Estimator (Kalman Filter)` 块（见 §16）。
- **Beta 参数报错**：Simscape `Battery (Table-Based)` 当"标称电压"与"初始电荷(对应 SOC)"不在同一条 OCV–SOC 曲线上时报警；用无限容量模式规避，真实调度应填好 OCV–SOC 表（见 §16）。
- **极化电压 $U_p$**：Thévenin RC 支路两端电压，反映浓差/活化极化；状态方程 $\dot U_p=-U_p/(R_1C_1)+I/C_1$（见 §16）。
- **SOH（健康状态，State of Health）**：电池当前可用容量相对额定容量的百分比，衡量老化程度，与循环寿命强相关。

**控制 / 仿真（补充）**

- **KVL / KCL**：基尔霍夫电压/电流定律；Simscape 物理网络自动满足 KCL/KVL（见 §8）。
- **PS-Simulink Converter / Simulink-PS Converter**：物理域↔信号域接口，前者物理→信号、后者信号→物理，跨域必须经它且显式指定单位（见 §8、§22.5）。
- **Goto / From**：用全局标签减少飞线、提升模型可读性的信号对（见 §8）。
- **To File / To Workspace**：数据导出模块；`matlab -batch` 下 ToWorkspace 数据被静默丢弃，**批处理取数必须用 To File**（SaveFormat=Timeseries）（见 §8）。
- **采样率陷阱 / 同频锁相**：采样周期等于开关周期时会与开关纹波同频锁相，均值被固定相位点污染→虚假直流偏差并误判均流比；采样率须避开开关频率及其整数分频（见 §8）。
- **Memory 块**：离散环可用、连续环慎用——其输出不连续，若驱动带连续状态的模块会逼死变步长（见 §8.1）。
- **人工代数环（Artificial Algebraic Loop）**：Atomic Subsystem/Model 引用把"输入直连输出"误判成代数环；勾选 *Minimize algebraic loop occurrences* 或拆分子系统化解（见 §8.1）。
- **Atomic Subsystem**：把一组块封装成原子单元，影响代数环判定与代码生成（见 §8.1）。
- **标幺化 PI 放大律（移植要点）**：pu 下误差信号被缩小 $V_{base}$ 倍，为保持控制强度须 $K_{p,pu}=K_{p,old}\times V_{base}$、$K_{i,pu}=K_{i,old}\times V_{base}$（如 48V 系统→pu 须放大 48 倍）；**直接沿用原参数会响应极迟钝甚至失稳**。标幺化后 PI 参数为无量纲纯数，跨电压等级复用只需改基值（见 §12 标幺化移植、§20.0 标幺化）。
- **标幺基准值选择**：电压/电流/功率除以各自额定基准得 pu 量；功率基准两侧须一致（常用同一 $S_{base}$），否则 pu 电流含义错位（见 §12）。

**故障与保护（补充）**

- **IEC 61557-8**：绝缘监测设备（IMD）的国际标准，规定 IMD 的功能与安全要求（见 §22.9 / §25.24）。
- **注入式 / 非注入式 IMD**：注入式主动叠加测试信号测 $R_{iso}$；非注入式被动监测漏电流/残压（见 §22.9）。
- **残流 / 残余电流检测**：高阻故障（HIF）时检测微小漏电流以识别弱故障，是直流安全难点（见 §13、§20.0 HIF）。
- **保护分区**：直流微网常按"直流变压器区 / 直流母线区 / 直流支路区"分区，各区配"主保护 + 后备保护"（把发电厂安全分级理念迁移到微电网的落点）。
- **极差配合（阶梯配合）**：各级保护动作延时/定值按阶梯差开，保证选择性——如源支路 I/II/III 段、负荷支路 I(2×额定)/II(1.5×)/III(1.1×) 段，靠变换器主动限流 + 定值配合实现。
- **反时限保护**：动作时间随故障电流增大而缩短（如 PCC 低电压反时限 $t\propto1/I^p$），用于并网/母线后备保护。
- **差动保护**：比较保护区内外电流差，作直流变压器/母线主保护。
- **方向过流保护**：带方向的过流保护，作支路主保护保证选择性。
- **ROCOC / ICCR（电流变化率检测）**：故障瞬间 $di/dt$ 远高于正常负载变化，即使幅值被变换器限流仍可高速识别（见 §13）。
- **$dv/dt$（电压变化率）**：故障检测辅助特征，配合 $di/dt$/高频暂态能量判别故障极性与区段（见 §13）。
- **电容放电段 / 二极管续流段 / 电源馈入段**：极间短路三阶段——电容经故障点放电（RLC 二阶响应、峰值由 $U_0/\omega L$ 决定）→ 电容放空后电感储能经反并联二极管续流 → 上游电源按控制限流持续馈入稳态（见 §13）。
- **RLC 二阶响应**：电容放电段母线电压解 $LC\frac{d^2u}{dt^2}+RC\frac{du}{dt}+u=0$，欠阻尼下衰减振荡（见 §13）。
- **临界切除时间 CCT（Critical Clearing Time）**：故障须在此时间内切除以防系统失稳的保护动作硬约束。
- **低电压穿越 LVRT**：外部电网故障时微网在允许时间内仍保持并网的能力，靠 PCC 反时限低电压保护实现。

**700V / 隔离（补充）**

- **GaN（氮化镓，Gallium Nitride）**：另一种宽禁带（WBG）器件，开关更快、但耐压通常低于 SiC，适合中低压高频场景；与 SiC 同属 WBG（见 §20.0 WBG）。

**数字逻辑（补充）**

- **D 触发器 / T 触发器 / JK 触发器**：三种常用时序元件——D 输出在时钟沿跟随输入（`Q*=D`，无禁止态）；T 在 `T=1` 时每拍翻转（`Q*=T⊕Q`）；JK 把 SR 的禁止态改成"翻转"（`Q*=JQ'+K'Q`），SR/D/T 都可由 JK 转换（见 §19）。
- **SR 锁存器 / 禁止态 / 特征方程**：SR 的 `Q*=S+R'Q` 约束 `SR=0`，`S=R=1` 为禁止态（输出不定）；保护用"S=故障、R=接地"实现永久自锁（见 §19、§20.0 锁存）。
- **边沿触发 vs 电平敏感**：触发器在时钟沿采样（边沿触发），锁存器在使能期间输出随输入变（电平敏感）；Simulink `S-R Flip-Flop` 虽属边沿型，当"带记忆的自锁开关"用（见 §19）。

**变压器（补充）**

- **高频变压器**：DAB/PSFB 中传递功率并兼作**电气隔离**与漏感媒介的变压器；设计须使额定 $V_1\approx nV_2$（匝比匹配）以保 ZVS、最小化回流功率（见 §12、§20.0 漏感/匝比/隔离）。
- **平衡电阻法（IMD）**：在母线正负极对地各并联一个大电阻（如 1MΩ 模拟健康、改小模拟劣化），通过检测中点电位偏移估算绝缘电阻 $R_{iso}$；是最简单的非注入式 IMD 原理之一（见 §22.9、§20.0 绝缘监测/IEC 61557-8）。

**Simulink 通用模块（补充）**

- **Rate Limiter（斜率限制器）**：限制信号变化率；软启动若加在 PID 输出而非 Vref 会放大冲击（§7 教训——治标不治本）。
- **Scope（示波器）**：实时观测波形的可视化模块，用它看 Vbus/Ibat/Vpv/IL 暂态（§11）。
- **Step / Ramp（阶跃 / 斜坡信号源）**：Ramp 用于软启动 Vref 从 0 缓升到目标（§7）；Step 用于投运门/阶跃扰动（§11、§25.9）。
- **Mean / RMS（均值 / 有效值）**：均值与有效值计算模块，IMD 测 $R_{iso}$ 与纹波测量常用（§22.9、§17）。
- **Solver Configuration（求解器配置）**：Simscape 物理网络的求解器配置块，2025b 强制其物理端口接地否则初始化报错（§8）。
- **From Workspace / To File**：数据导入/导出模块；闭环验证时 Python 优化结果经 From Workspace 送入 Simulink 驱动电池/柔性负载（第 28 天工作流）。

---

### 21. 系统拓扑演进史

> 本节按天给出**每个阶段的系统拓扑图**（ASCII 图，自包含、无需看别的文件）。演进主线：开环物理底座→ 电池可控→ 闭环调压→ 扰动验证→ 多源下垂→ 报告收口（拓扑不变）→ 故障注入→ 保护跳闸→ 700V 隔离架构 → **700V 三环闭环集成（调控权反转）** → **平均模型（信号流，无功率拓扑）**。每张图只画"相对上一阶段**新增/改动**的部分+主干"，看图时抓住两个问题：能量从哪来到哪去？谁在控制母线电压？

> **演进主线速查（按阶段）**：①开环基线（电池直挂定压）→②插半桥（电池可控）→③加 PI 闭环（母线自稳）→④扰动台（验证闭环）→⑤双源下垂（无通信均流）→⑥报告整合（拓扑锁定）→⑦短路注入（暴露直流故障）→⑧过流+欠压保护（装跳闸）→⑨700V+隔离 DAB（电压等级/架构升级）→⑩700V 三环闭环集成（调控权反转：Boost 当母线调压器，DAB/Buck 也闭环）→⑪平均模型（信号流/无功率拓扑，服务 24h 能量平衡）。每阶段只动"相对上阶段新增"的部分，主干与接地/域划分规则不变。

#### 21.1 开环基线（48V 物理底座）

![](figures/topo_2_1_day1-5.svg)

> 电路图由`draw_topology.py`代码绘制（下同）；端口标注：MOSFET 的 D/G/S、二极管的 A(阳)/K(阴)、电容与电池的 +/−、驱动源的 +/−。

- **谁定母线电压**：没人控制——电池直挂，母线被电池电压**被动钳死**在 ~48V。
- **本阶段要害**：物理底座三规（唯一接地 / 物理域 vs 信号域 / 传感器"电流串、电压并"）；Boost 只由固定 50% 脉冲驱动。
- **为什么从开环起步**：先用物理底座验证“接地唯一、物理域/信号域分开、传感器接法”三规与 Boost 固定脉冲能升压，排除后患再上控制。开环阶段母线=电池 OCV，不是“没调好”，是还没控制器。
- **注意**：本阶段无调压能力，光照/负载一变母线就跟着漂——这是后续加 PI 闭环的动机。

#### 21.2 插入半桥双向 Buck-Boost（电池能量可控）

![](figures/topo_2_2_day6.svg)

- **变化点**：电池从"直挂"改为"经半桥接入"——充/放电方向、功率大小第一次**可主动控制**（本天仍开环固定占空比）。
- **本阶段要害**：浮地驱动（上管跟源跑、下管踩实地）、死区防直通。
- **半桥为什么能控电池**：中点经电感接电池，调上下管占空比即可控充/放电方向与功率（开环固定占空比只是“能动”，还没按母线需求自动调）。
- **权衡**：半桥比全桥省两个开关但只能单级升降压；48V 电池与母线同地故用**非隔离**半桥（若跨电压等级才需隔离，见 22.9 的 DAB）。

#### 21.3 加 PI 电压闭环（母线自动稳 48V）

![](figures/topo_2_3_day7.svg)

- **变化点**：第一次有"控制器"——**母线调压执行器是电池侧半桥**（不是 PV 侧 Boost）。
- **本阶段要害**：PI 消静差、Transport Delay 破代数环、软启动防启动冲击。
- **为什么调压执行器是电池侧而非 PV 侧**：PV 侧 Boost 已专职 MPPT/升压，母线稳压交给带储能、可双向吞吐的电池变换器最经济（PV 不可控吸收=无法放电补缺口）。这是“绕大弯”的认知核心（见 §21 末关键认知）。
- **Transport Delay 破代数环**：PI 输出经占空比→电路→Vbus→回到 PI 输入，纯直接馈通会成代数环；插 1µs 延时打破（见 §22.5 CTRL-3）。

#### 21.4 扰动测试台（拓扑不变，加扰动+记录）

![](figures/topo_2_4_day8.svg)

- **变化点**：无新功率元件；验证闭环在暂态下撑得住（恢复时间/最大偏差/电池电流方向）。
- **本阶段要害**：duty=1 钳位根因曝光——增益太小时"闭环"其实是电池钳位，定稿 PI 参数（先 P 后 I 整定）。
- **扰动测什么**：典型给光照 1000→500 W/m² 或负载 20→10Ω 阶跃，记录母线恢复时间、超调、最大动态偏差、电池电流方向翻转；三者就是 §20 超调/调节时间/恢复时间 的实操。
- **duty=1 钳位根因**：增益太小时 PI 饱和到占空比=1，变换器退化为“电池直连母线”的钳位开关，看似稳其实没在调——把积分增益整定到足够大、让积分器在仿真窗内推满执行器，才真正闭环（见 §22.5 CTRL-4）。

#### 21.5 双源并联下垂（多源无通信均流）

![](figures/topo_2_5_day9.svg)

- **变化点**：+第二路电源（受控电流源）；PI 的 Vref 上叠加 k·Ibat 变成下垂。
- **本阶段要害**：符号陷阱（教材 V0−k·Iout，电流方向定义相反须用 +）；限幅只限源2 指令。
- **符号陷阱详解**：教材下垂写 $V_0-kI_{out}$（电流流出为正），定义电池充电电流为正（与流出反向），故必须改成 $V_0+kI_{bat}$，否则会变成“出力越多参考越高”的正反馈→母线崩溃。
- **限幅只限源2**：源1 是真实 PI 闭环、源2 是开环受控电流源；为防源2 开环尖峰（见 §20 尖峰电流）加 Saturation3(±10A)，但短路时短路电流绕过它——说明限幅≠保护（见 §22.7）。

#### 21.6 报告整合（拓扑无变化）

- 本阶段无电路改动（拓扑同 §21.5），用定稿下垂参数（PI + 饱和限幅）重跑波形，沉淀验证结论。
- 提示：报告里对比“下垂 vs 定压 PI”的三类静态误差（见 §20 静态误差），是这部分的核心结论。

##### 21.6.1 验证报告怎么写（初学模板）

> 不新增电路，关键是把前 9 天的模型与波形沉淀成一份**能交出去**的报告。下面给一份“必备 5 章 + 关键波形清单”，照着填即可。

- **第 1 章 系统定位与指标**：课题背景（烟草绿色园区光储直流微电网）、48V/700V 双母线架构、设计指标（母线稳态误差≤±1%、扰动恢复时间<某 ms、THD/纹波<某%）。
- **第 2 章 物理底座与建模规范**：唯一接地、物理域 vs 信号域、传感器接法（§8）；Boost/双向半桥/DAB 各自拓扑与端口（§21.1–22.3 / §21.9）。
- **第 3 章 控制策略与整定**：① 母线调压执行器 = 电池侧半桥（非 PV Boost）的认知（§21 末关键认知）；② PI 闭环 + Transport Delay 破环 + 软启动（§6/§7/§22.5）；③ 双源下垂的符号陷阱与限幅（§9/§21.5）；④ 整定结论：定压 PI 取较大积分增益压稳态误差，下垂 PI 禁用微分(D) 并用 Saturation 限幅(±10A) 防积分饱和与电流越限。
- **第 4 章 仿真验证与波形**：按“关键波形清单”贴 Scope 图并读数（见下）。
- **第 5 章 结论与不足**：闭环达成指标否？哪些未覆盖（如 MPPT、700V 闭环）留待后续。
- **关键波形清单（每张图配：稳态值 / 恢复时间 / 超调 / 最大偏差 / 电池电流方向）**：
 1. 光照 1000→500 W/m² 阶跃：母线 Vbus 恢复曲线（验收：恢复<某 ms、最大偏差<某 V）；
 2. 负载 20→10Ω 阶跃：同上；
 3. 双源下垂：两源电流均分曲线（验证无通信均流）；
 4. 软启动：0→48V 爬升无过冲（验证 Ramp 防冲击）；
 5. 对照图：下垂 vs 定压 PI 的三类静态误差（见 §20 静态误差）。
- **易错提醒**：报告里“波形”必须来自**定稿参数重跑**，不要用探索期 duty=1 钳位的假稳曲线（见 §21.4 duty=1 钳位根因）。

#### 21.7 极间短路故障注入（在 2.5 之上加故障支路）

![](figures/topo_2_7_day11.svg)

- **变化点**：+1 开关 +1 电阻，制造"火线碰零线"。
- **本阶段要害**：直流短路无过零点、di/dt 极高；**控制器限幅救不了短路**，必须靠开断。
- **为什么直流短路最危险**：无电流自然过零、di/dt 极高，电容放电+电池灌入的峰值电流（可达额定数十倍）绕过一切控制限幅——所以控制器“限幅救不了短路”，必须靠开断（引出 22.8）。
- **非金属性短路**：经 $R_f=0.1\Omega$ 注入而非纯 0Ω，更贴近真实接触电阻；即便如此末段母线仍被分压钳到 ~28V（见 §22.4 FLT-2）。

#### 21.8 过流+欠压保护（给故障支路装"跳闸"）

![](figures/topo_2_8_day12.svg)

- **变化点**：故障支路串入 Brk；新增"检测→确认→锁存→跳闸"逻辑链。
- **本阶段要害**：Q 不是 Qbar（接反则 Brk 常开、短路注不进）；投运门防启动误锁存；A 敏感性/B 选择性/C 鲁棒性三关验证。
- **保护链四步**：检测（|Ibat|>30A 或 Vbus<33.6V）→ 延时确认（20~50µs 滤毛刺）→ 锁存（SR 自锁不复归）→ 执行（Brk 串入故障支路开断）。对应继电保护四性（见 §20.0 故障与保护）。
- **两个易错点**：① Brk 门极必须接 Q 而非 Qbar，接反则常态开路、短路注不进；② 投运门（启动 10ms 不判故障）防上电软启动冲击误锁存（见 §22.7 PRT-6/PRT-8）。

#### 21.9 700V + 隔离 DAB（电压等级与架构升级）

![](figures/topo_2_9_day13.svg)

- **变化点**：48V→700V；电池接入方式从"非隔离半桥"升级为"**隔离 DAB**"（双全桥+变压器+漏感，副边 240V、匝比 n=240/700（即原:副 700:240 ≈ 2.92:1））。负载端：实际执行版（S3 三闭环）经 Buck 降至 400V（D=400/700≈0.571，见 §25.18）；早期开环 模型负载直挂 5000Ω；早期计划曾按 700V→48V（D≈0.069）。
- **本阶段要害**：① DAB 靠移相角 φ 一个旋钮管功率大小与方向，漏感是传输媒介兼 ZVS 来源；② 原副边**各自独立接地**，隔离才有效；③ 为**开环**建压（PID 增益近零），闭环调压留待后续。
- **为什么升 700V**：高压母线降线损、提功率密度（相同功率下电流更小、线缆更细），是新能源/储能并网前端常见母线等级；代价是必须用隔离+耐高压器件。
- **DAB 一个旋钮管双向**：移相角 φ 同时定功率大小与方向（φ>0 原边超前=电池充电，φ<0 反向放电），比“两路独立变流器”省器件；开环验证期 PID 增益近零只为建压，闭环调压留待后续（见 §22.5 700V Boost PI）。
- **独立接地**：原副边各自挂独立 Electrical Reference，隔离屏障才成立——否则 700V 会经公共地窜到 48V 侧，SELV 失效（见 §20.0 地环路/SELV）。

#### 21.10 700V 终态三环闭环集成

![](figures/topo_2_10_day14-15.svg)

- **变化点（相对 21.9 开环）**：700V 系统从"开环建压"升级为**三环全闭环**——① PV→Boost 的 **PI_Boost** 成为 700V 母线调压主执行器（D≈0.383 前馈 + PI 微调）；② 电池侧 **DAB** 由固定 φ 改为 **PI_DAB** 闭环管功率平衡/电池侧；③ 负载端 **Buck** 由直挂改为 **PI_Buck** 闭环稳 400V。同时补齐：φ 命令成形（ZOH_phi 50µs + RL_phi slew=30）、GD_Boost 门驱阈值语义（uHigh=2.0/uLow=0.8/vHigh=12）、C_block 100µF 隔直串漏感、Buck 投运门 15ms。
- **本阶段要害**：**调控权反转**——48V 系统母线调压执行器是电池侧半桥，PV Boost 只 MPPT；700V 终态反过来，**PV 侧 Boost 当母线调压器**（PV 不可控吸收、只能 Boost 推功率入母线），DAB 退居功率平衡/电池管理。这是 §21 末关键认知的"镜像闭环"。
- **三环分工与防冲突**：Boost 主打 700V 母线（吸收光伏盈余/补负载缺口）；DAB 主打电池充放电功率（φ 调功率大小方向）；Buck 主打 400V 负载。三者目标不同、不直接抢同一变量，避免双调压器撞车（母线临界时 PI_DAB 微调 P 有复振风险，故取中庸增益）。
- **安全叠层**：在终态上集成设备级硬保护（S5：UV490/OV805/OC_Bus43/OC_Bat20、投运门 15ms、确认窗 30µs，Brk 微秒级开断）+ 系统级 IMD 绝缘监测（浮地 IT 系统按 IEC 61557-8 持续测 $R_{iso}$，单点碰壳不停机、两点碰壳才成极间短路）。构成"硬开断 + 软监测 + 隔离屏障"三层安全（见 §22.9）。注意：IMD 是**监测叠层**，不改功率拓扑。
- **1µs 前向欧拉下 PI 须保守**：两环降级是仿真侧标准修复——Boost、Buck 等 PI 须按步长重定，防次谐波振荡（见 §22.5）。
- **为什么必须 ZOH_phi + RL_phi**：连续 PI 输出的 φ 每步在变 → 每周期 6 开关沿而非 2 → 漏感直流偏置 → 暂态伏秒失衡触发电池过流锁存；ZOH 使 φ 每周期只更新一次、RL 限制工作点跳变速率，C_block 与 RL_phi 缺一不可（见 §22.5 / §22.8）。

#### 21.11 平均模型（信号流功率平衡，无功率拓扑）

![](figures/topo_2_11_day16.svg)

- **变化点（范式级）**：从"详细物理 Simscape 模型（开关/电感/电容/变压器/保护逻辑全有）"彻底转向"**纯信号流功率平衡日均模型**"——画面里**没有任何电力电子元件**，只剩代数运算 + 一个 SOC 积分器。目的从"看微秒级开关暂态波形"变为"跑 24h 能量平衡 / SOC 轨迹"。
- **模型结构（信号流）**：`Ir/T/P_load_set`（Constant）→ `PV_P`（Fcn 单二极管近似，出 P_pv）→ `Sum(P_load + Gain(-1)·P_pv)` 得 `P_bat`（电池功率）→ `Integrator`（IC=0.5，前接 `Gain -1/(Cap·3600)`）积分得 `SOC` → `Saturation(0.1~0.9)` 限幅 → `V_bat = Fcn(200+80·SOC)` → `I_bat = Divide(P_bat/V_bat)`；`V_pv/V_bus(=700)/V_out(=400)` 取常量；8 路 `Mux` → `To File` 导出。
- **本阶段要害**：这是"**状态空间平均**"的工程化落地——丢掉开关细节、只保留**功率守恒 + 能量积分**。详细模型步长 1µs 跑 24h = 8.64e10 步（不现实）；平均模型以"功率/能量"为变量，秒级甚至分钟级步长即可，专门服务"长时间尺度（小时~天）的光储调度/优化"。
- **与详细模型的关系**：平均模型给"工况曲线 + SOC 轨迹"这种慢变量；详细模型在关键工况点（如短路、大扰动）验证"开关暂态是否安全"。两者互补——平均模型不是"简化错误版"，是"不同时间尺度的正确抽象"。
- **局限**：不含 CPL 负阻、不含开关损耗/谐波、不含保护动作；做长时间能量管理够用，做设备级安全/电能质量必须回详细模型（§22/§25）。

> ⚠️ **关键认知（贯穿 48V 系统）**：母线调压执行器是**电池侧双向变换器**（S1_top/S2_bot）；PV侧Boost由固定脉冲驱动，**不参与母线调压**。这是绕大弯的根因。（700V 系统相反：终态里 **Boost 成为母线调压器**，DAB/Buck 也闭环——详见 §21.10；早期开环 700V 见 §21.9。）

#### 21.12 直流电压等级优选对比仿真（对接申报书第①项）

> **背景与缺口**：第①项要求“深入研究直流电压等级序列的优选原则，确定适用于厂内动力、照明、空调节能负载的安全、高效配电电压等级”。现有模型只建了 **700V 一种母线电压**（§21.9/§21.10），其合理性主要靠 §12.2 DAB 损耗分解 + §12.1 SiC 1200V 选型论据，**缺少多电压等级的损耗-效率对比仿真来用数据说话**。本节能把“为什么选 700V”从定性论据升级为定量证据。

**仿真设计**
- 复用现有拓扑（PV→Boost→V_bus→Buck→400V 负载 + 电池 DAB），**仅参数化扫 `V_bus`**，固定功率等级（如总负载 10 kW）与线路阻抗：
 - 候选序列：`V_bus ∈ {400, 600, 700, 800} V`（覆盖低压直流常用档）。
- 每档跑稳态（含 MPPT、下垂、保护），记录：变换损耗（Boost/DAB/Buck）、器件电压应力、系统总效率、母线电流（线损 $I^2R$ 若含线路）。

**评估维度与权衡**
- **电压↑**：电流↓→线损↓、变换器导通损耗↓；但器件电压应力↑（须更高耐压，Si 900V 触顶→须 SiC 1200V，成本↑）、绝缘/安全要求↑。
- **电压↓**：器件便宜、绝缘易；但电流↑→线损↑、导通损耗↑、母排/线缆截面↑。
- **最优折中**：在 700V 附近，效率提升趋缓而绝缘成本陡增，故 700V 为性价比拐点（与 §12.2 互证）。

**交付**
- 一张“系统效率 / 总损耗 vs 母线电压”曲线 + 选型表（器件耐压、线缆截面、预计成本）。
- **对应申报书**：第①项“直流电压等级序列优选原则”——对比仿真坐实选型。

#### 21.13 多场景直流微电网拓扑与可复制性验证（对接技术路线“多场景拓扑模型”）

> **背景与缺口**：申报书“（二）技术路线”明确“构建多场景直流微电网拓扑模型”；第①项要求“提出适用于不同规模和应用场景的典型系统架构方案”。现有只建了“光伏+储能+负载”一种场景（§21.10 终态），**缺少规模/隔离维度的场景变体**。

**场景变体设计**
| 场景 | 规模 | 电池接口 | 适用 |
|---|---|---|---|
| A 小容量园区 | 低功率（<5 kW） | 半桥双向直挂母线（无隔离，§3/§21.2） | 改造园区、成本敏感 |
| B 大容量园区 | 高功率（≥10 kW） | 隔离 DAB（n:1 变压器，§12/§21.9） | 新建园区、安全/隔离要求高 |
| C 高光伏渗透率 | 参数化 Ir 曲线 | 同 B | 日照丰富地区 |

**验证内容**
- 各场景跑通稳态 + 光照/负载扰动（复用 §25.13 通用测试台），确认：① 母线稳；② 保护选择性（§13 已实测方法）仍成立；③ 架构可复制。
- 输出“典型系统架构方案”对比表（拓扑、电压等级、隔离方式、适用边界），直接支撑第①项“典型系统架构方案”。

**对应申报书**
- 技术路线“多场景直流微电网拓扑模型” + 第①项“不同规模和应用场景的典型系统架构方案”——用 A/B/C 三场景把“可复制、可推广”从口号变成可仿真证据。

### 22. 元器件参数库（按元器件类型分类）

> 本节把模型用到的全部元器件按**元器件类型**重分类（半导体开关 / 无源储能 / 能量源 / 负载与故障 / 控制器与信号处理 / 传感 / 故障保护 / 700V DAB 专用 / 绝缘监测 IMD）。每个元器件用一行描述：**作用**（通常干什么）/ **参数**（罗列该元器件的参数）/ **原则**（用什么公式定参数）/ **原理**（简述依据原理，格式“xxx原理：xxxxx”，公式详见 §23）/ **常用于**（出现在哪些固定搭配 §25）。换工况时按“原则”列公式重算即可，不必照搬任何固定数值。
> 各表下方的“解析”段整合了原“主要模块清单”的接线要点、选型公式与常见坑，便于改模型时对照。三节对照索引见 §25.23。

#### 22.1 半导体开关器件（MOSFET / 二极管 / 理想开关 / 双全桥）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| `MOSFET`（N 沟道，Boost 主开关 / 半桥上下管） | 受栅压控制的电子开关，作 Boost 主开关或电池侧半桥上下管 | Vth、Ron、BVDSS、Qg、Gate port | BVDSS≥(1.2~1.5)×V_max；Ron 取小降损耗；Vth 取驱动电平下可靠开通阈值 | MOSFET原理：靠栅-源电压 Vgs>Vth 形成沟道导通、Vgs=0 关断；导通电阻 Ron 决定导通损耗 | §25.1 Boost / §25.2 半桥浮地驱动 / §25.19 互补驱动 |
| `Diode`（续流二极管） | 开关关断时为电感电流提供续流路径 | Vf、Ron | 选超快恢复/肖特基，Vf、Ron 取小 | 二极管原理：正向偏置导通（阳极→阴极）、反向截止；续流时提供电感电流回路 | §25.1 Boost |
| DAB 双全桥（`S1~S8` 理想开关） | 构成 DAB 原/副边全桥，对角同开传输功率 | Rds、Vth、Gate port（电气端子对） | 用 `MOSFET(Ideal,Switching)`（Rds 取小、Vth 取能开通、port=ee.enum.controlport.elec）解 squareness 报错 | 全桥原理：对角开关同开形成电流回路，移相角 φ 定功率方向大小 | §25.10 DAB 隔离驱动 / §25.20 载波移相 |
| `Controlled Voltage Source`（栅极驱动） | 输出受控电压作 MOSFET 栅驱；上管须浮地 | 幅值、门极类型 | 幅值=驱动电平（如 12V） | 栅驱原理：用电压源给出 G-S 驱动；上管负极须接其源极（浮地）否则打不开 | §25.2 半桥浮地驱动 / §25.16 电气门极直驱 |
| 门极链（`Gain`+`Simulink-PS`+`CVS`） | 把 0/1 逻辑转成 0/驱动电平门极 | 增益、InputFilterTimeConstant | 滤波常数=0（不加延迟） | 门极链原理：Gain 把布尔幅值放大，Simulink-PS 转物理域，CVS 给出电气栅压 | §25.16 电气门极直驱 |

**解析**：
- MOSFET 选型：BVDSS 留 20–50% 裕量；Ron 小→导通损耗低；Qg/Coss 小→开关损耗低。
- 浮地驱动铁律：上管栅驱源的 `−` 端必须接其上源极，接成地则栅-源电压随中点浮动不足、上管打不开（§4）。
- 理想开关 `MOSFET(Ideal,Switching)` 门极 `port_option` 须写全名 `ee.enum.controlport.elec`（裸 `'elec'` 报未定义变量）。
- Diode 方向铁律：阳极朝开关节点（电感侧）、阴极朝输出/高电位侧，反接=输入对地短路。
- 门极链 `InputFilterTimeConstant` 必须设 0，否则给门极串一阶延迟导致相位错乱。

#### 22.2 无源储能与滤波元件（电感 / 电容 / 电阻）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| 电感（`L_Boost` / `L_bidir`） | 储能核心，决定电流纹波、保证 CCM | L、Series R、Initial current | 由纹波率 r=ΔIL/Iout≈0.2~0.4 反推 L=Vin·D·Ts/(r·Iout)；ΔIL=Vin·D·Ts/L；校核 Lcrit=(1−D)²·R·D/(2fs) 深 CCM | 电感原理：储能 W=½LI²，开关周期内充放能形成三角纹波；L 太小纹波炸/进 DCM，太大动态慢 | §25.1 Boost / §25.5 双向半桥 |
| 电容（`Cin_PV`/`Cin_Bat`/`Cout_Bus`/`C_block`） | 吸收开关纹波、稳定节点电压、隔直 | C、Initial voltage | C≥ΔIL/(8fs·ΔV)；兼顾阶跃支撑数十 ms；C_block 串漏感隔直 | 电容原理：电荷缓冲，电流积分平滑电压；Cout 越大母线越稳、上电浪涌越小；Initial voltage=目标值即“预充电” | §25.1 Boost / §25.12 物理地基 / §25.18 Buck |
| 电阻（`R_Load`/`Rf`） | 负载或故障低阻通路 | R | R=V²/P（负载）；Rf 取非金属性短路残余量级（约 0.1Ω） | 电阻原理：纯阻性耗能，无负阻效应（作 CPL 须警惕） | §25.4 闭环 / §25.13 扰动 / §25.8 短路注入 |

**解析**：
- 电感纹波公式（CCM）ΔIL=Vin·D·Ts/L；纹波率 r=ΔIL/Iout≈0.2~0.4 反推 L；并用 Lcrit 校核深 CCM（§23①）。
- 电容：Cout_Bus 兼母线支撑电容，Initial voltage 设目标母线电压=预充电消上电浪涌（§17/§22）。
- 电阻用法：R_Load 由 P=V²/R 定（可 Step 投切做重载扰动）；Rf=0.1Ω 模拟低阻故障，越小越近金属性短路。
- 电气参考（地）铁律：全网功率地唯一；隔离变压器两侧各挂独立 Reference，否则隔离失效、求解奇异（§1.2）。

#### 22.3 能量源（光伏 / 电池）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| 光伏阵列（`PV_Panel`） | 把光能转直流，系统一次能源 | Ir、Temperature、Number of series cells、Isc/Voc 系数 | N≈Vtarget/Vcell,mpp；Isc=Isc,ref·(G/GSTC)+α(T−Tref)；Voc=Voc,ref+β·ln(G/GSTC)+γ(T−Tref) | 光伏原理：光生电流与辐照线性、Voc 近似不动，改 Ir=平移 I–V 曲线；单二极管模型见 §1.1 | §25.1 Boost / §25.13 扰动 |
| 蓄电池（`Battery`） | 储能、母线调压执行器 / 支撑源 | Vnom、AH、charge(→SOC)、R1、容量模式 | OCV≈Vnom·SOC；SOC 由 Ibat·Δt/Qnom 积分；容量 Ah 由能量需求定 | 电池原理：OCV-SOC 近似线性，充电 I>0 端压更高；内阻压降 ±I·R1；SOC 积分按放电为正 | §25.5 双向半桥 / §25.10 DAB 隔离 |
| 辐照度源（`Ir` Constant） | 设定光照强度 | G（W/m²） | Ir=G；STC GSTC=1000 | 辐照原理：改 Ir 只上下平移 I–V 曲线，不改其它参数 | §25.13 扰动 |

**解析**：
- 单块晶硅 Voc,cell≈0.6~0.7V，60 串→Voc≈37.8V；Isc≈并联支路数×Isc,cell。
- 电池 OCV≈Vnom·SOC 简化线性（真实为曲线）；AH 定能量 E=Crated·Vnom。无限容量模式仅为规避 Simulink Battery beta 报错（MILP 改回有限容量）。
- 架构警示（探索期 what-if，非最终方案）：若把电池直接并联母线，母线被 OCV 钳位（SOC=50%→350V）；最终经 DAB 隔离接入（§12/§21.9），电池不直挂母线。

#### 22.4 负载与故障注入（电阻 / 开关）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| `R_Load`（负载电阻） | 系统负载 | R | R=V²/Pload（可 Step 投切） | 电阻原理：纯阻性耗能 | §25.4 闭环 / §25.13 扰动 |
| `Rf`（故障电阻） | 极间短路低阻通路 | R | 取非金属性短路残余接触电阻量级（约 0.1Ω） | 故障原理：短路峰值≈Vbus/Rf 绕过控制限幅，直流无过零须 μs 级开断（§23⑧） | §25.8 短路注入 |
| `Switch`/`Manual Switch`/`Step`（投切） | 定时/手动投切负载或注入故障 | 闭合时刻 | 在稳态建立后注入 | 投切原理：改变拓扑连接状态 | §25.8 短路 / §25.13 扰动 |

**解析**：R_Load / Rf 用法见 §22.2 电阻行；投切与故障注入元件见 §22.7 保护链与 §25.8/§25.13。

#### 22.5 控制器与信号处理（PI / 下垂 / 饱和 / 延时 / 比较 / PWM / 类型转换 / 增益 / 软启动 / 前馈）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| `PID Controller`（PI） | 闭环调节，输出占空比/电流指令 | P、I、D、Limit output、Anti-windup | 先 P 定响应速度、再 I 消静差；实践中禁用 D | PID原理：u=Kp·e+Ki∫e（PI）；对阶跃参考稳态误差→0（Type-1），下垂偏移不归它管（§23④） | §25.4 闭环 / §25.6 下垂 / §25.21 前馈+PI |
| `Ramp`+`Saturation`（软启动） | 参考从 0 缓升到目标，消上电冲击 | Slope、Sat 上下限 | Slope=Vtarget/tramp；须早于首个扰动 | 软启动原理：参考斜坡上升避免阶跃冲击；必须加在设定值而非 PID 输出 | §25.4 闭环 / §25.14 多斜坡对齐 |
| `Gain`（下垂系数/门极幅值） | 比例缩放信号 | Gain | 下垂 k=1/Rv；门极 Gain=0/1→0/驱动电平 | 增益原理：纯比例缩放 | §25.6 下垂 / §25.2 栅驱 |
| `Subtract`（误差比较器） | 算误差 e=Vref−Vbus | 端口符号 | e=Vref−Vbus（负反馈） | 比较原理：误差符号决定控制方向 | §25.4 闭环 |
| `Transport Delay`（破代数环） | 打破 PI→电路→PI 代数环 | Time delay | 取一个仿真步长量级（如 1µs） | 延迟原理：延迟>0 无 direct feedthrough 破纯信号环 | §25.15 破代数环 |
| `Repeating Sequence`（锯齿载波） | 生成 PWM 载波 | Time、Output | 周期=1/fsw，Output[0,1] | PWM 载波原理：周期锯齿波作调制波 | §25.3 PWM |
| `Relational Operator`（PWM/保护比较器） | 逐点比较，作 PWM 或阈值检测 | 运算符 | `<`：载波<D→1 | 比较原理：逐点比较生成方波/故障信号 | §25.3 PWM / §25.9 保护 |
| `Data Type Conversion` | 布尔→double 等 | 输出类型 | 布尔→double | 类型转换原理：域匹配才能进算术链 | §25.3 PWM |
| `Saturation`（限幅） | 限制幅值，兼破尖峰 | 上下限 | 由安全范围定 | 限幅原理：夹住信号在允许范围 | §25.6 下垂 / §25.9 保护 |
| `Zero-Order Hold`/`RateLimiter`（DAB φ） | 离散保持/变化率限制 | SampleTime / Slew | ZOH SampleTime=开关周期量级；RL Slew 由允许 φ 变化率定 | 采样/斜率原理：ZOH 消除连续 φ 直流偏置过流；RL 抑制暂态伏秒失衡 | §25.10 DAB / §25.20 移相 |
| `Fcn`（占空比前馈） | 直接算基准占空比 | 表达式 | D=1−Vin/Vout（稳态） | 前馈原理：PI 只补零头 | §25.21 前馈+PI |
| `To File`/`Scope`（记录） | 导出/看波形 | SaveFormat | Timeseries | 记录原理：采样存储供分析 | §25.7 数据记录 |

**解析**：
- PID 公式 u=Kp·e+Ki∫e+Kd·de/dt；用 PI（禁 D），抗积分饱和用 LimitIntegrator+积分硬限幅 ±5（单设 clamping/none 易饱死）。
- Ramp 必须加在参考设定值上（Vref 从 0 缓升），绝不加在 PID 输出（会猛甩占空比）。
- Subtract 符号：Vref−Vbus 才是负反馈；翻成 Vbus−Vref 变正反馈把母线推飞。
- Transport Delay 插在反馈进比较器/控制器之前，1µs 对 50kHz 动态可忽略。
- Repeating Sequence：`Time[0 20µs],Output[0 1]`→周期 20µs=50kHz，改 Time 长度即改 fsw。
- Relational 作 PWM：`载波<D ? 1:0`；Data Type Conversion 布尔→double 才能进数值运算。
- To File：`SaveFormat=Timeseries` 存 .mat；采样率避开 50kHz 整数分频（采样率陷阱）。

#### 22.6 传感与测量（电压 / 电流传感器 / 域接口）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| `Voltage Sensor` | 并联测电压 | — | 并联测压差 | 传感原理：电压并、电流串 | §25.11 域接口 |
| `Current Sensor` | 串联测支路电流 | — | 串联测支路 | 同上；I_Battery 正=充电负=放电 | §25.11 域接口 |
| `Simulink-PS` / `PS-Simulink Converter` | 信号↔物理域转换 | InputFilterTimeConstant | 门极用 0（去延迟） | 域转换原理：信号域与物理域接口，对偶一对 | §25.11 域接口 |
| `Solver Configuration` | 物理网络求解配置 | — | 默认；勿关 ModeIterationsInAlgLoops | 求解原理：出现代数环自动模式迭代（良性） | §25.12 物理地基 |

**解析**：
- 传感器极性：Voltage Sensor 并联、Current Sensor 串联；I_Battery 正=充电负=放电，接反是经典坑（电压全零/电流恒零）。
- Simulink-PS `InputFilterTimeConstant` 设 0（门极驱动），否则一阶滤波使门极波形发钝。
- PS-Simulink 把 V/A 转 Simulink 信号供控制/记录。
- Solver Configuration 代数环自动启用模式迭代算法，勿用 `feature('ModeIterationsInAlgLoops',0)` 关掉（关掉反而仿真失败）。

#### 22.7 故障与保护链（开关 / 断路器 / 锁存 / 逻辑）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| `Abs`（取绝对值） | 电池电流双向，过流看幅值 | — | — | 幅值原理：\|Ibat\| 反映过流大小 | §25.9 保护 |
| 过流/欠压阈值（`Relational`） | 检测故障 | 阈值 | 过流>Irated×(2~3)；欠压<0.7×Vnom | 阈值原理：留裕量远小于短路峰值，短路必触发 | §25.9 保护 |
| `OR`（逻辑或） | 过流/欠压任一成立即判故障 | 输入数 | 2 输入 | 或原理：任一致故障 | §25.9 保护 |
| 延时确认窗 + 投运门 | 滤毛刺、防上电误锁 | 延时 | 20~50µs 延时+AND；投运门 ~10ms+AND | 确认原理：延时滤开关纹波毛刺，投运门防软启动冲击 | §25.9 保护 |
| `S-R Flip-Flop`（锁存） | 故障自锁 | S/R | S=故障、R=0 | 锁存原理：置位后永久保持、不抖合 | §25.9 保护 |
| `Breaker`/`Ideal Switch`（开断） | 受控开断故障支路 | 门极约定 | G>阈值=开断 | 开断原理：切断故障电流通路 | §25.8 短路 / §25.9 保护 |

**解析**：
- S-R Flip-Flop 自锁：S=故障置位后 Q 永久保持 1；Breaker 栅极接 Q（非 Qbar）否则常态开路（踩坑）。
- 比较器阈值：过流 \|Ibat\|>Irated×2~3、欠压 Vbus<0.7×Vnom；太小误动、太大拒动。
- Breaker 门极约定 G>0.5V=开断（先实测确认），作保护执行机构断开故障支路。
- Manual Switch/Step 投切：手动或定时投切负载/故障，验证鲁棒性。

#### 22.8 700V DAB 专用（变压器 / 载波 / 移相 / 阈值 / 求解器）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| 高频变压器 | 原副边隔离 + 电压变换 | 匝比 n、漏感 Lk | n=V1/V2（额定匹配）；漏感取传输所需兼 ZVS | DAB 变压器原理：隔离+变比，漏感即功率传输媒介（设理想无漏感则传不过去） | §25.10 DAB / §25.20 移相 |
| `Repeating Sequence1`（DAB 三角载波） | 一条载波喂两阈值生成互补两路 | 周期、幅值 | 三角波关于 0 对称 | 载波原理：单载波双阈值生成互补 | §25.20 移相 |
| `Constant1/2`（双阈值） | 调死区 | 阈值 | 阈值差÷载波斜率=死区窗口（≥300ns） | 死区原理：两管都关间隙防直通+给 Coss 充放电 | §25.20 移相 |
| `Transport Delay1/2`（移相） | DAB 功率旋钮 | t_delay | t_delay=φ/(2π·fsw) | 移相原理：错相角 φ 定功率方向大小 | §25.10 DAB / §25.20 移相 |
| 8 路门极链 | 0/1→0/驱动电平门极 | 增益、滤波常数 0 | 门极幅值=驱动电平、滤波常数 0 | 同 §22.1 门极链 | §25.16 电气门极直驱 / §25.20 移相 |
| `PWM Generator (DC-DC)` | 生成错相方波 | Phase shift、Dead time、Duty | Phase shift=φ；Dead time≥300ns；Duty=50%×2 | DAB 驱动原理：错相角即移相角 | §25.10 DAB |
| 求解器（Simscape 局部 BE） | 700V 系统可用解 | 步长 | 变步长坍缩→定步长+局部 BE（步长 ~1µs） | 求解原理：理想开关+高频 DAB 下变步长步长坍缩，定步长+局部 BE 才稳 | §25.12 物理地基 |

**解析**：
- DAB 匝比 n=V1/V2（令额定 V1≈nV2，ZVS 最易、回流最小）；额定功率落 φ≈30~60° 留余量不到 90°（最大功率在 90°，§23③）。
- 死区 ≥300ns 防直通并给 Coss 充放电留时间。
- 漏感须满足 L≤V1²/(8k·P·fsw)（k=V1/(nV2)）且不小于 ZVS 最小值。
- 求解器：含 Simscape 刚性→变步长隐式求解；含理想开关+高频 DAB→定步长+局部 BE。具体求解器选型与步长配置见 §25.12。
- 权威参考：SPS 功率律见 TI SLUP430、KIT 500kW 报告；匝比取 V1/V2 见 EPRI DAB 磁件优化；SiC 栅驱 +18/+20V、关断 −3~−5V 见 onsemi/ROHM/CREE 指南。

#### 22.9 绝缘监测（IMD，系统级安全）

| 元器件 | 作用 | 参数 | 原则（公式） | 原理 | 常用于 |
| --- | --- | --- | --- | --- | --- |
| 对地绝缘电阻 `Riso`（建模用大电阻） | 模拟母线对地绝缘，劣化时改小 | R（健康~1MΩ、劣化~50kΩ） | Riso 报警≤100kΩ（典型，按 IEC 61557-8 与 Un 相关曲线） | 绝缘监测原理：浮地 IT 系统无回路，高阻故障初期不跳闸不报警；IMD 带电持续测整条母线对地 Riso，把隐性绝缘劣化变成可处理报警（“不靠接地保命、靠监测兜底”） | §25.24 IMD 建模搭配 |
| 注入源（受控交流源 + 高阻耦合） | 注入低频测试信号测 Riso | 频率、耦合阻抗 | 注入交流小信号经电容/电阻耦合 | 注入法原理：注入交流小信号对直流母线隔直，不改变工作点即可独立测对地泄漏阻抗；可检对称+不对称绝缘下降（平衡电桥法会对称失明） | §25.24 IMD 建模搭配 |
| `RMS`/`Mean` + `PS-Simulink` | 测返回信号算 Riso | — | 由返回电流幅相反推 Riso | 测量原理：测注入信号响应反推绝缘阻抗 | §25.24 IMD 建模搭配 |
| 比较器 + 报警/联动逻辑 | Riso<阈值→预警/跳闸/急停 | 阈值 | 预警/跳闸两级阈值 | 联动原理：IMD 抢在短路前预警，Brk 抢在短路后开断（软+硬双层） | §25.9 保护 / §25.24 IMD |

**解析（系统级安全）**：

- **为什么浮地系统必须配 IMD**：本系统电源侧是 IT 浮地系统（隔离变压器/DAB 把 700V 母线与电网、48V 侧电气隔离，无人工中性点接地），单点碰壳不形成大回路、不停机。但浮地有盲点：绝缘缓慢老化/受潮/小动物搭接会使某极 $R_{iso}$ 下降，因无回路、高阻故障（HIF）初期不跳闸不报警，直到另一极再碰地才变极间短路。IMD 即在“还没短路”阶段持续测 $R_{iso}$ 预警，把隐性劣化变可处理报警。类比：DAB/隔离变压器是“挡穿越性短路的墙”，IMD 是“定期体检的医生”，两者必须共存。
- **$R_{iso}$ 与报警阈值**：正负母线对地绝缘电阻的并联等效值（任一级下降都拉低总 $R_{iso}$）；工业 IT 系统常设报警 $\le 100\,\text{k}\Omega$（或按 IEC 61557-8 与 $U_n$ 相关曲线），动作分两级：预警（Warning）→ 报警/跳闸（Trip），有人场所取更保守值。测量前提：IMD 自身必须高阻输入，不能把浮地“拉成接地”。
- **三大测量法（初学者重点）**：

| 方法 | 怎么测 | 优点 | 致命弱点 |
| --- | --- | --- | --- |
| 平衡电桥法（早期） | 正/负母线对地各串大电阻到内部参考点，测桥臂电流算 $R_{iso}$ | 简单、低成本、不影响浮地 | 对称性失明：正负极绝缘对称下降时电桥仍平衡，测不出（漏报高风险） |
| 不平衡电桥法 | 故意让两桥臂不等、周期切换，解 $R_+$、$R_-$ 分别绝缘 | 能区分正负极，缓解对称失明 | 切换瞬间短暂扰动系统 |
| 注入信号法（现代主流，推荐） | IMD 内生成低频交流测试信号，经高阻耦合电容/电阻注入母线对地；测返回电流幅相反推 $R_{iso}$ | 可检对称+不对称下降；不影响直流工作点；能定位故障极 | 电路与算法稍复杂；需选频率避开系统噪声 |

 - 选型直觉：教学/低成本样机可用平衡桥法体会原理；**工程落地一律注入信号法**（Bender、Sensata SIM200、TI TIDA-010232 均此路线），它解决了“对称老化失明”这一最危险漏报。注入法能避雷：注入的是交流小信号、对直流母线隔直（电容耦合），既不改工作点又能独立测泄漏阻抗。
- **标准依据**：IEC 61557-8:2014《交流 1000V/直流 1500V 及以下 IT 系统绝缘监测装置》是 IMD 权威标准，规定测量范围/响应时间/精度/报警阈值/对系统扰动限制；做课题引用可证“方案合规”。配套 IEC 60364（低压电气装置 IT 要求）、IEC 62477（电力电子变换器安全）。
- **动作逻辑**：Riso<预警值→预警灯/本地报警；<跳闸值或同时漏电→联动上层（断 DAB/光伏接触器、触发 §25.9 系统级急停、上报 MQTT/Grafana）。IMD 与 Brk 层级不同：IMD 抢在短路前、Brk 抢在短路后（建模实现见 §25.24）。

### 23. 公式速查表

#### 23.1 电力电子 — Boost（PV 侧，CCM）

- 占空比：$D=1-\dfrac{V_{in}}{V_{out}}=\dfrac{V_{out}-V_{in}}{V_{out}}$（例：$V_{out}=2V_{in}\Rightarrow D\approx0.5$；一般直接代入两端电压求 $D$）
- 电压传输比：$V_{out}=\dfrac{V_{in}}{1-D}$（CCM 才成立）
- 电感电流纹波：$\Delta I_L=\dfrac{V_{in}\cdot D\cdot T_s}{L}=\dfrac{(V_{out}-V_{in})\,D\,T_s}{L}$（例：$V_{in}=24V、D=0.5、T_s=20\mu s、L=500\mu H\Rightarrow\Delta I_L\approx0.48A$）
- CCM/DCM 边界：$L_{crit}=\dfrac{(1-D)^2 R D}{2f_s}$（$I_{L,min}>0$ 即 CCM；纹波谷值触 0 进 DCM，增益公式失效）
- 输出纹波（**Boost**）：$\Delta V_{out}\approx\dfrac{I_{out}\cdot D}{C\cdot f_s}$（电容在开关导通期给负载放电；**Buck** 才是 $\Delta I_L/(8f_sC)$，二者勿混）
- **适用条件小结**：以上五式均要求 **CCM、电感电流连续、忽略寄生电阻**；一旦进入 DCM（纹波谷值触 0）电压传输比公式失效、增益随负载变（见 §20.0 CCM/DCM）。Boost 电感电流纹波公式里 $T_s=1/f_s$；代入典型值 $f_s=50kHz、L=500\mu H、D=0.5、V_{in}=24V\Rightarrow\Delta I_L\approx0.48A$。母线支撑电容即按 $\Delta V_{out}\approx I_{out}D/(Cf_s)$ 压纹波（Boost 放电公式；Buck 才是 $\Delta I_L/(8f_sC)$，见上式）。
- **物理含义**：占空比 D 直接定增益（升压比=1/(1−D)），所以 D 越接近 1 升压越猛、但占空比余量越小、右半平面零点越靠左→越难稳定（进阶：Boost 是非最小相位系统）。

#### 23.2 双向 Buck-Boost（电池侧半桥）

- Boost 倾向（放电）：$V_{bus}\approx\dfrac{V_{bat}}{1-D_{bot}}$（$D_{bot}$=下管占空比）
- Buck 倾向（充电）：$V_{bat}\approx D_{top}\cdot V_{bus}$（$D_{top}$=上管占空比）
- 互补约束：$D_{top}+D_{bot}=1$（忽略死区）
- **物理含义与适用条件**：上两式是半桥“单开关导通”时的近似（忽略死区与电感压降、假设 CCM）；$D_{top}+D_{bot}=1$ 来自“同一桥臂上下管互补、插死区”的驱动逻辑。双向 Buck-Boost 靠切换哪管主导，实现升压（放电）或降压（充电）同一级电路双向能量流。

#### 23.3 DAB（单移相 SPS）

- 移相占空比：$D_\phi=\dfrac{\phi}{\pi}$（φ=两侧方波相位差）
- 平均功率：$P=\dfrac{nV_1V_2}{2\pi^2 f_s L}\,\phi(\pi-\phi)$
- 最大功率（φ=π/2）：$P_{max}=\dfrac{nV_1V_2}{8 f_s L}$
- 回流功率：当 $V_1\neq nV_2$ 时周期内有能量倒灌段（不贡献净传输却增大 RMS 电流与导通损耗），匝比匹配时最小、失配时显著上升并吃掉 ZVS 裕量（深度优化见 §12.1 扩展移相 EPS/DPS/TPS）
- ZVS 条件：死区内 $|i_L|\ge\dfrac{2C_{oss}V_{dc}}{t_d}$（或能量式 $\tfrac12 L_{tot}I_{pk}^2>2C_{oss}V^2$）；SiC 的小 $C_{oss}$ 更“好开”
- **物理含义**：SPS 下平均功率是 φ 的抛物线，$P\propto\phi(\pi-\phi)$，φ=π/2(90°) 达峰、φ=0 不通、φ>π/2 后功率反而下降（控制不稳定区，应避免）。$D_\phi=\phi/\pi$ 是归一化移相占空比。
- **适用条件**：SPS 公式假设两侧方波各 50% 占空比、仅单一外移相、忽略死区与器件压降；电压失配（$V_1\neq nV_2$）时回流功率与电流应力显著上升→引入 EPS/DPS/TPS（见 §20.0 移相家族）。ZVS 条件可写成电流式或能量式，死区内 $|i_L|$ 必须足够把待开通管 $C_{oss}$ 抽放完。
- **典型数值（示例）**：取 $\phi\approx30^\circ、n\approx2.92、f_s=50kHz$、$L$ 为漏感，则 $P_{max}=nV_1V_2/(8f_sL)$；设计应使额定功率落在 $\phi\approx30\text{–}60^\circ$（留余量不到 90°）。若电池直挂为 48V（无 240V 中间级），则 $n=700/48\approx14.6$，数值随之变化。

#### 23.4 PID / PI

- 连续域：$u=K_p e+K_i\!\int\!e\,dt+K_d\dfrac{de}{dt}$（实践中常**禁用微分项 D**，退化为 PI 控制）
- 误差：$e=V_{ref}-V_{bus}$
- 离散化（后向欧拉 /  Simulink 实现）：$u(k)=u(k-1)+K_p[e(k)-e(k-1)]+K_i T_s e(k)$
- 抗积分饱和：clamping + LimitIntegrator ±5（单设`none`易饱死、单设`clamping`反恶化）
- 超调：$\sigma\%=\dfrac{V_{peak}-V_{ss}}{V_{ss}}\times100\%$；带宽 $\approx f_s/10\sim f_s/20$
- **物理含义**：连续域公式给出“理想”控制器；实践中常**禁用 D**（微分放大噪声、对开关模型无益），退化为纯 PI。离散式是 Simulink 实际跑的后向欧拉实现，与连续式在步长够小时一致。
- **适用条件**：PI 对阶跃参考稳态误差→0（Type-1），但下垂固有偏移不归它管（见 §20 静态误差三义）；抗积分饱和用 LimitIntegrator±5 而非单设 clamping/none（见 §20 抗积分饱和）。带宽 $\approx f_s/10\sim f_s/20$ 是“开关频率 vs 控制带宽”的经验划分。

#### 23.5 下垂控制（Droop）

- 下垂律：$V_{ref}=V_0 + k\cdot I_{bat}$（约定 $I_{bat}$ 正=充电，符号与教材 $V_0-k\cdot I_{out}$ 相反）
- 虚拟电阻：$k=1/R_v$，引入的电压偏移 $\Delta V_{droop}=V_{bus}-V_0=k\cdot I_{bat}$（设计代价，非控制误差）
- 均流比：两源出力反比于虚拟电阻比，$I_1:I_2=R_{v2}:R_{v1}$（例：$R_{v1}=10、R_{v2}=0.2\Rightarrow I_1:I_2=0.2:10=$**1:50**，源2 因虚拟电阻小得多而承担绝大部分电流）
- **物理含义**：下垂律把“电压参考”随本源电流下调，多源并联时谁电流大谁参考低→自动少出功，实现无通信按比例分功率。虚拟电阻 $R_v=1/k$ 是“看不见的串联电阻”，两源 $R_v$ 之比反比于出力比。
- **适用条件**：均流精度依赖各源虚拟电阻之比与线路对称；若线路电阻不对称或某源限幅触顶，实际比会偏离理论值（见 §20 功率分配精度）。下垂偏移 $\Delta V=kI$ 是设计代价，非控制误差，想减小就加大 $R_v$（但均流变慢/精度下降）。

#### 23.6 电池 / 储能

- SOC 积分：$SOC(t)=SOC(0)-\dfrac{1}{C_{rated}}\int I_{bat}dt$（放电为正）
- OCV 近似：$OCV\approx V_{nom}\cdot SOC$（例： **48V** 电池 $V_{nom}=48V$ 时 100%→48V、50%→24V；§21.11 平均模型用 $V_{bat}=200+80\cdot SOC$ 更贴近真实“中段平缓、两端陡”曲线，700V 是母线不是电池）
- 端压：放电 $V_{bat}=OCV-I R_1$，充电 $V_{bat}=OCV+I R_1$
- 容量能量：$E=C_{rated}V_{nom}$（例：$C=50Ah、V_{nom}=48V\Rightarrow E=2400Wh$）；$I_{sc}\propto G$（辐照度线性、Voc 近似不动）
- **物理含义**：SOC 积分按“放电为正”累计流出电量；OCV≈Vnom·SOC 是简化线性模型（真实 OCV-SOC 是曲线，锂电池中段平缓、两端陡）。端压公式含内阻压降 ±I·R1。
- **适用条件**：无限容量模式下 SOC 几乎不动，此时 SOC 积分只作示意；有限容量（MILP/真实循环）才看 SOC 真实变化。容量能量 $E=C_{rated}V_{nom}$ 给出 50Ah×48V=2400Wh 量级。

#### 23.7 MPPT（最大功率点跟踪）

- MPP 判据：$\dfrac{dP}{dV}=0$ 或 $\dfrac{dI}{dV}=-\dfrac{I}{V}$
- P\&O 扰动观察：$D(k+1)=D(k)\pm\Delta D$，按 $\mathrm{sign}(dP/dD)$ 决定步向（** 已实装，见 §25.22**）
- **物理含义**：MPP 判据 $\frac{dP}{dV}=0$（等价于 $\frac{dI}{dV}=-I/V$）指 PV 曲线上功率对电压斜率为零的点即膝部最大功率点。P\&O 用“扰动占空比→看功率变不变→决定下次扰动方向”的爬山法逼近 MPP。
- **适用条件**：P\&O 在光照缓变时有效，突变时会在 MPP 附近小幅振荡（步长 ΔD 取大收敛快但纹波大）；** 已实装，见 §25.22**。

#### 23.8 故障与保护

- 稳态故障电流：$I_{fault,ss}\approx V_{bus}/R_f$（例：$V_{bus}=48V、R_f=0.1\Omega\Rightarrow I_{fault}\approx480A$）
- 初始斜率：$\left.\dfrac{di}{dt}\right|_{t=0^+}\approx V_{bus}/L_{eq}$（峰值可达额定数十倍；直流无过零点）
- 开断过冲：$V_{spike}=L_{loop}\cdot di/dt$（例：可达母线电压的数倍）
- 跳闸阈值：欠压 $V_{bus}<70\%V_{nom}$、过流 $|I_{bat}|>(2\sim3)\times I_{rated}$；延时确认窗 $20\sim50\mu s$ + 投运门 $\sim10ms$（滤毛刺、防上电误锁）
- **物理含义**：稳态故障电流≈Vbus/Rf 是短路“稳态”近似（实际还含电感限制上升率）；初始斜率 $di/dt≈Vbus/L_{eq}$ 描述电容+电池灌入的上升速率（峰值可达额定数十倍）；开断过冲 $V_{spike}=L_{loop}di/dt$ 是断路器断感生高压（可达母线电压数倍）；跳闸阈值取 $0.7\times V_{nom}$ 欠压、$(2\sim3)\times I_{rated}$ 过流，配延时窗与投运门。
- **适用条件**：以上为“直流故障不可控、必须 μs 级开断”的定量依据；交流有电流过零可自然熄弧，直流没有——故常规交流断路器不能用于直流（见 §20.0 故障与保护）。

#### 23.9 仿真工程（选型原则与公式）

- 求解器选择：含 Simscape 物理网络且刚性适中 → 变步长（如 ode23t，MaxStep 取 $10^{-6}$ 量级）；含**理想开关 + 高频 DAB** 时变步长会在开关瞬间步长坍缩 → 改用定步长 + Simscape 局部 Backward Euler（步长同 $10^{-6}$ 量级）。
- 代数环破环：反馈链插入 Transport Delay $\approx 1\mu s$（远小于开关周期，对动态可忽略）。
- PID 整定原则：先定 $K_p$ 管响应速度、再调 $K_i$ 消静差；在 1µs 前向欧拉下 $K_i$ 取保守值以防激发开关次谐波（具体定稿数值见 §15 / §22.5，按工况微调）。
- 软启动斜率：Ramp 斜率 $=V_{target}/t_{ramp}$，$t_{ramp}$ 须早于首个扰动且远小于暂态时间；多路斜坡（参考/指令/光照）须对齐，否则错峰造尖峰。
- **适用条件小结**：求解器按系统刚性选（变步长 vs 定步长局部 BE）；Transport Delay 仅用于破代数环；PID 三组增益与软启动斜率的**具体取值按 §22.5 整定依据选取**，改工况时据此微调，勿在此硬编。

### 25. 模块固定搭配与功能电路

> **这一块是做什么的**：这是整份笔记的"积木图纸库"。搭 Simulink 模型时，单个模块（电阻、开关管、Gain……）本身没有意义，真正反复使用的是**几个模块按固定方式连起来实现一个功能**的组合——比如"锯齿载波+比较器+增益"合起来才是一个 PWM 调制器。本节把 所有模型里出现过的这类组合提炼成 22 个**固定搭配（模板）**，每个搭配按统一格式写四件事：**整体功能**（这个组合是干什么的）、**模块与角色**（需要哪几个模块、每个模块在里面干什么）、**连线**（怎么接）、**要点/禁忌**（参数怎么取、哪里容易错）。用途有二：① 复现——照抄任一搭配即可重建对应功能电路，不必回头翻模型；② 组装——搭新模型时按文末"全景速查"的顺序把搭配拼起来，就是一个完整的直流微电网仿真。每个搭配内容自成一体，不需要翻阅其它章节。

- **怎么用本节**：22 个搭配 = 22 张"积木图纸"。搭模型按文末「全景速查」顺序（地基→功率级→驱动→控制→安全→观测）把搭配拼起来即可。每个搭配四要素：整体功能 / 模块与角色 / 连线 / 要点禁忌，复现照抄即可，无需回翻模型。
- **命名规律**：`25.1–25.5` 是单一功能电路，`25.6–25.9` 是多源/故障/保护，`25.10–25.20` 是 DAB 与驱动细节，`25.21–25.22` 是进阶控制（前馈复合 / MPPT）。

#### 25.1 Boost 升压电路（PV → 母线，固定占空比）

- **整体功能**：把光伏升到母线——48V 系统 PV ~25V→48V（D≈50%），700V 系统 PV Vmp≈432V→700V（D≈0.383，升压比≈1.6 倍，见 §25.21）。
- **模块与角色**：

 -`Cin_PV`（100µF）：光伏侧输入电容，稳定 PV 电压、吸收开关纹波。

 -`L_Boost`（500µH，R=0.05Ω）：储能电感，决定电流纹波 ΔIL。

 -`MOSFET_Boost`（N沟道 Vth=3V/Ron=0.1Ω； 换成`MOSFET (Ideal, Switching)`Rds=0.02/Vth=6/elec 门极）：主开关，导通储能、关断释放。

 -`Diode_Boost`（Vf=0.8V/Ron=0.01Ω）：续流二极管，阳极接开关节点、阴极接输出（方向错则短路）。

 -`Cout_Bus`（500µF，初值 48V/700V）：母线支撑电容，稳压 + 预充电。

 -`Vpulse_Boost`（50kHz，D=50%）：**固定脉冲源直接驱动** Boost（/7–9 不做 MPPT）； 若做 MPPT 则占空比改由算法给。
- **连线**：PV+ → Cin_PV → (L_Boost 串联) → 开关节点；MOSFET 跨在开关节点与地之间（栅极低端驱动）；Diode 跨在开关节点与 Cout_Bus+ 之间；负载/电池并联在 Cout_Bus 两端。
- **接线铁律**：Diode 阳极接开关节点、阴极接输出；MOSFET 漏极接开关节点、源极接地（低端驱动）；L 串在 PV 与开关节点之间。
- **常见接错**：把 Diode 反接（阴极朝开关节点）→ 输入直接对地短路；占空比信号接错极性 → 输出反调。

#### 25.2 半桥浮地驱动搭配（栅极驱动）

- **整体功能**：把 0/1 控制信号变成能可靠开通上/下管的栅极电压，且上管用浮地驱动。
- **模块与角色**：

 -`Vgs_top`（Controlled Voltage Source）：上管驱动，**负极接 S1_top 源极**（浮地，随开关节点浮动），正极接 S1_top 栅极。

 -`Vgs_bot`（Controlled Voltage Source）：下管驱动，负极接地，正极接 S2_bot 栅极。
- 上游 PWM 调制输出（经 Gain=12）→ 两受控源的控制输入。
- **连线**：PWM 信号 → Gain(×12) →`Vgs_top`/`Vgs_bot`控制端；受控源 +/− 端子按浮地规则接 MOSFET 栅-源。
- **浮地驱动本质**：上管源极电位随半桥中点 0↔母线 跳动，故其驱动源负极必须"跟着源极走"，才能保证 $V_{GS}$ 恒定 12V；下管源极固定接地，驱动源负极接地即可。
- **常见接错**：上管驱动源负极接到地（而非 S1_top 源极）→ 中点抬高后 $V_{GS}$ 不足，上管开不通（半桥只下半桥在工作）。
- **48V/700V 对应**：48V 电池侧半桥、700V DAB 全桥都用同一套浮地/直驱思路（700V 理想开关用 25.16 电气门极直驱）。

#### 25.3 PWM 调制搭配（载波 + 比较 → 门极方波）

- **整体功能**：把"占空比信号（0~1）"变成"50kHz 方波"，驱动半桥上下管。
- **模块与角色**：

 -`Repeating Sequence`（Time:[0 20e-6]，Output:[0 1]）：生成 50kHz 锯齿载波。

 -`Relational Operator`（`<`）：逐点比较`载波 < 占空比`→ 输出布尔（占空比大则点高时间长）。

 -`Data Type Conversion`（double）：布尔 → double，使后续数值模块能处理。

 -`Gain`（12）：0/1 → 0/12V 门极幅值。
- （→ 进 25.2 的`Vgs_top/bot`受控源）
- **连线**：Repeating Sequence → Relational Operator 一端；占空比信号 → Relational Operator 另一端；Relational → Data Type Conversion → Gain(×12) → 受控电压源控制端。
- **PWM 比较器原理**：载波 $c(t)$ 与占空比 $d$（0~1）逐点比较，输出 $g(t)=1$ 当 $c(t)<d$ 否则 0 → 占空比=高电平时间占比。这就是 Simulink 里最朴素的"锯齿波比较式 PWM"（学术上称锯齿采样 / sawtooth carrier PWM）。
- **接线铁律**：载波进 Relational 一端、占空比进另一端，顺序/极性决定高低电平对应关系，错了只是相位反转，不会炸。
- **48V/700V 对应**：电池侧半桥用此搭配；Boost 侧用固定 `Vpulse`（25.1）不调压；700V 系统 DAB 见 25.20。
- **常见接错**：忘接 `Data Type Conversion`（布尔→double），下游 Gain/受控源接收不到数值。

#### 25.4 PI 电压闭环 + 软启动搭配（母线调压）

- **整体功能**：自动调占空比，使母线稳压在 48V。
- **模块与角色**：

 -`Ramp`+`Saturation`（Slope=2400，Sat 48/0）：软启动，Vref 从 0→48V 在 0.02s 升完，消除上电冲击（**加在设定值，不加在 PID 输出**）。

 -`V_Bus`传感器：测母线电压。

 -`Subtract`（`+-`）：算误差`error = Vref − Vbus`（符号错会变正反馈）。

 -`Transport Delay`（1µs）：打破代数环。

 -`PID Controller`（PI 结构：定压取较大积分增益压稳态误差，下垂禁用微分(D)；Limit output [0,1]、Anti-windup 限积分 ±5）：输出占空比。
- → 占空比进 25.3 PWM 调制 → 栅极。
- **连线**：Ramp→Saturation→Subtract(+)；V_Bus→Subtract(−)；Subtract→Transport Delay→PID→(占空比)。
- **PI 闭环公式**：$d(k)=K_p(V_{ref}-V_{bus})+K_i\int(V_{ref}-V_{bus})dt$ 输出限幅 [0,1] 即占空比合法范围；抗饱和用 Clamping/LimitIntegrator。
- **软启动必须加在设定值**：Ramp 让 Vref 0→48V 在 0.02s 升完，PID 始终"追一个慢慢长大的目标"，上电无冲击；加在 PID 输出会直接猛甩占空比。
- **Transport Delay 位置**：插在 Vbus 反馈进 Subtract 之前（破代数环，见 25.15）。
- **48V/700V 对应**：48V 用常规 PI 整定（较大积分增益）；700V 定压见 25.21（前馈复合，增益更小）。
- **限流升级（平均电流控制）**：若单环 PI 在大扰动下电流冲击过大，可在电压外环内再套一级"平均电流内环"（见 §6 二），内环直接控电感电流均值、天然限流，是工业储能双向变换器主流。双环结构下软启动仍加在**电压外环**设定值端（与单环口诀一致）。

#### 25.5 双向半桥电池变换器搭配（电池 ↔ 母线）

- **整体功能**：单级双向升降压，让电池充电（Buck）或放电（Boost）支援母线。
- **模块与角色**：

 -`Cin_Bat`（100µF，初值 48V）：电池侧电容，吸纹波。

 -`L_bidir`（500µH）：双向功率传输核心电感。

 -`S1_top`/`S2_bot`（N沟道 Vth=3V/Ron=0.1Ω）：半桥上下管。

 -`Vgs_top`/`Vgs_bot`（Controlled Voltage Source ×2）：栅极驱动。关键在**浮地**——上管 S1_top 的源极接的是半桥中点（电位随开关在 0~48V 之间跳），所以`Vgs_top`的负极必须接 S1_top 源极（跟着中点浮动）、正极接其栅极，才能保证栅-源电压始终是 12V；下管源极接地，`Vgs_bot`负极直接接地即可。若上管驱动也对地接，中点抬高后栅-源电压不足，上管开不通。

 -`Battery_48V`（48V/50Ah/SOC80%/R1=0.02Ω）。
- **连线**：电池+ → Cin_Bat → L_bidir → 半桥中点 → S1_top 接母线+、S2_bot 接地；两管互补导通实现 Buck/Boost（duty=1 钳位母线、duty<1 放电）。
- **半桥工作模式**：duty=1 时 S1 常通、母线被电池钳位；duty<1 时电池经 L 向母线放电（Boost 模式）；下管占空比大时母线向电池充电（Buck 模式）。单级双向，靠同一对管互补实现。
- **浮地再强调**：S1_top 源极 = 半桥中点（电位在 0~48V 跳），Vgs_top 负极必须接该中点；否则上管开不通。
- **接线铁律**：电池+ → Cin_Bat → L → 中点；S1_top 漏极接母线+、源极接中点；S2_bot 漏极接中点、源极接地。
- **48V/700V 对应**：48V 电池侧用此；700V 系统电池支撑+变换与笔记 §25.10 DAB 隔离双向驱动搭配。

#### 25.6 双源下垂并联搭配（无通信均流）

- **整体功能**：两路源并联带同一母线，靠下垂律自动按比例分担负荷，无需通信线。
- **模块与角色**：
- 源1：走 25.4 的完整 PI 闭环（稳 48V）。
- 源2（开环代数下垂律）：`Gain2`(×5) 把 Vbus 偏差放大成电流指令 →`Saturation3`(±10A) 限幅 → 受控电流源并入母线；`Gain1`(0.1) 设下垂系数。

 -`Subtract1 → Gain2 → Saturation3 → Solver Configuration`模型**既有代数环**（良性，仿真自动模式迭代，勿关）。
- **连线**：Vbus → Subtract1(−)（与 48V 参考相减）→ Gain1/Gain2 → Saturation3 → 源2 电流指令。
- **下垂律公式（开环源2）**：$I_2=\frac{V_{bus}-V_{ref}}{R_{drop}},\quad R_{drop}=1/Gain1=10\Omega$ 母线跌得越多，源2 出力越大，自动均流；$Gain2=5$ 把偏差放大成电流指令，Saturation3(±10A) 限幅。
- **代数环说明**：`Subtract1→Gain2→Saturation3→Solver Configuration` 是 Simscape 良性环，物理网络求解器自带迭代，勿破（见 25.15）。
- **48V/700V 对应**： 48V 双源并联；700V 系统用 DAB 代替下垂做功率分配（25.10）。

#### 25.7 数据记录搭配（波形导出）

- **整体功能**：把关键波形存成 Timeseries 供离线分析画图。
- **模块与角色**：
- 传感器（`V_PV`/`I_L_Boost`/`V_Bus`/`I_Battery`）：物理测量。

 -`PS-Simulink Converter`物理 → 信号（若传感器输出为物理量）。

 -`To File`（SaveFormat=Timeseries）×4：存 Vpv/ILboost/Vbus/Ibat 到 .mat。

 -`Scope`实时看波形。
- **连线**：被测点 → 传感器 →（PS-Simulink Converter）→ To File / Scope。
- **接线口诀**：**电流串、电压并**——Current Sensor 串联进被测支路，Voltage Sensor 并联在被测两端；接反是 经典坑（电压全零/电流恒零）。
- **To File 用法**：`timeseries` 对象存 .mat，事后 `plot(...)` 等分析；建议采样步 ≤1µs 以捕捉开关细节，但避开 50kHz 整数分频。
- **常见接错**：传感器反接（电压全零/电流恒零）；忘记 PS-Simulink Converter 直接把物理量接 To File 编译不过。

#### 25.8 极间短路故障注入搭配

- **整体功能**：在指定时刻把正负母线经小电阻短接，模拟直流故障。
- **模块与角色**：

 -`Step`/`Manual Switch`定时或手动触发故障。

 -`Resistor``Rf`（0.1Ω）：低阻故障通路（电流主由电压源经此提供，持续上升无自熄）。
- 接在正负母线之间（经开关投切）。
- **连线**：母线+ → Switch → Rf → 母线−（地）；Step 控制 Switch 通断。
- **故障物理**：母线+ → Switch → Rf(0.1Ω) → 母线−，闭合后电压源经极小电阻持续灌电流，无自熄，必须靠保护（25.9）开断。
- **Rf 取值**：0.1Ω 模拟近金属性短路；阻值越大故障电流越小、越接近"过载"而非"短路"。
- **48V/700V 对应**：两电压等级都可用同一注入结构，700V 故障电流能量更大，保护动作须更快。

#### 25.9 过流 + 欠压保护逻辑搭配

- **整体功能**：检测故障 → 自锁 → 开断故障回路，把灾难性跌落变成"短暂扰动后恢复"。
- **模块与角色**：

 -`Relational Operator`（`>`）：检测`|Ibat|>30A`（过流，先取绝对值）。

 -`Relational Operator`（`<`）：检测`Vbus<33.6V`（欠压）。

 -`OR`（逻辑或）：任一个成立 → 故障。
- **延时确认窗**（20~50µs 延时 + AND）：故障信号须持续超过确认窗才放行，滤掉开关纹波/瞬时毛刺，防误动；取值太长则开断过冲变大（踩坑「延时取值与开断过冲」）。
- **投运门**（Step 10ms + AND）：仿真启动后 10ms 内封锁 S 端——启动暂态（软启动未完成、电压未建立）本身就满足"欠压"，没有投运门会**启动瞬间误锁存**（踩坑「启动误锁存」）。

 -`S-R Flip-Flop`（S=故障，R=0）：**自锁**，故障后置位永久保持、不抖合。

 -`Breaker``Brk`（门极约定 G>0.5V=开断）：执行机构，**栅极接锁存器 Q（非 Qbar）**，否则常态开路。
- **连线**：Ibat→取绝对值→`>`(30A)→OR 一端；Vbus→`<`(33.6V)→OR 另一端；OR→延时确认窗→AND（另一端接 Step 投运门）→S-R 的 S 端（R=0 接地）；Q→Brk 栅极。
- **验证顺序（定式）**：A 敏感性（仅短路→必须跳）→ B 选择性（仅光照/负载扰动→不许误动）→ C 鲁棒性（扰动+短路同时→仍抢占跳闸），三关全过才算保护逻辑合格。
- **保护判据阈值**：过流 `|Ibat|>30A`（先取绝对值再比）、欠压 `Vbus<33.6V`（≈48V×70%）。阈值留裕度防误动/拒动。

#### 25.10 DAB 隔离双向驱动搭配

- **整体功能**：用单移相角 φ 同时控制电池↔700V 母线的功率大小与方向（原边超前=充电，副边超前=放电）。
- **模块与角色**：

 -`PWM Generator (DC-DC)`×2（原边、副边各一路）：设`Duty=50%``Phase shift=φ``Dead time≥300ns`；错相即移相角。
- 原/副边全桥各 4×`MOSFET (Ideal, Switching)`由两路 PWM 驱动。
- 高频变压器（匝比 原:副 700:240 ≈ 2.92:1，副边 240V，含漏感 Lk）：原副边隔离+变压，漏感即功率传输媒介。

 -`Electrical Reference`×2：原副边**各自独立接地**，隔离才有效。
- **连线**：原边 PWM → 原边全桥 4 管；副边 PWM（错相 φ）→ 副边全桥 4 管；两桥中点各接变压器一侧；变压器两侧各挂独立 Reference。
- **DAB 单移相功率公式（权威）**：原副边桥错相 $\phi$（rad）时，传输功率 $P=\frac{n\,V_1\,V_2}{2\pi f_{sw} L_k}\,\phi\left(1-\frac{|\phi|}{\pi}\right),\quad \phi\in[-\pi,\pi]$ 峰值在 $\phi=\pm\pi/2$：$P_{max}=\dfrac{nV_1V_2}{8 f_{sw} L_k}$。原边超前→功率流向副边（充电），反之为放电。
- **死区物理**：Dead time ≥300ns 让桥臂上下管关断间隙 > 开关关断时间，防直通且为 ZVS 前提。
- **独立接地**：原副边各挂独立 Reference，隔离才有效（否则两边地连一起、隔离失效）。
- **48V/700V 对应**：原边 700V、副边 240V、匝比（原:副）700:240 ≈ 2.92:1；漏感 Lk 即功率传输媒介，设理想无漏感则传不过去。

#### 25.11 信号–物理域接口搭配（Converter 对）

- **整体功能**：Simulink 信号域与 Simscape 物理域之间的双向桥。
- **模块与角色**：

 -`Simulink-PS Converter`（信号→物理）：上游控制信号 → 物理值（如进 25.2 受控电压源、或 直接驱动开关门极）；`InputFilterTimeConstant`0.001→ 改 0 去延迟。

 -`PS-Simulink Converter`（物理→信号）：传感器物理读数 → Simulink 信号（供 25.4/25.7）。
- **连线**：Simulink 信号 → Simulink-PS Converter → 物理网络输入端；物理量 → PS-Simulink Converter → Simulink 信号端。
- **Converter 对作用**：Simulink-PS（信号→物理）与 PS-Simulink（物理→信号）是跨域桥，缺一不可；任何 Simscape 网络都要有这对桥才能与控制/记录相连。
- **滤波时间常数**：门极驱动路径 `InputFilterTimeConstant=0`（去延迟）；普通信号测量可用小滤波平滑噪声。
- **常见接错**：把 PWM 信号直接怼进 CVS 的 PS 控制口（域不匹配）；应接电气 +/− 端子（见 25.16）。

#### 25.12 物理网络地基搭配（每个模型的"第0步"，全部通用）

- **整体功能**：让 Simscape 物理网络"合法"——有零电位基准、有求解器，缺一个模型直接编译不过。**任何新模型第一步先摆这套**。
- **模块与角色**：

 -`Electrical Reference`（电气参考/接地）：定义 0V 基准，全网**有且仅有一个**功率地（唯一接地规则）；例外： 隔离变压器两侧**各自独立**一个 Reference（共用则隔离失效、求解奇异）。

 -`Solver Configuration`物理网络求解器入口，**R2025b 强制其物理端口也要接地**，否则初始化报错。

 -`Configuration Parameters`（全局）：求解器`ode23t`（开环用`daessc`）、Variable-step、`MaxStep=1e-6`（≈Ts/20）、StopTime 按需（闭环看扰动 0.09s / 开环看建压 1.0s）。（**本笔记求解器设置的唯一权威来源，他处一律引用本节，不再复述具体数值**）
- **连线**：所有功率回路的"地"端 → 同一个 Electrical Reference；Solver Configuration 的物理端口 → 也挂到该地（或网络任意节点）。
- **口诀**："唯一接地、Converter 不能空、Solver 要挂网"。（踩坑：「唯一接地规则缺失」「Solver Configuration 物理端口未接地」）
- **唯一接地规则**：全网功率地有且仅一个；隔离变压器两侧各自独立 Reference 是例外（共用则隔离失效、求解奇异）。
- **Solver Configuration 物理端口**：R2025b 起其物理端口也要接地，否则初始化报错——这是"Solver 要挂网"口诀来源。

#### 25.13 扰动注入搭配（光照突变 + 负载投切，/12 通用测试台）

- **整体功能**：给闭环系统施加标准扰动（光照 1000→500→1000 W/m²、负载 20→10→20Ω），检验母线电压能否被拉回——所有"控制策略验证"的标配测试信号。
- **模块与角色**：
- 光照通道：`Step`/`Repeating Sequence`（阶跃时刻与幅值）→（可乘`Ramp1`光照软启动乘子，slope=50）→`Simulink-PS Converter`→ PV_Panel 的`Ir`输入。**为什么只改 Ir 就够**：Solar Cell 的光生电流与辐照度近似线性（Ir 减半 → 短路电流 Isc 减半），开路电压只随光照对数变化、几乎不动，所以改 Ir 等于把整条 I–V 曲线上下平移，其余 PV 参数（串联数、Voc、Rs 等）一律不用动。
- 负载通道：`Step`（t=0.03/0.06s）→ 控制`Switch`/`Breaker`把第二只电阻（10Ω）并入/切出母线；或直接切换 R_Load 挡位。
- 配套记录：25.7 数据记录搭配（4 路 To File，采样 1µs 避开开关频率）。
- **连线**：Step → (Simulink-PS Converter) → Ir 端口；Step → Switch 门极 → 第二电阻支路跨母线。
- **要点**：扰动首个阶跃须晚于软启动完成时刻（软启动 0.02s 到顶 → 扰动最早 0.03s）；采样率必须避开 50kHz 及其整数分频，否则同频锁相出虚假偏差（采样率陷阱）。
- **扰动信号设计**：光照 1000→500→1000 W/m²、负载 20→10→20Ω 是标准测试；首个阶跃晚于软启动完成（0.02s）即最早 0.03s。
- **48V/700V 对应**：两电压级通用同一扰动台；700V 系统扰动幅度更大、响应更慢（建压 1.0s）。

#### 25.14 多斜坡软启动对齐搭配（定式）

- **整体功能**：系统里有多个"需要缓升"的量（主参考 V0、源2 参考 V02、光照乘子）时，让它们**同一时刻到顶**，避免启动期互相打架产生冲击/尖峰。
- **模块与角色**：

 -`Ramp138`（V0，slope=2400）+`Saturation`(0~48)：源1 参考软启动。

 -`Ramp167`（V02，slope=2400）+`Saturation`源2 参考软启动。

 -`Ramp1/140`（光照乘子，slope=50）+`Saturation`(0~1)：光照从 0 缓升到 1000 W/m²。
- **连线**：各 Ramp → 各自 Saturation → 各自下游（Subtract 参考端 / 乘法器 / Ir 通道）。
- **要点**：三者都在 ≈0.02s 到顶（到顶时间=目标值÷斜率：48/2400=0.02s，1/50=0.02s，刻意取得一致）；**软启动一律加在设定值，不加在 PID 输出**（Rate Limiter 方案会放大冲击）。源2 曾因未对齐产生 t=0 尖峰，靠对齐 +`Saturation3`(±10A) 双保险修复。
- **48V/700V 对应**：48V 用 slope=2400（到顶 0.02s）；700V 用 slope=2000（到顶 0.35s）。

#### 25.15 破代数环搭配（Transport Delay 定式，/14 反复用）

- **整体功能**：控制环里全是直接馈通模块（Subtract/Gain/PID/Saturation）时会成代数环，插一个极小延迟破环，仿真才快才稳。
- **模块与角色**：

 -`Transport Delay`（1e-6 s = 开关周期的 1/20）：唯一模块，插在**反馈量进入比较器/控制器之前**。
- **连线**：反馈信号（如 Vbus 测量）→ Transport Delay → Subtract/PID 输入。
- **怎么判断有没有代数环**：环路里每个模块的"当前输出"都直接依赖"当前输入"（无积分器、无延迟、无记忆），信号绕一圈回到自己 → 就是代数环，仿真器每步要迭代求解，轻则变慢、重则报"algebraic loop"错。Subtract、Gain、Saturation、P 控制器全是直接馈通；Integrator、Unit Delay、Transport Delay 则能断环。
- **要点**：延时取控制周期 1~5 倍或开关周期 1/10 以内，对动态影响可忽略；**Simscape 物理侧的代数环（如`Subtract1→Gain2→Saturation3→Solver Configuration`）是良性的**——物理网络求解器本来就是联立方程迭代，这类环它自己能收敛，勿强行插延迟破坏、勿设`feature('ModeIterationsInAlgLoops',0)`（实测：关掉模式迭代反而导致仿真失败）。只有纯 Simulink 信号域的环才需要 Transport Delay 破。
- **Transport Delay 原理**：延迟 >0 时无直接馈通（不依赖当前输入算输出），从而破纯 Simulink 信号环；1µs 取开关周期 1/20，对动态可忽略（MathWorks 文档确认 Transport Delay 无 direct feedthrough）。
- **注意**：连续系统破环优先用极小一阶传递函数/Transport Delay，勿用 Memory 把连续信号离散化（MathWorks 官方建议）。

#### 25.16 电气门极直驱搭配（理想开关版栅极驱动）

- **整体功能**：驱动`MOSFET (Ideal, Switching)`（电气门极版）—— 起 700V 模型用它替代 N-Channel MOSFET，驱动接法与 25.2 的 PS 门极不同，单独成搭配。
- **模块与角色**：
- PWM/驱动信号（Simulink 域，0/12V）。

 -`Simulink-PS Converter`（**InputFilterTimeConstant=0**，默认 0.001s 会给门极加一阶延迟使波形发钝）。

 -`Controlled Voltage Source`（CVS）：把物理信号变成栅极电压源。

 -`MOSFET (Ideal, Switching)`（`port_option=ee.enum.controlport.elec`，Rds=0.02Ω、Vth=6V）：门极是**电气端子对**。
- **连线**：PWM 信号 → Simulink-PS Converter → CVS 控制端；**CVS 电气 (+) → MOSFET G、CVS 电气 (−) → MOSFET S**（构成驱动回路）。
- **三大禁忌**（对应连踩 3 报错）：①`replace_block`换管后门极不会自动重连，必须手动接；② 枚举必须写全名`'ee.enum.controlport.elec'`（裸`'elec'`报"函数或变量无法识别"）；③ 门极不能接 CVS 的 PS 控制输入端（域不匹配），必须接电气 +/− 端子。
- **与 25.2 区别**：25.2 是 PS 门极（受控电压源驱动 N-Channel MOSFET 的 ps 口）；实际是电气门极直驱理想开关，驱动接法不同。

#### 25.17 700V 开环建压搭配（闭环前必经）

- **整体功能**：在加任何 PI 闭环之前，先用纯开环（固定前馈占空比 + 固定移相）验证 700V 母线能建起来、DAB 副边 240V 电池能充上——这是 §25.21（前馈+PI 复合）和 §25.10/§25.20（DAB 驱动）的“地基验证”步骤。开环只验证“拓扑/接线/增益量级对不对”，不验证稳压。
- **模块与角色**：
- `Fcn`（D=1−Vin/Vout，Vin≈432V、Vout=700V→D≈0.383）：Boost 开环固定前馈占空比，直接进 §25.3/§25.21 的 PWM 比较器。
- `Transport Delay`(1.67µs)：DAB 固定移相 φ≈30°（见 §23③），进 §25.20 手搓 DAB 驱动。
- 700V 功率级（§25.1 / §22.2 高电压功率级 L/C/C_block）、DAB 全桥（§25.10 / §22.8）。
- **连线**：同 §25.1 + §25.20，只是 PI 增益取近零（仅建压，见 §22.5 HV-6）。
- **要点**：开环验证三件事——①Boost 前馈 D≈0.383 能否把母线拉到 700V；②DAB 固定 φ≈30° 能否在副边建立 240V；③各驱动/接地/死区无误。都过了再上 §25.21 的前馈+PI 闭环。
- **对应**：原理见 §23①（Boost 前馈 D）/§23③（DAB φ 移相）；元器件见 §22.2 高电压功率级 / §22.5 700V Boost PI / §22.8 DAB 全桥；功能电路见 §25.1/§25.10/§25.20。

#### 25.18 Buck 降压负载供电搭配（700V 母线 → 400V 负载，实际执行）

- **整体功能**：负载不能直挂 700V，经 Buck 降到用电电压（如 48V）供电，构成"Boost 升压 + DAB 储能 + Buck 供电"三端 700V 架构的负载端。
- **模块与角色**：
- 开关管（上位，串联在母线正极与电感之间）+ 续流二极管（下位，阴极接开关-电感节点、阳极接地）——与 Boost 位置对偶。

 -`Inductor`+ 输出`Capacitor`LC 滤波，输出近直流。
- PWM 驱动：实际执行版 D=Vout/Vin=400/700≈0.571（负载 Buck 输出 400V）；早期计划曾按 48V 负载则 D=48/700≈0.069，**极小占空比**受最小导通时间限制（对比：700V Boost 实际 D≈0.383，占空比量级接近、均远离最小导通时间限制）。

 -`R_Load`用电负载。
- **连线**：700V 母线+ → 开关管 → 电感 → 输出电容/负载；二极管从"开关-电感节点"接地续流。
- **要点**：Buck 输出电压 ≈ D×Vin（CCM）；占空比过小时注意开关最小导通时间与电感电流断续（DCM）问题。
- **拓扑对偶**：Buck 开关管在上位、二极管下位续流，与 Boost 位置对偶（Boost 二极管上位、开关管下位）。
- **DCM 注意**：占空比过小易进断续模式（DCM），输出公式变为 $V_{out}=f(D,L,负载)$，需校核电感电流是否连续。
- **48V/700V 对应**：700V 母线经 Buck 降到 48V 供负载；与 Boost/DAB 构成三端 700V 架构。

#### 25.19 互补驱动信号生成搭配（半桥上下管，实际实现）

> 互补信号**不是** NOT 逻辑门，而是"电压幅值相减"。

- **整体功能**：由一路 PWM 门极信号（0/12V）生成下管的互补信号，保证任意时刻上下管指令相反（上开下关、上关下开），这是 25.5 双向半桥的驱动前提。
- **模块与角色**：
- 上管通道：25.3 PWM 链输出`Gain`(×12) → 直接进`PS_Converter_top`（上管驱动，0/12V）。

 -`Constant1`（=12）：门极满幅基准。

 -`Subtract`（`+-`）：算`12 − 上管信号`→ 上管 12V 时下管 0V、上管 0V 时下管 12V，**在电压幅值层直接取补**。

 -`PS_Converter_bot`→`Vgs_bot`下管驱动通道。
- **连线**：Gain(×12) 输出**分两路**——① → PS_Converter_top → Vgs_top；② → Subtract 的`−`端；Constant1(12) → Subtract 的`+`端；Subtract → PS_Converter_bot → Vgs_bot。
- **要点**：① 此法零个逻辑模块，纯算术取补，信号域全程 double；② **本搭配无死区**——两管切换发生在同一瞬间，而真实开关管关断需要时间，上管还没完全关、下管就开了，会造成母线经上下管直通对地短路（shoot-through）；理想开关仿真里切换是瞬时的所以侥幸不炸，真实电路/700V 系统必须加死区（在每次切换时插入一小段"两管都关"的间隙，做法如 25.20 的 ±双阈值法：同一载波过两个错开的阈值，阈值差÷载波斜率=死区时长）；③ 若上管信号取自布尔（Relational 输出），先过 Data Type Conversion 再乘 12，Subtract 才能正确运算。
- **无死区风险（重点）**：两管切换同一瞬间，真实开关管关断需时间 → 上管未全关下管已开 → 直通短路（shoot-through）。理想开关瞬时切换侥幸不炸；真实/700V 必须加死区（见 25.20 ±双阈值法）。
- **常见接错**：上管信号取自布尔未过 Data Type Conversion 就乘 12，Subtract 算错。

#### 25.20 DAB 载波移相 + 死区生成搭配（实际实现版，区别于 25.10 理论版）

> `PWM Generator (DC-DC)`（见 §25.10）是理论推荐版；实际手搓版用"一条载波 + 两个阈值 + Transport Delay"实现，两版都要会。

- **整体功能**：不用 PWM Generator 专用块，用基础模块实现 DAB 单移相驱动：8 管两桥、对角同开、副桥错相 φ、且**自带死区**。
- **模块与角色**：

 -`Repeating Sequence1`DAB 高频**三角载波**（关于 0 对称）。

 -`Constant1`(−0.003) /`Constant2`(+0.003) +`Relational Operator1/2`（`<=`）：**同一载波过两个微错开的阈值** → 得两路近似 50% 的互补方波，阈值差 0.006 对应的载波爬升时间就是**死区窗口**（两管都关的间隙），一举两得。

 -`Data Type Conversion1/2`+`Transport Delay1/2`（1.67µs）：把原边两路方波各延时 1.67µs 作为副边桥信号——**延时即移相角 φ**（φ = 2π·f_s·t_delay）。

 -`Gain1~8`（×12）：8 路 0/1 → 0/12V。

 -`Simulink-PS Converter`×8（**门信号 `InputFilterTimeConstant=1e-9`**：须极小滤波——既避免给门极加可见一阶延迟，又避免设 0 在某些版本下关闭滤波而惰性（日后切滤波模式即失效）； 700V 验证值 1e-9，非 0、非 0.001）→`Controlled Voltage Source`×8 → S1~S8 门极。接法是电气直驱：CVS 电气 (+) 接 MOSFET 栅极 G、电气 (−) 接源极 S，构成独立驱动回路（理想开关的门极是电气端子对，不能把 PWM 信号直接怼到 PS 控制口，同 25.16）。
- **连线**：载波 → RelOp1（vs −0.003）→ ① 原边对角组 A（Gain6/Gain8 → S 对角两管）② → DTC → Transport Delay(1.67µs) → 副边对角组 A（Gain1/Gain4）；载波 → RelOp2（vs +0.003）→ ① 原边对角组 B（Gain5/Gain7）② → DTC → Delay → 副边对角组 B（Gain2/Gain3）。
- **要点**：① 移相量改`Transport Delay`的 DelayTime 即可（闭环可让 PI 输出直接驱动该延时的替代结构或改调 Boost，选了后者）；② 死区大小 = 阈值差 ÷ 载波斜率，改 ±0.003 就是调死区；③ 副边信号是"原边的纯延时复制"，天然保证两桥波形一致性——这正是单移相（SPS）的定义。
- **移相 = 延时**：原边方波各延时 `Transport Delay(1.67µs)` 作副边信号，延时即移相角 $\phi=2\pi f_s t_{delay}$（1.67µs×50kHz×360°≈30°）。
- **为什么两版都要会**：§25.10 的 `PWM Generator (DC-DC)` 是理论推荐版，实际手搓版自带死区更贴合真实驱动，两版实现一致、都要掌握。
- **48V/700V 对应**：原边 700V、副边 240V、匝比（原:副）= 700:240 ≈ 2.92:1（若按 V_sec/V_pri 记则 n=240/700，但 **DAB 功率公式的 $n$ 须取 $V_1/V_2\approx2.92$**，勿误用其倒数）；副边是"原边纯延时复制"，保证两桥波形一致——这正是单移相 SPS 定义。

#### 25.21 占空比前馈 + PI 微调复合搭配（700V Boost 调压核心）

> **pu 标幺化整定推导（为什么高电压等级的 PI 增益要缩小）**：PI 的输入是电压误差（单位 V）、输出是占空比（0~1 无量纲）。同样"母线跌 1%"，低压系统误差是小数值、高压系统误差是大数值——差了十余倍，但需要的占空比修正量差不多。若把误差先除以基准电压变成标幺值 e_pu=e/V_base，则**无量纲增益 K_p(pu) 在不同电压等级下几乎相同**；不做除法时等效为把实际增益按 1/V_base 缩放：K_p(实际) = K_p(pu)/V_base。即电压等级升高时 K_p 按 1/V_base 缩小，再叠加"前馈已扛走大部分占空比、PI 只修零头"，最终实际增益比低压系统小 2~3 个数量级。这就是跨电压等级复用 PI 参数的标幺化方法。

- **整体功能**：Boost 升压比约 1.6 倍（PV Vmp≈432V → 700V）时，纯 PI 从零猛拉占空比既慢又易过冲；改用"**稳态公式算基准占空比（前馈）+ PI 只补小误差（反馈）**"，启动快、PI 负担小、增益可以取得很小。
- **模块与角色**：

 -`Ramp`(slope=2000) +`Saturation`(0~700)：700V 参考软启动。

 -`Saturation1`(45~700)：前馈输入下限保护——防止启动初期 u→0 使基准占空比公式发散。

 -`Fcn`（按 Boost 稳态公式 $D=1-V_{in}/V_{out}$，PV 工作电压 $V_{in}≈432V$（Vmp）、目标 $V_{out}=700V$，故基准占空比≈0.383；u=目标/实测电压）。

 -`PID Controller`（PI 按标幺值保守整定、增益远小于定压环；Limit output [−0.3，+0.05] + Clamping 抗饱和）：只输出**小修正量**。

 - `Duty_Sum`：`D = D_前馈 + ΔD_PI`。

 -`Duty_Sat`（[0.05，0.968]）：总占空比限幅，0.968 对应最大升压比上限。
- （增强）`FF_Delay`（Transport Delay 1µs）：前馈输入从"参考电压"改接"**实测母线电压**"，扰动时前馈自动跟随，抗扰更快。
- **连线**：Ramp→Saturation→Subtract(+)，实测 Vbus→Subtract(−)→Transport Delay→PID→Duty_Sum(in1)；Saturation1→Fcn→Duty_Sum(in2)；Duty_Sum→Duty_Sat→25.3 PWM 比较器。
- **要点**：① 前馈承担 96% 的占空比、PI 只管零点几个百分点，所以 PI 增益比 48V 系统小 2~3 个数量级——这正是 pu 标幺化整定的直观体现（K_p(pu) 不变，实际增益 ∝ 1/V_base）；② PI 输出限幅必须**不对称**（上限小、下限大），因为前馈已接近上限，正向余量本来就小；③ 该结构下软启动仍加在设定值端，与 25.4 口诀一致。
- **48V/700V 对应**：48V 定压用常规 PI 整定；700V 用前馈复合 PI（增益小 2~3 数量级）。

#### 25.22 MPPT P\&O 算法搭配（已实装）

- **整体功能**：替代 25.1 的固定 D=50% 脉冲源，动态调占空比把 PV 推到最大功率点（MPP）。**原理**：光伏 I–V 曲线上存在唯一一点使 P=V×I 最大（dP/dV=0），且该点随光照/温度移动，固定占空比不可能一直踩在这点上；P\&O（扰动观察法）的判据是——每拍给占空比一个小扰动 ΔD，比较本拍功率 P_k 与上拍 P\_{k-1}：**功率变大 → 扰动方向对了，继续同方向；功率变小 → 方向错了，反向**，如此在 MPP 附近来回试探。稳态必然在 MPP 两侧小幅抖动，这是 P\&O 的固有代价。
- **模块与角色（模板）**：

 -`V_PV`/`I_PV`传感器 +`PS-Simulink Converter`采样 PV 电压电流。

 -`Zero-Order Hold`（MPPT 周期，如 1ms，远慢于开关周期）：算法节拍。

 -`MATLAB Function`（或 Memory + 手搭逻辑）：P\&O 核心——`P_k>P_{k-1}`保持扰动方向，否则反向，`D_{k+1}=D_k±ΔD`。

 -`Saturation`（如 0.1~0.9）：占空比安全限幅。
- → 输出接 25.3 PWM 调制替换`Vpulse_Boost`。
- **要点**：① MPPT 与母线调压不能同时由同一个 Boost 承担——一个变换器只有一个占空比自由度，只能控制一个量：让它追最大功率点，就没法同时稳母线电压；所以架构里 Boost 若做 MPPT，母线调压必须交给电池侧变换器/DAB（这正是通过扰动实验得出的关键认知："谁调压、谁定功率"必须分工）；② 步长 ΔD 大→跟踪快但稳态抖动大，小→稳但慢，变步长可兼顾；③ 接入后 25.1 的`Vpulse_Boost`整条作废，驱动改走 25.3 的载波比较链。

> **P&O 接入效果**：用扰动观察法（`V_pv`×`I_pv`→Product→低通→dP/dV 方向逻辑→`Vref += 3·dir`→Sat[300,560]）替换固定 `Vref_pv`，Boost 自动追最大功率点；瞬态过冲为正常扰动摆动（非缺陷），稳态锁定 MPP 附近。

> **搭配全景速查**：一个完整模型 = 25.12 地基 → 功率级（25.1 Boost / 25.5 双向半桥 / 25.10·25.20 DAB / 25.18 Buck）→ 驱动（25.2 浮地 CVS / 25.16 电气门极直驱 + 25.3 PWM 调制 + 25.19 互补生成）→ 控制（25.4 PI 闭环 / 25.21 前馈+PI 复合 + 25.14 软启动对齐 + 25.15 破环，多源加 25.6 下垂，进阶 25.22 MPPT）→ 安全（25.8 故障注入 + 25.9 保护逻辑）→ 观测（25.7 记录 + 25.13 扰动注入 + 25.11 域接口）。

---

#### 25.23 三节对照索引（公式速查 §23 ↔ 元器件库 §22 ↔ 功能电路 §25）

> 把“前面公式速查表（§23）+ 元器件库（§22）+ 固定搭配与功能电路（§25）”三节打通：任一功能电路在 §25，都能在此表查到它依赖的原理公式（§23）与所用元器件（§22，按元器件类型与名称定位，不依赖原编号）；反之上两节缺对应电路的，本节也补齐了 §25.17（700V 开环建压，此前被引用但正文缺失）。

| §25 功能电路 | §23 原理公式 | §22 元器件库 |
| --- | --- | --- |
| 25.1 Boost 升压 | ① Boost(CCM) | 22.1 MOSFET/Diode、22.2 电感/电容、22.3 PV |
| 25.2 半桥浮地驱动 | （浮地驱动） | 22.1 Controlled Voltage Source |
| 25.3 PWM 调制 | （PWM 载波） | 22.5 Repeating Sequence/Relational/Data Type Conversion |
| 25.4 PI 闭环+软启动 | ④ PID/PI、⑦ 软启动 | 22.5 PID/Ramp+Sat/Subtract |
| 25.5 双向半桥电池 | ② 双向 Buck-Boost | 22.1 MOSFET、22.2 电感、22.3 Battery |
| 25.6 双源下垂 | ⑤ 下垂律 | 22.5 Gain/Saturation |
| 25.7 数据记录 | — | 22.5 To File/Scope |
| 25.8 极间短路注入 | ⑧ 故障保护 | 22.4 Rf/Switch、22.7 Breaker |
| 25.9 过流+欠压保护 | ⑧ 故障保护 | 22.7 Abs/阈值/OR/延时/锁存/Breaker |
| 25.10 DAB 隔离双向 | ③ DAB(SPS) | 22.8 高频变压器/PWM Generator/门极链 |
| 25.11 域接口 | — | 22.6 Simulink-PS/PS-Simulink |
| 25.12 物理地基 | — | 22.2 Electrical Reference、22.6/22.8 Solver Configuration |
| 25.13 扰动注入 | ⑥ 电池/辐照 | 22.3 Ir、22.4 R_Load |
| 25.14 多斜坡对齐 | ⑦ 软启动 | 22.5 Ramp+Sat |
| 25.15 破代数环 | — | 22.5 Transport Delay |
| 25.16 电气门极直驱 | — | 22.1 门极链 |
| 25.17 700V 开环建压 | ① 前馈 D、③ φ 移相 | 22.2 高电压功率级、22.5 700V Boost PI、22.8 DAB 全桥 |
| 25.18 Buck 降压供电 | ① Buck | 22.2 电容、22.5 Buck PI |
| 25.19 互补驱动 | — | 22.1 MOSFET/Controlled Voltage Source |
| 25.20 DAB 移相死区 | ③ 移相=t_delay、死区≥300ns | 22.8 载波/双阈值/Transport Delay/门极链 |
| 25.21 前馈+PI 复合 | ① 前馈 D、④ PID | 22.5 Fcn/PID |
| 25.22 MPPT P&O | ⑦ MPPT | 22.3 PV（已实装） |
| 25.24 IMD 建模搭配 | （绝缘监测原理 §22.9） | 22.9 对地绝缘电阻/注入源/RMS/比较器 |

#### 25.24 IMD 建模搭配（绝缘监测仿真实现）

> 把 IMD 建模实操路线固化成固定搭配；对应元器件见 §22.9，原理见 §22.9 绝缘监测原理。

- **拓扑位置**：在母线正/负极对地各挂一个大电阻 `Riso`（初始设很大如 1MΩ 模拟健康，改成 50kΩ 模拟劣化），即母线对地绝缘的并联等效模型。
- **注入测试信号**：用一个受控交流源经高阻耦合（电容/电阻）注入母线对地；注入频率取避开系统噪声的低频（典型 0.1~几 Hz / 几 kHz 视型号）。
- **测量**：用 `PS-Simulink Converter` + `RMS`/`Mean` 测返回信号幅相，按注入法原理反推 `Riso`。
- **报警/联动**：比较器判 `Riso < 预警值`→预警灯/本地报警；`< 跳闸值`或同时漏电→联动上层（断 DAB/光伏接触器、触发 §25.9 系统级急停、上报 MQTT/Grafana）。
- **与非注入式的区别**：平衡/不平衡电桥法靠母线直流工作点检测，对称老化会“失明”；注入法隔直测、可检对称+不对称，工程落地一律此法（§22.9）。
- **动作逻辑与上层联动（文本图）**：

```
[IMD 持续测 Riso] ──(Riso < 预警值)──> 预警灯/本地报警
 │
 └─(Riso < 跳闸值 或 同时出现漏电)──> 联动上层控制器
 │
 ┌─────────────────────────┘
 ▼
 ① 断开 DAB / 光伏并网接触器（防故障扩大）
 ② 触发 §25.9 保护逻辑里的“系统级急停”
 ③ 上报 MQTT/Grafana（见课题第 4 层边缘原型）
```
- **与已有保护的关系**：§25.8 极间短路是“**已短路、靠 Brk 开断**”的硬保护；IMD 是“**短路发生前、靠监测预警**”的软保护。两者层级不同：IMD 抢在短路之前，Brk 抢在短路之后。
---
