# 研究生科研软件使用说明

> 适用对象：福建理工大学电气工程研究生（研究方向 3：多时间尺度"能-碳"指标智能调控）
> 整理时间：2026-08-09
> 本文档讲"怎么用"你已装好的软件（Obsidian / Anaconda / VS Code / MATLAB），不是讲怎么装。安装复盘见文末第八节。

---

## 〇、先认清楚：你装了哪几样，各自在科研里干啥

| 软件 | 你在科研里用它做什么 |
|---|---|
| **Obsidian** | 管学习笔记 / 知识库：双向链接 + 关系图谱，把计划、手册、论文精读串成网络（你之前问过 Typora，结论见第一节） |
| **Anaconda / conda** | 管 Python 版本和依赖包；课题代码全跑在 `PV_Storage_Flex_Control` 这个隔离环境里 |
| **VS Code** | 写 Python / Markdown 代码和笔记；接 Git 做版本管理 |
| └ GitLens | 在编辑器里看每一行代码"谁、什么时候、为什么改的" |
| └ Markdown Preview Enhanced | 实时预览 `.md` 笔记（公式、流程图都能显示） |
| **Jupyter Notebook** | 边写边跑、做数据探索和画图（已装在环境里） |
| **MATLAB / Simulink** | 功率电子、控制环路、系统级暂态仿真（你有光储直流微电网笔记） |

> 关于 **Typora**：你之前问过"Typora 能不能替代 Obsidian"。结论——**两者不冲突，分工不同**：Obsidian 重"知识网络"（链接+图谱），适合长期积累研究方向3的知识体系；Typora 是所见即所得的单文档写作，界面干净、导出 PDF/Word 好看，但没有双向链接和图谱，不适合做互相引用的笔记库。写单篇长文/汇报可以用 Typora，做科研笔记体系还是 Obsidian 更合适。

---

## 一、Obsidian 怎么用（笔记与知识库）

### 1. 它干嘛用
本地优先的 Markdown 笔记软件，核心是**双向链接**和**关系图谱**。你的学习计划、Python 手册、论文精读、实验记录都能变成互相链接的知识网络，而不是一堆散文件。适合做科研"第二大脑"。

### 2. 把现有笔记直接变成库（Vault）
你已经有 `D:\python project\` 下一堆 `.md`（读研学习方向计划、Python模块速查手册、软件使用说明、英语学习路线）。
- 打开 Obsidian → **"打开本地仓库"（Open folder as vault）** → 选 `D:\python project\`。
- 这样所有 `.md` 立即出现在左侧文件栏，不用复制搬家。

### 3. 基本操作
- **新建笔记**：左侧文件栏 `+` 号，或 `Ctrl+N`。
- **双向链接**：在笔记里输入 `[[` 会弹出已有笔记名，选它就能链过去；被链接的笔记右侧出现"反向链接"(backlinks)，告诉你谁引用了它。例：在实验笔记里写 `[[读研学习方向计划]]` 就链回主计划。
- **标签**：`#计划` `#优化` `#论文` `#仿真` 给笔记打标签，左侧标签栏自动聚合。
- **关系图谱**：左侧点"图谱"(Graph) 看所有笔记的链接关系网，研究脉络一目了然。

### 4. 推荐插件（设置 → 社区插件 → 浏览市场）
- **Daily Notes / 日记**：每天一篇，记实验进度（对应你计划里的"每日时间分配"）。
- **Dataview**：用查询语句把带标签的笔记汇总成表格（如"列出所有 `#论文` 笔记"）。
- **Templater / Templates**：给新笔记套固定模板（如论文三遍读法的提取模板）。
- **Kanban**：看板管理任务（对应里程碑甘特图）。
- 注：Obsidian **自带实时预览**，不需要再装 VS Code 那个 Markdown Preview Enhanced。

### 5. 数据在哪 / 怎么同步
- 笔记就是 `D:\python project\` 里的纯 `.md` 文件，随便备份、丢 GitHub 都行。
- 多设备同步：官方 Sync（付费）或免费方案（Git 同步 / 插件 Remotely Save 接网盘）。

---

## 二、Anaconda / conda 怎么用（每天都会碰）

### 1. 打开正确的终端
- Windows 开始菜单搜 **"Anaconda Prompt (anaconda3)"**；或用普通 PowerShell / CMD 也行（前提是装 Anaconda 时勾了 "Add to PATH"）。
- 打开后看一眼：命令行最前面如果有 `(base)`，说明 conda 已经加载好了。

### 2. 激活课题环境（最关键的一步）
```bash
conda activate PV_Storage_Flex_Control
```
激活后前缀变成 `(PV_Storage_Flex_Control)`。**跑任何 Python 代码之前，都必须先激活它**，否则会报 `ModuleNotFoundError: No module named 'numpy'`。

### 3. 常用命令速查表

| 想做的事 | 命令 |
|---|---|
| 看本机有哪些环境 | `conda env list` |
| 进入课题环境 | `conda activate PV_Storage_Flex_Control` |
| 退出当前环境 | `conda deactivate` |
| 看环境里装了哪些包 | `conda list` 或 `pip list` |
| 装一个包 | `pip install 包名` |
| 卸一个包 | `pip uninstall 包名` |
| 升级一个包 | `pip install -U 包名` |
| 删掉整个环境 | `conda env remove -n 环境名` |

### 4. 装包用 pip 还是 conda？
- 默认用 **pip**（已经配好清华源，速度快）。
- numpy / pandas / scipy 这类基础库两者都行；torch、xgboost 等用 pip 更稳。

### 5. 备份 / 复原环境（换机、师弟接手用）
```bash
conda env export > environment.yml     # 把当前环境导出成文件
conda env create -f environment.yml    # 别人照这个文件一键重建
```

---

## 三、VS Code 怎么用

### 1. 打开项目文件夹
菜单 **文件 → 打开文件夹**，选 `D:\python project\` 或 `D:\python project\PV_Storage_Flex_Control\`。

### 2. 选对 Python 解释器（新手最常踩的坑）
`Ctrl+Shift+P` → 输入 `Python: Select Interpreter` → 选带 **`PV_Storage_Flex_Control`** 的那一项。选错解释器，代码就会找不到你装好的包。

### 3. 跑 Python 文件
- **方法 A**：打开 `.py` 文件，点右上角的 ▶（Run Python File）。
- **方法 B**（推荐，最不容易出错）：先在终端激活环境，再直接跑。
  ```bash
  conda activate PV_Storage_Flex_Control
  cd D:\python project\PV_Storage_Flex_Control
  python exercises/ex02_forecast_practice.py
  ```
  脚本跑完会在同目录生成图片（代码里用了 `savefig`），去文件夹里找 `.png` 看结果即可。

### 4. GitLens 怎么用
- 打开任意代码文件，行号旁边会显示"这一行是谁、什么时候改的"（blame 信息）。
- 左侧活动栏点**源代码管理**（分支图标）→ 看提交历史、对比每次改动。
- 用途：配合 GitHub（你的账号 @glatm）做版本管理，写论文代码再也不怕改乱、丢失。

### 5. Markdown Preview Enhanced 怎么用
- 打开任意 `.md`（比如你的学习计划）→ 右键 → **Markdown Preview Enhanced: Open Preview** → 右侧实时渲染，LaTeX 公式和 Mermaid 流程图都能正常显示。
- 也可 `Ctrl+Shift+P` 搜 "Markdown Preview Enhanced"。

---

## 四、Jupyter Notebook 怎么用（想边写边看结果时）

环境里已装好 `notebook`。两种用法：
- **命令行启动**：`jupyter notebook` → 浏览器自动打开 → New → Python 3 新建笔记本。
- **在 VS Code 里**：新建 `xxx.ipynb` 文件，每个单元格写一段代码，`Shift+Enter` 逐个运行；适合做数据清洗、预测建模、画图探索，结果紧跟在代码下面，比纯 `.py` 直观。

---

## 五、MATLAB / Simulink 怎么用（控制仿真）

### 1. 打开与基本计算
- 开始菜单搜 **MATLAB** 启动（学校授权版，别用盗版 KMS）。
- 命令行窗口直接算：`A = [1 2; 3 4]; inv(A)`。

### 2. Simulink 建模（你已有完整笔记）
- 主页 → **Simulink** → 空白模型。
- 从**库浏览器**拖模块（电源 / 控制器 / 示波器 Scope）连线搭系统。
- 跑仿真：点 ▶ 运行 → 双击 **Scope** 看波形。
- 你的 `D:\MATLAB Project\Simulink\光伏储能直流微电网仿真\笔记.md` 是完整知识库，照着 Day1–17 复现即可（开环基线 → PI 闭环 → 双源下垂 → 700V 三环 → MPPT）。

### 3. 和 Python 怎么分工
- **MATLAB / Simulink**：功率电子细节、控制环路、系统级暂态仿真。
- **Python**：数据处理、源-荷预测、优化求解（CVXPY / Gurobi）、画图、生成论文数值结果。

---

## 六、典型科研工作流（组合拳）

以写论文代码 + 攒知识的一天为例：
1. **记灵感 / 读论文**：Obsidian 打开 `D:\python project\` 仓库，daily note 记进度，用 `[[ ]]` 链回主计划。
2. **写代码**：VS Code 打开项目，先 `Ctrl+Shift+P` 选好 `PV_Storage_Flex_Control` 解释器。
3. **跑代码**：终端激活环境，`python 脚本.py`，看终端输出和生成的图片。
4. **验证控制策略**：切到 MATLAB / Simulink 跑仿真，看 Scope 波形。
5. **归档**：晚上用 GitLens 把当天代码提交到 GitHub；Obsidian 笔记可一并进 Git 同步。

---

## 七、常见使用问题速查

| 现象 | 原因 / 解决办法 |
|---|---|
| `ModuleNotFoundError: No module named 'numpy'` | 环境没激活，先 `conda activate PV_Storage_Flex_Control` |
| VS Code 里能跑、终端跑不了 | 终端没激活环境，或解释器选错 |
| 图片里中文显示成方块 | 代码加 `plt.rcParams['font.sans-serif'] = ['SimHei']` |
| `'conda' 不是内部或外部命令` | 装 Anaconda 没勾 PATH；改用 Anaconda Prompt，或重装时勾选 |
| pip 装包很慢 | 确认清华源：`pip config list` |
| `import torch` 报错 | 还没装（等 5070 到货再装），不是你的问题 |
| gurobipy 跑大算例报错 | 还没激活，用学校邮箱 `@fjut.edu.cn` 申请 `grbgetkey` |
| Obsidian 里找不到我的笔记 | 打开仓库时选错了文件夹，重新 "Open folder as vault" 选 `D:\python project\` |

---

## 八、安装复盘（简短，换机照抄）

- **Obsidian**：官网下载安装；打开 `D:\python project\` 作为仓库(Vault)。可选 Typora 写单篇长文。
- **Anaconda 2026.07**（conda 26.5.3），装于 `C:\Users\PC\anaconda3`，安装时勾选 "Add to PATH"。
- 旧 `C:\Users\PC\Miniconda`（conda 2.2.8 古董版）已弃用。
- 课题环境 **`PV_Storage_Flex_Control`，Python 3.14.6**，已装 numpy/pandas/scipy/sklearn/xgboost/lightgbm/cvxpy/Pyomo/gurobipy/pandapower/pypsa/networkx/matplotlib/seaborn/notebook/plotly/openpyxl/xlrd/statsmodels/sktime 等。
- **torch 系列故意未装**（5070 未购）；**gurobipy 待 `@fjut.edu.cn` 激活**。
- pip 已设**清华源**：`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple`。
- **VS Code** + 插件 **GitLens**、**Markdown Preview Enhanced**。
- **MATLAB** 走学校正版授权。
