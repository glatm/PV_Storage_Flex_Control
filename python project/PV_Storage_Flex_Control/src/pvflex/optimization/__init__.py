# pvflex/optimization —— 方面2 能-碳调控子包
# -----------------------------------------------------------------------------
# 【新手看这里】整个程序最核心的"大脑"。它根据预测，决定电池每一刻
#   该充电还是放电、要不要从电网买电，目标是在满足用电的前提下
#   尽量省钱 + 少排碳。
#
# 【核心文件】
#   dispatch.py    ——  怎么算最优充放电计划（数学上的"优化问题求解"）
#   rolling_eval.py ——  全年滚动评估 + 敏感性分析（论文里的关键结果在这出）
#
# 【这个包最需要先搞懂的一个概念：凸性】
#   同一个调度问题，有两种数学写法：
#     · MILP（严格但慢）：显式写出"同一时刻不能又买又卖"这类条件，求解要几百毫秒
#     · LP（简化但快）：不写这个条件，几毫秒就算完，但只有在满足特定前提时才跟 MILP 等价
#   本项目通过"上网价 ≤ 最低零售价"这一条配置约束，让两者严格等价
#   （实测差异 0.000%），于是全年 219 天 × 上千次敏感性扫描才跑得动。
#   详细解释见 dispatch.py 里 solve_dispatch_lp 的"初学者小课堂"。
# -----------------------------------------------------------------------------
from pvflex.optimization.dispatch import (
    solve_dispatch, baseline_no_storage, run_day_ahead_stochastic,
    run_real_time_robust, print_optimize_summary,
)
