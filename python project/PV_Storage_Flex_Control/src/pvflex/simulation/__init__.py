# pvflex/simulation —— 700V 模型背景参数子包
# -----------------------------------------------------------------------------
# 【新手看这里】这个包目前只是"背景参考资料"，不参与数据分析计算。
#   里面列的是你 Simulink 仿真模型（PV_Microgrid_700V）里的电压、控制增益等参数，
#   方便你写论文/看代码时知道系统在说什么。将来要做电路动态仿真才会真正用到。
#
# 【为什么不做仿真的模块也要单独建一个包？】
#   因为课题是"光储直柔直流微电网"，Simulink 侧和 Python 侧要讲同一套东西：
#   母线 700V、低压 48V、三环闭环、MPPT —— 这些术语在两边必须一致，
#   否则答辩时容易被问"你代码里的电压和模型里的对得上吗"。
#   把参数集中放这里，就是那个"对得上"的凭证。
#
# 【⚠️ 注意区分的两个层次】
#   Simulink 层（本包背景）：毫秒级电压电流波形，验证控制环稳定性；
#   Python 层（本项目主体）：小时级电量电价碳排，做经济调度优化。
#   两者时间尺度差 6 个数量级，是互补关系而不是同一件事。
# -----------------------------------------------------------------------------
from pvflex.simulation.model_params import (
    BUS_VOLTAGE_V, OUTPUT_VOLTAGE_V, VREF_PV, VREF_OUT,
    K_DROOP, G12_BOOST, G12_BUCK, IR_CONST, D_BOOST_OPEN, D_BUCK_OPEN,
)
