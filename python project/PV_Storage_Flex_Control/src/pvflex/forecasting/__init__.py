# pvflex/forecasting —— 方面1 源-荷预测子包
# -----------------------------------------------------------------------------
# 【新手看这里】这个包负责"猜未来"：预测明天太阳能发多少电、园区用多少电。
#   猜得越准，后面调度电池才能做得越好（预测错了，计划就白做）。
#
# 【里面有什么】
#     - forecast.py  现有的方法（朴素季节性 + XGBoost 融合）
#     - deep.py      深度学习方法（LSTM / Transformer / N-BEATS）
#     - benchmark.py 把各种方法放一起比、做消融（看哪个零件真的有用）
#
# 【⚠️ 读这个包前先建立一个正确预期：负荷预测误差天生比光伏大】
#   实测结果：光伏相对 RMSE ≈ 5~6%（很好），负荷相对 RMSE ≈ 30%（很差）。
#   这不是模型不行，而是数据决定的：
#     · 光伏有物理规律——太阳几点升、几点落、阴天少发，都有迹可循；
#     · 负荷是人的行为加设备启停，而且我们只有"每天总用电量"这一个数，
#       要把它摊成 24 小时本来就是"猜的"（这一步就注定了 20%+ 的误差）。
#   所以论文里不要硬吹负荷预测精度，而要老实说明"受数据分辨率限制"，
#   并把"申请逐时电量表"列为下一步改进方向 —— 这样说反而更可信。
# -----------------------------------------------------------------------------
from pvflex.forecasting.forecast import run_forecast, print_forecast_summary
