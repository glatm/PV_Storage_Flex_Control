# =============================================================================
# requirements.txt  ——  第三方库清单
# -----------------------------------------------------------------------------
# 作用：一键安装本工程需要的所有库。
# 用法（在你激活的 conda 环境 PV_Storage_Flex_Control 里）：
#     pip install -r requirements.txt
#
# 说明：
#   - 下面大部分库你本机 conda 环境已经装好了，这文件主要是“留底”和“换电脑时复用”。
#   - 版本号没写死，默认装最新兼容版；若要锁版本，写成 包名==1.2.3 即可。
#   - pulp 是“可选”：没装也能跑（自动用启发式兜底），但装了能做精确优化。
# =============================================================================

numpy
pandas
matplotlib
scikit-learn
xgboost
lightgbm
pulp
pypdf             # 净购电曲线还原（netload 模式账单锚定）与电价校准（pdf_readers）必需
torch>=2.0         # 预测基准对比(benchmark 模式)的 LSTM/Transformer/N-BEATS 深度学习基线，CPU 即可运行
